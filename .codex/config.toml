# Project-scoped Codex configuration for OGFI ERP.
# Keep parallel work bounded. Use parallel agents primarily for independent discovery,
# review, and structured deliberation; keep a single parent agent responsible for integration.
[agents]
max_threads = 4
max_depth = 1
job_max_runtime_seconds = 1800
