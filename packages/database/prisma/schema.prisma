generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum RecordStatus {
  ACTIVE
  INACTIVE
  ARCHIVED
}

enum SupplierAccreditationStatus {
  PENDING_REVIEW
  APPROVED
  SUSPENDED
  BLOCKED
}

enum LocationType {
  BRANCH
  WAREHOUSE
  COMMISSARY
  CENTRAL_KITCHEN
  HEAD_OFFICE
  PROJECT_SITE
  TEMPORARY_SITE
}

enum ScopeType {
  COMPANY
  BRAND
  LOCATION
  DEPARTMENT
  PROJECT
}

enum AccessLevel {
  VIEW
  OPERATE
  APPROVE
  MANAGE
}

enum PurchaseRequestStatus {
  DRAFT
  SUBMITTED
  PENDING_APPROVAL
  APPROVED
  REJECTED
  RETURNED
  CANCELLED
}

enum ApprovalStatus {
  PENDING
  APPROVED
  REJECTED
  RETURNED
  CANCELLED
}

enum ApprovalStepStatus {
  WAITING
  PENDING
  APPROVED
  REJECTED
  RETURNED
  SKIPPED
}

enum AccountingPeriodStatus {
  FUTURE
  OPEN
  SOFT_CLOSED
  LOCKED
  REOPENED
}

enum FinanceCloseRunType {
  READINESS
  MONTH_END
  LOCK_CANDIDATE
}

enum FinanceCloseRunStatus {
  OPEN
  VALIDATING
  BLOCKED
  READY_FOR_REVIEW
  READY_TO_CLOSE
  CLOSED
  CANCELLED
}

enum FinanceCloseChecklistType {
  BRANCH_DEPOSITS
  BANK_RECONCILIATION
  AP_EXCEPTIONS
  PAYMENT_RELEASES
  PETTY_CASH
  CASH_ADVANCES
  INVENTORY_CUTOFF
  MANUAL_JOURNALS
  TRIAL_BALANCE
  WORKFORCE_REVIEW
  MANAGEMENT_SIGNOFF
}

enum FinanceCloseChecklistStatus {
  PENDING
  PASS
  FAIL
  NOT_APPLICABLE
  WAIVED
}

enum FinanceCloseExceptionSeverity {
  BLOCKER
  SIGNIFICANT
  INFO
}

enum FinanceCloseExceptionState {
  OPEN
  ACKNOWLEDGED
  RESOLVED
  WAIVED
  CANCELLED
}

enum FinanceCloseAttemptAction {
  START_VALIDATION
  COMPLETE_CHECK
  WAIVE_CHECK
  ASSIGN_EXCEPTION
  RESOLVE_EXCEPTION
  SUBMIT_FOR_REVIEW
  MARK_READY
  REOPEN_PERIOD
  CANCEL_RUN
}

enum FinanceCloseAttemptResult {
  IN_PROGRESS
  SUCCEEDED
  FAILED
}

enum FinanceFiscalYearStatus {
  DRAFT
  OPEN
  CLOSED
}

enum FinanceNormalBalance {
  DEBIT
  CREDIT
}

enum FinanceAccountStatementSection {
  BALANCE_SHEET
  INCOME_STATEMENT
  CASH_FLOW
  EQUITY
}

enum FinancePostingRuleStatus {
  DRAFT
  ACTIVE
  RETIRED
}

enum FinanceJournalStatus {
  DRAFT
  SUBMITTED
  IN_REVIEW
  APPROVED
  POSTING_IN_PROGRESS
  POSTED
  FAILED
  REVERSED
  SUPERSEDED
  CANCELLED
}

enum FinanceJournalType {
  MANUAL
  REVERSAL
  AUTOMATED
}

enum FinancePostingAttemptStatus {
  REQUESTED
  IN_PROGRESS
  SUCCEEDED
  FAILED
  CANCELLED
}

enum FinancePostingAttemptAction {
  POST
  REVERSE
}

enum BudgetStatus {
  DRAFT
  SUBMITTED
  UNDER_REVIEW
  APPROVED
  RETURNED
  REJECTED
  ACTIVE
  PARTIALLY_RELEASED
  CLOSED
  CANCELLED
  ARCHIVED
}

enum BudgetLineStatus {
  DRAFT
  ACTIVE
  SUSPENDED
  CLOSED
  ARCHIVED
}

enum BudgetRevisionStatus {
  DRAFT
  SUBMITTED
  UNDER_REVIEW
  APPROVED
  REJECTED
  CANCELLED
}

enum BudgetRevisionType {
  ORIGINAL
  AMENDMENT
  REBASE
  REALLOCATION
}

enum BudgetCommitmentStatus {
  PENDING
  APPROVED
  CONSUMED
  REVERSED
  CANCELLED
  VOIDED
}

enum BudgetSourceEventType {
  PURCHASE_REQUEST
  PURCHASE_ORDER
  PURCHASE_ORDER_LINE
  GOODS_RECEIPT
  INVENTORY_TRANSFER
  EXPENSE_REQUEST
  AP_INVOICE
  PAYMENT_REQUEST
  MANUAL
}

enum ExpenseRequestStatus {
  DRAFT
  SUBMITTED
  AWAITING_APPROVAL
  RETURNED_FOR_REVISION
  APPROVED
  IN_PROGRESS
  COMPLETED
  CANCELLED
  REJECTED
  ON_HOLD
  REVERSED
  VOIDED
}

enum ExpenseRequestUrgency {
  NORMAL
  URGENT
  EMERGENCY
}

enum ExpenseBudgetStatus {
  BUDGETED
  PARTIALLY_BUDGETED
  UNBUDGETED
  OVER_BUDGET
  NOT_APPLICABLE
}

enum ExpenseRequestSourceType {
  AP_INVOICE
  AP_INVOICE_LINE
  PAYMENT_REQUEST
  PAYMENT_REQUEST_LINE
  PURCHASE_REQUEST
  PURCHASE_ORDER
  GOODS_RECEIPT
  PROJECT
  MANUAL
}

enum CashAdvanceRequestStatus {
  DRAFT
  SUBMITTED
  AWAITING_APPROVAL
  RETURNED_FOR_REVISION
  APPROVED
  RELEASE_PENDING
  RELEASED_OFFLINE
  PARTIALLY_LIQUIDATED
  FULLY_LIQUIDATED
  CLOSED
  CANCELLED
  REJECTED
  ON_HOLD
  VOIDED
}

enum CashAdvanceSourceType {
  STANDALONE
  EXPENSE_REQUEST
  PAYMENT_REQUEST
  SUPPLIER_DIRECT
  MANUAL
}

enum CashAdvanceMovementType {
  ISSUE
  REVERSAL
  LIQUIDATION_SETTLEMENT
  ADJUSTMENT
  VOID
}

enum CashAdvanceLiquidationStatus {
  DRAFT
  SUBMITTED
  UNDER_REVIEW
  RETURNED_FOR_REVISION
  APPROVED
  REJECTED
  CANCELLED
  CLOSURE_READY
  CLOSED
  REVERSED
}

enum PettyCashFundStatus {
  DRAFT
  ACTIVE
  SUSPENDED
  ARCHIVED
}

enum PettyCashRequestType {
  REPLENISHMENT
  DISBURSEMENT
}

enum PettyCashRequestStatus {
  DRAFT
  SUBMITTED
  AWAITING_APPROVAL
  RETURNED_FOR_REVISION
  APPROVED
  FULFILLED_OFFLINE
  CLOSED
  REJECTED
  CANCELLED
  VOIDED
}

enum PettyCashLiquidationStatus {
  DRAFT
  SUBMITTED
  UNDER_REVIEW
  RETURNED_FOR_REVISION
  APPROVED
  CLOSED
  REJECTED
  CANCELLED
  REVERSED
}

enum PettyCashLedgerEntryType {
  OPENING
  REPLENISHMENT
  ISSUE
  RETURN
  LIQUIDATION_SETTLEMENT
  SHORTAGE_ADJUSTMENT
  OVERAGE_ADJUSTMENT
  REVERSAL
  VOID
}

enum ApInvoiceStatus {
  DRAFT
  SUBMITTED
  MATCH_PENDING
  MATCHED
  MATCHED_WITHIN_TOLERANCE
  ON_HOLD
  DISPUTED
  APPROVED_EXCEPTION
  PAYMENT_READY
  REJECTED
  CANCELLED
  REVERSED
}

enum ApMatchStatus {
  NOT_EVALUATED
  EXACT_MATCH
  WITHIN_TOLERANCE
  VARIANCE_HOLD
  DISPUTED
  APPROVED_EXCEPTION
}

enum ApExceptionStatus {
  OPEN
  DISPUTED
  APPROVED
  REJECTED
  RESOLVED
  CANCELLED
}

enum ApDuplicateRisk {
  CLEAN
  POTENTIAL
  BLOCKED
  ALLOWED_BY_EXCEPTION
}

enum ApDuplicateSignalType {
  SAME_SUPPLIER_AND_INVOICE_NUMBER
  SAME_SUPPLIER_NUMBER_DATE_TOTAL
  SAME_DOCUMENT_HASH
}

enum SupplierCreditNoteStatus {
  DRAFT
  PENDING_APPLICATION
  APPLIED
  CANCELLED
  VOIDED
}

enum PaymentRequestStatus {
  DRAFT
  SUBMITTED
  AWAITING_APPROVAL
  APPROVED
  RETURNED_FOR_REVISION
  REJECTED
  CANCELLED
  ON_HOLD
  VOIDED
}

enum PaymentReleaseStatus {
  DRAFT
  READY_FOR_RELEASE
  RELEASED
  RECONCILIATION_PENDING
  PARTIALLY_RECONCILED
  FULLY_RECONCILED
  ON_HOLD
  EXCEPTION
  CANCELLED
  REVERSED
}

enum PaymentReleaseMethod {
  BANK_TRANSFER
  CHECK
  CASH
  MANUAL_REFERENCE
}

enum PaymentReleaseExecutionStatus {
  REQUESTED
  IN_PROGRESS
  SUCCEEDED
  FAILED
  CANCELLED
}

enum FinancePayeeType {
  SUPPLIER
  EMPLOYEE
  USER_CUSTODIAN
  EXTERNAL
}

enum FinancePayeeStatus {
  ACTIVE
  INACTIVE
  BLOCKED
}

enum DisbursementRequestStatus {
  DRAFT
  SUBMITTED
  AWAITING_APPROVAL
  APPROVED
  RETURNED_FOR_REVISION
  REJECTED
  CANCELLED
  ON_HOLD
}

enum DisbursementSourceType {
  CASH_ADVANCE
  PETTY_CASH
  EMPLOYEE_REIMBURSEMENT
  MANUAL_CONTROL
}

enum EmployeeStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
  TERMINATED
  LEAVE_OF_ABSENCE
}

enum EmployeeEmploymentType {
  FULL_TIME
  PART_TIME
  CONTRACT
  TEMPORARY
  INTERN
}

enum EmployeeAssignmentType {
  PRIMARY
  SECONDMENT
  PROJECT_COVERAGE
  TEMPORARY
}

enum EmployeeAssignmentStatus {
  PLANNED
  ACTIVE
  ENDED
  CANCELLED
}

enum EmployeeLeaveType {
  VACATION
  SICK
  PERSONAL
  EMERGENCY
  MATERNITY
  PATERNITY
  OTHER
}

enum EmployeeLeaveStatus {
  DRAFT
  SUBMITTED
  UNDER_REVIEW
  APPROVED
  REJECTED
  RETURNED_FOR_REVISION
  CANCELLED
  TAKEN
}

enum EmployeeOvertimeType {
  REGULAR
  WEEKEND
  HOLIDAY
  NIGHT_SHIFT
  EMERGENCY
}

enum EmployeeOvertimeStatus {
  DRAFT
  SUBMITTED
  UNDER_REVIEW
  APPROVED
  REJECTED
  CANCELLED
  COMPLETED
}

enum EmployeeTrainingStatus {
  PLANNED
  COMPLETED
  EXPIRED
  CANCELLED
}

enum EmployeeDocumentType {
  ID_DOCUMENT
  LICENSE
  HEALTH_CERT
  FOOD_HANDLER_CERT
  SAFETY_TRAINING
  OTHER
}

enum EmployeeDocumentStatus {
  ACTIVE
  EXPIRED
  REVOKED
  REPLACED
  LOST
}

enum WorkforceScheduleStatus {
  DRAFT
  SUBMITTED
  UNDER_REVIEW
  APPROVED
  PUBLISHED
  IN_PROGRESS
  CLOSED
  RETURNED_FOR_REVISION
  REJECTED
  CANCELLED
}

enum WorkforceShiftType {
  OPENING
  MID
  CLOSING
  SPLIT
  OVERNIGHT
  SPECIAL_EVENT
}

enum WorkforceScheduleLineStatus {
  PLANNED
  ASSIGNED
  GAP
  COVERED
  CANCELLED
}

enum AttendanceImportBatchStatus {
  DRAFT
  IMPORTED
  VALIDATING
  REVIEW_READY
  EXCEPTION_LIST
  REJECTED
  VOIDED
}

enum AttendanceImportLineStatus {
  ACCEPTED
  DUPLICATE
  UNMATCHED_EMPLOYEE
  INVALID_TIME
  LOCATION_MISMATCH
  LEAVE_CONFLICT
  OVERTIME_CONFLICT
  VOIDED
}

enum BankAccountType {
  CHECKING
  SAVINGS
  TREASURY
  OTHER
}

enum BankAccountStatus {
  DRAFT
  PENDING_VERIFICATION
  ACTIVE
  SUSPENDED
  INACTIVE
  ARCHIVED
}

enum BranchCashDepositStatus {
  DRAFT
  SUBMITTED
  NEED_CLARIFICATION
  FINANCE_APPROVED
  QUEUED_FOR_RECONCILIATION
  MATCHED
  EXCEPTION
  CANCELLED
  RECONCILED
}

enum BankStatementStatus {
  DRAFT
  FILE_UPLOADED
  VALIDATION_PASSED
  VALIDATION_FAILED
  READY_FOR_RECONCILIATION
  LOCKED
  CANCELLED
}

enum BankStatementLineStatus {
  UNMATCHED
  MATCHED
  PARTIALLY_MATCHED
  EXCEPTION
  ON_HOLD
  VOIDED
}

enum BankReconciliationStatus {
  OPEN
  IN_PROGRESS
  MATCHED
  EXCEPTION
  CLOSED
  REOPENED
  VOIDED
}

enum BankReconciliationMatchStatus {
  PROPOSED
  MATCHED
  DISPUTED
  REVERSED
  VOIDED
}

enum BankReconciliationSourceType {
  BRANCH_CASH_DEPOSIT
  PAYMENT_RELEASE
  PAYMENT_REQUEST
  FINANCE_JOURNAL
  UNKNOWN
}

enum FinancePostingRuleSide {
  DEBIT
  CREDIT
}

enum FinanceDimensionType {
  BRAND
  LOCATION
  DEPARTMENT
  COST_CENTER
  PROJECT
}

enum InventoryMovementType {
  RECEIPT_IN
  TRANSFER_OUT
  TRANSFER_IN
  WASTAGE_OUT
  ADJUSTMENT_IN
  ADJUSTMENT_OUT
  OPENING_BALANCE_IN
  COUNT_VARIANCE_IN
  COUNT_VARIANCE_OUT
  REVERSAL
}

enum ProjectTemplateStatus {
  DRAFT
  PUBLISHED
  ARCHIVED
}

enum ProjectStatus {
  DRAFT
  ACTIVE
  ON_HOLD
  COMPLETED
  CANCELLED
  ARCHIVED
}

enum ProjectMemberRole {
  SPONSOR
  MANAGER
  CONTRIBUTOR
  VIEWER
  ADMINISTRATOR
}

enum ProjectTaskStatus {
  BACKLOG
  PLANNED
  IN_PROGRESS
  WAITING_FOR_APPROVAL
  BLOCKED
  FOR_REVIEW
  COMPLETED
  CANCELLED
}

enum ProjectTaskPriority {
  LOW
  NORMAL
  HIGH
  CRITICAL
}

enum ProjectBlockerStatus {
  OPEN
  RESOLVED
  ACCEPTED
  CANCELLED
}

enum ProjectBlockerSeverity {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum ProjectRiskStatus {
  OPEN
  MITIGATING
  MITIGATED
  ACCEPTED
  REALIZED
  CLOSED
  CANCELLED
}

enum ProjectRiskRating {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum ProjectCalendarDateKind {
  DATE_ONLY
  SCHEDULED_DATETIME
}

enum ProjectMilestoneStatus {
  PLANNED
  ACHIEVED
  CANCELLED
}

enum ProjectRequirementKind {
  EVIDENCE
  SIGNOFF
}

enum ProjectRequirementStatus {
  PENDING
  SUBMITTED
  APPROVED
  RETURNED
  WAIVED
  CANCELLED
}

model Tenant {
  id                                      String                                   @id @default(uuid()) @db.Uuid
  name                                    String
  status                                  RecordStatus                             @default(ACTIVE)
  defaultTimezone                         String                                   @default("Asia/Manila")
  themeConfig                             Json?
  createdAt                               DateTime                                 @default(now())
  updatedAt                               DateTime                                 @updatedAt
  companies                               Company[]
  brands                                  Brand[]
  locations                               Location[]
  departments                             Department[]
  costCenters                             CostCenter[]
  users                                   User[]
  roles                                   Role[]
  permissions                             Permission[]
  suppliers                               Supplier[]
  supplierItemLinks                       SupplierItemLink[]
  supplierPriceHistory                    SupplierPriceHistory[]
  itemCategories                          ItemCategory[]
  uoms                                    Uom[]
  items                                   Item[]
  approvalRules                           ApprovalRule[]
  approvalInstances                       ApprovalInstance[]
  purchaseRequests                        PurchaseRequest[]
  purchaseRequestComments                 PurchaseRequestComment[]
  quotationRequests                       QuotationRequest[]
  supplierQuotations                      SupplierQuotation[]
  quotationRecommendations                QuotationRecommendation[]
  purchaseOrders                          PurchaseOrder[]
  purchaseOrderLines                      PurchaseOrderLine[]
  purchaseOrderAmendments                 PurchaseOrderAmendment[]
  purchaseOrderBalanceClosures            PurchaseOrderBalanceClosure[]
  inventoryLocations                      InventoryLocation[]
  inventoryMovements                      InventoryMovement[]
  inventoryBalances                       InventoryBalance[]
  goodsReceipts                           GoodsReceipt[]
  goodsReceiptLines                       GoodsReceiptLine[]
  inventoryTransfers                      InventoryTransfer[]
  inventoryTransferLines                  InventoryTransferLine[]
  inventoryTransferReceipts               InventoryTransferReceipt[]
  inventoryTransferReceiptLines           InventoryTransferReceiptLine[]
  notifications                           Notification[]
  auditEvents                             AuditEvent[]
  attachments                             Attachment[]
  controlledEvidenceAttachments           ControlledEvidenceAttachment[]
  companyPolicySettings                   CompanyPolicySetting[]
  releaseReadinessGates                   ReleaseReadinessGate[]
  deploymentEvidenceRecords               DeploymentEvidenceRecord[]
  enablementEvidenceRecords               EnablementEvidenceRecord[]
  uatEvidenceRecords                      UatEvidenceRecord[]
  releaseBoardDecisions                   ReleaseBoardDecision[]
  highRiskScopeRequests                   HighRiskScopeRequest[]
  sensitiveRoleRequests                   SensitiveRoleRequest[]
  breakGlassAccessGrants                  BreakGlassAccessGrant[]
  privilegedMfaEnrollments                PrivilegedMfaEnrollment[]
  authSessionInvalidations                AuthSessionInvalidation[]
  stockCountSessions                      StockCountSession[]
  stockCountLines                         StockCountLine[]
  wastageReports                          WastageReport[]
  wastageLines                            WastageLine[]
  wastagePolicies                         WastagePolicy[]
  operationalReasonCodes                  OperationalReasonCode[]
  stockAdjustments                        StockAdjustment[]
  stockAdjustmentLines                    StockAdjustmentLine[]
  projectTemplates                        ProjectTemplate[]
  projects                                Project[]
  projectMembers                          ProjectMember[]
  projectActivityEvents                   ProjectActivityEvent[]
  projectTasks                            ProjectTask[]
  projectTaskAssignees                    ProjectTaskAssignee[]
  projectTaskChecklistItems               ProjectTaskChecklistItem[]
  projectComments                         ProjectComment[]
  projectBlockers                         ProjectBlocker[]
  projectRisks                            ProjectRisk[]
  projectMilestones                       ProjectMilestone[]
  projectRecordLinks                      ProjectRecordLink[]
  projectAttachments                      ProjectAttachment[]
  projectRequirements                     ProjectRequirement[]
  recipes                                 Recipe[]
  recipeVersions                          RecipeVersion[]
  recipeLines                             RecipeLine[]
  menuItems                               MenuItem[]
  menuPrices                              MenuPrice[]
  restaurantSalesImportBatches            RestaurantSalesImportBatch[]
  restaurantSalesImportLines              RestaurantSalesImportLine[]
  branchOperationalChecklists             BranchOperationalChecklist[]
  branchOperationalChecklistLines         BranchOperationalChecklistLine[]
  foodSafetyLogs                          FoodSafetyLog[]
  foodSafetyReadings                      FoodSafetyReading[]
  operationalIncidents                    OperationalIncident[]
  maintenanceTickets                      MaintenanceTicket[]
  fiscalYears                             FiscalYear[]
  accountingPeriods                       AccountingPeriod[]
  financeAccountClasses                   FinanceAccountClass[]
  chartOfAccounts                         ChartOfAccount[]
  financePostingRules                     FinancePostingRule[]
  financePostingRuleAccountMaps           FinancePostingRuleAccountMap[]
  financePostingRuleDimensionRequirements FinancePostingRuleDimensionRequirement[]
  financeJournals                         FinanceJournal[]
  financeJournalLines                     FinanceJournalLine[]
  financeJournalPostingAttempts           FinanceJournalPostingAttempt[]
  budgets                                 Budget[]
  budgetLines                             BudgetLine[]
  budgetRevisions                         BudgetRevision[]
  budgetCommitments                       BudgetCommitment[]
  expenseRequests                         ExpenseRequest[]
  expenseRequestLines                     ExpenseRequestLine[]
  expenseRequestSourceLinks               ExpenseRequestSourceLink[]
  cashAdvanceRequests                     CashAdvanceRequest[]
  cashAdvanceMovements                    CashAdvanceMovement[]
  cashAdvanceLiquidations                 CashAdvanceLiquidation[]
  cashAdvanceLiquidationLines             CashAdvanceLiquidationLine[]
  pettyCashFunds                          PettyCashFund[]
  pettyCashRequests                       PettyCashRequest[]
  pettyCashLedgerEntries                  PettyCashLedgerEntry[]
  pettyCashLiquidations                   PettyCashLiquidation[]
  pettyCashLiquidationLines               PettyCashLiquidationLine[]
  financeCloseRuns                        FinanceCloseRun[]
  financeCloseChecklistItems              FinanceCloseChecklistItem[]
  financeCloseExceptions                  FinanceCloseException[]
  financeCloseAttempts                    FinanceCloseAttempt[]
  apInvoices                              ApInvoice[]
  apInvoiceLines                          ApInvoiceLine[]
  apInvoiceMatchResults                   ApInvoiceMatchResult[]
  apInvoiceExceptions                     ApInvoiceException[]
  apInvoiceDuplicateSignals               ApInvoiceDuplicateSignal[]
  supplierCreditNotes                     SupplierCreditNote[]
  financePayees                           FinancePayee[]
  paymentRequests                         PaymentRequest[]
  paymentRequestLines                     PaymentRequestLine[]
  disbursementRequests                    NonSupplierDisbursementRequest[]
  paymentReleases                         PaymentRelease[]
  paymentReleaseAllocations               PaymentReleaseAllocation[]
  paymentReleaseExecutions                PaymentReleaseExecution[]
  employees                               Employee[]
  employeeAssignments                     EmployeeAssignment[]
  employeeLeaveRequests                   EmployeeLeaveRequest[]
  employeeOvertimeRecords                 EmployeeOvertimeRecord[]
  employeeTrainingRecords                 EmployeeTrainingRecord[]
  employeeComplianceDocuments             EmployeeComplianceDocument[]
  workforceSchedules                      WorkforceSchedule[]
  workforceScheduleLines                  WorkforceScheduleLine[]
  attendanceImportBatches                 AttendanceImportBatch[]
  attendanceImportLines                   AttendanceImportLine[]
  bankAccounts                            BankAccount[]
  branchCashDeposits                      BranchCashDeposit[]
  bankStatements                          BankStatement[]
  bankStatementLines                      BankStatementLine[]
  bankReconciliations                     BankReconciliation[]
  bankReconciliationMatches               BankReconciliationMatch[]
}

model Company {
  id                                      String                                   @id @default(uuid()) @db.Uuid
  tenantId                                String                                   @db.Uuid
  code                                    String
  legalName                               String
  tradingName                             String?
  taxIdentifier                           String?
  currencyCode                            String
  timezone                                String                                   @default("Asia/Manila")
  status                                  RecordStatus                             @default(ACTIVE)
  createdAt                               DateTime                                 @default(now())
  updatedAt                               DateTime                                 @updatedAt
  tenant                                  Tenant                                   @relation(fields: [tenantId], references: [id])
  brands                                  Brand[]
  locations                               Location[]
  departments                             Department[]
  costCenters                             CostCenter[]
  suppliers                               Supplier[]
  supplierItemLinks                       SupplierItemLink[]
  supplierPriceHistory                    SupplierPriceHistory[]
  itemCategories                          ItemCategory[]
  uoms                                    Uom[]
  items                                   Item[]
  approvalRules                           ApprovalRule[]
  approvalInstances                       ApprovalInstance[]
  purchaseRequests                        PurchaseRequest[]
  purchaseRequestComments                 PurchaseRequestComment[]
  quotationRequests                       QuotationRequest[]
  supplierQuotations                      SupplierQuotation[]
  quotationRecommendations                QuotationRecommendation[]
  purchaseOrders                          PurchaseOrder[]
  purchaseOrderLines                      PurchaseOrderLine[]
  purchaseOrderAmendments                 PurchaseOrderAmendment[]
  purchaseOrderBalanceClosures            PurchaseOrderBalanceClosure[]
  inventoryLocations                      InventoryLocation[]
  inventoryMovements                      InventoryMovement[]
  inventoryBalances                       InventoryBalance[]
  goodsReceipts                           GoodsReceipt[]
  goodsReceiptLines                       GoodsReceiptLine[]
  inventoryTransfers                      InventoryTransfer[]
  inventoryTransferLines                  InventoryTransferLine[]
  inventoryTransferReceipts               InventoryTransferReceipt[]
  inventoryTransferReceiptLines           InventoryTransferReceiptLine[]
  notifications                           Notification[]
  auditEvents                             AuditEvent[]
  controlledEvidenceAttachments           ControlledEvidenceAttachment[]
  policySettings                          CompanyPolicySetting[]
  releaseReadinessGates                   ReleaseReadinessGate[]
  deploymentEvidenceRecords               DeploymentEvidenceRecord[]
  enablementEvidenceRecords               EnablementEvidenceRecord[]
  uatEvidenceRecords                      UatEvidenceRecord[]
  releaseBoardDecisions                   ReleaseBoardDecision[]
  highRiskScopeRequests                   HighRiskScopeRequest[]
  sensitiveRoleRequests                   SensitiveRoleRequest[]
  breakGlassAccessGrants                  BreakGlassAccessGrant[]
  privilegedMfaEnrollments                PrivilegedMfaEnrollment[]
  authSessionInvalidations                AuthSessionInvalidation[]
  stockCountSessions                      StockCountSession[]
  stockCountLines                         StockCountLine[]
  wastageReports                          WastageReport[]
  wastageLines                            WastageLine[]
  wastagePolicies                         WastagePolicy[]
  operationalReasonCodes                  OperationalReasonCode[]
  stockAdjustments                        StockAdjustment[]
  stockAdjustmentLines                    StockAdjustmentLine[]
  projectTemplates                        ProjectTemplate[]
  projects                                Project[]
  projectMembers                          ProjectMember[]
  projectActivityEvents                   ProjectActivityEvent[]
  projectTasks                            ProjectTask[]
  projectTaskAssignees                    ProjectTaskAssignee[]
  projectTaskChecklistItems               ProjectTaskChecklistItem[]
  projectComments                         ProjectComment[]
  projectBlockers                         ProjectBlocker[]
  projectRisks                            ProjectRisk[]
  projectMilestones                       ProjectMilestone[]
  projectRecordLinks                      ProjectRecordLink[]
  projectAttachments                      ProjectAttachment[]
  projectRequirements                     ProjectRequirement[]
  recipes                                 Recipe[]
  recipeVersions                          RecipeVersion[]
  recipeLines                             RecipeLine[]
  menuItems                               MenuItem[]
  menuPrices                              MenuPrice[]
  restaurantSalesImportBatches            RestaurantSalesImportBatch[]
  restaurantSalesImportLines              RestaurantSalesImportLine[]
  branchOperationalChecklists             BranchOperationalChecklist[]
  branchOperationalChecklistLines         BranchOperationalChecklistLine[]
  foodSafetyLogs                          FoodSafetyLog[]
  foodSafetyReadings                      FoodSafetyReading[]
  operationalIncidents                    OperationalIncident[]
  maintenanceTickets                      MaintenanceTicket[]
  fiscalYears                             FiscalYear[]
  accountingPeriods                       AccountingPeriod[]
  financeAccountClasses                   FinanceAccountClass[]
  chartOfAccounts                         ChartOfAccount[]
  financePostingRules                     FinancePostingRule[]
  financePostingRuleAccountMaps           FinancePostingRuleAccountMap[]
  financePostingRuleDimensionRequirements FinancePostingRuleDimensionRequirement[]
  financeJournals                         FinanceJournal[]
  financeJournalLines                     FinanceJournalLine[]
  financeJournalPostingAttempts           FinanceJournalPostingAttempt[]
  budgets                                 Budget[]
  budgetLines                             BudgetLine[]
  budgetRevisions                         BudgetRevision[]
  budgetCommitments                       BudgetCommitment[]
  expenseRequests                         ExpenseRequest[]
  expenseRequestLines                     ExpenseRequestLine[]
  expenseRequestSourceLinks               ExpenseRequestSourceLink[]
  cashAdvanceRequests                     CashAdvanceRequest[]
  cashAdvanceMovements                    CashAdvanceMovement[]
  cashAdvanceLiquidations                 CashAdvanceLiquidation[]
  cashAdvanceLiquidationLines             CashAdvanceLiquidationLine[]
  pettyCashFunds                          PettyCashFund[]
  pettyCashRequests                       PettyCashRequest[]
  pettyCashLedgerEntries                  PettyCashLedgerEntry[]
  pettyCashLiquidations                   PettyCashLiquidation[]
  pettyCashLiquidationLines               PettyCashLiquidationLine[]
  financeCloseRuns                        FinanceCloseRun[]
  financeCloseChecklistItems              FinanceCloseChecklistItem[]
  financeCloseExceptions                  FinanceCloseException[]
  financeCloseAttempts                    FinanceCloseAttempt[]
  apInvoices                              ApInvoice[]
  apInvoiceLines                          ApInvoiceLine[]
  apInvoiceMatchResults                   ApInvoiceMatchResult[]
  apInvoiceExceptions                     ApInvoiceException[]
  apInvoiceDuplicateSignals               ApInvoiceDuplicateSignal[]
  supplierCreditNotes                     SupplierCreditNote[]
  financePayees                           FinancePayee[]
  paymentRequests                         PaymentRequest[]
  paymentRequestLines                     PaymentRequestLine[]
  disbursementRequests                    NonSupplierDisbursementRequest[]
  paymentReleases                         PaymentRelease[]
  paymentReleaseAllocations               PaymentReleaseAllocation[]
  paymentReleaseExecutions                PaymentReleaseExecution[]
  employees                               Employee[]
  employeeAssignments                     EmployeeAssignment[]
  employeeLeaveRequests                   EmployeeLeaveRequest[]
  employeeOvertimeRecords                 EmployeeOvertimeRecord[]
  employeeTrainingRecords                 EmployeeTrainingRecord[]
  employeeComplianceDocuments             EmployeeComplianceDocument[]
  workforceSchedules                      WorkforceSchedule[]
  workforceScheduleLines                  WorkforceScheduleLine[]
  attendanceImportBatches                 AttendanceImportBatch[]
  attendanceImportLines                   AttendanceImportLine[]
  bankAccounts                            BankAccount[]
  branchCashDeposits                      BranchCashDeposit[]
  bankStatements                          BankStatement[]
  bankStatementLines                      BankStatementLine[]
  bankReconciliations                     BankReconciliation[]
  bankReconciliationMatches               BankReconciliationMatch[]

  @@unique([tenantId, code])
  @@index([tenantId, status])
}

model Brand {
  id                        String                           @id @default(uuid()) @db.Uuid
  tenantId                  String                           @db.Uuid
  companyId                 String                           @db.Uuid
  name                      String
  code                      String
  status                    RecordStatus                     @default(ACTIVE)
  createdAt                 DateTime                         @default(now())
  updatedAt                 DateTime                         @updatedAt
  tenant                    Tenant                           @relation(fields: [tenantId], references: [id])
  company                   Company                          @relation(fields: [companyId], references: [id])
  locations                 Location[]
  purchaseRequests          PurchaseRequest[]
  purchaseOrders            PurchaseOrder[]
  projects                  Project[]
  recipes                   Recipe[]
  menuItems                 MenuItem[]
  salesImportBatches        RestaurantSalesImportBatch[]
  salesImportLines          RestaurantSalesImportLine[]
  operationalChecklists     BranchOperationalChecklist[]
  operationalChecklistLines BranchOperationalChecklistLine[]
  foodSafetyLogs            FoodSafetyLog[]
  foodSafetyReadings        FoodSafetyReading[]
  operationalIncidents      OperationalIncident[]
  maintenanceTickets        MaintenanceTicket[]
  employeeAssignments       EmployeeAssignment[]
  budgets                   Budget[]
  budgetLines               BudgetLine[]
  expenseRequests           ExpenseRequest[]
  expenseRequestLines       ExpenseRequestLine[]
  cashAdvanceRequests       CashAdvanceRequest[]
  pettyCashFunds            PettyCashFund[]
  workforceSchedules        WorkforceSchedule[]
  workforceScheduleLines    WorkforceScheduleLine[]
  attendanceImportBatches   AttendanceImportBatch[]
  attendanceImportLines     AttendanceImportLine[]

  @@unique([companyId, code])
  @@unique([id, tenantId, companyId])
  @@index([tenantId, companyId, status])
}

model Location {
  id                            String                           @id @default(uuid()) @db.Uuid
  tenantId                      String                           @db.Uuid
  companyId                     String                           @db.Uuid
  brandId                       String?                          @db.Uuid
  locationType                  LocationType
  code                          String
  name                          String
  address                       String?
  timezone                      String                           @default("Asia/Manila")
  status                        RecordStatus                     @default(ACTIVE)
  createdAt                     DateTime                         @default(now())
  updatedAt                     DateTime                         @updatedAt
  tenant                        Tenant                           @relation(fields: [tenantId], references: [id])
  company                       Company                          @relation(fields: [companyId], references: [id])
  brand                         Brand?                           @relation(fields: [brandId], references: [id])
  purchaseRequests              PurchaseRequest[]
  purchaseOrders                PurchaseOrder[]                  @relation("PurchaseOrderDeliveryLocation")
  inventoryLocations            InventoryLocation[]
  goodsReceipts                 GoodsReceipt[]                   @relation("GoodsReceiptReceivingLocation")
  sourceInventoryTransfers      InventoryTransfer[]              @relation("InventoryTransferSourceLocation")
  destinationInventoryTransfers InventoryTransfer[]              @relation("InventoryTransferDestinationLocation")
  notifications                 Notification[]
  projects                      Project[]
  menuPrices                    MenuPrice[]
  salesImportBatches            RestaurantSalesImportBatch[]
  salesImportLines              RestaurantSalesImportLine[]
  operationalChecklists         BranchOperationalChecklist[]
  operationalChecklistLines     BranchOperationalChecklistLine[]
  foodSafetyLogs                FoodSafetyLog[]
  foodSafetyReadings            FoodSafetyReading[]
  operationalIncidents          OperationalIncident[]
  maintenanceTickets            MaintenanceTicket[]
  highRiskScopeRequests         HighRiskScopeRequest[]
  breakGlassAccessGrants        BreakGlassAccessGrant[]
  paymentRequests               PaymentRequest[]
  paymentRequestLines           PaymentRequestLine[]
  disbursementRequests          NonSupplierDisbursementRequest[]
  paymentReleases               PaymentRelease[]
  employees                     Employee[]
  employeeAssignments           EmployeeAssignment[]
  employeeLeaveRequests         EmployeeLeaveRequest[]
  employeeOvertimeRecords       EmployeeOvertimeRecord[]
  bankAccounts                  BankAccount[]
  branchCashDeposits            BranchCashDeposit[]
  budgets                       Budget[]
  budgetLines                   BudgetLine[]
  expenseRequests               ExpenseRequest[]
  expenseRequestLines           ExpenseRequestLine[]
  cashAdvanceRequests           CashAdvanceRequest[]
  cashAdvanceMovements          CashAdvanceMovement[]
  cashAdvanceLiquidations       CashAdvanceLiquidation[]
  cashAdvanceLiquidationLines   CashAdvanceLiquidationLine[]
  pettyCashFunds                PettyCashFund[]
  pettyCashRequests             PettyCashRequest[]
  pettyCashLedgerEntries        PettyCashLedgerEntry[]
  pettyCashLiquidations         PettyCashLiquidation[]
  pettyCashLiquidationLines     PettyCashLiquidationLine[]
  workforceSchedules            WorkforceSchedule[]
  workforceScheduleLines        WorkforceScheduleLine[]
  attendanceImportBatches       AttendanceImportBatch[]
  attendanceImportLines         AttendanceImportLine[]

  @@unique([companyId, code])
  @@unique([id, tenantId, companyId])
  @@index([tenantId, companyId, status])
}

model Department {
  id                     String                  @id @default(uuid()) @db.Uuid
  tenantId               String                  @db.Uuid
  companyId              String                  @db.Uuid
  name                   String
  code                   String
  status                 RecordStatus            @default(ACTIVE)
  tenant                 Tenant                  @relation(fields: [tenantId], references: [id])
  company                Company                 @relation(fields: [companyId], references: [id])
  costCenters            CostCenter[]
  purchaseRequests       PurchaseRequest[]
  purchaseOrders         PurchaseOrder[]
  projects               Project[]
  employeeAssignments    EmployeeAssignment[]
  budgets                Budget[]
  budgetLines            BudgetLine[]
  expenseRequests        ExpenseRequest[]
  expenseRequestLines    ExpenseRequestLine[]
  cashAdvanceRequests    CashAdvanceRequest[]
  workforceSchedules     WorkforceSchedule[]
  workforceScheduleLines WorkforceScheduleLine[]

  @@unique([companyId, code])
  @@unique([id, tenantId, companyId])
}

model CostCenter {
  id                     String                  @id @default(uuid()) @db.Uuid
  tenantId               String                  @db.Uuid
  companyId              String                  @db.Uuid
  departmentId           String?                 @db.Uuid
  code                   String
  name                   String
  status                 RecordStatus            @default(ACTIVE)
  tenant                 Tenant                  @relation(fields: [tenantId], references: [id])
  company                Company                 @relation(fields: [companyId], references: [id])
  department             Department?             @relation(fields: [departmentId], references: [id])
  purchaseRequests       PurchaseRequest[]
  purchaseOrders         PurchaseOrder[]
  projects               Project[]
  employeeAssignments    EmployeeAssignment[]
  budgets                Budget[]
  budgetLines            BudgetLine[]
  expenseRequests        ExpenseRequest[]
  expenseRequestLines    ExpenseRequestLine[]
  cashAdvanceRequests    CashAdvanceRequest[]
  workforceSchedules     WorkforceSchedule[]
  workforceScheduleLines WorkforceScheduleLine[]

  @@unique([companyId, code])
  @@unique([id, tenantId, companyId])
}

model User {
  id                                  String                         @id @default(uuid()) @db.Uuid
  tenantId                            String                         @db.Uuid
  email                               String
  displayName                         String
  status                              RecordStatus                   @default(ACTIVE)
  lastLoginAt                         DateTime?
  createdAt                           DateTime                       @default(now())
  updatedAt                           DateTime                       @updatedAt
  tenant                              Tenant                         @relation(fields: [tenantId], references: [id])
  roleAssignments                     UserRoleAssignment[]
  scopeAssignments                    UserScopeAssignment[]
  targetedHighRiskScopeRequests       HighRiskScopeRequest[]         @relation("HighRiskScopeRequestTargetUser")
  requestedHighRiskScopeRequests      HighRiskScopeRequest[]         @relation("HighRiskScopeRequestRequester")
  reviewedHighRiskScopeRequests       HighRiskScopeRequest[]         @relation("HighRiskScopeRequestReviewer")
  targetedSensitiveRoleRequests       SensitiveRoleRequest[]         @relation("SensitiveRoleRequestTargetUser")
  requestedSensitiveRoleRequests      SensitiveRoleRequest[]         @relation("SensitiveRoleRequestRequester")
  reviewedSensitiveRoleRequests       SensitiveRoleRequest[]         @relation("SensitiveRoleRequestReviewer")
  targetedBreakGlassAccessGrants      BreakGlassAccessGrant[]        @relation("BreakGlassAccessGrantTargetUser")
  requestedBreakGlassAccessGrants     BreakGlassAccessGrant[]        @relation("BreakGlassAccessGrantRequester")
  approvedBreakGlassAccessGrants      BreakGlassAccessGrant[]        @relation("BreakGlassAccessGrantApprover")
  revokedBreakGlassAccessGrants       BreakGlassAccessGrant[]        @relation("BreakGlassAccessGrantRevoker")
  reviewedBreakGlassAccessGrants      BreakGlassAccessGrant[]        @relation("BreakGlassAccessGrantReviewer")
  targetedPrivilegedMfaEnrollments    PrivilegedMfaEnrollment[]      @relation("PrivilegedMfaEnrollmentTargetUser")
  attestedPrivilegedMfaEnrollments    PrivilegedMfaEnrollment[]      @relation("PrivilegedMfaEnrollmentAttester")
  verifiedPrivilegedMfaEnrollments    PrivilegedMfaEnrollment[]      @relation("PrivilegedMfaEnrollmentVerifier")
  revokedPrivilegedMfaEnrollments     PrivilegedMfaEnrollment[]      @relation("PrivilegedMfaEnrollmentRevoker")
  targetedAuthSessionInvalidations    AuthSessionInvalidation[]      @relation("AuthSessionInvalidationTargetUser")
  requestedAuthSessionInvalidations   AuthSessionInvalidation[]      @relation("AuthSessionInvalidationRequestedBy")
  createdDeploymentEvidenceRecords    DeploymentEvidenceRecord[]     @relation("DeploymentEvidenceCreatedBy")
  verifiedDeploymentEvidenceRecords   DeploymentEvidenceRecord[]     @relation("DeploymentEvidenceVerifiedBy")
  rejectedDeploymentEvidenceRecords   DeploymentEvidenceRecord[]     @relation("DeploymentEvidenceRejectedBy")
  createdEnablementEvidenceRecords    EnablementEvidenceRecord[]     @relation("EnablementEvidenceCreatedBy")
  verifiedEnablementEvidenceRecords   EnablementEvidenceRecord[]     @relation("EnablementEvidenceVerifiedBy")
  rejectedEnablementEvidenceRecords   EnablementEvidenceRecord[]     @relation("EnablementEvidenceRejectedBy")
  createdUatEvidenceRecords           UatEvidenceRecord[]            @relation("UatEvidenceCreatedBy")
  verifiedUatEvidenceRecords          UatEvidenceRecord[]            @relation("UatEvidenceVerifiedBy")
  rejectedUatEvidenceRecords          UatEvidenceRecord[]            @relation("UatEvidenceRejectedBy")
  chairedReleaseBoardDecisions        ReleaseBoardDecision[]         @relation("ReleaseBoardDecisionChair")
  requestedPurchaseRequests           PurchaseRequest[]              @relation("PurchaseRequestRequester")
  purchaseRequestComments             PurchaseRequestComment[]
  createdQuotationRequests            QuotationRequest[]
  preparedQuoteRecommendations        QuotationRecommendation[]
  createdPurchaseOrders               PurchaseOrder[]
  cancelledPurchaseOrders             PurchaseOrder[]                @relation("PurchaseOrderCanceller")
  requestedPurchaseOrderAmendments    PurchaseOrderAmendment[]       @relation("PurchaseOrderAmendmentRequester")
  approvedPurchaseOrderAmendments     PurchaseOrderAmendment[]       @relation("PurchaseOrderAmendmentApprover")
  requestedPurchaseOrderClosures      PurchaseOrderBalanceClosure[]  @relation("PurchaseOrderBalanceClosureRequester")
  approvedPurchaseOrderClosures       PurchaseOrderBalanceClosure[]  @relation("PurchaseOrderBalanceClosureApprover")
  receivedGoodsReceipts               GoodsReceipt[]                 @relation("GoodsReceiptReceiver")
  reversedGoodsReceipts               GoodsReceipt[]                 @relation("GoodsReceiptReverser")
  requestedInventoryTransfers         InventoryTransfer[]            @relation("InventoryTransferRequester")
  dispatchedInventoryTransfers        InventoryTransfer[]            @relation("InventoryTransferDispatcher")
  receivedInventoryTransfers          InventoryTransfer[]            @relation("InventoryTransferReceiver")
  receivedInventoryTransferReceipts   InventoryTransferReceipt[]     @relation("InventoryTransferReceiptReceiver")
  reversedInventoryTransferReceipts   InventoryTransferReceipt[]     @relation("InventoryTransferReceiptReverser")
  createdStockCountSessions           StockCountSession[]            @relation("StockCountCreator")
  assignedStockCountSessions          StockCountSession[]            @relation("StockCountAssignee")
  reviewedStockCountSessions          StockCountSession[]            @relation("StockCountReviewer")
  countedStockCountLines              StockCountLine[]               @relation("StockCountLineCounter")
  reportedWastageReports              WastageReport[]                @relation("WastageReportReporter")
  reviewedWastageReports              WastageReport[]                @relation("WastageReportReviewer")
  postedWastageReports                WastageReport[]                @relation("WastageReportPoster")
  reversedWastageReports              WastageReport[]                @relation("WastageReportReverser")
  requestedStockAdjustments           StockAdjustment[]              @relation("StockAdjustmentRequester")
  cancelledStockAdjustments           StockAdjustment[]              @relation("StockAdjustmentCanceller")
  postedStockAdjustments              StockAdjustment[]              @relation("StockAdjustmentPoster")
  reversedStockAdjustments            StockAdjustment[]              @relation("StockAdjustmentReverser")
  createdProjectTemplates             ProjectTemplate[]              @relation("ProjectTemplateCreator")
  updatedProjectTemplates             ProjectTemplate[]              @relation("ProjectTemplateUpdater")
  sponsoredProjects                   Project[]                      @relation("ProjectSponsor")
  managedProjects                     Project[]                      @relation("ProjectManager")
  createdProjects                     Project[]                      @relation("ProjectCreator")
  updatedProjects                     Project[]                      @relation("ProjectUpdater")
  archivedProjects                    Project[]                      @relation("ProjectArchiver")
  projectMemberships                  ProjectMember[]                @relation("ProjectMemberUser")
  addedProjectMembers                 ProjectMember[]                @relation("ProjectMemberAdder")
  removedProjectMembers               ProjectMember[]                @relation("ProjectMemberRemover")
  projectActivityEvents               ProjectActivityEvent[]         @relation("ProjectActivityActor")
  ownedProjectTasks                   ProjectTask[]                  @relation("ProjectTaskOwner")
  completedProjectTasks               ProjectTask[]                  @relation("ProjectTaskCompleter")
  blockedProjectTasks                 ProjectTask[]                  @relation("ProjectTaskBlocker")
  cancelledProjectTasks               ProjectTask[]                  @relation("ProjectTaskCanceller")
  reopenedProjectTasks                ProjectTask[]                  @relation("ProjectTaskReopener")
  createdProjectTasks                 ProjectTask[]                  @relation("ProjectTaskCreator")
  updatedProjectTasks                 ProjectTask[]                  @relation("ProjectTaskUpdater")
  projectTaskAssignments              ProjectTaskAssignee[]          @relation("ProjectTaskAssigneeUser")
  assignedProjectTasks                ProjectTaskAssignee[]          @relation("ProjectTaskAssigner")
  removedProjectTaskAssignments       ProjectTaskAssignee[]          @relation("ProjectTaskAssignmentRemover")
  completedProjectChecklistItems      ProjectTaskChecklistItem[]     @relation("ProjectChecklistCompleter")
  createdProjectChecklistItems        ProjectTaskChecklistItem[]     @relation("ProjectChecklistCreator")
  updatedProjectChecklistItems        ProjectTaskChecklistItem[]     @relation("ProjectChecklistUpdater")
  authoredProjectComments             ProjectComment[]               @relation("ProjectCommentAuthor")
  editedProjectComments               ProjectComment[]               @relation("ProjectCommentEditor")
  archivedProjectComments             ProjectComment[]               @relation("ProjectCommentArchiver")
  ownedProjectBlockers                ProjectBlocker[]               @relation("ProjectBlockerOwner")
  reportedProjectBlockers             ProjectBlocker[]               @relation("ProjectBlockerReporter")
  resolvedProjectBlockers             ProjectBlocker[]               @relation("ProjectBlockerResolver")
  ownedProjectRisks                   ProjectRisk[]                  @relation("ProjectRiskOwner")
  resolvedProjectRisks                ProjectRisk[]                  @relation("ProjectRiskResolver")
  createdProjectRisks                 ProjectRisk[]                  @relation("ProjectRiskCreator")
  updatedProjectRisks                 ProjectRisk[]                  @relation("ProjectRiskUpdater")
  ownedProjectMilestones              ProjectMilestone[]             @relation("ProjectMilestoneOwner")
  achievedProjectMilestones           ProjectMilestone[]             @relation("ProjectMilestoneAchiever")
  cancelledProjectMilestones          ProjectMilestone[]             @relation("ProjectMilestoneCanceller")
  createdProjectMilestones            ProjectMilestone[]             @relation("ProjectMilestoneCreator")
  updatedProjectMilestones            ProjectMilestone[]             @relation("ProjectMilestoneUpdater")
  createdProjectRecordLinks           ProjectRecordLink[]            @relation("ProjectRecordLinkCreator")
  updatedProjectRecordLinks           ProjectRecordLink[]            @relation("ProjectRecordLinkUpdater")
  archivedProjectRecordLinks          ProjectRecordLink[]            @relation("ProjectRecordLinkArchiver")
  createdProjectAttachments           ProjectAttachment[]            @relation("ProjectAttachmentCreator")
  archivedProjectAttachments          ProjectAttachment[]            @relation("ProjectAttachmentArchiver")
  ownedProjectRequirements            ProjectRequirement[]           @relation("ProjectRequirementOwner")
  reviewedProjectRequirements         ProjectRequirement[]           @relation("ProjectRequirementReviewer")
  submittedProjectRequirements        ProjectRequirement[]           @relation("ProjectRequirementSubmitter")
  decidedProjectRequirements          ProjectRequirement[]           @relation("ProjectRequirementDecider")
  createdProjectRequirements          ProjectRequirement[]           @relation("ProjectRequirementCreator")
  updatedProjectRequirements          ProjectRequirement[]           @relation("ProjectRequirementUpdater")
  createdControlledEvidenceLinks      ControlledEvidenceAttachment[] @relation("ControlledEvidenceAttachmentCreator")
  archivedControlledEvidenceLinks     ControlledEvidenceAttachment[] @relation("ControlledEvidenceAttachmentArchiver")
  requestedPaymentRequests            PaymentRequest[]               @relation("PaymentRequestRequester")
  submittedPaymentRequests            PaymentRequest[]               @relation("PaymentRequestSubmitter")
  approvedPaymentRequests             PaymentRequest[]               @relation("PaymentRequestApprover")
  rejectedPaymentRequests             PaymentRequest[]               @relation("PaymentRequestRejecter")
  cancelledPaymentRequests            PaymentRequest[]               @relation("PaymentRequestCanceller")
  createdPaymentRequestLines          PaymentRequestLine[]           @relation("PaymentRequestLineCreator")
  createdSupplierCreditNotes          SupplierCreditNote[]           @relation("SupplierCreditNoteCreator")
  cancelledSupplierCreditNotes        SupplierCreditNote[]           @relation("SupplierCreditNoteCanceller")
  createdPaymentReleases              PaymentRelease[]               @relation("PaymentReleaseCreator")
  releasedPaymentReleases             PaymentRelease[]               @relation("PaymentReleaseReleaser")
  heldPaymentReleases                 PaymentRelease[]               @relation("PaymentReleaseHolder")
  cancelledPaymentReleases            PaymentRelease[]               @relation("PaymentReleaseCanceller")
  reversedPaymentReleases             PaymentRelease[]               @relation("PaymentReleaseReverser")
  createdPaymentReleaseAllocations    PaymentReleaseAllocation[]     @relation("PaymentReleaseAllocationCreator")
  paymentReleaseExecutions            PaymentReleaseExecution[]      @relation("PaymentReleaseExecutionActor")
  ownedBudgets                        Budget[]                       @relation("BudgetOwner")
  createdBudgets                      Budget[]                       @relation("BudgetCreator")
  submittedBudgets                    Budget[]                       @relation("BudgetSubmitter")
  approvedBudgets                     Budget[]                       @relation("BudgetApprover")
  cancelledBudgets                    Budget[]                       @relation("BudgetCanceller")
  requestedBudgetRevisions            BudgetRevision[]               @relation("BudgetRevisionRequester")
  reviewedBudgetRevisions             BudgetRevision[]               @relation("BudgetRevisionReviewer")
  requestedBudgetCommitments          BudgetCommitment[]             @relation("BudgetCommitmentRequester")
  approvedBudgetCommitments           BudgetCommitment[]             @relation("BudgetCommitmentApprover")
  consumedBudgetCommitments           BudgetCommitment[]             @relation("BudgetCommitmentConsumer")
  reversedBudgetCommitments           BudgetCommitment[]             @relation("BudgetCommitmentReverser")
  requestedExpenseRequests            ExpenseRequest[]               @relation("ExpenseRequestRequester")
  submittedExpenseRequests            ExpenseRequest[]               @relation("ExpenseRequestSubmitter")
  approvedExpenseRequests             ExpenseRequest[]               @relation("ExpenseRequestApprover")
  returnedExpenseRequests             ExpenseRequest[]               @relation("ExpenseRequestReturner")
  rejectedExpenseRequests             ExpenseRequest[]               @relation("ExpenseRequestRejecter")
  cancelledExpenseRequests            ExpenseRequest[]               @relation("ExpenseRequestCanceller")
  completedExpenseRequests            ExpenseRequest[]               @relation("ExpenseRequestCompleter")
  reversedExpenseRequests             ExpenseRequest[]               @relation("ExpenseRequestReverser")
  createdExpenseRequestLines          ExpenseRequestLine[]           @relation("ExpenseRequestLineCreator")
  updatedExpenseRequestLines          ExpenseRequestLine[]           @relation("ExpenseRequestLineUpdater")
  createdExpenseRequestSourceLinks    ExpenseRequestSourceLink[]     @relation("ExpenseRequestSourceLinkCreator")
  financePayeeUserLinks               FinancePayee[]                 @relation("FinancePayeeUser")
  createdFinancePayees                FinancePayee[]                 @relation("FinancePayeeCreator")
  requestedDisbursements              NonSupplierDisbursementRequest[] @relation("DisbursementRequester")
  submittedDisbursements              NonSupplierDisbursementRequest[] @relation("DisbursementSubmitter")
  approvedDisbursements               NonSupplierDisbursementRequest[] @relation("DisbursementApprover")
  rejectedDisbursements               NonSupplierDisbursementRequest[] @relation("DisbursementRejecter")
  cancelledDisbursements              NonSupplierDisbursementRequest[] @relation("DisbursementCanceller")
  requestedCashAdvances               CashAdvanceRequest[]           @relation("CashAdvanceRequester")
  beneficiaryCashAdvances             CashAdvanceRequest[]           @relation("CashAdvanceBeneficiary")
  submittedCashAdvances               CashAdvanceRequest[]           @relation("CashAdvanceSubmitter")
  approvedCashAdvances                CashAdvanceRequest[]           @relation("CashAdvanceApprover")
  returnedCashAdvances                CashAdvanceRequest[]           @relation("CashAdvanceReturner")
  rejectedCashAdvances                CashAdvanceRequest[]           @relation("CashAdvanceRejecter")
  cancelledCashAdvances               CashAdvanceRequest[]           @relation("CashAdvanceCanceller")
  closedCashAdvances                  CashAdvanceRequest[]           @relation("CashAdvanceCloser")
  actedCashAdvanceMovements           CashAdvanceMovement[]          @relation("CashAdvanceMovementActor")
  submittedCashAdvanceLiquidations    CashAdvanceLiquidation[]       @relation("CashAdvanceLiquidationSubmitter")
  reviewedCashAdvanceLiquidations     CashAdvanceLiquidation[]       @relation("CashAdvanceLiquidationReviewer")
  approvedCashAdvanceLiquidations     CashAdvanceLiquidation[]       @relation("CashAdvanceLiquidationApprover")
  cancelledCashAdvanceLiquidations    CashAdvanceLiquidation[]       @relation("CashAdvanceLiquidationCanceller")
  closedCashAdvanceLiquidations       CashAdvanceLiquidation[]       @relation("CashAdvanceLiquidationCloser")
  createdCashAdvanceLines             CashAdvanceLiquidationLine[]   @relation("CashAdvanceLiquidationLineCreator")
  custodiedPettyCashFunds             PettyCashFund[]                @relation("PettyCashFundCustodian")
  createdPettyCashFunds               PettyCashFund[]                @relation("PettyCashFundCreator")
  updatedPettyCashFunds               PettyCashFund[]                @relation("PettyCashFundUpdater")
  requestedPettyCashRequests          PettyCashRequest[]             @relation("PettyCashRequestRequester")
  submittedPettyCashRequests          PettyCashRequest[]             @relation("PettyCashRequestSubmitter")
  approvedPettyCashRequests           PettyCashRequest[]             @relation("PettyCashRequestApprover")
  returnedPettyCashRequests           PettyCashRequest[]             @relation("PettyCashRequestReturner")
  rejectedPettyCashRequests           PettyCashRequest[]             @relation("PettyCashRequestRejecter")
  cancelledPettyCashRequests          PettyCashRequest[]             @relation("PettyCashRequestCanceller")
  closedPettyCashRequests             PettyCashRequest[]             @relation("PettyCashRequestCloser")
  postedPettyCashLedgerEntries        PettyCashLedgerEntry[]         @relation("PettyCashLedgerEntryPoster")
  voidedPettyCashLedgerEntries        PettyCashLedgerEntry[]         @relation("PettyCashLedgerEntryVoider")
  submittedPettyCashLiquidations      PettyCashLiquidation[]         @relation("PettyCashLiquidationSubmitter")
  reviewedPettyCashLiquidations       PettyCashLiquidation[]         @relation("PettyCashLiquidationReviewer")
  approvedPettyCashLiquidations       PettyCashLiquidation[]         @relation("PettyCashLiquidationApprover")
  cancelledPettyCashLiquidations      PettyCashLiquidation[]         @relation("PettyCashLiquidationCanceller")
  closedPettyCashLiquidations         PettyCashLiquidation[]         @relation("PettyCashLiquidationCloser")
  createdPettyCashLiquidationLines    PettyCashLiquidationLine[]     @relation("PettyCashLiquidationLineCreator")
  employeeProfiles                    Employee[]                     @relation("EmployeeUser")
  createdEmployees                    Employee[]                     @relation("EmployeeCreator")
  updatedEmployees                    Employee[]                     @relation("EmployeeUpdater")
  createdEmployeeAssignments          EmployeeAssignment[]           @relation("EmployeeAssignmentCreator")
  requestedEmployeeLeaveRequests      EmployeeLeaveRequest[]         @relation("EmployeeLeaveRequestedBy")
  approvedEmployeeLeaveRequests       EmployeeLeaveRequest[]         @relation("EmployeeLeaveApprovedBy")
  createdEmployeeLeaveRequests        EmployeeLeaveRequest[]         @relation("EmployeeLeaveCreatedBy")
  updatedEmployeeLeaveRequests        EmployeeLeaveRequest[]         @relation("EmployeeLeaveUpdatedBy")
  requestedEmployeeOvertimeRecords    EmployeeOvertimeRecord[]       @relation("EmployeeOvertimeRequestedBy")
  approvedEmployeeOvertimeRecords     EmployeeOvertimeRecord[]       @relation("EmployeeOvertimeApprovedBy")
  createdEmployeeOvertimeRecords      EmployeeOvertimeRecord[]       @relation("EmployeeOvertimeCreatedBy")
  updatedEmployeeOvertimeRecords      EmployeeOvertimeRecord[]       @relation("EmployeeOvertimeUpdatedBy")
  createdEmployeeTrainingRecords      EmployeeTrainingRecord[]       @relation("EmployeeTrainingCreatedBy")
  updatedEmployeeTrainingRecords      EmployeeTrainingRecord[]       @relation("EmployeeTrainingUpdatedBy")
  createdEmployeeDocuments            EmployeeComplianceDocument[]   @relation("EmployeeDocumentCreatedBy")
  updatedEmployeeDocuments            EmployeeComplianceDocument[]   @relation("EmployeeDocumentUpdatedBy")
  createdWorkforceSchedules           WorkforceSchedule[]            @relation("WorkforceScheduleCreator")
  submittedWorkforceSchedules         WorkforceSchedule[]            @relation("WorkforceScheduleSubmitter")
  approvedWorkforceSchedules          WorkforceSchedule[]            @relation("WorkforceScheduleApprover")
  publishedWorkforceSchedules         WorkforceSchedule[]            @relation("WorkforceSchedulePublisher")
  cancelledWorkforceSchedules         WorkforceSchedule[]            @relation("WorkforceScheduleCanceller")
  createdWorkforceScheduleLines       WorkforceScheduleLine[]        @relation("WorkforceScheduleLineCreator")
  updatedWorkforceScheduleLines       WorkforceScheduleLine[]        @relation("WorkforceScheduleLineUpdater")
  createdAttendanceImportBatches      AttendanceImportBatch[]        @relation("AttendanceImportBatchCreator")
  reviewedAttendanceImportBatches     AttendanceImportBatch[]        @relation("AttendanceImportBatchReviewer")
  voidedAttendanceImportBatches       AttendanceImportBatch[]        @relation("AttendanceImportBatchVoider")
  createdBankAccounts                 BankAccount[]                  @relation("BankAccountCreator")
  declaredBranchCashDeposits          BranchCashDeposit[]            @relation("BranchCashDepositDeclarer")
  verifiedBranchCashDeposits          BranchCashDeposit[]            @relation("BranchCashDepositVerifier")
  createdBankStatements               BankStatement[]                @relation("BankStatementCreator")
  preparedBankReconciliations         BankReconciliation[]           @relation("BankReconciliationPreparer")
  reviewedBankReconciliations         BankReconciliation[]           @relation("BankReconciliationReviewer")
  approvedBankReconciliations         BankReconciliation[]           @relation("BankReconciliationApprover")
  matchedBankReconciliationMatches    BankReconciliationMatch[]      @relation("BankReconciliationMatchMatcher")
  initiatedFinanceCloseRuns           FinanceCloseRun[]              @relation("FinanceCloseRunInitiator")
  ownedFinanceCloseChecklistItems     FinanceCloseChecklistItem[]    @relation("FinanceCloseChecklistOwner")
  completedFinanceCloseChecklistItems FinanceCloseChecklistItem[]    @relation("FinanceCloseChecklistCompleter")
  reviewedFinanceCloseChecklistItems  FinanceCloseChecklistItem[]    @relation("FinanceCloseChecklistReviewer")
  raisedFinanceCloseExceptions        FinanceCloseException[]        @relation("FinanceCloseExceptionRaiser")
  assignedFinanceCloseExceptions      FinanceCloseException[]        @relation("FinanceCloseExceptionAssignee")
  resolvedFinanceCloseExceptions      FinanceCloseException[]        @relation("FinanceCloseExceptionResolver")
  attemptedFinanceCloses              FinanceCloseAttempt[]          @relation("FinanceCloseAttemptActor")
  notifications                       Notification[]
  auditEvents                         AuditEvent[]

  @@unique([tenantId, email])
}

model Role {
  id                    String                 @id @default(uuid()) @db.Uuid
  tenantId              String?                @db.Uuid
  code                  String
  name                  String
  systemRole            Boolean                @default(false)
  status                RecordStatus           @default(ACTIVE)
  tenant                Tenant?                @relation(fields: [tenantId], references: [id])
  permissions           RolePermission[]
  assignments           UserRoleAssignment[]
  sensitiveRoleRequests SensitiveRoleRequest[]

  @@unique([tenantId, code])
}

model Permission {
  id          String           @id @default(uuid()) @db.Uuid
  tenantId    String?          @db.Uuid
  code        String           @unique
  module      String
  action      String
  description String?
  tenant      Tenant?          @relation(fields: [tenantId], references: [id])
  roles       RolePermission[]
}

model RolePermission {
  roleId       String     @db.Uuid
  permissionId String     @db.Uuid
  role         Role       @relation(fields: [roleId], references: [id])
  permission   Permission @relation(fields: [permissionId], references: [id])

  @@id([roleId, permissionId])
}

model UserRoleAssignment {
  id       String       @id @default(uuid()) @db.Uuid
  userId   String       @db.Uuid
  roleId   String       @db.Uuid
  startsAt DateTime     @default(now())
  endsAt   DateTime?
  status   RecordStatus @default(ACTIVE)
  user     User         @relation(fields: [userId], references: [id])
  role     Role         @relation(fields: [roleId], references: [id])
}

model UserScopeAssignment {
  id          String       @id @default(uuid()) @db.Uuid
  userId      String       @db.Uuid
  scopeType   ScopeType
  scopeId     String       @db.Uuid
  accessLevel AccessLevel
  startsAt    DateTime     @default(now())
  endsAt      DateTime?
  status      RecordStatus @default(ACTIVE)
  user        User         @relation(fields: [userId], references: [id])

  @@index([userId, scopeType, scopeId, status])
}

model HighRiskScopeRequest {
  id                String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId          String      @db.Uuid
  companyId         String      @db.Uuid
  targetUserId      String      @db.Uuid
  locationId        String      @db.Uuid
  accessLevel       AccessLevel
  status            String      @default("PENDING")
  reason            String
  evidenceReference String
  requestedByUserId String      @db.Uuid
  reviewedByUserId  String?     @db.Uuid
  reviewReason      String?
  createdAt         DateTime    @default(now())
  reviewedAt        DateTime?
  updatedAt         DateTime    @default(now()) @updatedAt
  tenant            Tenant      @relation(fields: [tenantId], references: [id])
  company           Company     @relation(fields: [companyId], references: [id])
  targetUser        User        @relation("HighRiskScopeRequestTargetUser", fields: [targetUserId], references: [id])
  location          Location    @relation(fields: [locationId], references: [id])
  requestedByUser   User        @relation("HighRiskScopeRequestRequester", fields: [requestedByUserId], references: [id])
  reviewedByUser    User?       @relation("HighRiskScopeRequestReviewer", fields: [reviewedByUserId], references: [id])

  @@index([tenantId, companyId, status])
  @@index([targetUserId, status])
  @@index([locationId, status])
}

model SensitiveRoleRequest {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId          String    @db.Uuid
  companyId         String    @db.Uuid
  targetUserId      String    @db.Uuid
  roleId            String    @db.Uuid
  status            String    @default("PENDING")
  reason            String
  evidenceReference String
  requestedByUserId String    @db.Uuid
  reviewedByUserId  String?   @db.Uuid
  reviewReason      String?
  createdAt         DateTime  @default(now())
  reviewedAt        DateTime?
  updatedAt         DateTime  @default(now()) @updatedAt
  tenant            Tenant    @relation(fields: [tenantId], references: [id])
  company           Company   @relation(fields: [companyId], references: [id])
  targetUser        User      @relation("SensitiveRoleRequestTargetUser", fields: [targetUserId], references: [id])
  role              Role      @relation(fields: [roleId], references: [id])
  requestedByUser   User      @relation("SensitiveRoleRequestRequester", fields: [requestedByUserId], references: [id])
  reviewedByUser    User?     @relation("SensitiveRoleRequestReviewer", fields: [reviewedByUserId], references: [id])

  @@index([tenantId, companyId, status])
  @@index([targetUserId, status])
  @@index([roleId, status])
}

model BreakGlassAccessGrant {
  id                          String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId                    String      @db.Uuid
  companyId                   String      @db.Uuid
  targetUserId                String      @db.Uuid
  locationId                  String      @db.Uuid
  accessLevel                 AccessLevel
  status                      String      @default("PENDING_REVIEW")
  reason                      String
  evidenceReference           String
  requestedUntil              DateTime
  assignmentId                String?     @db.Uuid
  requestedByUserId           String      @db.Uuid
  approvedByUserId            String?     @db.Uuid
  approvalReason              String?
  approvedAt                  DateTime?
  revokedByUserId             String?     @db.Uuid
  revocationReason            String?
  revokedAt                   DateTime?
  postReviewedByUserId        String?     @db.Uuid
  postReviewOutcome           String?
  postReviewReason            String?
  postReviewEvidenceReference String?
  postReviewedAt              DateTime?
  createdAt                   DateTime    @default(now())
  updatedAt                   DateTime    @default(now()) @updatedAt
  tenant                      Tenant      @relation(fields: [tenantId], references: [id])
  company                     Company     @relation(fields: [companyId], references: [id])
  targetUser                  User        @relation("BreakGlassAccessGrantTargetUser", fields: [targetUserId], references: [id])
  location                    Location    @relation(fields: [locationId], references: [id])
  requestedByUser             User        @relation("BreakGlassAccessGrantRequester", fields: [requestedByUserId], references: [id])
  approvedByUser              User?       @relation("BreakGlassAccessGrantApprover", fields: [approvedByUserId], references: [id])
  revokedByUser               User?       @relation("BreakGlassAccessGrantRevoker", fields: [revokedByUserId], references: [id])
  postReviewedByUser          User?       @relation("BreakGlassAccessGrantReviewer", fields: [postReviewedByUserId], references: [id])

  @@index([tenantId, companyId, status])
  @@index([targetUserId, status])
  @@index([locationId, status])
  @@index([requestedUntil, status])
}

model PrivilegedMfaEnrollment {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId          String    @db.Uuid
  companyId         String    @db.Uuid
  targetUserId      String    @db.Uuid
  providerName      String
  providerSubject   String?
  status            String    @default("PENDING_VERIFICATION")
  evidenceReference String
  attestationNote   String
  attestedByUserId  String    @db.Uuid
  attestedAt        DateTime  @default(now())
  verifiedByUserId  String?   @db.Uuid
  verificationNote  String?
  verifiedAt        DateTime?
  revokedByUserId   String?   @db.Uuid
  revocationReason  String?
  revokedAt         DateTime?
  sourceDecisionId  String    @default("DEC-0036")
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @default(now()) @updatedAt
  tenant            Tenant    @relation(fields: [tenantId], references: [id])
  company           Company   @relation(fields: [companyId], references: [id])
  targetUser        User      @relation("PrivilegedMfaEnrollmentTargetUser", fields: [targetUserId], references: [id])
  attestedByUser    User      @relation("PrivilegedMfaEnrollmentAttester", fields: [attestedByUserId], references: [id])
  verifiedByUser    User?     @relation("PrivilegedMfaEnrollmentVerifier", fields: [verifiedByUserId], references: [id])
  revokedByUser     User?     @relation("PrivilegedMfaEnrollmentRevoker", fields: [revokedByUserId], references: [id])

  @@unique([companyId, targetUserId, providerName])
  @@index([tenantId, companyId, status])
  @@index([targetUserId, status])
}

model AuthSessionInvalidation {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId          String    @db.Uuid
  companyId         String?   @db.Uuid
  targetUserId      String    @db.Uuid
  requestedByUserId String?   @db.Uuid
  status            String    @default("PENDING_PROVIDER")
  reason            String
  sourceEventType   String
  sourceRecordId    String?
  demoEpochEnforced Boolean   @default(true)
  providerName      String?
  providerReference String?
  completedAt       DateTime?
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @default(now()) @updatedAt
  tenant            Tenant    @relation(fields: [tenantId], references: [id])
  company           Company?  @relation(fields: [companyId], references: [id])
  targetUser        User      @relation("AuthSessionInvalidationTargetUser", fields: [targetUserId], references: [id])
  requestedByUser   User?     @relation("AuthSessionInvalidationRequestedBy", fields: [requestedByUserId], references: [id])

  @@index([tenantId, companyId, status])
  @@index([targetUserId, status])
  @@index([createdAt, status])
}

model ProjectTemplate {
  id                    String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId              String                @db.Uuid
  companyId             String                @db.Uuid
  code                  String
  name                  String
  projectType           String
  status                ProjectTemplateStatus @default(DRAFT)
  isRestrictedDefault   Boolean               @default(false)
  configJson            Json
  sourceTemplateId      String?               @db.Uuid
  lineageRootTemplateId String?               @db.Uuid
  revisionNumber        Int                   @default(1)
  version               Int                   @default(1)
  publishedAt           DateTime?
  publishReason         String?
  archivedAt            DateTime?
  archiveReason         String?
  createdByUserId       String                @db.Uuid
  updatedByUserId       String                @db.Uuid
  createdAt             DateTime              @default(now())
  updatedAt             DateTime              @updatedAt
  tenant                Tenant                @relation(fields: [tenantId], references: [id])
  company               Company               @relation(fields: [companyId], references: [id])
  sourceTemplate        ProjectTemplate?      @relation("ProjectTemplateRevisionSource", fields: [sourceTemplateId], references: [id], onDelete: Restrict)
  derivedRevisions      ProjectTemplate[]     @relation("ProjectTemplateRevisionSource")
  lineageRootTemplate   ProjectTemplate?      @relation("ProjectTemplateLineageRoot", fields: [lineageRootTemplateId], references: [id], onDelete: Restrict)
  lineageRevisions      ProjectTemplate[]     @relation("ProjectTemplateLineageRoot")
  createdBy             User                  @relation("ProjectTemplateCreator", fields: [createdByUserId], references: [id])
  updatedBy             User                  @relation("ProjectTemplateUpdater", fields: [updatedByUserId], references: [id])
  projects              Project[]

  @@unique([companyId, code])
  @@unique([companyId, lineageRootTemplateId, revisionNumber], map: "ProjectTemplate_companyId_lineageRootTemplateId_revisionNumber_")
  @@index([tenantId, companyId, status])
  @@index([tenantId, companyId, lineageRootTemplateId, revisionNumber], map: "ProjectTemplate_tenantId_companyId_lineageRootTemplateId_revisi")
  @@index([sourceTemplateId])
}

model Project {
  id                   String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId             String                     @db.Uuid
  companyId            String                     @db.Uuid
  templateId           String?                    @db.Uuid
  code                 String
  name                 String
  status               ProjectStatus              @default(DRAFT)
  projectType          String
  brandId              String?                    @db.Uuid
  locationId           String?                    @db.Uuid
  departmentId         String?                    @db.Uuid
  costCenterId         String?                    @db.Uuid
  sponsorUserId        String                     @db.Uuid
  managerUserId        String                     @db.Uuid
  isRestricted         Boolean                    @default(false)
  startAt              DateTime?
  targetEndAt          DateTime?
  actualEndAt          DateTime?
  startDate            DateTime?                  @db.Date
  targetEndDate        DateTime?                  @db.Date
  actualEndDate        DateTime?                  @db.Date
  description          String?
  templateSnapshotJson Json?
  projectConfigJson    Json?
  archivedAt           DateTime?
  archivedByUserId     String?                    @db.Uuid
  archiveReason        String?
  version              Int                        @default(1)
  createdByUserId      String                     @db.Uuid
  updatedByUserId      String                     @db.Uuid
  createdAt            DateTime                   @default(now())
  updatedAt            DateTime                   @updatedAt
  tenant               Tenant                     @relation(fields: [tenantId], references: [id])
  company              Company                    @relation(fields: [companyId], references: [id])
  template             ProjectTemplate?           @relation(fields: [templateId], references: [id])
  brand                Brand?                     @relation(fields: [brandId, tenantId, companyId], references: [id, tenantId, companyId], onDelete: Restrict)
  location             Location?                  @relation(fields: [locationId, tenantId, companyId], references: [id, tenantId, companyId], onDelete: Restrict)
  department           Department?                @relation(fields: [departmentId, tenantId, companyId], references: [id, tenantId, companyId], onDelete: Restrict)
  costCenter           CostCenter?                @relation(fields: [costCenterId, tenantId, companyId], references: [id, tenantId, companyId], onDelete: Restrict)
  sponsor              User                       @relation("ProjectSponsor", fields: [sponsorUserId], references: [id])
  manager              User                       @relation("ProjectManager", fields: [managerUserId], references: [id])
  archivedBy           User?                      @relation("ProjectArchiver", fields: [archivedByUserId], references: [id])
  createdBy            User                       @relation("ProjectCreator", fields: [createdByUserId], references: [id])
  updatedBy            User                       @relation("ProjectUpdater", fields: [updatedByUserId], references: [id])
  members              ProjectMember[]
  activityEvents       ProjectActivityEvent[]
  tasks                ProjectTask[]
  taskAssignees        ProjectTaskAssignee[]
  checklistItems       ProjectTaskChecklistItem[]
  comments             ProjectComment[]
  blockers             ProjectBlocker[]
  risks                ProjectRisk[]
  milestones           ProjectMilestone[]
  recordLinks          ProjectRecordLink[]
  attachments          ProjectAttachment[]
  requirements         ProjectRequirement[]

  @@unique([companyId, code])
  @@unique([id, tenantId, companyId])
  @@index([tenantId, companyId, status])
  @@index([tenantId, companyId, isRestricted])
  @@index([tenantId, companyId, brandId, status])
  @@index([tenantId, companyId, locationId, status])
  @@index([tenantId, companyId, departmentId, status])
  @@index([tenantId, companyId, costCenterId, status])
  @@index([managerUserId, status])
  @@index([sponsorUserId, status])
}

model ProjectMember {
  id              String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId        String            @db.Uuid
  companyId       String            @db.Uuid
  projectId       String            @db.Uuid
  userId          String            @db.Uuid
  projectRole     ProjectMemberRole
  status          RecordStatus      @default(ACTIVE)
  addedByUserId   String            @db.Uuid
  removedAt       DateTime?
  removedByUserId String?           @db.Uuid
  createdAt       DateTime          @default(now())
  updatedAt       DateTime          @updatedAt
  tenant          Tenant            @relation(fields: [tenantId], references: [id])
  company         Company           @relation(fields: [companyId], references: [id])
  project         Project           @relation(fields: [projectId], references: [id])
  user            User              @relation("ProjectMemberUser", fields: [userId], references: [id])
  addedBy         User              @relation("ProjectMemberAdder", fields: [addedByUserId], references: [id])
  removedBy       User?             @relation("ProjectMemberRemover", fields: [removedByUserId], references: [id])

  @@unique([projectId, userId])
  @@index([tenantId, companyId, status])
  @@index([tenantId, companyId, userId, status])
  @@index([projectId, projectRole, status])
}

model ProjectActivityEvent {
  id            String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId      String   @db.Uuid
  companyId     String   @db.Uuid
  projectId     String   @db.Uuid
  actorUserId   String   @db.Uuid
  eventType     String
  entityType    String
  entityId      String   @db.Uuid
  occurredAt    DateTime @default(now())
  requestId     String?
  correlationId String?
  reason        String?
  beforeData    Json?
  afterData     Json?
  metadata      Json?
  tenant        Tenant   @relation(fields: [tenantId], references: [id])
  company       Company  @relation(fields: [companyId], references: [id])
  project       Project  @relation(fields: [projectId], references: [id])
  actor         User     @relation("ProjectActivityActor", fields: [actorUserId], references: [id])

  @@index([tenantId, companyId, projectId, occurredAt], map: "ProjectActivityEvent_tenantId_companyId_projectId_occurredAt_id")
  @@index([tenantId, entityType, entityId, occurredAt], map: "ProjectActivityEvent_tenantId_entityType_entityId_occurredAt_id")
  @@index([actorUserId, occurredAt])
}

model ProjectTask {
  id                   String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId             String                     @db.Uuid
  companyId            String                     @db.Uuid
  projectId            String                     @db.Uuid
  taskKey              String
  title                String
  description          String?
  status               ProjectTaskStatus          @default(BACKLOG)
  priority             ProjectTaskPriority        @default(NORMAL)
  ownerUserId          String                     @db.Uuid
  startAt              DateTime?
  dueAt                DateTime?
  startDate            DateTime?                  @db.Date
  dueDate              DateTime?                  @db.Date
  isBlocked            Boolean                    @default(false)
  blockedReason        String?
  blockedAt            DateTime?
  blockedByUserId      String?                    @db.Uuid
  completedAt          DateTime?
  completedByUserId    String?                    @db.Uuid
  completionNote       String?
  cancelledAt          DateTime?
  cancelledByUserId    String?                    @db.Uuid
  cancelReason         String?
  lastReopenedAt       DateTime?
  lastReopenedByUserId String?                    @db.Uuid
  lastReopenReason     String?
  version              Int                        @default(1)
  archivedAt           DateTime?
  createdByUserId      String                     @db.Uuid
  updatedByUserId      String                     @db.Uuid
  createdAt            DateTime                   @default(now())
  updatedAt            DateTime                   @updatedAt
  tenant               Tenant                     @relation(fields: [tenantId], references: [id])
  company              Company                    @relation(fields: [companyId], references: [id])
  project              Project                    @relation(fields: [projectId, tenantId, companyId], references: [id, tenantId, companyId])
  owner                User                       @relation("ProjectTaskOwner", fields: [ownerUserId], references: [id])
  blockedBy            User?                      @relation("ProjectTaskBlocker", fields: [blockedByUserId], references: [id])
  completedBy          User?                      @relation("ProjectTaskCompleter", fields: [completedByUserId], references: [id])
  cancelledBy          User?                      @relation("ProjectTaskCanceller", fields: [cancelledByUserId], references: [id])
  lastReopenedBy       User?                      @relation("ProjectTaskReopener", fields: [lastReopenedByUserId], references: [id])
  createdBy            User                       @relation("ProjectTaskCreator", fields: [createdByUserId], references: [id])
  updatedBy            User                       @relation("ProjectTaskUpdater", fields: [updatedByUserId], references: [id])
  assignees            ProjectTaskAssignee[]
  checklistItems       ProjectTaskChecklistItem[]
  comments             ProjectComment[]
  blockers             ProjectBlocker[]
  risks                ProjectRisk[]
  recordLinks          ProjectRecordLink[]
  attachments          ProjectAttachment[]
  requirements         ProjectRequirement[]

  @@unique([projectId, taskKey])
  @@unique([id, tenantId, companyId, projectId])
  @@index([tenantId, companyId, projectId, status])
  @@index([tenantId, companyId, ownerUserId, status])
  @@index([tenantId, companyId, dueAt, status])
  @@index([projectId, priority, status])
}

model ProjectTaskAssignee {
  id               String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId         String       @db.Uuid
  companyId        String       @db.Uuid
  projectId        String       @db.Uuid
  taskId           String       @db.Uuid
  userId           String       @db.Uuid
  status           RecordStatus @default(ACTIVE)
  assignedByUserId String       @db.Uuid
  removedAt        DateTime?
  removedByUserId  String?      @db.Uuid
  createdAt        DateTime     @default(now())
  updatedAt        DateTime     @updatedAt
  tenant           Tenant       @relation(fields: [tenantId], references: [id])
  company          Company      @relation(fields: [companyId], references: [id])
  project          Project      @relation(fields: [projectId, tenantId, companyId], references: [id, tenantId, companyId])
  task             ProjectTask  @relation(fields: [taskId, tenantId, companyId, projectId], references: [id, tenantId, companyId, projectId])
  user             User         @relation("ProjectTaskAssigneeUser", fields: [userId], references: [id])
  assignedBy       User         @relation("ProjectTaskAssigner", fields: [assignedByUserId], references: [id])
  removedBy        User?        @relation("ProjectTaskAssignmentRemover", fields: [removedByUserId], references: [id])

  @@unique([taskId, userId])
  @@index([tenantId, companyId, userId, status])
  @@index([tenantId, companyId, projectId, status])
  @@index([taskId, status])
}

model ProjectTaskChecklistItem {
  id                String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId          String      @db.Uuid
  companyId         String      @db.Uuid
  projectId         String      @db.Uuid
  taskId            String      @db.Uuid
  title             String
  position          Int
  isRequired        Boolean     @default(false)
  isCompleted       Boolean     @default(false)
  completedAt       DateTime?
  completedByUserId String?     @db.Uuid
  archivedAt        DateTime?
  createdByUserId   String      @db.Uuid
  updatedByUserId   String      @db.Uuid
  createdAt         DateTime    @default(now())
  updatedAt         DateTime    @updatedAt
  tenant            Tenant      @relation(fields: [tenantId], references: [id])
  company           Company     @relation(fields: [companyId], references: [id])
  project           Project     @relation(fields: [projectId, tenantId, companyId], references: [id, tenantId, companyId])
  task              ProjectTask @relation(fields: [taskId, tenantId, companyId, projectId], references: [id, tenantId, companyId, projectId], map: "ProjectTaskChecklistItem_taskId_tenantId_companyId_projectId_fk")
  completedBy       User?       @relation("ProjectChecklistCompleter", fields: [completedByUserId], references: [id])
  createdBy         User        @relation("ProjectChecklistCreator", fields: [createdByUserId], references: [id])
  updatedBy         User        @relation("ProjectChecklistUpdater", fields: [updatedByUserId], references: [id])

  @@unique([taskId, position])
  @@index([tenantId, companyId, projectId, taskId], map: "ProjectTaskChecklistItem_tenantId_companyId_projectId_taskId_id")
}

model ProjectComment {
  id               String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId         String              @db.Uuid
  companyId        String              @db.Uuid
  projectId        String              @db.Uuid
  taskId           String              @db.Uuid
  authorUserId     String              @db.Uuid
  body             String
  status           RecordStatus        @default(ACTIVE)
  editedAt         DateTime?
  editedByUserId   String?             @db.Uuid
  archivedAt       DateTime?
  archivedByUserId String?             @db.Uuid
  archiveReason    String?
  createdAt        DateTime            @default(now())
  updatedAt        DateTime            @updatedAt
  tenant           Tenant              @relation(fields: [tenantId], references: [id])
  company          Company             @relation(fields: [companyId], references: [id])
  project          Project             @relation(fields: [projectId, tenantId, companyId], references: [id, tenantId, companyId])
  task             ProjectTask         @relation(fields: [taskId, tenantId, companyId, projectId], references: [id, tenantId, companyId, projectId])
  author           User                @relation("ProjectCommentAuthor", fields: [authorUserId], references: [id])
  editedBy         User?               @relation("ProjectCommentEditor", fields: [editedByUserId], references: [id])
  archivedBy       User?               @relation("ProjectCommentArchiver", fields: [archivedByUserId], references: [id])
  attachments      ProjectAttachment[]

  @@index([tenantId, companyId, projectId, taskId, createdAt], map: "ProjectComment_tenantId_companyId_projectId_taskId_createdAt_id")
  @@index([authorUserId, createdAt])
}

model ProjectAttachment {
  id               String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId         String          @db.Uuid
  companyId        String          @db.Uuid
  projectId        String          @db.Uuid
  taskId           String?         @db.Uuid
  commentId        String?         @db.Uuid
  requirementId    String?         @db.Uuid
  attachmentId     String          @db.Uuid
  purpose          String
  caption          String?
  status           RecordStatus    @default(ACTIVE)
  archivedAt       DateTime?
  archivedByUserId String?         @db.Uuid
  archiveReason    String?
  createdByUserId  String          @db.Uuid
  createdAt        DateTime        @default(now())
  updatedAt        DateTime        @updatedAt
  tenant           Tenant          @relation(fields: [tenantId], references: [id])
  company          Company         @relation(fields: [companyId], references: [id])
  project          Project         @relation(fields: [projectId, tenantId, companyId], references: [id, tenantId, companyId])
  task             ProjectTask?    @relation(fields: [taskId, tenantId, companyId, projectId], references: [id, tenantId, companyId, projectId])
  comment          ProjectComment?     @relation(fields: [commentId], references: [id], onDelete: Restrict)
  requirement      ProjectRequirement? @relation("ProjectRequirementAttachments", fields: [requirementId, tenantId, companyId, projectId], references: [id, tenantId, companyId, projectId], onDelete: Restrict, map: "ProjectAttachment_requirementId_tenantId_companyId_projectId_fk")
  attachment       Attachment      @relation(fields: [attachmentId], references: [id])
  createdBy        User            @relation("ProjectAttachmentCreator", fields: [createdByUserId], references: [id])
  archivedBy       User?           @relation("ProjectAttachmentArchiver", fields: [archivedByUserId], references: [id])

  @@index([tenantId, companyId, projectId, status])
  @@index([tenantId, companyId, taskId, status])
  @@index([tenantId, companyId, commentId, status])
  @@index([tenantId, companyId, requirementId, status])
  @@index([attachmentId, status])
}

model ProjectBlocker {
  id               String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId         String                 @db.Uuid
  companyId        String                 @db.Uuid
  projectId        String                 @db.Uuid
  taskId           String                 @db.Uuid
  reason           String
  blockerType      String
  severity         ProjectBlockerSeverity @default(MEDIUM)
  ownerUserId      String                 @db.Uuid
  reportedAt       DateTime               @default(now())
  reportedByUserId String                 @db.Uuid
  nextReviewAt     DateTime?
  status           ProjectBlockerStatus   @default(OPEN)
  resolvedAt       DateTime?
  resolvedByUserId String?                @db.Uuid
  resolutionNote   String?
  createdAt        DateTime               @default(now())
  updatedAt        DateTime               @updatedAt
  tenant           Tenant                 @relation(fields: [tenantId], references: [id])
  company          Company                @relation(fields: [companyId], references: [id])
  project          Project                @relation(fields: [projectId, tenantId, companyId], references: [id, tenantId, companyId])
  task             ProjectTask            @relation(fields: [taskId, tenantId, companyId, projectId], references: [id, tenantId, companyId, projectId])
  owner            User                   @relation("ProjectBlockerOwner", fields: [ownerUserId], references: [id])
  reportedBy       User                   @relation("ProjectBlockerReporter", fields: [reportedByUserId], references: [id])
  resolvedBy       User?                  @relation("ProjectBlockerResolver", fields: [resolvedByUserId], references: [id])

  @@index([tenantId, companyId, projectId, status])
  @@index([taskId, status])
  @@index([ownerUserId, status])
}

model ProjectMilestone {
  id                String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId          String                  @db.Uuid
  companyId         String                  @db.Uuid
  projectId         String                  @db.Uuid
  title             String
  description       String?
  status            ProjectMilestoneStatus  @default(PLANNED)
  dateKind          ProjectCalendarDateKind @default(DATE_ONLY)
  targetDate        DateTime?               @db.Date
  targetAt          DateTime?
  ownerUserId       String                  @db.Uuid
  isAtRisk          Boolean                 @default(false)
  atRiskReason      String?
  achievedAt        DateTime?
  achievedByUserId  String?                 @db.Uuid
  cancelledAt       DateTime?
  cancelledByUserId String?                 @db.Uuid
  cancelReason      String?
  version           Int                     @default(1)
  archivedAt        DateTime?
  createdByUserId   String                  @db.Uuid
  updatedByUserId   String                  @db.Uuid
  createdAt         DateTime                @default(now())
  updatedAt         DateTime                @updatedAt
  tenant            Tenant                  @relation(fields: [tenantId], references: [id])
  company           Company                 @relation(fields: [companyId], references: [id])
  project           Project                 @relation(fields: [projectId, tenantId, companyId], references: [id, tenantId, companyId])
  owner             User                    @relation("ProjectMilestoneOwner", fields: [ownerUserId], references: [id])
  achievedBy        User?                   @relation("ProjectMilestoneAchiever", fields: [achievedByUserId], references: [id])
  cancelledBy       User?                   @relation("ProjectMilestoneCanceller", fields: [cancelledByUserId], references: [id])
  createdBy         User                    @relation("ProjectMilestoneCreator", fields: [createdByUserId], references: [id])
  updatedBy         User                    @relation("ProjectMilestoneUpdater", fields: [updatedByUserId], references: [id])
  risks             ProjectRisk[]
  recordLinks       ProjectRecordLink[]

  @@unique([id, tenantId, companyId, projectId])
  @@index([tenantId, companyId, projectId, status])
  @@index([tenantId, companyId, targetDate, status])
  @@index([tenantId, companyId, targetAt, status])
  @@index([ownerUserId, status])
}

model ProjectRisk {
  id                   String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId             String            @db.Uuid
  companyId            String            @db.Uuid
  projectId            String            @db.Uuid
  taskId               String?           @db.Uuid
  milestoneId          String?           @db.Uuid
  title                String
  description          String?
  category             String
  likelihood           ProjectRiskRating
  impact               ProjectRiskRating
  severity             ProjectRiskRating
  status               ProjectRiskStatus @default(OPEN)
  ownerUserId          String            @db.Uuid
  targetMitigationDate DateTime?         @db.Date
  mitigationPlan       String?
  resolvedAt           DateTime?
  resolvedByUserId     String?           @db.Uuid
  resolutionNote       String?
  evidenceReference    String?
  lastReopenedAt       DateTime?
  lastReopenReason     String?
  version              Int               @default(1)
  archivedAt           DateTime?
  createdByUserId      String            @db.Uuid
  updatedByUserId      String            @db.Uuid
  createdAt            DateTime          @default(now())
  updatedAt            DateTime          @updatedAt
  tenant               Tenant            @relation(fields: [tenantId], references: [id])
  company              Company           @relation(fields: [companyId], references: [id])
  project              Project           @relation(fields: [projectId, tenantId, companyId], references: [id, tenantId, companyId])
  task                 ProjectTask?      @relation(fields: [taskId, tenantId, companyId, projectId], references: [id, tenantId, companyId, projectId])
  milestone            ProjectMilestone? @relation(fields: [milestoneId, tenantId, companyId, projectId], references: [id, tenantId, companyId, projectId])
  owner                User              @relation("ProjectRiskOwner", fields: [ownerUserId], references: [id])
  resolvedBy           User?             @relation("ProjectRiskResolver", fields: [resolvedByUserId], references: [id], onDelete: Restrict)
  createdBy            User              @relation("ProjectRiskCreator", fields: [createdByUserId], references: [id])
  updatedBy            User              @relation("ProjectRiskUpdater", fields: [updatedByUserId], references: [id])

  @@unique([id, tenantId, companyId, projectId])
  @@index([tenantId, companyId, projectId, status])
  @@index([tenantId, companyId, projectId, taskId])
  @@index([tenantId, companyId, projectId, milestoneId])
  @@index([tenantId, companyId, ownerUserId, status])
  @@index([tenantId, companyId, severity, status])
  @@index([tenantId, companyId, targetMitigationDate, status])
}

model ProjectRecordLink {
  id               String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId         String            @db.Uuid
  companyId        String            @db.Uuid
  projectId        String            @db.Uuid
  taskId           String?           @db.Uuid
  milestoneId      String?           @db.Uuid
  requirementId    String?           @db.Uuid
  sourceRecordType String
  sourceRecordId   String            @db.Uuid
  relationType     String            @default("RELATED")
  linkLabel        String
  createdByUserId  String            @db.Uuid
  updatedByUserId  String            @db.Uuid
  archivedAt       DateTime?
  archivedByUserId String?           @db.Uuid
  archiveReason    String?
  createdAt        DateTime          @default(now())
  updatedAt        DateTime          @updatedAt
  tenant           Tenant            @relation(fields: [tenantId], references: [id])
  company          Company           @relation(fields: [companyId], references: [id])
  project          Project           @relation(fields: [projectId, tenantId, companyId], references: [id, tenantId, companyId])
  task             ProjectTask?      @relation(fields: [taskId, tenantId, companyId, projectId], references: [id, tenantId, companyId, projectId])
  milestone        ProjectMilestone? @relation(fields: [milestoneId, tenantId, companyId, projectId], references: [id, tenantId, companyId, projectId])
  requirement      ProjectRequirement? @relation("ProjectRequirementRecordLinks", fields: [requirementId, tenantId, companyId, projectId], references: [id, tenantId, companyId, projectId], onDelete: Restrict, map: "ProjectRecordLink_requirementId_tenantId_companyId_projectId_fk")
  createdBy        User              @relation("ProjectRecordLinkCreator", fields: [createdByUserId], references: [id])
  updatedBy        User              @relation("ProjectRecordLinkUpdater", fields: [updatedByUserId], references: [id])
  archivedBy       User?             @relation("ProjectRecordLinkArchiver", fields: [archivedByUserId], references: [id])

  @@unique([id, tenantId, companyId, projectId])
  @@index([tenantId, companyId, projectId, archivedAt])
  @@index([tenantId, companyId, projectId, taskId, archivedAt], map: "ProjectRecordLink_tenantId_companyId_projectId_taskId_archivedA")
  @@index([tenantId, companyId, projectId, milestoneId, archivedAt], map: "ProjectRecordLink_tenantId_companyId_projectId_milestoneId_arch")
  @@index([tenantId, companyId, projectId, requirementId, archivedAt], map: "ProjectRecordLink_tenantId_companyId_projectId_requirementId_ar")
  @@index([tenantId, companyId, sourceRecordType, sourceRecordId], map: "ProjectRecordLink_tenantId_companyId_sourceRecordType_sourceRec")
  @@index([createdByUserId, createdAt])
}

model ProjectRequirement {
  id                String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId             String                   @db.Uuid
  companyId            String                   @db.Uuid
  projectId            String                   @db.Uuid
  taskId               String?                  @db.Uuid
  kind                 ProjectRequirementKind
  code                 String
  label                String
  evidenceType         String?
  signoffStage         String?
  evidenceNote         String?
  isRequired           Boolean                  @default(true)
  ownerUserId          String                   @db.Uuid
  reviewerUserId       String?                  @db.Uuid
  status               ProjectRequirementStatus @default(PENDING)
  submittedAt          DateTime?
  submittedByUserId    String?                  @db.Uuid
  decisionAt           DateTime?
  decidedByUserId      String?                  @db.Uuid
  decisionReason       String?
  version              Int                      @default(1)
  archivedAt           DateTime?
  createdByUserId      String                   @db.Uuid
  updatedByUserId      String                   @db.Uuid
  createdAt            DateTime                 @default(now())
  updatedAt            DateTime                 @updatedAt
  tenant               Tenant                   @relation(fields: [tenantId], references: [id])
  company              Company                  @relation(fields: [companyId], references: [id])
  project           Project                  @relation(fields: [projectId, tenantId, companyId], references: [id, tenantId, companyId], map: "ProjectRequirement_projectId_tenantId_companyId_fkey")
  task              ProjectTask?             @relation(fields: [taskId, tenantId, companyId, projectId], references: [id, tenantId, companyId, projectId], onDelete: Restrict, map: "ProjectRequirement_taskId_tenantId_companyId_projectId_fkey")
  owner                User                     @relation("ProjectRequirementOwner", fields: [ownerUserId], references: [id])
  reviewer             User?                    @relation("ProjectRequirementReviewer", fields: [reviewerUserId], references: [id])
  submittedBy          User?                    @relation("ProjectRequirementSubmitter", fields: [submittedByUserId], references: [id])
  decidedBy            User?                    @relation("ProjectRequirementDecider", fields: [decidedByUserId], references: [id])
  createdBy            User                     @relation("ProjectRequirementCreator", fields: [createdByUserId], references: [id])
  updatedBy            User                     @relation("ProjectRequirementUpdater", fields: [updatedByUserId], references: [id])
  attachments          ProjectAttachment[] @relation("ProjectRequirementAttachments")
  recordLinks          ProjectRecordLink[] @relation("ProjectRequirementRecordLinks")

  @@unique([projectId, kind, code])
  @@unique([id, tenantId, companyId, projectId])
  @@index([tenantId, companyId, projectId, status])
  @@index([tenantId, companyId, ownerUserId, status])
  @@index([tenantId, companyId, reviewerUserId, status])
}

model Supplier {
  id                  String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId            String                       @db.Uuid
  companyId           String                       @db.Uuid
  supplierCode        String
  legalName           String
  tradingName         String?
  taxIdentifier       String?
  status              RecordStatus                 @default(ACTIVE)
  accreditationStatus SupplierAccreditationStatus  @default(PENDING_REVIEW)
  paymentTerms        String?
  createdAt           DateTime                     @default(now())
  updatedAt           DateTime                     @default(now()) @updatedAt
  tenant              Tenant                       @relation(fields: [tenantId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  company             Company                      @relation(fields: [companyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  contacts            SupplierContact[]
  itemLinks           SupplierItemLink[]
  priceHistory        SupplierPriceHistory[]
  quotations          SupplierQuotation[]
  purchaseOrders      PurchaseOrder[]
  goodsReceipts       GoodsReceipt[]
  apInvoices          ApInvoice[]
  supplierCreditNotes SupplierCreditNote[]
  financePayees       FinancePayee[]
  paymentRequests     PaymentRequest[]
  paymentReleases     PaymentRelease[]
  expenseRequests     ExpenseRequest[]
  cashAdvanceRequests CashAdvanceRequest[]
  cashAdvanceLines    CashAdvanceLiquidationLine[]
  pettyCashLines      PettyCashLiquidationLine[]

  @@unique([companyId, supplierCode])
  @@index([tenantId, companyId, status])
  @@index([tenantId, companyId, accreditationStatus])
}

model SupplierContact {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  supplierId String   @db.Uuid
  name       String
  role       String?
  email      String?
  phone      String?
  isPrimary  Boolean  @default(false)
  createdAt  DateTime @default(now())
  supplier   Supplier @relation(fields: [supplierId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([supplierId, isPrimary])
}

model ItemCategory {
  id                          String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId                    String         @db.Uuid
  companyId                   String         @db.Uuid
  parentId                    String?        @db.Uuid
  categoryCode                String
  categoryName                String
  inventoryClass              String
  requiresExpiryTracking      Boolean        @default(false)
  requiresLotTracking         Boolean        @default(false)
  defaultWastageRequiresPhoto Boolean        @default(false)
  status                      RecordStatus   @default(ACTIVE)
  createdAt                   DateTime       @default(now())
  updatedAt                   DateTime       @default(now()) @updatedAt
  tenant                      Tenant         @relation(fields: [tenantId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  company                     Company        @relation(fields: [companyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  parent                      ItemCategory?  @relation("ItemCategoryHierarchy", fields: [parentId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  children                    ItemCategory[] @relation("ItemCategoryHierarchy")
  items                       Item[]

  @@unique([companyId, categoryCode])
  @@index([tenantId, companyId, status])
}

model Uom {
  id                            String                         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId                      String                         @db.Uuid
  companyId                     String                         @db.Uuid
  uomCode                       String
  uomName                       String
  uomType                       String
  decimalPrecision              Int                            @default(0)
  status                        RecordStatus                   @default(ACTIVE)
  createdAt                     DateTime                       @default(now())
  updatedAt                     DateTime                       @default(now()) @updatedAt
  tenant                        Tenant                         @relation(fields: [tenantId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  company                       Company                        @relation(fields: [companyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  baseItems                     Item[]                         @relation("BaseUom")
  purchaseItems                 Item[]                         @relation("PurchaseUom")
  issueItems                    Item[]                         @relation("IssueUom")
  conversionsFrom               ItemUomConversion[]            @relation("ConversionFromUom")
  conversionsTo                 ItemUomConversion[]            @relation("ConversionToUom")
  purchaseRequestLines          PurchaseRequestLine[]
  supplierItemLinks             SupplierItemLink[]
  supplierPriceHistory          SupplierPriceHistory[]
  supplierQuoteLines            SupplierQuotationLine[]
  purchaseOrderLines            PurchaseOrderLine[]
  enteredInventoryMovements     InventoryMovement[]            @relation("InventoryMovementEnteredUom")
  baseInventoryMovements        InventoryMovement[]            @relation("InventoryMovementBaseUom")
  inventoryBalances             InventoryBalance[]             @relation("InventoryBalanceBaseUom")
  goodsReceiptLines             GoodsReceiptLine[]
  inventoryTransferLines        InventoryTransferLine[]
  inventoryTransferReceiptLines InventoryTransferReceiptLine[]
  stockCountLines               StockCountLine[]
  wastageLines                  WastageLine[]
  stockAdjustmentLines          StockAdjustmentLine[]
  recipeVersionYields           RecipeVersion[]                @relation("RecipeVersionYieldUom")
  recipeVersionServings         RecipeVersion[]                @relation("RecipeVersionServingUom")
  recipeLines                   RecipeLine[]

  @@unique([companyId, uomCode])
  @@index([tenantId, companyId, status])
}

model Item {
  id                            String                         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId                      String                         @db.Uuid
  companyId                     String                         @db.Uuid
  itemCode                      String
  itemName                      String
  itemCategoryId                String                         @db.Uuid
  itemType                      String
  baseUomId                     String                         @db.Uuid
  purchaseUomId                 String?                        @db.Uuid
  issueUomId                    String?                        @db.Uuid
  trackInventory                Boolean                        @default(true)
  trackExpiry                   Boolean                        @default(false)
  trackLot                      Boolean                        @default(false)
  requiresReceivingInspection   Boolean                        @default(false)
  status                        RecordStatus                   @default(ACTIVE)
  createdAt                     DateTime                       @default(now())
  updatedAt                     DateTime                       @default(now()) @updatedAt
  tenant                        Tenant                         @relation(fields: [tenantId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  company                       Company                        @relation(fields: [companyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  category                      ItemCategory                   @relation(fields: [itemCategoryId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  baseUom                       Uom                            @relation("BaseUom", fields: [baseUomId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  purchaseUom                   Uom?                           @relation("PurchaseUom", fields: [purchaseUomId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  issueUom                      Uom?                           @relation("IssueUom", fields: [issueUomId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  uomConversions                ItemUomConversion[]
  purchaseRequestLines          PurchaseRequestLine[]
  supplierItemLinks             SupplierItemLink[]
  supplierPriceHistory          SupplierPriceHistory[]
  supplierQuoteLines            SupplierQuotationLine[]
  purchaseOrderLines            PurchaseOrderLine[]
  inventoryMovements            InventoryMovement[]
  inventoryBalances             InventoryBalance[]
  goodsReceiptLines             GoodsReceiptLine[]
  inventoryTransferLines        InventoryTransferLine[]
  inventoryTransferReceiptLines InventoryTransferReceiptLine[]
  stockCountLines               StockCountLine[]
  wastageLines                  WastageLine[]
  stockAdjustmentLines          StockAdjustmentLine[]
  recipeLines                   RecipeLine[]

  @@unique([companyId, itemCode])
  @@index([tenantId, companyId, status])
}

model ItemUomConversion {
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  itemId           String   @db.Uuid
  fromUomId        String   @db.Uuid
  toUomId          String   @db.Uuid
  conversionFactor Decimal  @db.Decimal(18, 6)
  roundingRule     String
  createdAt        DateTime @default(now())
  item             Item     @relation(fields: [itemId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  fromUom          Uom      @relation("ConversionFromUom", fields: [fromUomId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  toUom            Uom      @relation("ConversionToUom", fields: [toUomId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([itemId, fromUomId, toUomId])
  @@index([itemId])
}

model SupplierItemLink {
  id               String                 @id @default(uuid()) @db.Uuid
  tenantId         String                 @db.Uuid
  companyId        String                 @db.Uuid
  supplierId       String                 @db.Uuid
  itemId           String                 @db.Uuid
  purchaseUomId    String                 @db.Uuid
  supplierSku      String?
  supplierItemName String?
  leadTimeDays     Int?
  minOrderQty      Decimal?               @db.Decimal(18, 6)
  preferredRank    Int?
  status           RecordStatus           @default(ACTIVE)
  createdAt        DateTime               @default(now())
  updatedAt        DateTime               @updatedAt
  tenant           Tenant                 @relation(fields: [tenantId], references: [id])
  company          Company                @relation(fields: [companyId], references: [id])
  supplier         Supplier               @relation(fields: [supplierId], references: [id])
  item             Item                   @relation(fields: [itemId], references: [id])
  purchaseUom      Uom                    @relation(fields: [purchaseUomId], references: [id])
  priceHistory     SupplierPriceHistory[]

  @@unique([supplierId, itemId, purchaseUomId])
  @@index([tenantId, companyId, status])
  @@index([itemId, status])
}

model SupplierPriceHistory {
  id                 String            @id @default(uuid()) @db.Uuid
  tenantId           String            @db.Uuid
  companyId          String            @db.Uuid
  supplierId         String            @db.Uuid
  itemId             String            @db.Uuid
  supplierItemLinkId String?           @db.Uuid
  uomId              String            @db.Uuid
  currencyCode       String
  unitPrice          Decimal           @db.Decimal(18, 6)
  effectiveFrom      DateTime
  effectiveTo        DateTime?
  sourceDocumentId   String?           @db.Uuid
  createdAt          DateTime          @default(now())
  tenant             Tenant            @relation(fields: [tenantId], references: [id])
  company            Company           @relation(fields: [companyId], references: [id])
  supplier           Supplier          @relation(fields: [supplierId], references: [id])
  item               Item              @relation(fields: [itemId], references: [id])
  supplierItemLink   SupplierItemLink? @relation(fields: [supplierItemLinkId], references: [id])
  uom                Uom               @relation(fields: [uomId], references: [id])

  @@index([tenantId, companyId, supplierId, itemId, effectiveFrom], map: "SupplierPriceHistory_tenantId_companyId_supplierId_itemId_effec")
  @@index([supplierItemLinkId, effectiveFrom])
}

model Recipe {
  id                 String          @id @default(uuid()) @db.Uuid
  tenantId           String          @db.Uuid
  companyId          String          @db.Uuid
  brandId            String?         @db.Uuid
  currentVersionId   String?         @db.Uuid
  publishedVersionId String?         @db.Uuid
  recipeCode         String
  recipeName         String
  recipeType         String          @default("MENU")
  ownerDepartment    String?
  status             RecordStatus    @default(ACTIVE)
  createdByUserId    String?         @db.Uuid
  createdAt          DateTime        @default(now())
  updatedAt          DateTime        @updatedAt
  tenant             Tenant          @relation(fields: [tenantId], references: [id])
  company            Company         @relation(fields: [companyId], references: [id])
  brand              Brand?          @relation(fields: [brandId], references: [id])
  versions           RecipeVersion[]

  @@unique([companyId, recipeCode])
  @@index([tenantId, companyId, brandId, status])
  @@index([currentVersionId])
  @@index([publishedVersionId])
}

model RecipeVersion {
  id                    String       @id @default(uuid()) @db.Uuid
  tenantId              String       @db.Uuid
  companyId             String       @db.Uuid
  recipeId              String       @db.Uuid
  versionNo             Int
  status                String       @default("DRAFT")
  yieldQuantity         Decimal      @db.Decimal(18, 6)
  yieldUomId            String       @db.Uuid
  servingQuantity       Decimal      @db.Decimal(18, 6)
  servingUomId          String       @db.Uuid
  targetFoodCostPercent Decimal?     @db.Decimal(9, 4)
  supersedesVersionId   String?      @db.Uuid
  publishedAt           DateTime?
  publishedByUserId     String?      @db.Uuid
  reason                String?
  notes                 String?
  effectiveFrom         DateTime?
  effectiveTo           DateTime?
  approvedAt            DateTime?
  approvedByUserId      String?      @db.Uuid
  createdByUserId       String?      @db.Uuid
  createdAt             DateTime     @default(now())
  updatedAt             DateTime     @updatedAt
  tenant                Tenant       @relation(fields: [tenantId], references: [id])
  company               Company      @relation(fields: [companyId], references: [id])
  recipe                Recipe       @relation(fields: [recipeId], references: [id])
  yieldUom              Uom          @relation("RecipeVersionYieldUom", fields: [yieldUomId], references: [id])
  servingUom            Uom          @relation("RecipeVersionServingUom", fields: [servingUomId], references: [id])
  lines                 RecipeLine[] @relation("RecipeVersionLines")
  usedAsSubRecipeLines  RecipeLine[] @relation("RecipeLineSubRecipeVersion")
  menuItems             MenuItem[]

  @@unique([recipeId, versionNo])
  @@index([tenantId, companyId, status])
  @@index([supersedesVersionId])
  @@index([publishedByUserId])
}

model RecipeLine {
  id                 String         @id @default(uuid()) @db.Uuid
  tenantId           String         @db.Uuid
  companyId          String         @db.Uuid
  recipeVersionId    String         @db.Uuid
  lineNo             Int
  lineType           String         @default("INGREDIENT")
  itemId             String?        @db.Uuid
  subRecipeVersionId String?        @db.Uuid
  quantity           Decimal        @db.Decimal(18, 6)
  uomId              String         @db.Uuid
  yieldLossPercent   Decimal?       @db.Decimal(9, 4)
  preparationNote    String?
  createdAt          DateTime       @default(now())
  tenant             Tenant         @relation(fields: [tenantId], references: [id])
  company            Company        @relation(fields: [companyId], references: [id])
  recipeVersion      RecipeVersion  @relation("RecipeVersionLines", fields: [recipeVersionId], references: [id])
  item               Item?          @relation(fields: [itemId], references: [id])
  subRecipeVersion   RecipeVersion? @relation("RecipeLineSubRecipeVersion", fields: [subRecipeVersionId], references: [id])
  uom                Uom            @relation(fields: [uomId], references: [id])

  @@unique([recipeVersionId, lineNo])
  @@index([tenantId, companyId])
  @@index([itemId])
  @@index([subRecipeVersionId])
}

model MenuItem {
  id                     String                      @id @default(uuid()) @db.Uuid
  tenantId               String                      @db.Uuid
  companyId              String                      @db.Uuid
  brandId                String?                     @db.Uuid
  menuItemCode           String
  menuItemName           String
  menuCategory           String?
  currentRecipeVersionId String?                     @db.Uuid
  status                 RecordStatus                @default(ACTIVE)
  createdAt              DateTime                    @default(now())
  updatedAt              DateTime                    @updatedAt
  tenant                 Tenant                      @relation(fields: [tenantId], references: [id])
  company                Company                     @relation(fields: [companyId], references: [id])
  brand                  Brand?                      @relation(fields: [brandId], references: [id])
  currentRecipeVersion   RecipeVersion?              @relation(fields: [currentRecipeVersionId], references: [id])
  prices                 MenuPrice[]
  salesImportLines       RestaurantSalesImportLine[]

  @@unique([companyId, menuItemCode])
  @@index([tenantId, companyId, brandId, status])
}

model MenuPrice {
  id            String    @id @default(uuid()) @db.Uuid
  tenantId      String    @db.Uuid
  companyId     String    @db.Uuid
  menuItemId    String    @db.Uuid
  locationId    String?   @db.Uuid
  decisionId    String?   @db.Uuid
  status        String    @default("APPLIED")
  currencyCode  String
  price         Decimal   @db.Decimal(18, 6)
  effectiveFrom DateTime
  effectiveTo   DateTime?
  createdAt     DateTime  @default(now())
  tenant        Tenant    @relation(fields: [tenantId], references: [id])
  company       Company   @relation(fields: [companyId], references: [id])
  menuItem      MenuItem  @relation(fields: [menuItemId], references: [id])
  location      Location? @relation(fields: [locationId], references: [id])

  @@index([tenantId, companyId, menuItemId, effectiveFrom])
  @@index([locationId])
  @@index([decisionId])
  @@index([status])
}

model RecipeVersionTransition {
  id                String    @id @default(uuid()) @db.Uuid
  tenantId          String    @db.Uuid
  companyId         String    @db.Uuid
  brandId           String?   @db.Uuid
  recipeId          String    @db.Uuid
  recipeVersionId   String    @db.Uuid
  action            String
  fromStatus        String
  toStatus          String
  actorUserId       String?   @db.Uuid
  approvedByUserId  String?   @db.Uuid
  approvedAt        DateTime?
  reason            String?
  evidenceReference String?
  idempotencyKey    String?
  createdAt         DateTime  @default(now())

  @@unique([recipeVersionId, idempotencyKey])
  @@index([tenantId, companyId, brandId, recipeId])
  @@index([recipeVersionId, createdAt])
  @@index([action, toStatus])
}

model MenuPriceDecision {
  id                String    @id @default(uuid()) @db.Uuid
  tenantId          String    @db.Uuid
  companyId         String    @db.Uuid
  brandId           String?   @db.Uuid
  menuItemId        String    @db.Uuid
  locationId        String?   @db.Uuid
  requestedPrice    Decimal   @db.Decimal(18, 6)
  currencyCode      String
  effectiveFrom     DateTime
  effectiveTo       DateTime?
  status            String    @default("DRAFT")
  requestedByUserId String?   @db.Uuid
  approvedByUserId  String?   @db.Uuid
  approvedAt        DateTime?
  appliedByUserId   String?   @db.Uuid
  appliedAt         DateTime?
  reason            String?
  evidenceReference String?
  idempotencyKey    String?
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt

  @@unique([tenantId, companyId, menuItemId, locationId, idempotencyKey], map: "MenuPriceDecision_tenantId_companyId_menuItemId_locationId_idem")
  @@index([tenantId, companyId, brandId, locationId, status], map: "MenuPriceDecision_tenantId_companyId_brandId_locationId_status_")
  @@index([menuItemId, effectiveFrom])
}

model RestaurantSalesImportBatch {
  id               String                      @id @default(uuid()) @db.Uuid
  tenantId         String                      @db.Uuid
  companyId        String                      @db.Uuid
  brandId          String?                     @db.Uuid
  locationId       String                      @db.Uuid
  businessDate     DateTime
  sourceSystem     String
  importRef        String
  status           String                      @default("POSTED")
  currencyCode     String
  totalNetSales    Decimal                     @db.Decimal(18, 6)
  totalQuantity    Decimal                     @db.Decimal(18, 6)
  importedByUserId String?                     @db.Uuid
  importedAt       DateTime                    @default(now())
  createdAt        DateTime                    @default(now())
  tenant           Tenant                      @relation(fields: [tenantId], references: [id])
  company          Company                     @relation(fields: [companyId], references: [id])
  brand            Brand?                      @relation(fields: [brandId], references: [id])
  location         Location                    @relation(fields: [locationId], references: [id])
  lines            RestaurantSalesImportLine[]

  @@unique([companyId, locationId, businessDate, sourceSystem, importRef], map: "RestaurantSalesImportBatch_companyId_locationId_businessDate_so")
  @@index([tenantId, companyId, brandId, locationId, businessDate, status], map: "RestaurantSalesImportBatch_tenantId_companyId_brandId_locationI")
}

model RestaurantSalesImportLine {
  id               String                     @id @default(uuid()) @db.Uuid
  tenantId         String                     @db.Uuid
  companyId        String                     @db.Uuid
  brandId          String?                    @db.Uuid
  locationId       String                     @db.Uuid
  batchId          String                     @db.Uuid
  menuItemId       String                     @db.Uuid
  businessDate     DateTime
  salesChannel     String                     @default("STORE")
  quantitySold     Decimal                    @db.Decimal(18, 6)
  grossSalesAmount Decimal                    @db.Decimal(18, 6)
  discountAmount   Decimal                    @db.Decimal(18, 6)
  netSalesAmount   Decimal                    @db.Decimal(18, 6)
  currencyCode     String
  createdAt        DateTime                   @default(now())
  tenant           Tenant                     @relation(fields: [tenantId], references: [id])
  company          Company                    @relation(fields: [companyId], references: [id])
  brand            Brand?                     @relation(fields: [brandId], references: [id])
  location         Location                   @relation(fields: [locationId], references: [id])
  batch            RestaurantSalesImportBatch @relation(fields: [batchId], references: [id])
  menuItem         MenuItem                   @relation(fields: [menuItemId], references: [id])

  @@unique([batchId, menuItemId, salesChannel])
  @@index([tenantId, companyId, brandId, locationId, businessDate], map: "RestaurantSalesImportLine_tenantId_companyId_brandId_locationId")
  @@index([menuItemId, businessDate])
}

model BranchOperationalChecklist {
  id                String                           @id @default(uuid()) @db.Uuid
  tenantId          String                           @db.Uuid
  companyId         String                           @db.Uuid
  brandId           String?                          @db.Uuid
  locationId        String                           @db.Uuid
  businessDate      DateTime
  shiftType         String
  status            String                           @default("IN_PROGRESS")
  checklistName     String
  openedByUserId    String?                          @db.Uuid
  submittedByUserId String?                          @db.Uuid
  submittedAt       DateTime?
  reviewedByUserId  String?                          @db.Uuid
  reviewedAt        DateTime?
  exceptionCount    Int                              @default(0)
  completionPercent Decimal                          @default(0) @db.Decimal(9, 4)
  createdAt         DateTime                         @default(now())
  updatedAt         DateTime                         @updatedAt
  tenant            Tenant                           @relation(fields: [tenantId], references: [id])
  company           Company                          @relation(fields: [companyId], references: [id])
  brand             Brand?                           @relation(fields: [brandId], references: [id])
  location          Location                         @relation(fields: [locationId], references: [id])
  lines             BranchOperationalChecklistLine[]

  @@unique([companyId, locationId, businessDate, shiftType], map: "BranchOperationalChecklist_companyId_locationId_businessDate_sh")
  @@index([tenantId, companyId, brandId, locationId, businessDate, status], map: "BranchOperationalChecklist_tenantId_companyId_brandId_locationI")
}

model BranchOperationalChecklistLine {
  id                String                     @id @default(uuid()) @db.Uuid
  tenantId          String                     @db.Uuid
  companyId         String                     @db.Uuid
  brandId           String?                    @db.Uuid
  locationId        String                     @db.Uuid
  checklistId       String                     @db.Uuid
  lineNo            Int
  area              String
  checkName         String
  expectedResult    String
  result            String                     @default("PENDING")
  severity          String                     @default("NORMAL")
  evidenceReference String?
  notes             String?
  completedByUserId String?                    @db.Uuid
  completedAt       DateTime?
  createdAt         DateTime                   @default(now())
  tenant            Tenant                     @relation(fields: [tenantId], references: [id])
  company           Company                    @relation(fields: [companyId], references: [id])
  brand             Brand?                     @relation(fields: [brandId], references: [id])
  location          Location                   @relation(fields: [locationId], references: [id])
  checklist         BranchOperationalChecklist @relation(fields: [checklistId], references: [id])

  @@unique([checklistId, lineNo])
  @@index([tenantId, companyId, brandId, locationId], map: "BranchOperationalChecklistLine_tenantId_companyId_brandId_locat")
  @@index([result, severity])
}

model FoodSafetyLog {
  id               String              @id @default(uuid()) @db.Uuid
  tenantId         String              @db.Uuid
  companyId        String              @db.Uuid
  brandId          String?             @db.Uuid
  locationId       String              @db.Uuid
  businessDate     DateTime
  logType          String
  status           String              @default("IN_PROGRESS")
  title            String
  recordedByUserId String?             @db.Uuid
  reviewedByUserId String?             @db.Uuid
  reviewedAt       DateTime?
  exceptionCount   Int                 @default(0)
  createdAt        DateTime            @default(now())
  updatedAt        DateTime            @updatedAt
  tenant           Tenant              @relation(fields: [tenantId], references: [id])
  company          Company             @relation(fields: [companyId], references: [id])
  brand            Brand?              @relation(fields: [brandId], references: [id])
  location         Location            @relation(fields: [locationId], references: [id])
  readings         FoodSafetyReading[]

  @@unique([companyId, locationId, businessDate, logType])
  @@index([tenantId, companyId, brandId, locationId, businessDate, status], map: "FoodSafetyLog_tenantId_companyId_brandId_locationId_businessDat")
}

model FoodSafetyReading {
  id                String        @id @default(uuid()) @db.Uuid
  tenantId          String        @db.Uuid
  companyId         String        @db.Uuid
  brandId           String?       @db.Uuid
  locationId        String        @db.Uuid
  logId             String        @db.Uuid
  lineNo            Int
  station           String
  readingType       String
  readingValue      Decimal?      @db.Decimal(18, 6)
  readingUom        String?
  expectedMinValue  Decimal?      @db.Decimal(18, 6)
  expectedMaxValue  Decimal?      @db.Decimal(18, 6)
  result            String        @default("PENDING")
  severity          String        @default("NORMAL")
  correctiveAction  String?
  evidenceReference String?
  recordedAt        DateTime?
  createdAt         DateTime      @default(now())
  tenant            Tenant        @relation(fields: [tenantId], references: [id])
  company           Company       @relation(fields: [companyId], references: [id])
  brand             Brand?        @relation(fields: [brandId], references: [id])
  location          Location      @relation(fields: [locationId], references: [id])
  log               FoodSafetyLog @relation(fields: [logId], references: [id])

  @@unique([logId, lineNo])
  @@index([tenantId, companyId, brandId, locationId])
  @@index([result, severity])
}

model OperationalIncident {
  id                String    @id @default(uuid()) @db.Uuid
  tenantId          String    @db.Uuid
  companyId         String    @db.Uuid
  brandId           String?   @db.Uuid
  locationId        String    @db.Uuid
  incidentNumber    String
  incidentDate      DateTime
  category          String
  severity          String
  status            String    @default("OPEN")
  title             String
  summary           String
  reportedByUserId  String?   @db.Uuid
  ownerUserId       String?   @db.Uuid
  sourceRecordType  String?
  sourceRecordId    String?   @db.Uuid
  correctiveAction  String?
  evidenceReference String?
  dueAt             DateTime?
  resolvedAt        DateTime?
  closedAt          DateTime?
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt
  tenant            Tenant    @relation(fields: [tenantId], references: [id])
  company           Company   @relation(fields: [companyId], references: [id])
  brand             Brand?    @relation(fields: [brandId], references: [id])
  location          Location  @relation(fields: [locationId], references: [id])

  @@unique([companyId, incidentNumber])
  @@index([tenantId, companyId, brandId, locationId, incidentDate, status], map: "OperationalIncident_tenantId_companyId_brandId_locationId_incid")
  @@index([severity, status])
  @@index([sourceRecordType, sourceRecordId])
}

model MaintenanceTicket {
  id                String    @id @default(uuid()) @db.Uuid
  tenantId          String    @db.Uuid
  companyId         String    @db.Uuid
  brandId           String?   @db.Uuid
  locationId        String    @db.Uuid
  ticketNumber      String
  requestedAt       DateTime
  category          String
  assetName         String
  assetArea         String
  priority          String
  status            String    @default("OPEN")
  title             String
  description       String
  reportedByUserId  String?   @db.Uuid
  ownerUserId       String?   @db.Uuid
  sourceIncidentId  String?   @db.Uuid
  downtimeMinutes   Int?
  targetDueAt       DateTime?
  completedAt       DateTime?
  correctiveAction  String?
  evidenceReference String?
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt
  tenant            Tenant    @relation(fields: [tenantId], references: [id])
  company           Company   @relation(fields: [companyId], references: [id])
  brand             Brand?    @relation(fields: [brandId], references: [id])
  location          Location  @relation(fields: [locationId], references: [id])

  @@unique([companyId, ticketNumber])
  @@index([tenantId, companyId, brandId, locationId, requestedAt, status], map: "MaintenanceTicket_tenantId_companyId_brandId_locationId_request")
  @@index([priority, status])
  @@index([sourceIncidentId])
}

model OperationalCorrectionRecord {
  id                String    @id @default(uuid()) @db.Uuid
  tenantId          String    @db.Uuid
  companyId         String    @db.Uuid
  brandId           String?   @db.Uuid
  locationId        String?   @db.Uuid
  targetEntityType  String
  targetEntityId    String    @db.Uuid
  correctionType    String
  status            String    @default("DRAFT")
  requestedByUserId String?   @db.Uuid
  approvedByUserId  String?   @db.Uuid
  approvedAt        DateTime?
  appliedByUserId   String?   @db.Uuid
  appliedAt         DateTime?
  reason            String
  evidenceReference String?
  idempotencyKey    String?
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt

  @@unique([tenantId, companyId, targetEntityType, targetEntityId, idempotencyKey], map: "OperationalCorrectionRecord_tenantId_companyId_targetEntityType")
  @@index([tenantId, companyId, brandId, locationId, status], map: "OperationalCorrectionRecord_tenantId_companyId_brandId_location")
  @@index([targetEntityType, targetEntityId])
  @@index([correctionType, status])
}

model OperationalStatusTransition {
  id                String   @id @default(uuid()) @db.Uuid
  tenantId          String   @db.Uuid
  companyId         String   @db.Uuid
  brandId           String?  @db.Uuid
  locationId        String?  @db.Uuid
  targetEntityType  String
  targetEntityId    String   @db.Uuid
  action            String
  fromStatus        String
  toStatus          String
  actorUserId       String?  @db.Uuid
  reason            String?
  evidenceReference String?
  idempotencyKey    String?
  createdAt         DateTime @default(now())

  @@unique([tenantId, companyId, targetEntityType, targetEntityId, idempotencyKey], map: "OperationalStatusTransition_tenantId_companyId_targetEntityType")
  @@index([tenantId, companyId, brandId, locationId], map: "OperationalStatusTransition_tenantId_companyId_brandId_location")
  @@index([targetEntityType, targetEntityId, createdAt], map: "OperationalStatusTransition_targetEntityType_targetEntityId_cre")
  @@index([action, fromStatus, toStatus])
}

model WorkflowTransitionPolicy {
  id                 String   @id @default(uuid()) @db.Uuid
  domain             String
  action             String
  fromStatus         String
  toStatus           String
  permissionCode     String
  requiresReason     Boolean  @default(false)
  requiresEvidence   Boolean  @default(false)
  blocksSelfApproval Boolean  @default(false)
  terminal           Boolean  @default(false)
  active             Boolean  @default(true)
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  @@unique([domain, action, fromStatus])
  @@index([domain, fromStatus, active])
  @@index([permissionCode])
}

model PurchaseRequest {
  id                  String                   @id @default(uuid()) @db.Uuid
  publicReference     String
  tenantId            String                   @db.Uuid
  companyId           String                   @db.Uuid
  brandId             String?                  @db.Uuid
  requestLocationId   String                   @db.Uuid
  requesterUserId     String                   @db.Uuid
  departmentId        String?                  @db.Uuid
  costCenterId        String?                  @db.Uuid
  requiredDate        DateTime
  urgency             String
  justification       String
  status              PurchaseRequestStatus    @default(DRAFT)
  currentApprovalStep Int?
  version             Int                      @default(1)
  createdAt           DateTime                 @default(now())
  updatedAt           DateTime                 @updatedAt
  tenant              Tenant                   @relation(fields: [tenantId], references: [id])
  company             Company                  @relation(fields: [companyId], references: [id])
  brand               Brand?                   @relation(fields: [brandId], references: [id])
  requestLocation     Location                 @relation(fields: [requestLocationId], references: [id])
  requester           User                     @relation("PurchaseRequestRequester", fields: [requesterUserId], references: [id])
  department          Department?              @relation(fields: [departmentId], references: [id])
  costCenter          CostCenter?              @relation(fields: [costCenterId], references: [id])
  lines               PurchaseRequestLine[]
  comments            PurchaseRequestComment[]
  quotationRequests   QuotationRequest[]
  purchaseOrders      PurchaseOrder[]

  @@unique([companyId, publicReference])
  @@index([tenantId, companyId, status])
  @@index([tenantId, requestLocationId, status])
}

model PurchaseRequestComment {
  id                String          @id @default(uuid()) @db.Uuid
  purchaseRequestId String          @db.Uuid
  tenantId          String          @db.Uuid
  companyId         String          @db.Uuid
  authorUserId      String          @db.Uuid
  body              String
  createdAt         DateTime        @default(now())
  tenant            Tenant          @relation(fields: [tenantId], references: [id])
  company           Company         @relation(fields: [companyId], references: [id])
  purchaseRequest   PurchaseRequest @relation(fields: [purchaseRequestId], references: [id])
  author            User            @relation(fields: [authorUserId], references: [id])

  @@index([tenantId, companyId, purchaseRequestId, createdAt], map: "PurchaseRequestComment_tenantId_companyId_purchaseRequestId_cre")
}

model PurchaseRequestLine {
  id                 String                  @id @default(uuid()) @db.Uuid
  purchaseRequestId  String                  @db.Uuid
  itemId             String?                 @db.Uuid
  uomId              String?                 @db.Uuid
  lineNumber         Int
  description        String
  requestedQty       Decimal                 @db.Decimal(18, 6)
  estimatedUnitCost  Decimal                 @default(0) @db.Decimal(18, 6)
  estimatedLineTotal Decimal                 @default(0) @db.Decimal(18, 6)
  budgetLineId       String?                 @db.Uuid
  uomCode            String
  purpose            String
  notes              String?
  purchaseRequest    PurchaseRequest         @relation(fields: [purchaseRequestId], references: [id])
  item               Item?                   @relation(fields: [itemId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  uom                Uom?                    @relation(fields: [uomId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  budgetLine         BudgetLine?             @relation(fields: [budgetLineId], references: [id])
  supplierQuoteLines SupplierQuotationLine[]
  purchaseOrderLines PurchaseOrderLine[]

  @@unique([purchaseRequestId, lineNumber])
  @@index([itemId])
  @@index([uomId])
  @@index([budgetLineId])
}

model QuotationRequest {
  id                String                    @id @default(uuid()) @db.Uuid
  tenantId          String                    @db.Uuid
  companyId         String                    @db.Uuid
  publicReference   String
  purchaseRequestId String                    @db.Uuid
  status            String                    @default("OPEN")
  requiredDate      DateTime
  createdByUserId   String                    @db.Uuid
  createdAt         DateTime                  @default(now())
  tenant            Tenant                    @relation(fields: [tenantId], references: [id])
  company           Company                   @relation(fields: [companyId], references: [id])
  purchaseRequest   PurchaseRequest           @relation(fields: [purchaseRequestId], references: [id])
  createdBy         User                      @relation(fields: [createdByUserId], references: [id])
  supplierQuotes    SupplierQuotation[]
  recommendations   QuotationRecommendation[]
  purchaseOrders    PurchaseOrder[]

  @@unique([companyId, publicReference])
  @@unique([purchaseRequestId])
  @@unique([id, tenantId, companyId])
  @@index([tenantId, companyId, status])
}

model SupplierQuotation {
  id                 String                    @id @default(uuid()) @db.Uuid
  quotationRequestId String                    @db.Uuid
  tenantId           String                    @db.Uuid
  companyId          String                    @db.Uuid
  supplierId         String                    @db.Uuid
  quoteReference     String
  quoteDate          DateTime
  currencyCode       String
  totalAmount        Decimal                   @db.Decimal(18, 6)
  validityDate       DateTime?
  terms              String?
  status             String                    @default("RECORDED")
  createdAt          DateTime                  @default(now())
  tenant             Tenant                    @relation(fields: [tenantId], references: [id])
  company            Company                   @relation(fields: [companyId], references: [id])
  quotationRequest   QuotationRequest          @relation(fields: [quotationRequestId], references: [id])
  supplier           Supplier                  @relation(fields: [supplierId], references: [id])
  lines              SupplierQuotationLine[]
  recommendations    QuotationRecommendation[]
  purchaseOrders     PurchaseOrder[]

  @@unique([id, quotationRequestId])
  @@index([tenantId, companyId, supplierId])
  @@index([quotationRequestId])
}

model QuotationRecommendation {
  id                          String            @id @default(uuid()) @db.Uuid
  tenantId                    String            @db.Uuid
  companyId                   String            @db.Uuid
  quotationRequestId          String            @db.Uuid
  selectedSupplierQuotationId String            @db.Uuid
  preparedByUserId            String            @db.Uuid
  status                      String            @default("DRAFT")
  currencyCode                String
  selectedEvaluatedTotal      Decimal           @db.Decimal(18, 6)
  lowestEvaluatedTotal        Decimal           @db.Decimal(18, 6)
  quoteCount                  Int
  isLowestEvaluatedCost       Boolean
  selectionReason             String
  nonLowestJustification      String?
  singleSourceJustification   String?
  evaluationSnapshot          Json
  version                     Int               @default(1)
  submittedAt                 DateTime?
  approvedAt                  DateTime?
  createdAt                   DateTime          @default(now())
  updatedAt                   DateTime          @default(now()) @updatedAt
  tenant                      Tenant            @relation(fields: [tenantId], references: [id])
  company                     Company           @relation(fields: [companyId], references: [id])
  quotationRequest            QuotationRequest  @relation(fields: [quotationRequestId, tenantId, companyId], references: [id, tenantId, companyId], map: "QuotationRecommendation_quotationRequest_scope_fkey")
  selectedSupplierQuotation   SupplierQuotation @relation(fields: [selectedSupplierQuotationId, quotationRequestId], references: [id, quotationRequestId], map: "QuotationRecommendation_selectedQuote_request_fkey")
  preparedBy                  User              @relation(fields: [preparedByUserId], references: [id])
  purchaseOrder               PurchaseOrder?

  @@unique([quotationRequestId, version])
  @@index([tenantId, companyId, status])
  @@index([quotationRequestId, status])
  @@index([selectedSupplierQuotationId])
}

model SupplierQuotationLine {
  id                  String               @id @default(uuid()) @db.Uuid
  supplierQuotationId String               @db.Uuid
  sourcePrLineId      String?              @db.Uuid
  itemId              String?              @db.Uuid
  quantity            Decimal              @db.Decimal(18, 6)
  uomId               String               @db.Uuid
  unitPrice           Decimal              @db.Decimal(18, 6)
  lineTotal           Decimal              @db.Decimal(18, 6)
  availabilityStatus  String
  leadTimeDays        Int?
  notes               String?
  supplierQuotation   SupplierQuotation    @relation(fields: [supplierQuotationId], references: [id])
  sourcePrLine        PurchaseRequestLine? @relation(fields: [sourcePrLineId], references: [id])
  item                Item?                @relation(fields: [itemId], references: [id])
  uom                 Uom                  @relation(fields: [uomId], references: [id])
  purchaseOrderLines  PurchaseOrderLine[]

  @@index([supplierQuotationId])
  @@index([sourcePrLineId])
}

model PurchaseOrder {
  id                          String                        @id @default(uuid()) @db.Uuid
  tenantId                    String                        @db.Uuid
  companyId                   String                        @db.Uuid
  brandId                     String?                       @db.Uuid
  publicReference             String
  purchaseRequestId           String                        @db.Uuid
  quotationRequestId          String                        @db.Uuid
  quotationRecommendationId   String                        @unique @db.Uuid
  selectedSupplierQuotationId String                        @db.Uuid
  supplierId                  String                        @db.Uuid
  deliveryLocationId          String                        @db.Uuid
  departmentId                String?                       @db.Uuid
  costCenterId                String?                       @db.Uuid
  currencyCode                String
  subtotalAmount              Decimal                       @default(0) @db.Decimal(18, 6)
  taxAmount                   Decimal                       @default(0) @db.Decimal(18, 6)
  discountAmount              Decimal                       @default(0) @db.Decimal(18, 6)
  totalAmount                 Decimal                       @db.Decimal(18, 6)
  expectedDeliveryDate        DateTime
  status                      String                        @default("DRAFT")
  cancellationSubtype         String?
  cancellationReason          String?
  cancelledAt                 DateTime?
  cancelledByUserId           String?                       @db.Uuid
  sourceSnapshot              Json
  createdByUserId             String                        @db.Uuid
  createdAt                   DateTime                      @default(now())
  updatedAt                   DateTime                      @updatedAt
  tenant                      Tenant                        @relation(fields: [tenantId], references: [id])
  company                     Company                       @relation(fields: [companyId], references: [id])
  brand                       Brand?                        @relation(fields: [brandId], references: [id])
  purchaseRequest             PurchaseRequest               @relation(fields: [purchaseRequestId], references: [id])
  quotationRequest            QuotationRequest              @relation(fields: [quotationRequestId, tenantId, companyId], references: [id, tenantId, companyId], map: "PurchaseOrder_quotationRequest_scope_fkey")
  quotationRecommendation     QuotationRecommendation       @relation(fields: [quotationRecommendationId], references: [id])
  selectedSupplierQuotation   SupplierQuotation             @relation(fields: [selectedSupplierQuotationId, quotationRequestId], references: [id, quotationRequestId], map: "PurchaseOrder_selectedQuote_request_fkey")
  supplier                    Supplier                      @relation(fields: [supplierId], references: [id])
  deliveryLocation            Location                      @relation("PurchaseOrderDeliveryLocation", fields: [deliveryLocationId], references: [id])
  department                  Department?                   @relation(fields: [departmentId], references: [id])
  costCenter                  CostCenter?                   @relation(fields: [costCenterId], references: [id])
  createdBy                   User                          @relation(fields: [createdByUserId], references: [id])
  cancelledBy                 User?                         @relation("PurchaseOrderCanceller", fields: [cancelledByUserId], references: [id], onDelete: SetNull)
  lines                       PurchaseOrderLine[]
  goodsReceipts               GoodsReceipt[]
  amendments                  PurchaseOrderAmendment[]
  balanceClosures             PurchaseOrderBalanceClosure[]
  apInvoices                  ApInvoice[]

  @@unique([companyId, publicReference])
  @@index([tenantId, companyId, status])
  @@index([tenantId, deliveryLocationId, status])
  @@index([purchaseRequestId])
  @@index([quotationRequestId])
  @@index([selectedSupplierQuotationId])
  @@index([supplierId])
  @@index([tenantId, companyId, cancellationSubtype], map: "PurchaseOrder_tenantId_companyId_cancellationSubtype_idx")
  @@index([cancelledByUserId])
  @@index([cancelledAt])
}

model PurchaseOrderLine {
  id                        String                 @id @default(uuid()) @db.Uuid
  purchaseOrderId           String                 @db.Uuid
  tenantId                  String                 @db.Uuid
  companyId                 String                 @db.Uuid
  sourcePrLineId            String?                @db.Uuid
  sourceSupplierQuoteLineId String?                @db.Uuid
  itemId                    String?                @db.Uuid
  uomId                     String                 @db.Uuid
  lineNumber                Int
  description               String
  orderedQty                Decimal                @db.Decimal(18, 6)
  receivedQty               Decimal                @default(0) @db.Decimal(18, 6)
  cancelledQty              Decimal                @default(0) @db.Decimal(18, 6)
  unitPrice                 Decimal                @db.Decimal(18, 6)
  taxAmount                 Decimal                @default(0) @db.Decimal(18, 6)
  discountAmount            Decimal                @default(0) @db.Decimal(18, 6)
  lineTotal                 Decimal                @db.Decimal(18, 6)
  budgetLineId              String?                @db.Uuid
  availabilityStatus        String?
  leadTimeDays              Int?
  notes                     String?
  purchaseOrder             PurchaseOrder          @relation(fields: [purchaseOrderId], references: [id])
  tenant                    Tenant                 @relation(fields: [tenantId], references: [id])
  company                   Company                @relation(fields: [companyId], references: [id])
  sourcePrLine              PurchaseRequestLine?   @relation(fields: [sourcePrLineId], references: [id])
  sourceSupplierQuoteLine   SupplierQuotationLine? @relation(fields: [sourceSupplierQuoteLineId], references: [id])
  item                      Item?                  @relation(fields: [itemId], references: [id])
  uom                       Uom                    @relation(fields: [uomId], references: [id])
  budgetLine                BudgetLine?            @relation(fields: [budgetLineId], references: [id])
  goodsReceiptLines         GoodsReceiptLine[]
  apInvoiceLines            ApInvoiceLine[]
  apInvoiceMatchResults     ApInvoiceMatchResult[]

  @@unique([purchaseOrderId, lineNumber])
  @@index([tenantId, companyId])
  @@index([sourcePrLineId])
  @@index([sourceSupplierQuoteLineId])
  @@index([itemId])
  @@index([tenantId, companyId, budgetLineId])
}

model InventoryLocation {
  id                                String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId                          String                  @db.Uuid
  companyId                         String                  @db.Uuid
  locationId                        String                  @db.Uuid
  code                              String
  name                              String
  storageType                       String?
  status                            RecordStatus            @default(ACTIVE)
  createdAt                         DateTime                @default(now())
  updatedAt                         DateTime                @updatedAt
  tenant                            Tenant                  @relation(fields: [tenantId], references: [id])
  company                           Company                 @relation(fields: [companyId], references: [id])
  location                          Location                @relation(fields: [locationId], references: [id])
  movements                         InventoryMovement[]
  relatedMovements                  InventoryMovement[]     @relation("InventoryMovementRelatedInventoryLocation")
  balances                          InventoryBalance[]
  goodsReceiptLines                 GoodsReceiptLine[]
  sourceInventoryTransferLines      InventoryTransferLine[] @relation("InventoryTransferLineSourceInventoryLocation")
  destinationInventoryTransferLines InventoryTransferLine[] @relation("InventoryTransferLineDestinationInventoryLocation")
  stockCountSessions                StockCountSession[]
  stockCountLines                   StockCountLine[]
  wastageReports                    WastageReport[]
  wastageLines                      WastageLine[]
  wastagePolicies                   WastagePolicy[]
  stockAdjustments                  StockAdjustment[]
  stockAdjustmentLines              StockAdjustmentLine[]

  @@unique([companyId, code])
  @@index([tenantId, companyId, locationId, status])
}

model PurchaseOrderBalanceClosure {
  id                              String        @id @default(uuid()) @db.Uuid
  tenantId                        String        @db.Uuid
  companyId                       String        @db.Uuid
  purchaseOrderId                 String        @db.Uuid
  requestedByUserId               String        @db.Uuid
  approvedByUserId                String?       @db.Uuid
  status                          String        @default("PENDING_APPROVAL")
  reason                          String
  supplierNoticeReference         String?
  supplierNoticeUnavailableReason String?
  notes                           String?
  lineSnapshot                    Json
  totalClosedQuantity             Decimal       @default(0) @db.Decimal(18, 6)
  totalClosedValue                Decimal       @default(0) @db.Decimal(18, 6)
  requestedAt                     DateTime      @default(now())
  approvedAt                      DateTime?
  rejectedAt                      DateTime?
  rejectionReason                 String?
  createdAt                       DateTime      @default(now())
  updatedAt                       DateTime      @updatedAt
  tenant                          Tenant        @relation(fields: [tenantId], references: [id])
  company                         Company       @relation(fields: [companyId], references: [id])
  purchaseOrder                   PurchaseOrder @relation(fields: [purchaseOrderId], references: [id])
  requestedBy                     User          @relation("PurchaseOrderBalanceClosureRequester", fields: [requestedByUserId], references: [id])
  approvedBy                      User?         @relation("PurchaseOrderBalanceClosureApprover", fields: [approvedByUserId], references: [id])

  @@index([tenantId, companyId, status, requestedAt], map: "PurchaseOrderBalanceClosure_tenantId_companyId_status_requested")
  @@index([purchaseOrderId, status])
  @@index([requestedByUserId])
  @@index([approvedByUserId])
}

model PurchaseOrderAmendment {
  id                              String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId                        String        @db.Uuid
  companyId                       String        @db.Uuid
  purchaseOrderId                 String        @db.Uuid
  requestedByUserId               String        @db.Uuid
  approvedByUserId                String?       @db.Uuid
  status                          String        @default("PENDING_APPROVAL")
  reason                          String
  supplierNoticeReference         String?
  supplierNoticeUnavailableReason String?
  beforeSnapshot                  Json
  proposedSnapshot                Json
  requestedAt                     DateTime      @default(now())
  approvedAt                      DateTime?
  rejectedAt                      DateTime?
  rejectionReason                 String?
  appliedAt                       DateTime?
  createdAt                       DateTime      @default(now())
  updatedAt                       DateTime      @updatedAt
  tenant                          Tenant        @relation(fields: [tenantId], references: [id])
  company                         Company       @relation(fields: [companyId], references: [id])
  purchaseOrder                   PurchaseOrder @relation(fields: [purchaseOrderId], references: [id])
  requestedBy                     User          @relation("PurchaseOrderAmendmentRequester", fields: [requestedByUserId], references: [id])
  approvedBy                      User?         @relation("PurchaseOrderAmendmentApprover", fields: [approvedByUserId], references: [id])

  @@index([tenantId, companyId, status, requestedAt], map: "PurchaseOrderAmendment_tenantId_companyId_status_requestedAt_id")
  @@index([purchaseOrderId, status])
  @@index([requestedByUserId])
  @@index([approvedByUserId])
}

model StockCountSession {
  id                  String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId            String            @db.Uuid
  companyId           String            @db.Uuid
  inventoryLocationId String            @db.Uuid
  publicReference     String
  countType           String
  scopeType           String            @default("ALL_ITEMS")
  status              String            @default("DRAFT")
  blindCount          Boolean           @default(true)
  freezeMovements     Boolean           @default(false)
  scheduledDate       DateTime?
  cutoffAt            DateTime?
  startedAt           DateTime?
  submittedAt         DateTime?
  reviewedAt          DateTime?
  cancelledAt         DateTime?
  cancellationReason  String?
  reviewNotes         String?
  createdByUserId     String            @db.Uuid
  assignedToUserId    String?           @db.Uuid
  reviewedByUserId    String?           @db.Uuid
  createdAt           DateTime          @default(now())
  updatedAt           DateTime          @updatedAt
  tenant              Tenant            @relation(fields: [tenantId], references: [id])
  company             Company           @relation(fields: [companyId], references: [id])
  inventoryLocation   InventoryLocation @relation(fields: [inventoryLocationId], references: [id])
  createdBy           User              @relation("StockCountCreator", fields: [createdByUserId], references: [id])
  assignedTo          User?             @relation("StockCountAssignee", fields: [assignedToUserId], references: [id])
  reviewedBy          User?             @relation("StockCountReviewer", fields: [reviewedByUserId], references: [id])
  lines               StockCountLine[]
  stockAdjustments    StockAdjustment[]

  @@unique([companyId, publicReference])
  @@index([tenantId, companyId, status, scheduledDate])
  @@index([tenantId, companyId, inventoryLocationId, status], map: "StockCountSession_tenantId_companyId_inventoryLocationId_status")
}

model StockCountLine {
  id                      String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  stockCountSessionId     String                @db.Uuid
  tenantId                String                @db.Uuid
  companyId               String                @db.Uuid
  inventoryLocationId     String                @db.Uuid
  itemId                  String                @db.Uuid
  uomId                   String                @db.Uuid
  lineNumber              Int
  lotKey                  String                @default("NOLOT|NOEXP")
  lotNumber               String?
  expiryDate              DateTime?
  systemQuantityBaseUom   Decimal               @default(0) @db.Decimal(18, 6)
  countedQuantityBaseUom  Decimal?              @db.Decimal(18, 6)
  varianceQuantityBaseUom Decimal?              @db.Decimal(18, 6)
  notes                   String?
  countedByUserId         String?               @db.Uuid
  countedAt               DateTime?
  createdAt               DateTime              @default(now())
  updatedAt               DateTime              @updatedAt
  stockCountSession       StockCountSession     @relation(fields: [stockCountSessionId], references: [id])
  tenant                  Tenant                @relation(fields: [tenantId], references: [id])
  company                 Company               @relation(fields: [companyId], references: [id])
  inventoryLocation       InventoryLocation     @relation(fields: [inventoryLocationId], references: [id])
  item                    Item                  @relation(fields: [itemId], references: [id])
  uom                     Uom                   @relation(fields: [uomId], references: [id])
  countedBy               User?                 @relation("StockCountLineCounter", fields: [countedByUserId], references: [id])
  stockAdjustmentLines    StockAdjustmentLine[]

  @@unique([stockCountSessionId, itemId, lotKey])
  @@unique([stockCountSessionId, lineNumber])
  @@index([tenantId, companyId, stockCountSessionId, itemId], map: "StockCountLine_tenantId_companyId_stockCountSessionId_itemId_id")
  @@index([inventoryLocationId, itemId, lotKey])
}

model WastagePolicy {
  id                      String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId                String             @db.Uuid
  companyId               String             @db.Uuid
  name                    String
  policyVersion           String             @default("v1")
  priority                Int                @default(100)
  isActive                Boolean            @default(true)
  inventoryLocationId     String?            @db.Uuid
  wastageType             String?
  reasonCode              String?
  minimumEstimatedCost    Decimal?           @db.Decimal(18, 6)
  requiresEvidence        Boolean            @default(false)
  repeatLookbackDays      Int                @default(30)
  repeatItemLocationCount Int                @default(3)
  repeatReporterCount     Int                @default(5)
  notes                   String?
  createdAt               DateTime           @default(now())
  updatedAt               DateTime           @updatedAt
  tenant                  Tenant             @relation(fields: [tenantId], references: [id])
  company                 Company            @relation(fields: [companyId], references: [id])
  inventoryLocation       InventoryLocation? @relation(fields: [inventoryLocationId], references: [id])

  @@index([tenantId, companyId, isActive, priority])
  @@index([tenantId, companyId, inventoryLocationId, wastageType, reasonCode], map: "WastagePolicy_tenantId_companyId_inventoryLocationId_wastageTyp")
}

model OperationalReasonCode {
  id               String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId         String       @db.Uuid
  companyId        String       @db.Uuid
  workflow         String
  code             String
  label            String
  appliesTo        String?
  requiresEvidence Boolean      @default(false)
  status           RecordStatus @default(ACTIVE)
  sortOrder        Int          @default(100)
  notes            String?
  createdAt        DateTime     @default(now())
  updatedAt        DateTime     @default(now()) @updatedAt
  tenant           Tenant       @relation(fields: [tenantId], references: [id])
  company          Company      @relation(fields: [companyId], references: [id])

  @@unique([companyId, workflow, code])
  @@index([tenantId, companyId, workflow, status, sortOrder], map: "OperationalReasonCode_tenantId_companyId_workflow_status_sortOr")
  @@index([tenantId, companyId, workflow, appliesTo, status], map: "OperationalReasonCode_tenantId_companyId_workflow_appliesTo_sta")
}

model WastageReport {
  id                  String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId            String            @db.Uuid
  companyId           String            @db.Uuid
  inventoryLocationId String            @db.Uuid
  publicReference     String
  reportedByUserId    String            @db.Uuid
  reviewedByUserId    String?           @db.Uuid
  postedByUserId      String?           @db.Uuid
  reversedByUserId    String?           @db.Uuid
  status              String            @default("DRAFT")
  wastageType         String
  reasonCode          String
  evidenceReference   String?
  notes               String?
  totalEstimatedCost  Decimal           @default(0) @db.Decimal(18, 6)
  policyFlags         Json?
  policySnapshot      Json?
  evidenceRequired    Boolean           @default(false)
  evidenceSatisfied   Boolean           @default(true)
  submittedAt         DateTime?
  reviewedAt          DateTime?
  postedAt            DateTime?
  reversedAt          DateTime?
  cancelledAt         DateTime?
  cancellationReason  String?
  reviewNotes         String?
  reversalReason      String?
  createdAt           DateTime          @default(now())
  updatedAt           DateTime          @updatedAt
  tenant              Tenant            @relation(fields: [tenantId], references: [id])
  company             Company           @relation(fields: [companyId], references: [id])
  inventoryLocation   InventoryLocation @relation(fields: [inventoryLocationId], references: [id])
  reportedBy          User              @relation("WastageReportReporter", fields: [reportedByUserId], references: [id])
  reviewedBy          User?             @relation("WastageReportReviewer", fields: [reviewedByUserId], references: [id])
  postedBy            User?             @relation("WastageReportPoster", fields: [postedByUserId], references: [id])
  reversedBy          User?             @relation("WastageReportReverser", fields: [reversedByUserId], references: [id])
  lines               WastageLine[]

  @@unique([companyId, publicReference])
  @@index([tenantId, companyId, status, createdAt])
  @@index([tenantId, companyId, status, postedAt])
  @@index([tenantId, companyId, status, reversedAt])
  @@index([tenantId, companyId, inventoryLocationId, status])
  @@index([reportedByUserId])
  @@index([postedByUserId])
  @@index([reversedByUserId])
}

model WastageLine {
  id                  String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  wastageReportId     String             @db.Uuid
  tenantId            String             @db.Uuid
  companyId           String             @db.Uuid
  inventoryLocationId String             @db.Uuid
  itemId              String             @db.Uuid
  uomId               String             @db.Uuid
  lineNumber          Int
  description         String
  quantity            Decimal            @db.Decimal(18, 6)
  quantityBaseUom     Decimal            @db.Decimal(18, 6)
  estimatedUnitCost   Decimal            @default(0) @db.Decimal(18, 6)
  estimatedTotalCost  Decimal            @default(0) @db.Decimal(18, 6)
  reasonCode          String
  evidenceReference   String?
  photoRequired       Boolean            @default(false)
  postedMovementId    String?            @unique @db.Uuid
  lotNumber           String?
  expiryDate          DateTime?
  notes               String?
  createdAt           DateTime           @default(now())
  updatedAt           DateTime           @updatedAt
  wastageReport       WastageReport      @relation(fields: [wastageReportId], references: [id])
  tenant              Tenant             @relation(fields: [tenantId], references: [id])
  company             Company            @relation(fields: [companyId], references: [id])
  inventoryLocation   InventoryLocation  @relation(fields: [inventoryLocationId], references: [id])
  item                Item               @relation(fields: [itemId], references: [id])
  uom                 Uom                @relation(fields: [uomId], references: [id])
  postedMovement      InventoryMovement? @relation("WastageLinePostedMovement", fields: [postedMovementId], references: [id])

  @@unique([wastageReportId, lineNumber])
  @@index([tenantId, companyId, wastageReportId, itemId])
  @@index([inventoryLocationId, itemId])
  @@index([tenantId, companyId, inventoryLocationId, itemId, createdAt], map: "WastageLine_tenantId_companyId_inventoryLocationId_itemId_creat")
}

model StockAdjustment {
  id                        String                @id @default(uuid()) @db.Uuid
  tenantId                  String                @db.Uuid
  companyId                 String                @db.Uuid
  inventoryLocationId       String                @db.Uuid
  publicReference           String
  requestedByUserId         String                @db.Uuid
  cancelledByUserId         String?               @db.Uuid
  postedByUserId            String?               @db.Uuid
  reversedByUserId          String?               @db.Uuid
  status                    String                @default("DRAFT")
  adjustmentType            String
  reasonCode                String
  reasonDescription         String
  evidenceReference         String?
  sourceDocumentType        String?
  sourceDocumentId          String?               @db.Uuid
  sourceStockCountSessionId String?               @db.Uuid
  totalEstimatedValueImpact Decimal               @default(0) @db.Decimal(18, 6)
  submittedAt               DateTime?
  postedAt                  DateTime?
  reversedAt                DateTime?
  cancelledAt               DateTime?
  cancellationReason        String?
  reversalReason            String?
  createdAt                 DateTime              @default(now())
  updatedAt                 DateTime              @updatedAt
  tenant                    Tenant                @relation(fields: [tenantId], references: [id])
  company                   Company               @relation(fields: [companyId], references: [id])
  inventoryLocation         InventoryLocation     @relation(fields: [inventoryLocationId], references: [id])
  requestedBy               User                  @relation("StockAdjustmentRequester", fields: [requestedByUserId], references: [id])
  cancelledBy               User?                 @relation("StockAdjustmentCanceller", fields: [cancelledByUserId], references: [id])
  postedBy                  User?                 @relation("StockAdjustmentPoster", fields: [postedByUserId], references: [id])
  reversedBy                User?                 @relation("StockAdjustmentReverser", fields: [reversedByUserId], references: [id])
  sourceStockCountSession   StockCountSession?    @relation(fields: [sourceStockCountSessionId], references: [id])
  lines                     StockAdjustmentLine[]

  @@unique([companyId, publicReference])
  @@index([tenantId, companyId, status, createdAt])
  @@index([tenantId, companyId, inventoryLocationId, status], map: "StockAdjustment_tenantId_companyId_inventoryLocationId_status_i")
  @@index([requestedByUserId])
  @@index([cancelledByUserId])
  @@index([postedByUserId])
  @@index([reversedByUserId])
}

model StockAdjustmentLine {
  id                     String             @id @default(uuid()) @db.Uuid
  stockAdjustmentId      String             @db.Uuid
  tenantId               String             @db.Uuid
  companyId              String             @db.Uuid
  inventoryLocationId    String             @db.Uuid
  itemId                 String             @db.Uuid
  uomId                  String             @db.Uuid
  lineNumber             Int
  lotKey                 String             @default("NOLOT|NOEXP")
  lotNumber              String?
  expiryDate             DateTime?
  systemQuantityBaseUom  Decimal            @default(0) @db.Decimal(18, 6)
  quantityDeltaBaseUom   Decimal            @db.Decimal(18, 6)
  unitCost               Decimal            @default(0) @db.Decimal(18, 6)
  estimatedValueImpact   Decimal            @default(0) @db.Decimal(18, 6)
  reasonCode             String
  notes                  String?
  evidenceReference      String?
  postedMovementId       String?            @unique @db.Uuid
  sourceStockCountLineId String?            @db.Uuid
  createdAt              DateTime           @default(now())
  updatedAt              DateTime           @updatedAt
  stockAdjustment        StockAdjustment    @relation(fields: [stockAdjustmentId], references: [id])
  tenant                 Tenant             @relation(fields: [tenantId], references: [id])
  company                Company            @relation(fields: [companyId], references: [id])
  inventoryLocation      InventoryLocation  @relation(fields: [inventoryLocationId], references: [id])
  item                   Item               @relation(fields: [itemId], references: [id])
  uom                    Uom                @relation(fields: [uomId], references: [id])
  postedMovement         InventoryMovement? @relation("StockAdjustmentLinePostedMovement", fields: [postedMovementId], references: [id])
  sourceStockCountLine   StockCountLine?    @relation(fields: [sourceStockCountLineId], references: [id])

  @@unique([stockAdjustmentId, lineNumber])
  @@index([tenantId, companyId, stockAdjustmentId, itemId], map: "StockAdjustmentLine_tenantId_companyId_stockAdjustmentId_itemId")
  @@index([inventoryLocationId, itemId, lotKey])
}

model InventoryMovement {
  id                                 String                        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId                           String                        @db.Uuid
  companyId                          String                        @db.Uuid
  inventoryLocationId                String                        @db.Uuid
  relatedInventoryLocationId         String?                       @db.Uuid
  itemId                             String                        @db.Uuid
  movementType                       InventoryMovementType
  occurredAt                         DateTime
  enteredQuantity                    Decimal                       @db.Decimal(18, 6)
  enteredUomId                       String                        @db.Uuid
  quantityDeltaBaseUom               Decimal                       @db.Decimal(18, 6)
  baseUomId                          String                        @db.Uuid
  lotNumber                          String?
  expiryDate                         DateTime?
  unitCost                           Decimal?                      @db.Decimal(18, 6)
  totalCost                          Decimal?                      @db.Decimal(18, 6)
  sourceDocumentType                 String
  sourceDocumentId                   String                        @db.Uuid
  sourceDocumentLineId               String?                       @db.Uuid
  sourceEventKey                     String
  reasonCode                         String?
  notes                              String?
  reversalOfMovementId               String?                       @db.Uuid
  postedByUserId                     String                        @db.Uuid
  createdAt                          DateTime                      @default(now())
  tenant                             Tenant                        @relation(fields: [tenantId], references: [id])
  company                            Company                       @relation(fields: [companyId], references: [id])
  inventoryLocation                  InventoryLocation             @relation(fields: [inventoryLocationId], references: [id])
  relatedInventoryLocation           InventoryLocation?            @relation("InventoryMovementRelatedInventoryLocation", fields: [relatedInventoryLocationId], references: [id])
  item                               Item                          @relation(fields: [itemId], references: [id])
  enteredUom                         Uom                           @relation("InventoryMovementEnteredUom", fields: [enteredUomId], references: [id])
  baseUom                            Uom                           @relation("InventoryMovementBaseUom", fields: [baseUomId], references: [id])
  reversalOfMovement                 InventoryMovement?            @relation("InventoryMovementReversal", fields: [reversalOfMovementId], references: [id])
  reversalMovements                  InventoryMovement[]           @relation("InventoryMovementReversal")
  postedWastageLines                 WastageLine[]                 @relation("WastageLinePostedMovement")
  postedStockAdjustmentLines         StockAdjustmentLine[]         @relation("StockAdjustmentLinePostedMovement")
  postedGoodsReceiptLine             GoodsReceiptLine?             @relation("GoodsReceiptLinePostedMovement")
  postedInventoryTransferReceiptLine InventoryTransferReceiptLine? @relation("InventoryTransferReceiptLinePostedMovement")

  @@unique([tenantId, companyId, sourceDocumentType, sourceDocumentId, sourceEventKey], map: "InventoryMovement_tenantId_companyId_sourceDocumentType_sourceD")
  @@index([tenantId, companyId, inventoryLocationId, itemId, occurredAt], map: "InventoryMovement_tenantId_companyId_inventoryLocationId_itemId")
  @@index([relatedInventoryLocationId])
  @@index([sourceDocumentType, sourceDocumentId])
  @@index([reversalOfMovementId])
}

model InventoryBalance {
  id                  String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId            String            @db.Uuid
  companyId           String            @db.Uuid
  inventoryLocationId String            @db.Uuid
  itemId              String            @db.Uuid
  lotKey              String            @default("UNTRACKED")
  lotNumber           String?
  expiryDate          DateTime?
  baseUomId           String            @db.Uuid
  qtyOnHand           Decimal           @default(0) @db.Decimal(18, 6)
  version             Int               @default(1)
  updatedAt           DateTime          @updatedAt
  tenant              Tenant            @relation(fields: [tenantId], references: [id])
  company             Company           @relation(fields: [companyId], references: [id])
  inventoryLocation   InventoryLocation @relation(fields: [inventoryLocationId], references: [id])
  item                Item              @relation(fields: [itemId], references: [id])
  baseUom             Uom               @relation("InventoryBalanceBaseUom", fields: [baseUomId], references: [id])

  @@unique([inventoryLocationId, itemId, lotKey])
  @@index([tenantId, companyId, inventoryLocationId])
  @@index([tenantId, companyId, itemId])
}

model InventoryTransfer {
  id                    String                         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId              String                         @db.Uuid
  companyId             String                         @db.Uuid
  publicReference       String
  sourceLocationId      String                         @db.Uuid
  destinationLocationId String                         @db.Uuid
  requestedByUserId     String                         @db.Uuid
  transferType          String
  purpose               String
  requiredByDate        DateTime?
  status                String                         @default("DRAFT")
  submittedAt           DateTime?
  dispatchedAt          DateTime?
  receivedAt            DateTime?
  cancelledAt           DateTime?
  cancellationReason    String?
  dispatchedByUserId    String?                        @db.Uuid
  receivedByUserId      String?                        @db.Uuid
  createdAt             DateTime                       @default(now())
  updatedAt             DateTime                       @updatedAt
  tenant                Tenant                         @relation(fields: [tenantId], references: [id])
  company               Company                        @relation(fields: [companyId], references: [id])
  sourceLocation        Location                       @relation("InventoryTransferSourceLocation", fields: [sourceLocationId], references: [id])
  destinationLocation   Location                       @relation("InventoryTransferDestinationLocation", fields: [destinationLocationId], references: [id])
  requestedBy           User                           @relation("InventoryTransferRequester", fields: [requestedByUserId], references: [id])
  dispatchedBy          User?                          @relation("InventoryTransferDispatcher", fields: [dispatchedByUserId], references: [id])
  receivedBy            User?                          @relation("InventoryTransferReceiver", fields: [receivedByUserId], references: [id])
  lines                 InventoryTransferLine[]
  receipts              InventoryTransferReceipt[]
  receiptLines          InventoryTransferReceiptLine[]

  @@unique([companyId, publicReference])
  @@index([tenantId, companyId, status])
  @@index([tenantId, companyId, sourceLocationId, status], map: "InventoryTransfer_tenantId_companyId_sourceLocationId_status_id")
  @@index([tenantId, companyId, destinationLocationId, status], map: "InventoryTransfer_tenantId_companyId_destinationLocationId_stat")
  @@index([requiredByDate])
}

model InventoryTransferLine {
  id                             String                         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  inventoryTransferId            String                         @db.Uuid
  tenantId                       String                         @db.Uuid
  companyId                      String                         @db.Uuid
  sourceInventoryLocationId      String                         @db.Uuid
  destinationInventoryLocationId String                         @db.Uuid
  itemId                         String                         @db.Uuid
  uomId                          String                         @db.Uuid
  lineNumber                     Int
  description                    String
  requestedQty                   Decimal                        @db.Decimal(18, 6)
  approvedQty                    Decimal                        @default(0) @db.Decimal(18, 6)
  preparedQty                    Decimal                        @default(0) @db.Decimal(18, 6)
  dispatchedQty                  Decimal                        @default(0) @db.Decimal(18, 6)
  receivedQty                    Decimal                        @default(0) @db.Decimal(18, 6)
  rejectedQty                    Decimal                        @default(0) @db.Decimal(18, 6)
  damagedQty                     Decimal                        @default(0) @db.Decimal(18, 6)
  discrepancyQty                 Decimal                        @default(0) @db.Decimal(18, 6)
  lotNumber                      String?
  expiryDate                     DateTime?
  notes                          String?
  inventoryTransfer              InventoryTransfer              @relation(fields: [inventoryTransferId], references: [id])
  tenant                         Tenant                         @relation(fields: [tenantId], references: [id])
  company                        Company                        @relation(fields: [companyId], references: [id])
  sourceInventoryLocation        InventoryLocation              @relation("InventoryTransferLineSourceInventoryLocation", fields: [sourceInventoryLocationId], references: [id])
  destinationInventoryLocation   InventoryLocation              @relation("InventoryTransferLineDestinationInventoryLocation", fields: [destinationInventoryLocationId], references: [id])
  item                           Item                           @relation(fields: [itemId], references: [id])
  uom                            Uom                            @relation(fields: [uomId], references: [id])
  receiptLines                   InventoryTransferReceiptLine[]

  @@unique([inventoryTransferId, lineNumber])
  @@index([tenantId, companyId])
  @@index([sourceInventoryLocationId])
  @@index([destinationInventoryLocationId])
  @@index([itemId])
}

model InventoryTransferReceipt {
  id                  String                         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId            String                         @db.Uuid
  companyId           String                         @db.Uuid
  inventoryTransferId String                         @db.Uuid
  receivedByUserId    String                         @db.Uuid
  reversedByUserId    String?                        @db.Uuid
  status              String                         @default("DRAFT")
  receivedAt          DateTime                       @default(now())
  postedAt            DateTime?
  reversedAt          DateTime?
  reversalReason      String?
  discrepancyFlag     Boolean                        @default(false)
  discrepancySummary  String?
  notes               String?
  createdAt           DateTime                       @default(now())
  updatedAt           DateTime                       @updatedAt
  tenant              Tenant                         @relation(fields: [tenantId], references: [id])
  company             Company                        @relation(fields: [companyId], references: [id])
  inventoryTransfer   InventoryTransfer              @relation(fields: [inventoryTransferId], references: [id])
  receivedBy          User                           @relation("InventoryTransferReceiptReceiver", fields: [receivedByUserId], references: [id])
  reversedBy          User?                          @relation("InventoryTransferReceiptReverser", fields: [reversedByUserId], references: [id])
  lines               InventoryTransferReceiptLine[]

  @@index([tenantId, companyId, status])
  @@index([inventoryTransferId, status])
  @@index([receivedByUserId])
  @@index([reversedByUserId])
}

model InventoryTransferReceiptLine {
  id                      String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  transferReceiptId       String                   @db.Uuid
  inventoryTransferId     String                   @db.Uuid
  inventoryTransferLineId String                   @db.Uuid
  tenantId                String                   @db.Uuid
  companyId               String                   @db.Uuid
  itemId                  String                   @db.Uuid
  uomId                   String                   @db.Uuid
  lineNumber              Int
  dispatchedQtySnapshot   Decimal                  @db.Decimal(18, 6)
  acceptedQty             Decimal                  @default(0) @db.Decimal(18, 6)
  rejectedQty             Decimal                  @default(0) @db.Decimal(18, 6)
  damagedQty              Decimal                  @default(0) @db.Decimal(18, 6)
  discrepancyQty          Decimal                  @default(0) @db.Decimal(18, 6)
  outstandingQty          Decimal                  @default(0) @db.Decimal(18, 6)
  discrepancyType         String?
  discrepancyReason       String?
  evidenceReference       String?
  postedMovementId        String?                  @unique @db.Uuid
  createdAt               DateTime                 @default(now())
  updatedAt               DateTime                 @updatedAt
  transferReceipt         InventoryTransferReceipt @relation(fields: [transferReceiptId], references: [id])
  inventoryTransfer       InventoryTransfer        @relation(fields: [inventoryTransferId], references: [id])
  inventoryTransferLine   InventoryTransferLine    @relation(fields: [inventoryTransferLineId], references: [id])
  tenant                  Tenant                   @relation(fields: [tenantId], references: [id])
  company                 Company                  @relation(fields: [companyId], references: [id])
  item                    Item                     @relation(fields: [itemId], references: [id])
  uom                     Uom                      @relation(fields: [uomId], references: [id])
  postedMovement          InventoryMovement?       @relation("InventoryTransferReceiptLinePostedMovement", fields: [postedMovementId], references: [id], onDelete: Restrict)

  @@unique([transferReceiptId, lineNumber])
  @@index([tenantId, companyId])
  @@index([inventoryTransferId])
  @@index([inventoryTransferLineId])
}

model GoodsReceipt {
  id                            String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId                      String             @db.Uuid
  companyId                     String             @db.Uuid
  publicReference               String
  purchaseOrderId               String             @db.Uuid
  supplierId                    String             @db.Uuid
  receivingLocationId           String             @db.Uuid
  receivedByUserId              String             @db.Uuid
  receivedAt                    DateTime
  status                        String             @default("DRAFT")
  supplierDeliveryReceiptNumber String?
  discrepancyFlag               Boolean            @default(false)
  discrepancySummary            String?
  notes                         String?
  postedAt                      DateTime?
  reversedByUserId              String?            @db.Uuid
  reversedAt                    DateTime?
  reversalReason                String?
  createdAt                     DateTime           @default(now())
  updatedAt                     DateTime           @updatedAt
  tenant                        Tenant             @relation(fields: [tenantId], references: [id])
  company                       Company            @relation(fields: [companyId], references: [id])
  purchaseOrder                 PurchaseOrder      @relation(fields: [purchaseOrderId], references: [id])
  supplier                      Supplier           @relation(fields: [supplierId], references: [id])
  receivingLocation             Location           @relation("GoodsReceiptReceivingLocation", fields: [receivingLocationId], references: [id])
  receivedBy                    User               @relation("GoodsReceiptReceiver", fields: [receivedByUserId], references: [id])
  reversedBy                    User?              @relation("GoodsReceiptReverser", fields: [reversedByUserId], references: [id])
  lines                         GoodsReceiptLine[]
  apInvoices                    ApInvoice[]

  @@unique([companyId, publicReference])
  @@index([tenantId, companyId, status])
  @@index([tenantId, companyId, status, reversedAt])
  @@index([reversedByUserId])
  @@index([purchaseOrderId])
  @@index([receivingLocationId, status])
}

model GoodsReceiptLine {
  id                             String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  goodsReceiptId                 String                 @db.Uuid
  tenantId                       String                 @db.Uuid
  companyId                      String                 @db.Uuid
  purchaseOrderLineId            String                 @db.Uuid
  inventoryDestinationLocationId String                 @db.Uuid
  itemId                         String                 @db.Uuid
  uomId                          String                 @db.Uuid
  lineNumber                     Int
  description                    String
  orderedQty                     Decimal                @db.Decimal(18, 6)
  deliveredQty                   Decimal                @db.Decimal(18, 6)
  acceptedQty                    Decimal                @default(0) @db.Decimal(18, 6)
  rejectedQty                    Decimal                @default(0) @db.Decimal(18, 6)
  damagedQty                     Decimal                @default(0) @db.Decimal(18, 6)
  shortQty                       Decimal                @default(0) @db.Decimal(18, 6)
  unitCost                       Decimal?               @db.Decimal(18, 6)
  conditionStatus                String                 @default("ACCEPTED")
  discrepancyType                String?
  discrepancyReason              String?
  evidenceReference              String?
  lotNumber                      String?
  expiryDate                     DateTime?
  postedMovementId               String?                @unique @db.Uuid
  notes                          String?
  goodsReceipt                   GoodsReceipt           @relation(fields: [goodsReceiptId], references: [id])
  tenant                         Tenant                 @relation(fields: [tenantId], references: [id])
  company                        Company                @relation(fields: [companyId], references: [id])
  purchaseOrderLine              PurchaseOrderLine      @relation(fields: [purchaseOrderLineId], references: [id])
  inventoryDestinationLocation   InventoryLocation      @relation(fields: [inventoryDestinationLocationId], references: [id])
  item                           Item                   @relation(fields: [itemId], references: [id])
  uom                            Uom                    @relation(fields: [uomId], references: [id])
  postedMovement                 InventoryMovement?     @relation("GoodsReceiptLinePostedMovement", fields: [postedMovementId], references: [id], onDelete: Restrict)
  apInvoiceLines                 ApInvoiceLine[]
  apInvoiceMatchResults          ApInvoiceMatchResult[]

  @@unique([goodsReceiptId, lineNumber])
  @@index([tenantId, companyId])
  @@index([purchaseOrderLineId])
  @@index([inventoryDestinationLocationId])
}

model CompanyPolicySetting {
  id               String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId         String       @db.Uuid
  companyId        String       @db.Uuid
  key              String
  category         String
  label            String
  description      String
  value            Json
  defaultValue     Json
  valueType        String
  unit             String?
  options          Json?
  sourceDecisionId String       @default("DEC-0036")
  isDefault        Boolean      @default(true)
  status           RecordStatus @default(ACTIVE)
  updatedByUserId  String?      @db.Uuid
  createdAt        DateTime     @default(now())
  updatedAt        DateTime     @updatedAt
  tenant           Tenant       @relation(fields: [tenantId], references: [id])
  company          Company      @relation(fields: [companyId], references: [id])

  @@unique([companyId, key])
  @@index([tenantId, companyId, category, status])
}

model FiscalYear {
  id                String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId          String                  @db.Uuid
  companyId         String                  @db.Uuid
  code              String
  name              String
  startDate         DateTime
  endDate           DateTime
  status            FinanceFiscalYearStatus @default(DRAFT)
  isDefault         Boolean                 @default(false)
  createdByUserId   String?                 @db.Uuid
  createdAt         DateTime                @default(now())
  updatedAt         DateTime                @updatedAt
  tenant            Tenant                  @relation(fields: [tenantId], references: [id])
  company           Company                 @relation(fields: [companyId], references: [id])
  accountingPeriods AccountingPeriod[]
  budgets           Budget[]

  @@unique([companyId, code])
  @@index([tenantId, companyId, status])
  @@index([tenantId, companyId, isDefault])
  @@index([tenantId, companyId, startDate, endDate])
}

model AccountingPeriod {
  id                         String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId                   String                      @db.Uuid
  companyId                  String                      @db.Uuid
  fiscalYearId               String                      @db.Uuid
  periodNumber               Int
  code                       String
  name                       String
  startDate                  DateTime
  endDate                    DateTime
  status                     AccountingPeriodStatus      @default(FUTURE)
  softClosedAt               DateTime?
  lockedAt                   DateTime?
  reopenedAt                 DateTime?
  reopenedUntil              DateTime?
  closeEvidenceReference     String?
  createdByUserId            String?                     @db.Uuid
  createdAt                  DateTime                    @default(now())
  updatedAt                  DateTime                    @updatedAt
  tenant                     Tenant                      @relation(fields: [tenantId], references: [id])
  company                    Company                     @relation(fields: [companyId], references: [id])
  fiscalYear                 FiscalYear                  @relation(fields: [fiscalYearId], references: [id])
  financeJournals            FinanceJournal[]
  bankReconciliations        BankReconciliation[]
  budgetLines                BudgetLine[]
  financeCloseRuns           FinanceCloseRun[]
  financeCloseChecklistItems FinanceCloseChecklistItem[]
  financeCloseExceptions     FinanceCloseException[]
  financeCloseAttempts       FinanceCloseAttempt[]

  @@unique([fiscalYearId, periodNumber])
  @@unique([companyId, code])
  @@index([tenantId, companyId, status, startDate])
  @@index([tenantId, companyId, startDate, endDate])
}

model FinanceCloseRun {
  id                  String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId            String                      @db.Uuid
  companyId           String                      @db.Uuid
  accountingPeriodId  String                      @db.Uuid
  publicReference     String
  runType             FinanceCloseRunType         @default(READINESS)
  status              FinanceCloseRunStatus       @default(OPEN)
  sourceWindowStartAt DateTime?
  sourceWindowEndAt   DateTime?
  initiatedByUserId   String                      @db.Uuid
  submittedAt         DateTime?
  readyAt             DateTime?
  closedAt            DateTime?
  cancelledAt         DateTime?
  reason              String?
  evidenceReference   String?
  configSnapshot      Json?
  notes               String?
  idempotencyKey      String?
  version             Int                         @default(1)
  createdAt           DateTime                    @default(now())
  updatedAt           DateTime                    @updatedAt
  tenant              Tenant                      @relation(fields: [tenantId], references: [id])
  company             Company                     @relation(fields: [companyId], references: [id])
  accountingPeriod    AccountingPeriod            @relation(fields: [accountingPeriodId], references: [id])
  initiatedBy         User                        @relation("FinanceCloseRunInitiator", fields: [initiatedByUserId], references: [id])
  checklistItems      FinanceCloseChecklistItem[]
  exceptions          FinanceCloseException[]
  attempts            FinanceCloseAttempt[]

  @@unique([companyId, publicReference])
  @@unique([tenantId, companyId, accountingPeriodId, runType], map: "FinanceCloseRun_tenantId_companyId_accountingPeriodId_runType_k")
  @@unique([tenantId, companyId, idempotencyKey])
  @@index([tenantId, companyId, status, createdAt])
  @@index([tenantId, companyId, accountingPeriodId, status], map: "FinanceCloseRun_tenantId_companyId_accountingPeriodId_status_id")
}

model FinanceCloseChecklistItem {
  id                 String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String                      @db.Uuid
  companyId          String                      @db.Uuid
  accountingPeriodId String                      @db.Uuid
  financeCloseRunId  String                      @db.Uuid
  checklistType      FinanceCloseChecklistType
  label              String
  sequence           Int
  isRequired         Boolean                     @default(true)
  status             FinanceCloseChecklistStatus @default(PENDING)
  ownerUserId        String?                     @db.Uuid
  completedByUserId  String?                     @db.Uuid
  reviewedByUserId   String?                     @db.Uuid
  dueAt              DateTime?
  completedAt        DateTime?
  reviewedAt         DateTime?
  resultSummary      String?
  exceptionReason    String?
  evidenceReference  String?
  sourceSnapshot     Json?
  createdAt          DateTime                    @default(now())
  updatedAt          DateTime                    @updatedAt
  tenant             Tenant                      @relation(fields: [tenantId], references: [id])
  company            Company                     @relation(fields: [companyId], references: [id])
  accountingPeriod   AccountingPeriod            @relation(fields: [accountingPeriodId], references: [id])
  financeCloseRun    FinanceCloseRun             @relation(fields: [financeCloseRunId], references: [id])
  owner              User?                       @relation("FinanceCloseChecklistOwner", fields: [ownerUserId], references: [id])
  completedBy        User?                       @relation("FinanceCloseChecklistCompleter", fields: [completedByUserId], references: [id])
  reviewedBy         User?                       @relation("FinanceCloseChecklistReviewer", fields: [reviewedByUserId], references: [id])

  @@unique([financeCloseRunId, checklistType])
  @@index([tenantId, companyId, accountingPeriodId, status], map: "FinanceCloseChecklistItem_tenantId_companyId_accountingPeriodId")
  @@index([tenantId, companyId, ownerUserId, status], map: "FinanceCloseChecklistItem_tenantId_companyId_ownerUserId_status")
}

model FinanceCloseException {
  id                 String                        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String                        @db.Uuid
  companyId          String                        @db.Uuid
  accountingPeriodId String                        @db.Uuid
  financeCloseRunId  String?                       @db.Uuid
  exceptionType      String
  severity           FinanceCloseExceptionSeverity @default(SIGNIFICANT)
  state              FinanceCloseExceptionState    @default(OPEN)
  title              String
  description        String
  sourceEntityType   String?
  sourceEntityId     String?
  sourceSnapshot     Json?
  raisedByUserId     String                        @db.Uuid
  assignedToUserId   String?                       @db.Uuid
  resolvedByUserId   String?                       @db.Uuid
  dueAt              DateTime?
  acknowledgedAt     DateTime?
  resolvedAt         DateTime?
  waivedAt           DateTime?
  resolutionReason   String?
  evidenceReference  String?
  createdAt          DateTime                      @default(now())
  updatedAt          DateTime                      @updatedAt
  tenant             Tenant                        @relation(fields: [tenantId], references: [id])
  company            Company                       @relation(fields: [companyId], references: [id])
  accountingPeriod   AccountingPeriod              @relation(fields: [accountingPeriodId], references: [id])
  financeCloseRun    FinanceCloseRun?              @relation(fields: [financeCloseRunId], references: [id])
  raisedBy           User                          @relation("FinanceCloseExceptionRaiser", fields: [raisedByUserId], references: [id])
  assignedTo         User?                         @relation("FinanceCloseExceptionAssignee", fields: [assignedToUserId], references: [id])
  resolvedBy         User?                         @relation("FinanceCloseExceptionResolver", fields: [resolvedByUserId], references: [id])

  @@index([tenantId, companyId, accountingPeriodId, state, severity], map: "FinanceCloseException_tenantId_companyId_accountingPeriodId_sta")
  @@index([tenantId, companyId, state, dueAt])
  @@index([tenantId, companyId, sourceEntityType, sourceEntityId], map: "FinanceCloseException_tenantId_companyId_sourceEntityType_sourc")
}

model FinanceCloseAttempt {
  id                 String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String                    @db.Uuid
  companyId          String                    @db.Uuid
  accountingPeriodId String                    @db.Uuid
  financeCloseRunId  String                    @db.Uuid
  action             FinanceCloseAttemptAction
  result             FinanceCloseAttemptResult @default(IN_PROGRESS)
  idempotencyKey     String
  attemptedByUserId  String                    @db.Uuid
  attemptedAt        DateTime                  @default(now())
  failureCode        String?
  failureContext     Json?
  notes              String?
  tenant             Tenant                    @relation(fields: [tenantId], references: [id])
  company            Company                   @relation(fields: [companyId], references: [id])
  accountingPeriod   AccountingPeriod          @relation(fields: [accountingPeriodId], references: [id])
  financeCloseRun    FinanceCloseRun           @relation(fields: [financeCloseRunId], references: [id])
  attemptedBy        User                      @relation("FinanceCloseAttemptActor", fields: [attemptedByUserId], references: [id])

  @@unique([tenantId, companyId, idempotencyKey])
  @@index([tenantId, companyId, accountingPeriodId, action], map: "FinanceCloseAttempt_tenantId_companyId_accountingPeriodId_actio")
  @@index([tenantId, companyId, financeCloseRunId, result], map: "FinanceCloseAttempt_tenantId_companyId_financeCloseRunId_result")
}

model FinanceAccountClass {
  id               String                         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId         String                         @db.Uuid
  companyId        String                         @db.Uuid
  code             String
  name             String
  normalBalance    FinanceNormalBalance
  statementSection FinanceAccountStatementSection
  sortOrder        Int                            @default(100)
  status           RecordStatus                   @default(ACTIVE)
  createdByUserId  String?                        @db.Uuid
  createdAt        DateTime                       @default(now())
  updatedAt        DateTime                       @updatedAt
  tenant           Tenant                         @relation(fields: [tenantId], references: [id])
  company          Company                        @relation(fields: [companyId], references: [id])
  accounts         ChartOfAccount[]

  @@unique([companyId, code])
  @@index([tenantId, companyId, status])
  @@index([tenantId, companyId, statementSection, sortOrder], map: "FinanceAccountClass_tenantId_companyId_statementSection_sortOrd")
}

model ChartOfAccount {
  id                     String                         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId               String                         @db.Uuid
  companyId              String                         @db.Uuid
  accountClassId         String                         @db.Uuid
  parentAccountId        String?                        @db.Uuid
  code                   String
  name                   String
  description            String?
  normalBalance          FinanceNormalBalance
  isHeader               Boolean                        @default(false)
  postingAllowed         Boolean                        @default(false)
  activeFrom             DateTime?
  activeTo               DateTime?
  status                 RecordStatus                   @default(ACTIVE)
  createdByUserId        String?                        @db.Uuid
  createdAt              DateTime                       @default(now())
  updatedAt              DateTime                       @updatedAt
  tenant                 Tenant                         @relation(fields: [tenantId], references: [id])
  company                Company                        @relation(fields: [companyId], references: [id])
  accountClass           FinanceAccountClass            @relation(fields: [accountClassId], references: [id])
  parentAccount          ChartOfAccount?                @relation("ChartOfAccountHierarchy", fields: [parentAccountId], references: [id])
  childAccounts          ChartOfAccount[]               @relation("ChartOfAccountHierarchy")
  postingRuleAccountMaps FinancePostingRuleAccountMap[]
  financeJournalLines    FinanceJournalLine[]
  bankAccounts           BankAccount[]
  budgetLines            BudgetLine[]

  @@unique([companyId, code])
  @@index([tenantId, companyId, status, postingAllowed])
  @@index([tenantId, companyId, accountClassId])
  @@index([parentAccountId])
}

model FinancePostingRule {
  id                     String                                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId               String                                   @db.Uuid
  companyId              String                                   @db.Uuid
  code                   String
  name                   String
  version                Int                                      @default(1)
  sourceType             String
  sourceEvent            String
  postingConsequenceType String
  description            String
  configuration          Json
  ruleMetadata           Json?
  isConfigOnly           Boolean                                  @default(true)
  isExecutionEnabled     Boolean                                  @default(false)
  requiresApproval       Boolean                                  @default(true)
  requiresEvidence       Boolean                                  @default(false)
  status                 FinancePostingRuleStatus                 @default(DRAFT)
  effectiveFrom          DateTime?
  effectiveTo            DateTime?
  createdByUserId        String?                                  @db.Uuid
  createdAt              DateTime                                 @default(now())
  updatedAt              DateTime                                 @updatedAt
  tenant                 Tenant                                   @relation(fields: [tenantId], references: [id])
  company                Company                                  @relation(fields: [companyId], references: [id])
  accountMaps            FinancePostingRuleAccountMap[]
  dimensionRequirements  FinancePostingRuleDimensionRequirement[]
  financeJournals        FinanceJournal[]

  @@unique([companyId, code, version])
  @@index([tenantId, companyId, sourceType, sourceEvent, status], map: "FinancePostingRule_tenantId_companyId_sourceType_sourceEvent_st")
  @@index([tenantId, companyId, isExecutionEnabled, status], map: "FinancePostingRule_tenantId_companyId_isExecutionEnabled_status")
  @@index([tenantId, companyId, status, effectiveFrom])
}

model FinancePostingRuleAccountMap {
  id              String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId        String                 @db.Uuid
  companyId       String                 @db.Uuid
  postingRuleId   String                 @db.Uuid
  accountId       String                 @db.Uuid
  side            FinancePostingRuleSide
  lineRole        String
  amountSource    String
  sortOrder       Int                    @default(100)
  status          RecordStatus           @default(ACTIVE)
  createdByUserId String?                @db.Uuid
  createdAt       DateTime               @default(now())
  updatedAt       DateTime               @updatedAt
  tenant          Tenant                 @relation(fields: [tenantId], references: [id])
  company         Company                @relation(fields: [companyId], references: [id])
  postingRule     FinancePostingRule     @relation(fields: [postingRuleId], references: [id])
  account         ChartOfAccount         @relation(fields: [accountId], references: [id])

  @@unique([postingRuleId, lineRole, side, accountId], map: "FinancePostingRuleAccountMap_postingRuleId_lineRole_side_accoun")
  @@index([tenantId, companyId, postingRuleId, status], map: "FinancePostingRuleAccountMap_tenantId_companyId_postingRuleId_s")
  @@index([tenantId, companyId, accountId, status], map: "FinancePostingRuleAccountMap_tenantId_companyId_accountId_statu")
}

model FinancePostingRuleDimensionRequirement {
  id                String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId          String               @db.Uuid
  companyId         String               @db.Uuid
  postingRuleId     String               @db.Uuid
  dimensionType     FinanceDimensionType
  required          Boolean              @default(true)
  allowDerivedValue Boolean              @default(true)
  sourceField       String?
  status            RecordStatus         @default(ACTIVE)
  createdByUserId   String?              @db.Uuid
  createdAt         DateTime             @default(now())
  updatedAt         DateTime             @updatedAt
  tenant            Tenant               @relation(fields: [tenantId], references: [id])
  company           Company              @relation(fields: [companyId], references: [id])
  postingRule       FinancePostingRule   @relation(fields: [postingRuleId], references: [id])

  @@unique([postingRuleId, dimensionType], map: "FinancePostingRuleDimensionRequirement_postingRuleId_dimensionT")
  @@index([tenantId, companyId, postingRuleId, status], map: "FinancePostingRuleDimensionRequirement_tenantId_companyId_posti")
  @@index([tenantId, companyId, dimensionType, required], map: "FinancePostingRuleDimensionRequirement_tenantId_companyId_dimen")
}

model FinanceJournal {
  id                     String                         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId               String                         @db.Uuid
  companyId              String                         @db.Uuid
  publicReference        String
  journalType            FinanceJournalType             @default(MANUAL)
  status                 FinanceJournalStatus           @default(DRAFT)
  currencyCode           String                         @default("PHP")
  accountingPeriodId     String                         @db.Uuid
  journalDate            DateTime
  postingDate            DateTime?
  description            String
  businessJustification  String
  evidenceReference      String?
  sourceDocumentType     String                         @default("MANUAL_JOURNAL")
  sourceDocumentId       String?                        @db.Uuid
  sourceEventKey         String
  postingConsequenceType String                         @default("MANUAL_JOURNAL")
  postingRuleId          String?                        @db.Uuid
  postingRuleVersion     Int?
  brandId                String?                        @db.Uuid
  locationId             String?                        @db.Uuid
  departmentId           String?                        @db.Uuid
  costCenterId           String?                        @db.Uuid
  projectId              String?                        @db.Uuid
  createdByUserId        String                         @db.Uuid
  submittedByUserId      String?                        @db.Uuid
  approvedByUserId       String?                        @db.Uuid
  postedByUserId         String?                        @db.Uuid
  reversedByUserId       String?                        @db.Uuid
  submittedAt            DateTime?
  approvedAt             DateTime?
  postedAt               DateTime?
  reversedAt             DateTime?
  reversalReason         String?
  reversalOfJournalId    String?                        @db.Uuid
  totalDebitAmountPhp    Decimal                        @default(0) @db.Decimal(18, 6)
  totalCreditAmountPhp   Decimal                        @default(0) @db.Decimal(18, 6)
  version                Int                            @default(1)
  createdAt              DateTime                       @default(now())
  updatedAt              DateTime                       @updatedAt
  tenant                 Tenant                         @relation(fields: [tenantId], references: [id])
  company                Company                        @relation(fields: [companyId], references: [id])
  accountingPeriod       AccountingPeriod               @relation(fields: [accountingPeriodId], references: [id])
  postingRule            FinancePostingRule?            @relation(fields: [postingRuleId], references: [id])
  reversalOfJournal      FinanceJournal?                @relation("FinanceJournalReversal", fields: [reversalOfJournalId], references: [id], onDelete: Restrict)
  reversalJournals       FinanceJournal[]               @relation("FinanceJournalReversal")
  lines                  FinanceJournalLine[]
  postingAttempts        FinanceJournalPostingAttempt[]

  @@unique([companyId, publicReference])
  @@unique([companyId, sourceDocumentType, sourceEventKey])
  @@index([tenantId, companyId, status, createdAt])
  @@index([tenantId, companyId, accountingPeriodId, status])
  @@index([tenantId, companyId, sourceDocumentType, sourceDocumentId], map: "FinanceJournal_tenantId_companyId_sourceDocumentType_sourceDocu")
  @@index([tenantId, companyId, journalDate])
  @@index([reversalOfJournalId])
  @@index([createdByUserId])
}

model FinanceJournalLine {
  id               String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  financeJournalId String                 @db.Uuid
  tenantId         String                 @db.Uuid
  companyId        String                 @db.Uuid
  lineNumber       Int
  accountId        String                 @db.Uuid
  amountSide       FinancePostingRuleSide
  amountPhp        Decimal                @db.Decimal(18, 6)
  lineDescription  String
  brandId          String?                @db.Uuid
  locationId       String?                @db.Uuid
  departmentId     String?                @db.Uuid
  costCenterId     String?                @db.Uuid
  projectId        String?                @db.Uuid
  supplierId       String?                @db.Uuid
  sourceLineType   String?
  sourceLineId     String?                @db.Uuid
  createdAt        DateTime               @default(now())
  updatedAt        DateTime               @updatedAt
  tenant           Tenant                 @relation(fields: [tenantId], references: [id])
  company          Company                @relation(fields: [companyId], references: [id])
  journal          FinanceJournal         @relation(fields: [financeJournalId], references: [id])
  account          ChartOfAccount         @relation(fields: [accountId], references: [id])

  @@unique([financeJournalId, lineNumber])
  @@index([tenantId, companyId, financeJournalId])
  @@index([tenantId, companyId, accountId])
  @@index([tenantId, companyId, brandId, locationId, departmentId, costCenterId, projectId], map: "FinanceJournalLine_tenantId_companyId_brandId_locationId_depart")
}

model FinanceJournalPostingAttempt {
  id                 String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String                      @db.Uuid
  companyId          String                      @db.Uuid
  financeJournalId   String?                     @db.Uuid
  idempotencyKey     String
  action             FinancePostingAttemptAction
  status             FinancePostingAttemptStatus @default(REQUESTED)
  requestedByUserId  String                      @db.Uuid
  resultJournalId    String?                     @db.Uuid
  failureCode        String?
  failureReason      String?
  requestPayloadHash String?
  createdAt          DateTime                    @default(now())
  updatedAt          DateTime                    @updatedAt
  tenant             Tenant                      @relation(fields: [tenantId], references: [id])
  company            Company                     @relation(fields: [companyId], references: [id])
  journal            FinanceJournal?             @relation(fields: [financeJournalId], references: [id])

  @@unique([companyId, idempotencyKey])
  @@index([tenantId, companyId, financeJournalId, status], map: "FinanceJournalPostingAttempt_tenantId_companyId_financeJournalI")
  @@index([tenantId, companyId, status, createdAt], map: "FinanceJournalPostingAttempt_tenantId_companyId_status_createdA")
}

model Budget {
  id                  String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId            String             @db.Uuid
  companyId           String             @db.Uuid
  publicReference     String
  fiscalYearId        String             @db.Uuid
  name                String
  description         String?
  budgetType          String             @default("OPERATING")
  status              BudgetStatus       @default(DRAFT)
  currencyCode        String             @default("PHP")
  brandId             String?            @db.Uuid
  locationId          String?            @db.Uuid
  departmentId        String?            @db.Uuid
  costCenterId        String?            @db.Uuid
  projectId           String?            @db.Uuid
  ownerUserId         String?            @db.Uuid
  createdByUserId     String             @db.Uuid
  submittedByUserId   String?            @db.Uuid
  approvedByUserId    String?            @db.Uuid
  cancelledByUserId   String?            @db.Uuid
  submittedAt         DateTime?
  approvedAt          DateTime?
  activatedAt         DateTime?
  closedAt            DateTime?
  cancelledAt         DateTime?
  cancellationReason  String?
  approvalInstanceId  String?            @db.Uuid
  totalOriginalAmount Decimal            @default(0) @db.Decimal(18, 6)
  totalRevisedAmount  Decimal            @default(0) @db.Decimal(18, 6)
  policyConfiguration Json?
  version             Int                @default(1)
  createdAt           DateTime           @default(now())
  updatedAt           DateTime           @updatedAt
  tenant              Tenant             @relation(fields: [tenantId], references: [id])
  company             Company            @relation(fields: [companyId], references: [id])
  fiscalYear          FiscalYear         @relation(fields: [fiscalYearId], references: [id])
  brand               Brand?             @relation(fields: [brandId], references: [id])
  location            Location?          @relation(fields: [locationId], references: [id])
  department          Department?        @relation(fields: [departmentId], references: [id])
  costCenter          CostCenter?        @relation(fields: [costCenterId], references: [id])
  owner               User?              @relation("BudgetOwner", fields: [ownerUserId], references: [id])
  createdBy           User               @relation("BudgetCreator", fields: [createdByUserId], references: [id])
  submittedBy         User?              @relation("BudgetSubmitter", fields: [submittedByUserId], references: [id])
  approvedBy          User?              @relation("BudgetApprover", fields: [approvedByUserId], references: [id])
  cancelledBy         User?              @relation("BudgetCanceller", fields: [cancelledByUserId], references: [id])
  lines               BudgetLine[]
  revisions           BudgetRevision[]
  commitments         BudgetCommitment[]

  @@unique([companyId, publicReference])
  @@index([tenantId, companyId, status, fiscalYearId])
  @@index([tenantId, companyId, brandId, locationId, departmentId, costCenterId, projectId], map: "Budget_tenantId_companyId_brandId_locationId_departmentId_costC")
  @@index([ownerUserId])
}

model BudgetLine {
  id                   String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  budgetId             String                @db.Uuid
  tenantId             String                @db.Uuid
  companyId            String                @db.Uuid
  accountingPeriodId   String?               @db.Uuid
  accountId            String?               @db.Uuid
  lineNumber           Int
  code                 String
  name                 String
  description          String?
  brandId              String?               @db.Uuid
  locationId           String?               @db.Uuid
  departmentId         String?               @db.Uuid
  costCenterId         String?               @db.Uuid
  projectId            String?               @db.Uuid
  periodStart          DateTime
  periodEnd            DateTime
  originalAmountPhp    Decimal               @default(0) @db.Decimal(18, 6)
  revisedAmountPhp     Decimal               @default(0) @db.Decimal(18, 6)
  reservedAmountPhp    Decimal               @default(0) @db.Decimal(18, 6)
  warningThresholdPct  Decimal               @default(80) @db.Decimal(5, 2)
  hardBlockPct         Decimal?              @db.Decimal(5, 2)
  status               BudgetLineStatus      @default(DRAFT)
  createdAt            DateTime              @default(now())
  updatedAt            DateTime              @updatedAt
  budget               Budget                @relation(fields: [budgetId], references: [id])
  tenant               Tenant                @relation(fields: [tenantId], references: [id])
  company              Company               @relation(fields: [companyId], references: [id])
  accountingPeriod     AccountingPeriod?     @relation(fields: [accountingPeriodId], references: [id])
  account              ChartOfAccount?       @relation(fields: [accountId], references: [id])
  brand                Brand?                @relation(fields: [brandId], references: [id])
  location             Location?             @relation(fields: [locationId], references: [id])
  department           Department?           @relation(fields: [departmentId], references: [id])
  costCenter           CostCenter?           @relation(fields: [costCenterId], references: [id])
  commitments          BudgetCommitment[]
  expenseRequestLines  ExpenseRequestLine[]
  purchaseRequestLines PurchaseRequestLine[]
  purchaseOrderLines   PurchaseOrderLine[]
  apInvoiceLines       ApInvoiceLine[]

  @@unique([budgetId, lineNumber])
  @@unique([budgetId, code])
  @@index([tenantId, companyId, status])
  @@index([tenantId, companyId, accountId])
  @@index([tenantId, companyId, brandId, locationId, departmentId, costCenterId, projectId], map: "BudgetLine_tenantId_companyId_brandId_locationId_departmentId_c")
  @@index([periodStart, periodEnd])
}

model BudgetRevision {
  id                String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  budgetId          String               @db.Uuid
  tenantId          String               @db.Uuid
  companyId         String               @db.Uuid
  revisionNumber    Int
  revisionType      BudgetRevisionType   @default(AMENDMENT)
  status            BudgetRevisionStatus @default(DRAFT)
  reason            String
  requestedByUserId String               @db.Uuid
  reviewedByUserId  String?              @db.Uuid
  requestedAt       DateTime             @default(now())
  reviewedAt        DateTime?
  effectiveFrom     DateTime?
  effectiveTo       DateTime?
  originalSnapshot  Json
  proposedSnapshot  Json
  approvedSnapshot  Json?
  budget            Budget               @relation(fields: [budgetId], references: [id])
  tenant            Tenant               @relation(fields: [tenantId], references: [id])
  company           Company              @relation(fields: [companyId], references: [id])
  requestedBy       User                 @relation("BudgetRevisionRequester", fields: [requestedByUserId], references: [id])
  reviewedBy        User?                @relation("BudgetRevisionReviewer", fields: [reviewedByUserId], references: [id])
  createdAt         DateTime             @default(now())
  updatedAt         DateTime             @updatedAt

  @@unique([budgetId, revisionNumber])
  @@index([tenantId, companyId, status])
  @@index([requestedByUserId])
  @@index([reviewedByUserId])
}

model BudgetCommitment {
  id                     String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  budgetId               String                       @db.Uuid
  budgetLineId           String                       @db.Uuid
  tenantId               String                       @db.Uuid
  companyId              String                       @db.Uuid
  sourceType             BudgetSourceEventType
  sourceId               String
  sourceLineId           String?
  sourceEventKey         String
  sourceEventAt          DateTime
  sourceReference        String
  sourceSnapshot         Json?
  status                 BudgetCommitmentStatus       @default(PENDING)
  committedAmountPhp     Decimal                      @db.Decimal(18, 6)
  consumedAmountPhp      Decimal                      @default(0) @db.Decimal(18, 6)
  releasedAmountPhp      Decimal                      @default(0) @db.Decimal(18, 6)
  requestedByUserId      String?                      @db.Uuid
  approvedByUserId       String?                      @db.Uuid
  consumedByUserId       String?                      @db.Uuid
  reversedByUserId       String?                      @db.Uuid
  requestedAt            DateTime?
  approvedAt             DateTime?
  consumedAt             DateTime?
  reversedAt             DateTime?
  reversalReason         String?
  reversalOfCommitmentId String?                      @db.Uuid
  budget                 Budget                       @relation(fields: [budgetId], references: [id])
  budgetLine             BudgetLine                   @relation(fields: [budgetLineId], references: [id])
  tenant                 Tenant                       @relation(fields: [tenantId], references: [id])
  company                Company                      @relation(fields: [companyId], references: [id])
  requestedBy            User?                        @relation("BudgetCommitmentRequester", fields: [requestedByUserId], references: [id])
  approvedBy             User?                        @relation("BudgetCommitmentApprover", fields: [approvedByUserId], references: [id])
  consumedBy             User?                        @relation("BudgetCommitmentConsumer", fields: [consumedByUserId], references: [id])
  reversedBy             User?                        @relation("BudgetCommitmentReverser", fields: [reversedByUserId], references: [id])
  reversalOfCommitment   BudgetCommitment?            @relation("BudgetCommitmentReversal", fields: [reversalOfCommitmentId], references: [id], onDelete: Restrict)
  reversalCommitments    BudgetCommitment[]           @relation("BudgetCommitmentReversal")
  cashAdvanceRequests    CashAdvanceRequest[]
  cashAdvanceLines       CashAdvanceLiquidationLine[]
  createdAt              DateTime                     @default(now())
  updatedAt              DateTime                     @updatedAt

  @@unique([companyId, sourceType, sourceId, sourceEventKey], map: "BudgetCommitment_companyId_sourceType_sourceId_sourceEventKey_k")
  @@unique([companyId, budgetLineId, sourceType, sourceId, sourceLineId], map: "BudgetCommitment_companyId_budgetLineId_sourceType_sourceId_sou")
  @@index([tenantId, companyId, status, sourceType])
  @@index([tenantId, companyId, budgetLineId, status])
  @@index([reversalOfCommitmentId])
}

model ExpenseRequest {
  id                   String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId             String                       @db.Uuid
  companyId            String                       @db.Uuid
  publicReference      String
  currencyCode         String                       @default("PHP")
  totalRequestedAmount Decimal                      @default(0) @db.Decimal(18, 6)
  settledAmount        Decimal                      @default(0) @db.Decimal(18, 6)
  status               ExpenseRequestStatus         @default(DRAFT)
  urgency              ExpenseRequestUrgency        @default(NORMAL)
  budgetStatus         ExpenseBudgetStatus          @default(NOT_APPLICABLE)
  requestDate          DateTime
  requiredByDate       DateTime?
  title                String
  requestReason        String
  categoryCode         String
  expenseType          String                       @default("OPERATING")
  branchImpactFlag     Boolean                      @default(false)
  supplierId           String?                      @db.Uuid
  brandId              String?                      @db.Uuid
  locationId           String                       @db.Uuid
  departmentId         String?                      @db.Uuid
  costCenterId         String?                      @db.Uuid
  projectId            String?                      @db.Uuid
  requestedByUserId    String                       @db.Uuid
  submittedByUserId    String?                      @db.Uuid
  approvedByUserId     String?                      @db.Uuid
  returnedByUserId     String?                      @db.Uuid
  rejectedByUserId     String?                      @db.Uuid
  cancelledByUserId    String?                      @db.Uuid
  completedByUserId    String?                      @db.Uuid
  reversedByUserId     String?                      @db.Uuid
  approvalInstanceId   String?                      @unique @db.Uuid
  submittedAt          DateTime?
  approvedAt           DateTime?
  returnedAt           DateTime?
  rejectedAt           DateTime?
  cancelledAt          DateTime?
  completedAt          DateTime?
  reversedAt           DateTime?
  returnReason         String?
  rejectionReason      String?
  cancellationReason   String?
  completionNotes      String?
  reversalReason       String?
  evidenceReference    String?
  budgetSnapshot       Json?
  idempotencyKey       String?
  version              Int                          @default(1)
  createdAt            DateTime                     @default(now())
  updatedAt            DateTime                     @updatedAt
  tenant               Tenant                       @relation(fields: [tenantId], references: [id])
  company              Company                      @relation(fields: [companyId], references: [id])
  supplier             Supplier?                    @relation(fields: [supplierId], references: [id])
  brand                Brand?                       @relation(fields: [brandId], references: [id])
  location             Location                     @relation(fields: [locationId], references: [id])
  department           Department?                  @relation(fields: [departmentId], references: [id])
  costCenter           CostCenter?                  @relation(fields: [costCenterId], references: [id])
  requestedBy          User                         @relation("ExpenseRequestRequester", fields: [requestedByUserId], references: [id])
  submittedBy          User?                        @relation("ExpenseRequestSubmitter", fields: [submittedByUserId], references: [id])
  approvedBy           User?                        @relation("ExpenseRequestApprover", fields: [approvedByUserId], references: [id])
  returnedBy           User?                        @relation("ExpenseRequestReturner", fields: [returnedByUserId], references: [id])
  rejectedBy           User?                        @relation("ExpenseRequestRejecter", fields: [rejectedByUserId], references: [id])
  cancelledBy          User?                        @relation("ExpenseRequestCanceller", fields: [cancelledByUserId], references: [id])
  completedBy          User?                        @relation("ExpenseRequestCompleter", fields: [completedByUserId], references: [id])
  reversedBy           User?                        @relation("ExpenseRequestReverser", fields: [reversedByUserId], references: [id])
  approvalInstance     ApprovalInstance?            @relation(fields: [approvalInstanceId], references: [id])
  lines                ExpenseRequestLine[]
  sourceLinks          ExpenseRequestSourceLink[]
  cashAdvanceRequests  CashAdvanceRequest[]
  cashAdvanceLines     CashAdvanceLiquidationLine[]

  @@unique([companyId, publicReference])
  @@unique([tenantId, companyId, idempotencyKey])
  @@index([tenantId, companyId, status, createdAt])
  @@index([tenantId, companyId, locationId, status])
  @@index([tenantId, companyId, supplierId, status])
  @@index([tenantId, companyId, departmentId, status])
  @@index([tenantId, companyId, costCenterId, status])
  @@index([tenantId, companyId, projectId, status])
  @@index([requestedByUserId, status])
}

model ExpenseRequestLine {
  id                 String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  expenseRequestId   String                     @db.Uuid
  tenantId           String                     @db.Uuid
  companyId          String                     @db.Uuid
  lineNumber         Int
  lineDate           DateTime
  description        String
  categoryCode       String
  requestedAmountPhp Decimal                    @db.Decimal(18, 6)
  taxAmountPhp       Decimal                    @default(0) @db.Decimal(18, 6)
  discountAmountPhp  Decimal                    @default(0) @db.Decimal(18, 6)
  lineTotalPhp       Decimal                    @db.Decimal(18, 6)
  budgetLineId       String?                    @db.Uuid
  brandId            String?                    @db.Uuid
  locationId         String?                    @db.Uuid
  departmentId       String?                    @db.Uuid
  costCenterId       String?                    @db.Uuid
  projectId          String?                    @db.Uuid
  evidenceReference  String?
  idempotencyKey     String?
  createdByUserId    String                     @db.Uuid
  updatedByUserId    String?                    @db.Uuid
  createdAt          DateTime                   @default(now())
  updatedAt          DateTime                   @updatedAt
  tenant             Tenant                     @relation(fields: [tenantId], references: [id])
  company            Company                    @relation(fields: [companyId], references: [id])
  expenseRequest     ExpenseRequest             @relation(fields: [expenseRequestId], references: [id])
  budgetLine         BudgetLine?                @relation(fields: [budgetLineId], references: [id])
  brand              Brand?                     @relation(fields: [brandId], references: [id])
  location           Location?                  @relation(fields: [locationId], references: [id])
  department         Department?                @relation(fields: [departmentId], references: [id])
  costCenter         CostCenter?                @relation(fields: [costCenterId], references: [id])
  createdBy          User                       @relation("ExpenseRequestLineCreator", fields: [createdByUserId], references: [id])
  updatedBy          User?                      @relation("ExpenseRequestLineUpdater", fields: [updatedByUserId], references: [id])
  sourceLinks        ExpenseRequestSourceLink[]

  @@unique([expenseRequestId, lineNumber])
  @@unique([tenantId, companyId, idempotencyKey])
  @@index([tenantId, companyId, expenseRequestId])
  @@index([tenantId, companyId, budgetLineId])
  @@index([tenantId, companyId, locationId])
}

model ExpenseRequestSourceLink {
  id                         String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId                   String                   @db.Uuid
  companyId                  String                   @db.Uuid
  expenseRequestId           String                   @db.Uuid
  expenseRequestLineId       String?                  @db.Uuid
  sourceDocumentType         ExpenseRequestSourceType
  sourceDocumentId           String
  sourceDocumentLineId       String?
  sourceEventKey             String
  sourceAmountSnapshotPhp    Decimal                  @default(0) @db.Decimal(18, 6)
  remainingAmountSnapshotPhp Decimal                  @default(0) @db.Decimal(18, 6)
  sourceDocumentSnapshot     Json?
  createdByUserId            String                   @db.Uuid
  createdAt                  DateTime                 @default(now())
  updatedAt                  DateTime                 @updatedAt
  tenant                     Tenant                   @relation(fields: [tenantId], references: [id])
  company                    Company                  @relation(fields: [companyId], references: [id])
  expenseRequest             ExpenseRequest           @relation(fields: [expenseRequestId], references: [id])
  expenseRequestLine         ExpenseRequestLine?      @relation(fields: [expenseRequestLineId], references: [id])
  createdBy                  User                     @relation("ExpenseRequestSourceLinkCreator", fields: [createdByUserId], references: [id])

  @@unique([tenantId, companyId, sourceDocumentType, sourceDocumentId, sourceEventKey], map: "ExpenseRequestSourceLink_source_event_key")
  @@unique([tenantId, companyId, expenseRequestId, sourceEventKey], map: "ExpenseRequestSourceLink_tenant_company_request_event_key")
  @@index([tenantId, companyId, expenseRequestId], map: "ExpenseRequestSourceLink_request_idx")
  @@index([tenantId, companyId, expenseRequestLineId], map: "ExpenseRequestSourceLink_request_line_idx")
  @@index([tenantId, companyId, sourceDocumentType, sourceDocumentId], map: "ExpenseRequestSourceLink_source_document_idx")
}

model CashAdvanceRequest {
  id                    String                           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId              String                   @db.Uuid
  companyId             String                   @db.Uuid
  publicReference       String
  currencyCode          String                   @default("PHP")
  requestedAmountPhp    Decimal                  @db.Decimal(18, 6)
  issuedAmountPhp       Decimal                  @default(0) @db.Decimal(18, 6)
  liquidatedAmountPhp   Decimal                  @default(0) @db.Decimal(18, 6)
  status                CashAdvanceRequestStatus @default(DRAFT)
  sourceType            CashAdvanceSourceType    @default(STANDALONE)
  budgetStatus          ExpenseBudgetStatus      @default(NOT_APPLICABLE)
  requestDate           DateTime
  dueDate               DateTime?
  title                 String
  purpose               String
  categoryCode          String
  supplierId            String?                  @db.Uuid
  brandId               String?                  @db.Uuid
  locationId            String                   @db.Uuid
  departmentId          String?                  @db.Uuid
  costCenterId          String?                  @db.Uuid
  projectId             String?                  @db.Uuid
  beneficiaryUserId     String?                  @db.Uuid
  expenseRequestId      String?                  @db.Uuid
  paymentRequestId      String?                  @db.Uuid
  disbursementRequests  NonSupplierDisbursementRequest[]
  budgetCommitmentId    String?                  @db.Uuid
  intendedBankAccountId String?                  @db.Uuid
  approvalInstanceId    String?                  @unique @db.Uuid
  requestedByUserId     String                   @db.Uuid
  submittedByUserId     String?                  @db.Uuid
  approvedByUserId      String?                  @db.Uuid
  returnedByUserId      String?                  @db.Uuid
  rejectedByUserId      String?                  @db.Uuid
  cancelledByUserId     String?                  @db.Uuid
  closedByUserId        String?                  @db.Uuid
  submittedAt           DateTime?
  approvedAt            DateTime?
  returnedAt            DateTime?
  rejectedAt            DateTime?
  cancelledAt           DateTime?
  closedAt              DateTime?
  returnReason          String?
  rejectionReason       String?
  cancellationReason    String?
  closureNotes          String?
  evidenceReference     String?
  budgetSnapshot        Json?
  idempotencyKey        String?
  version               Int                      @default(1)
  createdAt             DateTime                 @default(now())
  updatedAt             DateTime                 @updatedAt
  tenant                Tenant                   @relation(fields: [tenantId], references: [id])
  company               Company                  @relation(fields: [companyId], references: [id])
  supplier              Supplier?                @relation(fields: [supplierId], references: [id])
  brand                 Brand?                   @relation(fields: [brandId], references: [id])
  location              Location                 @relation(fields: [locationId], references: [id])
  department            Department?              @relation(fields: [departmentId], references: [id])
  costCenter            CostCenter?              @relation(fields: [costCenterId], references: [id])
  beneficiary           User?                    @relation("CashAdvanceBeneficiary", fields: [beneficiaryUserId], references: [id])
  expenseRequest        ExpenseRequest?          @relation(fields: [expenseRequestId], references: [id])
  paymentRequest        PaymentRequest?          @relation(fields: [paymentRequestId], references: [id])
  budgetCommitment      BudgetCommitment?        @relation(fields: [budgetCommitmentId], references: [id])
  intendedBankAccount   BankAccount?             @relation(fields: [intendedBankAccountId], references: [id])
  approvalInstance      ApprovalInstance?        @relation(fields: [approvalInstanceId], references: [id])
  requestedBy           User                     @relation("CashAdvanceRequester", fields: [requestedByUserId], references: [id])
  submittedBy           User?                    @relation("CashAdvanceSubmitter", fields: [submittedByUserId], references: [id])
  approvedBy            User?                    @relation("CashAdvanceApprover", fields: [approvedByUserId], references: [id])
  returnedBy            User?                    @relation("CashAdvanceReturner", fields: [returnedByUserId], references: [id])
  rejectedBy            User?                    @relation("CashAdvanceRejecter", fields: [rejectedByUserId], references: [id])
  cancelledBy           User?                    @relation("CashAdvanceCanceller", fields: [cancelledByUserId], references: [id])
  closedBy              User?                    @relation("CashAdvanceCloser", fields: [closedByUserId], references: [id])
  movements             CashAdvanceMovement[]
  liquidations          CashAdvanceLiquidation[]

  @@unique([companyId, publicReference])
  @@unique([tenantId, companyId, idempotencyKey])
  @@index([tenantId, companyId, status, createdAt])
  @@index([tenantId, companyId, locationId, status])
  @@index([tenantId, companyId, supplierId, status])
  @@index([tenantId, companyId, departmentId, status])
  @@index([tenantId, companyId, costCenterId, status])
  @@index([tenantId, companyId, projectId, status])
  @@index([tenantId, companyId, expenseRequestId])
  @@index([tenantId, companyId, paymentRequestId])
  @@index([requestedByUserId, status])
}

model CashAdvanceMovement {
  id                   String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId             String                  @db.Uuid
  companyId            String                  @db.Uuid
  locationId           String                  @db.Uuid
  cashAdvanceRequestId String                  @db.Uuid
  liquidationId        String?                 @db.Uuid
  actorUserId          String                  @db.Uuid
  movementType         CashAdvanceMovementType
  amountPhp            Decimal                 @db.Decimal(18, 6)
  currencyCode         String                  @default("PHP")
  sourceEventKey       String
  referenceNo          String?
  notes                String?
  idempotencyKey       String?
  createdAt            DateTime                @default(now())
  tenant               Tenant                  @relation(fields: [tenantId], references: [id])
  company              Company                 @relation(fields: [companyId], references: [id])
  location             Location                @relation(fields: [locationId], references: [id])
  cashAdvanceRequest   CashAdvanceRequest      @relation(fields: [cashAdvanceRequestId], references: [id])
  liquidation          CashAdvanceLiquidation? @relation(fields: [liquidationId], references: [id])
  actor                User                    @relation("CashAdvanceMovementActor", fields: [actorUserId], references: [id])

  @@unique([tenantId, companyId, sourceEventKey])
  @@unique([tenantId, companyId, idempotencyKey])
  @@index([tenantId, companyId, locationId, movementType], map: "CashAdvanceMovement_tenantId_companyId_locationId_movementType_")
  @@index([tenantId, companyId, cashAdvanceRequestId, createdAt], map: "CashAdvanceMovement_request_created_idx")
  @@index([tenantId, companyId, liquidationId], map: "CashAdvanceMovement_liquidation_idx")
}

model CashAdvanceLiquidation {
  id                   String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId             String                       @db.Uuid
  companyId            String                       @db.Uuid
  locationId           String                       @db.Uuid
  cashAdvanceRequestId String                       @db.Uuid
  publicReference      String
  currencyCode         String                       @default("PHP")
  claimedAmountPhp     Decimal                      @default(0) @db.Decimal(18, 6)
  approvedAmountPhp    Decimal                      @default(0) @db.Decimal(18, 6)
  amountReturnedPhp    Decimal                      @default(0) @db.Decimal(18, 6)
  overageAmountPhp     Decimal                      @default(0) @db.Decimal(18, 6)
  shortfallAmountPhp   Decimal                      @default(0) @db.Decimal(18, 6)
  status               CashAdvanceLiquidationStatus @default(DRAFT)
  submittedByUserId    String                       @db.Uuid
  reviewedByUserId     String?                      @db.Uuid
  approvedByUserId     String?                      @db.Uuid
  cancelledByUserId    String?                      @db.Uuid
  closedByUserId       String?                      @db.Uuid
  submittedAt          DateTime?
  reviewedAt           DateTime?
  approvedAt           DateTime?
  cancelledAt          DateTime?
  closedAt             DateTime?
  reviewReason         String?
  cancellationReason   String?
  closureNotes         String?
  evidenceReference    String?
  notes                String?
  idempotencyKey       String?
  createdAt            DateTime                     @default(now())
  updatedAt            DateTime                     @updatedAt
  tenant               Tenant                       @relation(fields: [tenantId], references: [id])
  company              Company                      @relation(fields: [companyId], references: [id])
  location             Location                     @relation(fields: [locationId], references: [id])
  cashAdvanceRequest   CashAdvanceRequest           @relation(fields: [cashAdvanceRequestId], references: [id])
  submittedBy          User                         @relation("CashAdvanceLiquidationSubmitter", fields: [submittedByUserId], references: [id])
  reviewedBy           User?                        @relation("CashAdvanceLiquidationReviewer", fields: [reviewedByUserId], references: [id])
  approvedBy           User?                        @relation("CashAdvanceLiquidationApprover", fields: [approvedByUserId], references: [id])
  cancelledBy          User?                        @relation("CashAdvanceLiquidationCanceller", fields: [cancelledByUserId], references: [id])
  closedBy             User?                        @relation("CashAdvanceLiquidationCloser", fields: [closedByUserId], references: [id])
  movements            CashAdvanceMovement[]
  lines                CashAdvanceLiquidationLine[]

  @@unique([companyId, publicReference])
  @@unique([tenantId, companyId, idempotencyKey])
  @@index([tenantId, companyId, status, createdAt], map: "CashAdvanceLiquidation_status_created_idx")
  @@index([tenantId, companyId, locationId, status], map: "CashAdvanceLiquidation_location_status_idx")
  @@index([tenantId, companyId, cashAdvanceRequestId, status], map: "CashAdvanceLiquidation_request_status_idx")
}

model CashAdvanceLiquidationLine {
  id                 String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String                 @db.Uuid
  companyId          String                 @db.Uuid
  locationId         String                 @db.Uuid
  liquidationId      String                 @db.Uuid
  lineNumber         Int
  spendDate          DateTime
  description        String
  categoryCode       String
  amountPhp          Decimal                @db.Decimal(18, 6)
  taxAmountPhp       Decimal                @default(0) @db.Decimal(18, 6)
  receiptReference   String?
  evidenceReference  String?
  supplierId         String?                @db.Uuid
  budgetCommitmentId String?                @db.Uuid
  expenseRequestId   String?                @db.Uuid
  paymentRequestId   String?                @db.Uuid
  idempotencyKey     String?
  createdByUserId    String                 @db.Uuid
  createdAt          DateTime               @default(now())
  updatedAt          DateTime               @updatedAt
  tenant             Tenant                 @relation(fields: [tenantId], references: [id])
  company            Company                @relation(fields: [companyId], references: [id])
  location           Location               @relation(fields: [locationId], references: [id])
  liquidation        CashAdvanceLiquidation @relation(fields: [liquidationId], references: [id])
  supplier           Supplier?              @relation(fields: [supplierId], references: [id])
  budgetCommitment   BudgetCommitment?      @relation(fields: [budgetCommitmentId], references: [id])
  expenseRequest     ExpenseRequest?        @relation(fields: [expenseRequestId], references: [id])
  paymentRequest     PaymentRequest?        @relation(fields: [paymentRequestId], references: [id])
  createdBy          User                   @relation("CashAdvanceLiquidationLineCreator", fields: [createdByUserId], references: [id])

  @@unique([liquidationId, lineNumber])
  @@unique([tenantId, companyId, idempotencyKey], map: "CashAdvanceLiquidationLine_tenantId_companyId_idempotencyKey_ke")
  @@index([tenantId, companyId, locationId], map: "CashAdvanceLiquidationLine_location_idx")
  @@index([tenantId, companyId, liquidationId], map: "CashAdvanceLiquidationLine_liquidation_idx")
  @@index([tenantId, companyId, budgetCommitmentId], map: "CashAdvanceLiquidationLine_budgetCommitment_idx")
  @@index([tenantId, companyId, expenseRequestId], map: "CashAdvanceLiquidationLine_expenseRequest_idx")
  @@index([tenantId, companyId, paymentRequestId], map: "CashAdvanceLiquidationLine_paymentRequest_idx")
}

model PettyCashFund {
  id                        String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId                  String                     @db.Uuid
  companyId                 String                     @db.Uuid
  publicReference           String
  code                      String
  name                      String
  currencyCode              String                     @default("PHP")
  openingBalancePhp         Decimal                    @default(0) @db.Decimal(18, 6)
  currentBalancePhp         Decimal                    @default(0) @db.Decimal(18, 6)
  targetBalancePhp          Decimal                    @default(0) @db.Decimal(18, 6)
  lowBalanceAlertPhp        Decimal                    @default(0) @db.Decimal(18, 6)
  status                    PettyCashFundStatus        @default(DRAFT)
  brandId                   String?                    @db.Uuid
  locationId                String                     @db.Uuid
  custodianUserId           String                     @db.Uuid
  createdByUserId           String                     @db.Uuid
  updatedByUserId           String?                    @db.Uuid
  activatedAt               DateTime?
  suspendedAt               DateTime?
  archivedAt                DateTime?
  evidenceReference         String?
  notes                     String?
  idempotencyKey            String?
  version                   Int                        @default(1)
  createdAt                 DateTime                   @default(now())
  updatedAt                 DateTime                   @updatedAt
  tenant                    Tenant                     @relation(fields: [tenantId], references: [id])
  company                   Company                    @relation(fields: [companyId], references: [id])
  brand                     Brand?                     @relation(fields: [brandId], references: [id])
  location                  Location                   @relation(fields: [locationId], references: [id])
  custodian                 User                       @relation("PettyCashFundCustodian", fields: [custodianUserId], references: [id])
  createdBy                 User                       @relation("PettyCashFundCreator", fields: [createdByUserId], references: [id])
  updatedBy                 User?                      @relation("PettyCashFundUpdater", fields: [updatedByUserId], references: [id])
  requests                  PettyCashRequest[]
  ledgerEntries             PettyCashLedgerEntry[]
  liquidations              PettyCashLiquidation[]
  pettyCashLiquidationLines PettyCashLiquidationLine[]

  @@unique([companyId, publicReference])
  @@unique([companyId, code])
  @@unique([tenantId, companyId, idempotencyKey])
  @@index([tenantId, companyId, status, locationId], map: "PettyCashFund_scope_status_idx")
  @@index([tenantId, companyId, custodianUserId, status], map: "PettyCashFund_custodian_status_idx")
}

model PettyCashRequest {
  id                   String                           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String                 @db.Uuid
  companyId          String                 @db.Uuid
  pettyCashFundId    String                 @db.Uuid
  publicReference    String
  requestType        PettyCashRequestType
  status             PettyCashRequestStatus @default(DRAFT)
  currencyCode       String                 @default("PHP")
  requestedAmountPhp Decimal                @db.Decimal(18, 6)
  approvedAmountPhp  Decimal                @default(0) @db.Decimal(18, 6)
  purpose            String
  justification      String
  dueBy              DateTime?
  requestedByUserId  String                 @db.Uuid
  submittedByUserId  String?                @db.Uuid
  approvedByUserId   String?                @db.Uuid
  returnedByUserId   String?                @db.Uuid
  rejectedByUserId   String?                @db.Uuid
  cancelledByUserId  String?                @db.Uuid
  closedByUserId     String?                @db.Uuid
  submittedAt        DateTime?
  approvedAt         DateTime?
  returnedAt         DateTime?
  rejectedAt         DateTime?
  cancelledAt        DateTime?
  closedAt           DateTime?
  returnReason       String?
  rejectionReason    String?
  cancellationReason String?
  closureNotes       String?
  evidenceReference  String?
  sourceDocumentType String?
  sourceDocumentId   String?
  sourceEventKey     String?
  approvalInstanceId String?                @unique @db.Uuid
  idempotencyKey     String?
  metadata           Json?
  createdAt          DateTime               @default(now())
  updatedAt          DateTime               @updatedAt
  tenant             Tenant                 @relation(fields: [tenantId], references: [id])
  company            Company                @relation(fields: [companyId], references: [id])
  fund               PettyCashFund          @relation(fields: [pettyCashFundId], references: [id])
  requestedBy        User                   @relation("PettyCashRequestRequester", fields: [requestedByUserId], references: [id])
  submittedBy        User?                  @relation("PettyCashRequestSubmitter", fields: [submittedByUserId], references: [id])
  approvedBy         User?                  @relation("PettyCashRequestApprover", fields: [approvedByUserId], references: [id])
  returnedBy         User?                  @relation("PettyCashRequestReturner", fields: [returnedByUserId], references: [id])
  rejectedBy         User?                  @relation("PettyCashRequestRejecter", fields: [rejectedByUserId], references: [id])
  cancelledBy        User?                  @relation("PettyCashRequestCanceller", fields: [cancelledByUserId], references: [id])
  closedBy           User?                  @relation("PettyCashRequestCloser", fields: [closedByUserId], references: [id])
  approvalInstance   ApprovalInstance?      @relation(fields: [approvalInstanceId], references: [id])
  ledgerEntries      PettyCashLedgerEntry[]
  disbursementRequests NonSupplierDisbursementRequest[]
  location           Location?              @relation(fields: [locationId], references: [id])
  locationId         String?                @db.Uuid

  @@unique([companyId, publicReference])
  @@unique([tenantId, companyId, idempotencyKey])
  @@unique([tenantId, companyId, sourceEventKey])
  @@index([tenantId, companyId, pettyCashFundId, status], map: "PettyCashRequest_fund_status_idx")
  @@index([tenantId, companyId, requestType, status], map: "PettyCashRequest_type_status_idx")
  @@index([requestedByUserId, status], map: "PettyCashRequest_requestedBy_status_idx")
  @@index([tenantId, companyId, locationId, status], map: "PettyCashRequest_location_scope_idx")
}

model PettyCashLedgerEntry {
  id                 String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String                   @db.Uuid
  companyId          String                   @db.Uuid
  pettyCashFundId    String                   @db.Uuid
  pettyCashRequestId String?                  @db.Uuid
  liquidationId      String?                  @db.Uuid
  entryType          PettyCashLedgerEntryType
  direction          Int
  amountPhp          Decimal                  @db.Decimal(18, 6)
  balanceBeforePhp   Decimal                  @default(0) @db.Decimal(18, 6)
  balanceAfterPhp    Decimal                  @default(0) @db.Decimal(18, 6)
  currencyCode       String                   @default("PHP")
  postedAt           DateTime
  postedByUserId     String                   @db.Uuid
  reason             String
  sourceEventKey     String
  idempotencyKey     String?
  voidedByUserId     String?                  @db.Uuid
  voidedAt           DateTime?
  notes              String?
  createdAt          DateTime                 @default(now())
  tenant             Tenant                   @relation(fields: [tenantId], references: [id])
  company            Company                  @relation(fields: [companyId], references: [id])
  fund               PettyCashFund            @relation(fields: [pettyCashFundId], references: [id])
  request            PettyCashRequest?        @relation(fields: [pettyCashRequestId], references: [id])
  liquidation        PettyCashLiquidation?    @relation(fields: [liquidationId], references: [id])
  postedBy           User                     @relation("PettyCashLedgerEntryPoster", fields: [postedByUserId], references: [id])
  voidedBy           User?                    @relation("PettyCashLedgerEntryVoider", fields: [voidedByUserId], references: [id])
  location           Location?                @relation(fields: [locationId], references: [id])
  locationId         String?                  @db.Uuid

  @@unique([tenantId, companyId, sourceEventKey], map: "PettyCashLedgerEntry_source_event_key")
  @@unique([tenantId, companyId, idempotencyKey], map: "PettyCashLedgerEntry_idempotency_key")
  @@index([tenantId, companyId, pettyCashFundId, postedAt], map: "PettyCashLedgerEntry_fund_posted_idx")
  @@index([tenantId, companyId, pettyCashRequestId, entryType], map: "PettyCashLedgerEntry_request_type_idx")
  @@index([tenantId, companyId, liquidationId], map: "PettyCashLedgerEntry_liquidation_idx")
  @@index([tenantId, companyId, locationId, postedAt], map: "PettyCashLedgerEntry_location_scope_idx")
}

model PettyCashLiquidation {
  id                 String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String                     @db.Uuid
  companyId          String                     @db.Uuid
  pettyCashFundId    String                     @db.Uuid
  publicReference    String
  cycleStart         DateTime
  cycleEnd           DateTime
  currencyCode       String                     @default("PHP")
  claimedAmountPhp   Decimal                    @default(0) @db.Decimal(18, 6)
  approvedAmountPhp  Decimal                    @default(0) @db.Decimal(18, 6)
  shortageAmountPhp  Decimal                    @default(0) @db.Decimal(18, 6)
  overageAmountPhp   Decimal                    @default(0) @db.Decimal(18, 6)
  status             PettyCashLiquidationStatus @default(DRAFT)
  submittedByUserId  String                     @db.Uuid
  reviewedByUserId   String?                    @db.Uuid
  approvedByUserId   String?                    @db.Uuid
  cancelledByUserId  String?                    @db.Uuid
  closedByUserId     String?                    @db.Uuid
  submittedAt        DateTime?
  reviewedAt         DateTime?
  approvedAt         DateTime?
  cancelledAt        DateTime?
  closedAt           DateTime?
  reviewReason       String?
  cancellationReason String?
  closureNotes       String?
  evidenceReference  String?
  idempotencyKey     String?
  createdAt          DateTime                   @default(now())
  updatedAt          DateTime                   @updatedAt
  tenant             Tenant                     @relation(fields: [tenantId], references: [id])
  company            Company                    @relation(fields: [companyId], references: [id])
  fund               PettyCashFund              @relation(fields: [pettyCashFundId], references: [id])
  submittedBy        User                       @relation("PettyCashLiquidationSubmitter", fields: [submittedByUserId], references: [id])
  reviewedBy         User?                      @relation("PettyCashLiquidationReviewer", fields: [reviewedByUserId], references: [id])
  approvedBy         User?                      @relation("PettyCashLiquidationApprover", fields: [approvedByUserId], references: [id])
  cancelledBy        User?                      @relation("PettyCashLiquidationCanceller", fields: [cancelledByUserId], references: [id])
  closedBy           User?                      @relation("PettyCashLiquidationCloser", fields: [closedByUserId], references: [id])
  ledgerEntries      PettyCashLedgerEntry[]
  lines              PettyCashLiquidationLine[]
  location           Location?                  @relation(fields: [locationId], references: [id])
  locationId         String?                    @db.Uuid

  @@unique([companyId, publicReference])
  @@unique([tenantId, companyId, idempotencyKey])
  @@index([tenantId, companyId, pettyCashFundId, status], map: "PettyCashLiquidation_fund_status_idx")
  @@index([tenantId, companyId, status, createdAt], map: "PettyCashLiquidation_status_created_idx")
  @@index([tenantId, companyId, locationId, status], map: "PettyCashLiquidation_location_scope_idx")
}

model PettyCashLiquidationLine {
  id                 String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String               @db.Uuid
  companyId          String               @db.Uuid
  pettyCashFundId    String               @db.Uuid
  liquidationId      String               @db.Uuid
  lineNumber         Int
  spendDate          DateTime
  categoryCode       String
  description        String
  amountPhp          Decimal              @db.Decimal(18, 6)
  taxAmountPhp       Decimal              @default(0) @db.Decimal(18, 6)
  receiptReference   String?
  evidenceReference  String?
  supplierId         String?              @db.Uuid
  sourceDocumentType String?
  sourceDocumentId   String?
  idempotencyKey     String?
  createdByUserId    String               @db.Uuid
  createdAt          DateTime             @default(now())
  updatedAt          DateTime             @updatedAt
  tenant             Tenant               @relation(fields: [tenantId], references: [id])
  company            Company              @relation(fields: [companyId], references: [id])
  fund               PettyCashFund        @relation(fields: [pettyCashFundId], references: [id])
  liquidation        PettyCashLiquidation @relation(fields: [liquidationId], references: [id])
  supplier           Supplier?            @relation(fields: [supplierId], references: [id])
  createdBy          User                 @relation("PettyCashLiquidationLineCreator", fields: [createdByUserId], references: [id])
  location           Location?            @relation(fields: [locationId], references: [id])
  locationId         String?              @db.Uuid

  @@unique([liquidationId, lineNumber])
  @@unique([tenantId, companyId, idempotencyKey])
  @@index([tenantId, companyId, pettyCashFundId], map: "PettyCashLiquidationLine_fund_idx")
  @@index([tenantId, companyId, liquidationId], map: "PettyCashLiquidationLine_liquidation_idx")
  @@index([tenantId, companyId, supplierId], map: "PettyCashLiquidationLine_supplier_idx")
  @@index([tenantId, companyId, locationId], map: "PettyCashLiquidationLine_location_scope_idx")
}

model ApInvoice {
  id                    String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId              String                     @db.Uuid
  companyId             String                     @db.Uuid
  publicReference       String
  supplierId            String                     @db.Uuid
  locationId            String?                    @db.Uuid
  purchaseOrderId       String?                    @db.Uuid
  goodsReceiptId        String?                    @db.Uuid
  currencyCode          String                     @default("PHP")
  supplierInvoiceNumber String
  invoiceDate           DateTime
  receivedAt            DateTime?
  dueDate               DateTime?
  paymentTermsDays      Int?
  subtotalAmount        Decimal                    @default(0) @db.Decimal(18, 6)
  taxAmount             Decimal                    @default(0) @db.Decimal(18, 6)
  discountAmount        Decimal                    @default(0) @db.Decimal(18, 6)
  freightAmount         Decimal                    @default(0) @db.Decimal(18, 6)
  totalAmount           Decimal                    @default(0) @db.Decimal(18, 6)
  status                ApInvoiceStatus            @default(DRAFT)
  matchStatus           ApMatchStatus              @default(NOT_EVALUATED)
  duplicateRisk         ApDuplicateRisk            @default(CLEAN)
  captureIdempotencyKey String?
  duplicateFingerprint  String?
  holdReason            String?
  nonPoReason           String?
  evidenceReference     String?
  createdByUserId       String                     @db.Uuid
  submittedByUserId     String?                    @db.Uuid
  reviewedByUserId      String?                    @db.Uuid
  cancelledByUserId     String?                    @db.Uuid
  reversedByUserId      String?                    @db.Uuid
  submittedAt           DateTime?
  reviewedAt            DateTime?
  cancelledAt           DateTime?
  cancelledReason       String?
  reversedAt            DateTime?
  reversalReason        String?
  createdAt             DateTime                   @default(now())
  updatedAt             DateTime                   @updatedAt
  tenant                Tenant                     @relation(fields: [tenantId], references: [id])
  company               Company                    @relation(fields: [companyId], references: [id])
  supplier              Supplier                   @relation(fields: [supplierId], references: [id])
  purchaseOrder         PurchaseOrder?             @relation(fields: [purchaseOrderId], references: [id])
  goodsReceipt          GoodsReceipt?              @relation(fields: [goodsReceiptId], references: [id])
  lines                 ApInvoiceLine[]
  matchResults          ApInvoiceMatchResult[]
  exceptions            ApInvoiceException[]
  duplicateSignals      ApInvoiceDuplicateSignal[] @relation("ApInvoiceDuplicateSignals")
  duplicateCandidates   ApInvoiceDuplicateSignal[] @relation("ApInvoiceDuplicateCandidate")
  supplierCreditNotes   SupplierCreditNote[]       @relation("SupplierCreditNoteOriginalInvoice")
  paymentRequestLines   PaymentRequestLine[]
  paymentReleaseLines   PaymentReleaseAllocation[]

  @@unique([companyId, publicReference])
  @@unique([tenantId, companyId, supplierId, supplierInvoiceNumber], map: "ApInvoice_tenantId_companyId_supplierId_supplierInvoiceNumber_k")
  @@unique([tenantId, companyId, captureIdempotencyKey])
  @@index([tenantId, companyId, status, matchStatus])
  @@index([tenantId, companyId, locationId, status])
  @@index([tenantId, companyId, supplierId, invoiceDate])
  @@index([tenantId, companyId, duplicateRisk, matchStatus])
  @@index([purchaseOrderId])
  @@index([goodsReceiptId])
}

model ApInvoiceLine {
  id                  String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  apInvoiceId         String                 @db.Uuid
  tenantId            String                 @db.Uuid
  companyId           String                 @db.Uuid
  lineNumber          Int
  purchaseOrderLineId String?                @db.Uuid
  goodsReceiptLineId  String?                @db.Uuid
  itemId              String?                @db.Uuid
  uomId               String?                @db.Uuid
  description         String
  invoicedQty         Decimal                @db.Decimal(18, 6)
  unitPrice           Decimal                @db.Decimal(18, 6)
  taxAmount           Decimal                @default(0) @db.Decimal(18, 6)
  discountAmount      Decimal                @default(0) @db.Decimal(18, 6)
  lineTotalAmount     Decimal                @db.Decimal(18, 6)
  budgetLineId        String?                @db.Uuid
  createdAt           DateTime               @default(now())
  updatedAt           DateTime               @updatedAt
  tenant              Tenant                 @relation(fields: [tenantId], references: [id])
  company             Company                @relation(fields: [companyId], references: [id])
  apInvoice           ApInvoice              @relation(fields: [apInvoiceId], references: [id])
  purchaseOrderLine   PurchaseOrderLine?     @relation(fields: [purchaseOrderLineId], references: [id])
  goodsReceiptLine    GoodsReceiptLine?      @relation(fields: [goodsReceiptLineId], references: [id])
  budgetLine          BudgetLine?            @relation(fields: [budgetLineId], references: [id])
  matchResults        ApInvoiceMatchResult[]

  @@unique([apInvoiceId, lineNumber])
  @@index([tenantId, companyId, apInvoiceId])
  @@index([tenantId, companyId, purchaseOrderLineId])
  @@index([tenantId, companyId, goodsReceiptLineId])
  @@index([tenantId, companyId, budgetLineId])
}

model ApInvoiceMatchResult {
  id                  String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId            String             @db.Uuid
  companyId           String             @db.Uuid
  apInvoiceId         String             @db.Uuid
  apInvoiceLineId     String?            @db.Uuid
  purchaseOrderLineId String?            @db.Uuid
  goodsReceiptLineId  String?            @db.Uuid
  matchSource         String             @default("PO_AND_RECEIPT")
  poQtyAtMatch        Decimal?           @db.Decimal(18, 6)
  receivedQtyToMatch  Decimal            @default(0) @db.Decimal(18, 6)
  invoicedQty         Decimal            @db.Decimal(18, 6)
  poUnitPrice         Decimal?           @db.Decimal(18, 6)
  invoicedUnitPrice   Decimal            @db.Decimal(18, 6)
  poLineTotal         Decimal?           @db.Decimal(18, 6)
  invoicedLineTotal   Decimal            @db.Decimal(18, 6)
  qtyVariance         Decimal            @default(0) @db.Decimal(18, 6)
  amountVariance      Decimal            @default(0) @db.Decimal(18, 6)
  taxVarianceAmount   Decimal            @default(0) @db.Decimal(18, 6)
  toleranceConfig     Json?
  status              ApMatchStatus      @default(NOT_EVALUATED)
  exceptionCode       String?
  exceptionNotes      String?
  reviewedByUserId    String?            @db.Uuid
  reviewedAt          DateTime?
  reviewedReason      String?
  createdAt           DateTime           @default(now())
  updatedAt           DateTime           @updatedAt
  tenant              Tenant             @relation(fields: [tenantId], references: [id])
  company             Company            @relation(fields: [companyId], references: [id])
  apInvoice           ApInvoice          @relation(fields: [apInvoiceId], references: [id])
  apInvoiceLine       ApInvoiceLine?     @relation(fields: [apInvoiceLineId], references: [id])
  purchaseOrderLine   PurchaseOrderLine? @relation(fields: [purchaseOrderLineId], references: [id])
  goodsReceiptLine    GoodsReceiptLine?  @relation(fields: [goodsReceiptLineId], references: [id])

  @@index([tenantId, companyId, status])
  @@index([tenantId, companyId, apInvoiceId])
  @@index([tenantId, companyId, purchaseOrderLineId])
  @@index([tenantId, companyId, goodsReceiptLineId])
}

model ApInvoiceException {
  id                String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId          String            @db.Uuid
  companyId         String            @db.Uuid
  apInvoiceId       String            @db.Uuid
  matchResultId     String?           @db.Uuid
  exceptionCode     String
  exceptionType     String
  status            ApExceptionStatus @default(OPEN)
  severity          String            @default("NORMAL")
  ownerUserId       String?           @db.Uuid
  dueAt             DateTime?
  reason            String
  evidenceReference String?
  resolvedByUserId  String?           @db.Uuid
  resolvedAt        DateTime?
  resolutionReason  String?
  createdByUserId   String            @db.Uuid
  createdAt         DateTime          @default(now())
  updatedAt         DateTime          @updatedAt
  tenant            Tenant            @relation(fields: [tenantId], references: [id])
  company           Company           @relation(fields: [companyId], references: [id])
  apInvoice         ApInvoice         @relation(fields: [apInvoiceId], references: [id])

  @@index([tenantId, companyId, status, severity])
  @@index([tenantId, companyId, apInvoiceId])
  @@index([ownerUserId, status])
}

model ApInvoiceDuplicateSignal {
  id                 String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String                @db.Uuid
  companyId          String                @db.Uuid
  apInvoiceId        String                @db.Uuid
  candidateInvoiceId String?               @db.Uuid
  signalType         ApDuplicateSignalType
  risk               ApDuplicateRisk
  duplicateKey       String?
  matchedAmount      Decimal               @default(0) @db.Decimal(18, 6)
  matchedDate        DateTime?
  duplicateSnapshot  Json?
  decidedByUserId    String?               @db.Uuid
  decidedAt          DateTime?
  decisionReason     String?
  createdAt          DateTime              @default(now())
  updatedAt          DateTime              @updatedAt
  tenant             Tenant                @relation(fields: [tenantId], references: [id])
  company            Company               @relation(fields: [companyId], references: [id])
  apInvoice          ApInvoice             @relation("ApInvoiceDuplicateSignals", fields: [apInvoiceId], references: [id])
  candidateInvoice   ApInvoice?            @relation("ApInvoiceDuplicateCandidate", fields: [candidateInvoiceId], references: [id])

  @@unique([apInvoiceId, signalType])
  @@index([tenantId, companyId, risk, createdAt])
  @@index([tenantId, companyId, signalType, risk])
}

model SupplierCreditNote {
  id                       String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId                 String                   @db.Uuid
  companyId                String                   @db.Uuid
  supplierId               String                   @db.Uuid
  originalApInvoiceId      String                   @db.Uuid
  publicReference          String
  supplierCreditNoteNumber String
  creditDate               DateTime
  receivedAt               DateTime?
  currencyCode             String                   @default("PHP")
  creditAmount             Decimal                  @db.Decimal(18, 6)
  status                   SupplierCreditNoteStatus @default(DRAFT)
  reasonCode               String
  reasonDescription        String
  evidenceReference        String?
  applicationNotes         String?
  idempotencyKey           String?
  createdByUserId          String                   @db.Uuid
  cancelledByUserId        String?                  @db.Uuid
  cancelledAt              DateTime?
  cancellationReason       String?
  createdAt                DateTime                 @default(now())
  updatedAt                DateTime                 @updatedAt
  tenant                   Tenant                   @relation(fields: [tenantId], references: [id])
  company                  Company                  @relation(fields: [companyId], references: [id])
  supplier                 Supplier                 @relation(fields: [supplierId], references: [id])
  originalApInvoice        ApInvoice                @relation("SupplierCreditNoteOriginalInvoice", fields: [originalApInvoiceId], references: [id])
  createdBy                User                     @relation("SupplierCreditNoteCreator", fields: [createdByUserId], references: [id])
  cancelledBy              User?                    @relation("SupplierCreditNoteCanceller", fields: [cancelledByUserId], references: [id])

  @@unique([tenantId, companyId, publicReference])
  @@unique([tenantId, companyId, supplierId, supplierCreditNoteNumber], map: "SupplierCreditNote_tenantId_companyId_supplierId_supplierCredit")
  @@unique([tenantId, companyId, idempotencyKey])
  @@index([tenantId, companyId, status])
  @@index([tenantId, companyId, supplierId, status])
  @@index([tenantId, companyId, originalApInvoiceId])
}

model FinancePayee {
  id                    String                           @id @default(uuid()) @db.Uuid
  tenantId              String                           @db.Uuid
  companyId             String                           @db.Uuid
  payeeType             FinancePayeeType
  status                FinancePayeeStatus               @default(ACTIVE)
  displayName           String
  legalName             String?
  supplierId            String?                          @db.Uuid
  userId                String?                          @db.Uuid
  paymentReferenceLabel String?
  evidenceReference     String?
  createdByUserId       String                           @db.Uuid
  createdAt             DateTime                         @default(now())
  updatedAt             DateTime                         @updatedAt
  tenant                Tenant                           @relation(fields: [tenantId], references: [id])
  company               Company                          @relation(fields: [companyId], references: [id])
  supplier              Supplier?                        @relation(fields: [supplierId], references: [id])
  user                  User?                            @relation("FinancePayeeUser", fields: [userId], references: [id])
  createdBy             User                             @relation("FinancePayeeCreator", fields: [createdByUserId], references: [id])
  disbursementRequests  NonSupplierDisbursementRequest[]

  @@unique([tenantId, companyId, payeeType, supplierId])
  @@unique([tenantId, companyId, payeeType, userId])
  @@index([tenantId, companyId, payeeType, status])
  @@index([tenantId, companyId, displayName])
}

model NonSupplierDisbursementRequest {
  id                   String                     @id @default(uuid()) @db.Uuid
  tenantId             String                     @db.Uuid
  companyId            String                     @db.Uuid
  locationId           String                     @db.Uuid
  payeeId              String                     @db.Uuid
  cashAdvanceRequestId String?                    @db.Uuid
  pettyCashRequestId   String?                    @db.Uuid
  publicReference      String
  currencyCode         String                     @default("PHP")
  amountPhp            Decimal                    @db.Decimal(18, 6)
  status               DisbursementRequestStatus  @default(DRAFT)
  sourceType           DisbursementSourceType     @default(MANUAL_CONTROL)
  sourceEventKey       String
  requestReason        String
  evidenceReference    String?
  holdReason           String?
  rejectionReason      String?
  cancellationReason   String?
  idempotencyKey       String?
  requestedByUserId    String                     @db.Uuid
  submittedByUserId    String?                    @db.Uuid
  approvedByUserId     String?                    @db.Uuid
  rejectedByUserId     String?                    @db.Uuid
  cancelledByUserId    String?                    @db.Uuid
  submittedAt          DateTime?
  approvedAt           DateTime?
  rejectedAt           DateTime?
  cancelledAt          DateTime?
  createdAt            DateTime                   @default(now())
  updatedAt            DateTime                   @updatedAt
  tenant               Tenant                     @relation(fields: [tenantId], references: [id])
  company              Company                    @relation(fields: [companyId], references: [id])
  location             Location                   @relation(fields: [locationId], references: [id])
  payee                FinancePayee               @relation(fields: [payeeId], references: [id])
  cashAdvanceRequest   CashAdvanceRequest?        @relation(fields: [cashAdvanceRequestId], references: [id])
  pettyCashRequest     PettyCashRequest?          @relation(fields: [pettyCashRequestId], references: [id])
  requestedBy          User                       @relation("DisbursementRequester", fields: [requestedByUserId], references: [id])
  submittedBy          User?                      @relation("DisbursementSubmitter", fields: [submittedByUserId], references: [id])
  approvedBy           User?                      @relation("DisbursementApprover", fields: [approvedByUserId], references: [id])
  rejectedBy           User?                      @relation("DisbursementRejecter", fields: [rejectedByUserId], references: [id])
  cancelledBy          User?                      @relation("DisbursementCanceller", fields: [cancelledByUserId], references: [id])

  @@unique([tenantId, companyId, publicReference], map: "NonSupplierDisbursementRequest_tenantId_companyId_publicReferen")
  @@unique([tenantId, companyId, sourceEventKey], map: "NonSupplierDisbursementRequest_tenantId_companyId_sourceEventKe")
  @@unique([tenantId, companyId, idempotencyKey], map: "NonSupplierDisbursementRequest_tenantId_companyId_idempotencyKe")
  @@index([tenantId, companyId, locationId, status], map: "NonSupplierDisbursementRequest_tenantId_companyId_locationId_st")
  @@index([tenantId, companyId, payeeId, status], map: "NonSupplierDisbursementRequest_tenantId_companyId_payeeId_statu")
  @@index([tenantId, companyId, cashAdvanceRequestId], map: "NonSupplierDisbursementRequest_tenantId_companyId_cashAdvanceRe")
  @@index([tenantId, companyId, pettyCashRequestId], map: "NonSupplierDisbursementRequest_tenantId_companyId_pettyCashRequ")
}

model PaymentRequest {
  id                   String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId             String                       @db.Uuid
  companyId            String                       @db.Uuid
  locationId           String                       @db.Uuid
  supplierId           String                       @db.Uuid
  publicReference      String
  currencyCode         String                       @default("PHP")
  totalRequestedAmount Decimal                      @default(0) @db.Decimal(18, 6)
  status               PaymentRequestStatus         @default(DRAFT)
  requestedByUserId    String                       @db.Uuid
  submittedByUserId    String?                      @db.Uuid
  approvedByUserId     String?                      @db.Uuid
  rejectedByUserId     String?                      @db.Uuid
  cancelledByUserId    String?                      @db.Uuid
  approvalInstanceId   String?                      @unique @db.Uuid
  submittedAt          DateTime?
  approvedAt           DateTime?
  rejectedAt           DateTime?
  cancelledAt          DateTime?
  returnedAt           DateTime?
  holdReason           String?
  requestReason        String
  evidenceReference    String?
  rejectionReason      String?
  cancellationReason   String?
  idempotencyKey       String?
  createdAt            DateTime                     @default(now())
  updatedAt            DateTime                     @updatedAt
  tenant               Tenant                       @relation(fields: [tenantId], references: [id])
  company              Company                      @relation(fields: [companyId], references: [id])
  location             Location                     @relation(fields: [locationId], references: [id])
  supplier             Supplier                     @relation(fields: [supplierId], references: [id])
  requestedBy          User                         @relation("PaymentRequestRequester", fields: [requestedByUserId], references: [id])
  submittedBy          User?                        @relation("PaymentRequestSubmitter", fields: [submittedByUserId], references: [id])
  approvedBy           User?                        @relation("PaymentRequestApprover", fields: [approvedByUserId], references: [id])
  rejectedBy           User?                        @relation("PaymentRequestRejecter", fields: [rejectedByUserId], references: [id])
  cancelledBy          User?                        @relation("PaymentRequestCanceller", fields: [cancelledByUserId], references: [id])
  approvalInstance     ApprovalInstance?            @relation(fields: [approvalInstanceId], references: [id])
  lines                PaymentRequestLine[]
  releases             PaymentRelease[]
  cashAdvanceRequests  CashAdvanceRequest[]
  cashAdvanceLines     CashAdvanceLiquidationLine[]

  @@unique([tenantId, companyId, publicReference])
  @@unique([tenantId, companyId, idempotencyKey])
  @@index([tenantId, companyId, status])
  @@index([tenantId, companyId, locationId, status])
  @@index([tenantId, companyId, supplierId, status])
  @@index([requestedByUserId, status])
}

model PaymentRequestLine {
  id                         String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId                   String                     @db.Uuid
  companyId                  String                     @db.Uuid
  locationId                 String                     @db.Uuid
  paymentRequestId           String                     @db.Uuid
  apInvoiceId                String                     @db.Uuid
  lineNumber                 Int
  requestedAmount            Decimal                    @db.Decimal(18, 6)
  invoiceTotalSnapshot       Decimal                    @db.Decimal(18, 6)
  invoiceOutstandingSnapshot Decimal                    @db.Decimal(18, 6)
  notes                      String?
  createdByUserId            String                     @db.Uuid
  createdAt                  DateTime                   @default(now())
  updatedAt                  DateTime                   @updatedAt
  tenant                     Tenant                     @relation(fields: [tenantId], references: [id])
  company                    Company                    @relation(fields: [companyId], references: [id])
  location                   Location                   @relation(fields: [locationId], references: [id])
  paymentRequest             PaymentRequest             @relation(fields: [paymentRequestId], references: [id])
  apInvoice                  ApInvoice                  @relation(fields: [apInvoiceId], references: [id])
  createdBy                  User                       @relation("PaymentRequestLineCreator", fields: [createdByUserId], references: [id])
  paymentReleaseAllocations  PaymentReleaseAllocation[]

  @@unique([paymentRequestId, apInvoiceId])
  @@unique([paymentRequestId, lineNumber])
  @@index([tenantId, companyId, locationId])
  @@index([tenantId, companyId, apInvoiceId])
  @@index([tenantId, companyId, paymentRequestId])
}

model PaymentRelease {
  id                    String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId              String                     @db.Uuid
  companyId             String                     @db.Uuid
  locationId            String                     @db.Uuid
  supplierId            String                     @db.Uuid
  paymentRequestId      String                     @db.Uuid
  bankAccountId         String                     @db.Uuid
  publicReference       String
  currencyCode          String                     @default("PHP")
  method                PaymentReleaseMethod       @default(BANK_TRANSFER)
  status                PaymentReleaseStatus       @default(DRAFT)
  totalRequestedAmount  Decimal                    @default(0) @db.Decimal(18, 6)
  releaseAmount         Decimal                    @default(0) @db.Decimal(18, 6)
  releasedAmount        Decimal                    @default(0) @db.Decimal(18, 6)
  sourceEventKey        String
  idempotencyKey        String?
  approvalInstanceId    String?                    @unique @db.Uuid
  reason                String
  holdReason            String?
  exceptionReason       String?
  cancellationReason    String?
  reversalReason        String?
  evidenceReference     String?
  releaseReference      String?
  scheduledAt           DateTime?
  releasedAt            DateTime?
  cancelledAt           DateTime?
  reversedAt            DateTime?
  heldAt                DateTime?
  createdByUserId       String                     @db.Uuid
  releasedByUserId      String?                    @db.Uuid
  heldByUserId          String?                    @db.Uuid
  cancelledByUserId     String?                    @db.Uuid
  reversedByUserId      String?                    @db.Uuid
  createdAt             DateTime                   @default(now())
  updatedAt             DateTime                   @updatedAt
  tenant                Tenant                     @relation(fields: [tenantId], references: [id])
  company               Company                    @relation(fields: [companyId], references: [id])
  location              Location                   @relation(fields: [locationId], references: [id])
  supplier              Supplier                   @relation(fields: [supplierId], references: [id])
  paymentRequest        PaymentRequest             @relation(fields: [paymentRequestId], references: [id])
  bankAccount           BankAccount                @relation(fields: [bankAccountId], references: [id])
  createdBy             User                       @relation("PaymentReleaseCreator", fields: [createdByUserId], references: [id])
  releasedBy            User?                      @relation("PaymentReleaseReleaser", fields: [releasedByUserId], references: [id])
  heldBy                User?                      @relation("PaymentReleaseHolder", fields: [heldByUserId], references: [id])
  cancelledBy           User?                      @relation("PaymentReleaseCanceller", fields: [cancelledByUserId], references: [id])
  reversedBy            User?                      @relation("PaymentReleaseReverser", fields: [reversedByUserId], references: [id])
  approvalInstance      ApprovalInstance?          @relation(fields: [approvalInstanceId], references: [id])
  allocations           PaymentReleaseAllocation[]
  executions            PaymentReleaseExecution[]
  reconciliationMatches BankReconciliationMatch[]

  @@unique([companyId, publicReference])
  @@unique([tenantId, companyId, paymentRequestId])
  @@unique([tenantId, companyId, sourceEventKey])
  @@unique([tenantId, companyId, idempotencyKey])
  @@index([tenantId, companyId, status])
  @@index([tenantId, companyId, locationId, status])
  @@index([tenantId, companyId, supplierId, status])
  @@index([tenantId, companyId, bankAccountId, status])
}

model PaymentReleaseAllocation {
  id                         String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId                   String             @db.Uuid
  companyId                  String             @db.Uuid
  paymentReleaseId           String             @db.Uuid
  paymentRequestLineId       String             @db.Uuid
  apInvoiceId                String             @db.Uuid
  allocatedAmount            Decimal            @db.Decimal(18, 6)
  requestLineSnapshotAmount  Decimal            @db.Decimal(18, 6)
  invoiceOutstandingSnapshot Decimal            @db.Decimal(18, 6)
  createdByUserId            String             @db.Uuid
  createdAt                  DateTime           @default(now())
  updatedAt                  DateTime           @updatedAt
  tenant                     Tenant             @relation(fields: [tenantId], references: [id])
  company                    Company            @relation(fields: [companyId], references: [id])
  paymentRelease             PaymentRelease     @relation(fields: [paymentReleaseId], references: [id])
  paymentRequestLine         PaymentRequestLine @relation(fields: [paymentRequestLineId], references: [id])
  apInvoice                  ApInvoice          @relation(fields: [apInvoiceId], references: [id])
  createdBy                  User               @relation("PaymentReleaseAllocationCreator", fields: [createdByUserId], references: [id])

  @@unique([tenantId, companyId, paymentReleaseId, paymentRequestLineId], map: "PaymentReleaseAllocation_tenantId_companyId_paymentReleaseId_pa")
  @@unique([tenantId, companyId, paymentRequestLineId], map: "PaymentReleaseAllocation_tenantId_companyId_paymentRequestLineI")
  @@index([tenantId, companyId, paymentReleaseId], map: "PaymentReleaseAllocation_tenantId_companyId_paymentReleaseId_id")
  @@index([tenantId, companyId, apInvoiceId])
}

model PaymentReleaseExecution {
  id                 String                        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String                        @db.Uuid
  companyId          String                        @db.Uuid
  paymentReleaseId   String                        @db.Uuid
  status             PaymentReleaseExecutionStatus @default(REQUESTED)
  idempotencyKey     String
  requestPayloadHash String?
  releaseReference   String?
  failureCode        String?
  failureReason      String?
  executionSnapshot  Json?
  actorUserId        String                        @db.Uuid
  createdAt          DateTime                      @default(now())
  updatedAt          DateTime                      @updatedAt
  tenant             Tenant                        @relation(fields: [tenantId], references: [id])
  company            Company                       @relation(fields: [companyId], references: [id])
  paymentRelease     PaymentRelease                @relation(fields: [paymentReleaseId], references: [id])
  actor              User                          @relation("PaymentReleaseExecutionActor", fields: [actorUserId], references: [id])

  @@unique([tenantId, companyId, idempotencyKey])
  @@index([tenantId, companyId, paymentReleaseId, status], map: "PaymentReleaseExecution_tenantId_companyId_paymentReleaseId_sta")
}

model Employee {
  id                  String                       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId            String                       @db.Uuid
  companyId           String                       @db.Uuid
  userId              String?                      @db.Uuid
  employeeCode        String
  legalName           String
  preferredName       String?
  jobTitle            String?
  emailPersonal       String?
  phoneNumber         String?
  dateOfBirth         DateTime?
  employmentType      EmployeeEmploymentType       @default(FULL_TIME)
  status              EmployeeStatus               @default(ACTIVE)
  hireDate            DateTime
  separationDate      DateTime?
  homeLocationId      String?                      @db.Uuid
  createdByUserId     String                       @db.Uuid
  updatedByUserId     String?                      @db.Uuid
  createdAt           DateTime                     @default(now())
  updatedAt           DateTime                     @updatedAt
  tenant              Tenant                       @relation(fields: [tenantId], references: [id])
  company             Company                      @relation(fields: [companyId], references: [id])
  user                User?                        @relation("EmployeeUser", fields: [userId], references: [id])
  homeLocation        Location?                    @relation(fields: [homeLocationId], references: [id])
  createdBy           User                         @relation("EmployeeCreator", fields: [createdByUserId], references: [id])
  updatedBy           User?                        @relation("EmployeeUpdater", fields: [updatedByUserId], references: [id])
  assignments         EmployeeAssignment[]
  leaveRequests       EmployeeLeaveRequest[]
  overtimeRecords     EmployeeOvertimeRecord[]
  trainingRecords     EmployeeTrainingRecord[]
  complianceDocuments EmployeeComplianceDocument[]
  scheduleLines       WorkforceScheduleLine[]
  attendanceLines     AttendanceImportLine[]

  @@unique([tenantId, companyId, employeeCode])
  @@unique([tenantId, companyId, userId])
  @@index([tenantId, companyId, status])
  @@index([tenantId, companyId, homeLocationId, status])
}

model EmployeeAssignment {
  id              String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId        String                   @db.Uuid
  companyId       String                   @db.Uuid
  employeeId      String                   @db.Uuid
  locationId      String                   @db.Uuid
  brandId         String?                  @db.Uuid
  departmentId    String?                  @db.Uuid
  costCenterId    String?                  @db.Uuid
  assignmentType  EmployeeAssignmentType   @default(PRIMARY)
  status          EmployeeAssignmentStatus @default(PLANNED)
  isPrimary       Boolean                  @default(false)
  roleLabel       String?
  effectiveFrom   DateTime
  effectiveTo     DateTime?
  reason          String?
  createdByUserId String                   @db.Uuid
  createdAt       DateTime                 @default(now())
  updatedAt       DateTime                 @updatedAt
  tenant          Tenant                   @relation(fields: [tenantId], references: [id])
  company         Company                  @relation(fields: [companyId], references: [id])
  employee        Employee                 @relation(fields: [employeeId], references: [id])
  location        Location                 @relation(fields: [locationId], references: [id])
  brand           Brand?                   @relation(fields: [brandId], references: [id])
  department      Department?              @relation(fields: [departmentId], references: [id])
  costCenter      CostCenter?              @relation(fields: [costCenterId], references: [id])
  createdBy       User                     @relation("EmployeeAssignmentCreator", fields: [createdByUserId], references: [id])

  @@index([tenantId, companyId, employeeId, status, effectiveFrom], map: "EmployeeAssignment_tenantId_companyId_employeeId_status_effecti")
  @@index([tenantId, companyId, locationId, status, effectiveFrom], map: "EmployeeAssignment_tenantId_companyId_locationId_status_effecti")
}

model EmployeeLeaveRequest {
  id                 String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String              @db.Uuid
  companyId          String              @db.Uuid
  employeeId         String              @db.Uuid
  locationId         String?             @db.Uuid
  leaveType          EmployeeLeaveType
  status             EmployeeLeaveStatus @default(DRAFT)
  requestedByUserId  String              @db.Uuid
  approvedByUserId   String?             @db.Uuid
  reason             String
  startDate          DateTime
  endDate            DateTime
  requestedMinutes   Int                 @default(0)
  approvalInstanceId String?             @unique @db.Uuid
  idempotencyKey     String?
  sourceEventKey     String
  approvedAt         DateTime?
  submittedAt        DateTime?
  decisionAt         DateTime?
  decisionNote       String?
  createdByUserId    String              @db.Uuid
  updatedByUserId    String?             @db.Uuid
  createdAt          DateTime            @default(now())
  updatedAt          DateTime            @updatedAt
  tenant             Tenant              @relation(fields: [tenantId], references: [id])
  company            Company             @relation(fields: [companyId], references: [id])
  employee           Employee            @relation(fields: [employeeId], references: [id])
  location           Location?           @relation(fields: [locationId], references: [id])
  requestedByUser    User                @relation("EmployeeLeaveRequestedBy", fields: [requestedByUserId], references: [id])
  approvedByUser     User?               @relation("EmployeeLeaveApprovedBy", fields: [approvedByUserId], references: [id])
  createdBy          User                @relation("EmployeeLeaveCreatedBy", fields: [createdByUserId], references: [id])
  updatedBy          User?               @relation("EmployeeLeaveUpdatedBy", fields: [updatedByUserId], references: [id])
  approvalInstance   ApprovalInstance?   @relation(fields: [approvalInstanceId], references: [id])

  @@index([tenantId, companyId, employeeId, status, startDate], map: "EmployeeLeaveRequest_tenantId_companyId_employeeId_status_start")
  @@index([tenantId, companyId, locationId, status, startDate], map: "EmployeeLeaveRequest_tenantId_companyId_locationId_status_start")
  @@index([tenantId, companyId, sourceEventKey])
}

model EmployeeOvertimeRecord {
  id                 String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String                 @db.Uuid
  companyId          String                 @db.Uuid
  employeeId         String                 @db.Uuid
  locationId         String?                @db.Uuid
  overtimeType       EmployeeOvertimeType
  status             EmployeeOvertimeStatus @default(DRAFT)
  workedStartAt      DateTime
  workedEndAt        DateTime
  requestedMinutes   Int
  reason             String
  requestedByUserId  String                 @db.Uuid
  approvedByUserId   String?                @db.Uuid
  approvalInstanceId String?                @unique @db.Uuid
  idempotencyKey     String?
  sourceEventKey     String
  approvedAt         DateTime?
  createdByUserId    String                 @db.Uuid
  updatedByUserId    String?                @db.Uuid
  createdAt          DateTime               @default(now())
  updatedAt          DateTime               @updatedAt
  tenant             Tenant                 @relation(fields: [tenantId], references: [id])
  company            Company                @relation(fields: [companyId], references: [id])
  employee           Employee               @relation(fields: [employeeId], references: [id])
  location           Location?              @relation(fields: [locationId], references: [id])
  requestedByUser    User                   @relation("EmployeeOvertimeRequestedBy", fields: [requestedByUserId], references: [id])
  approvedByUser     User?                  @relation("EmployeeOvertimeApprovedBy", fields: [approvedByUserId], references: [id])
  createdBy          User                   @relation("EmployeeOvertimeCreatedBy", fields: [createdByUserId], references: [id])
  updatedBy          User?                  @relation("EmployeeOvertimeUpdatedBy", fields: [updatedByUserId], references: [id])
  approvalInstance   ApprovalInstance?      @relation(fields: [approvalInstanceId], references: [id])

  @@index([tenantId, companyId, employeeId, status, workedStartAt], map: "EmployeeOvertimeRecord_tenantId_companyId_employeeId_status_wor")
  @@index([tenantId, companyId, locationId, status, workedStartAt], map: "EmployeeOvertimeRecord_tenantId_companyId_locationId_status_wor")
  @@index([tenantId, companyId, sourceEventKey])
}

model EmployeeTrainingRecord {
  id               String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId         String                 @db.Uuid
  companyId        String                 @db.Uuid
  employeeId       String                 @db.Uuid
  trainingCode     String
  trainingName     String
  provider         String?
  scheduledAt      DateTime?
  completedAt      DateTime?
  validUntil       DateTime?
  status           EmployeeTrainingStatus @default(PLANNED)
  attachmentId     String?                @db.Uuid
  createdByUserId  String                 @db.Uuid
  updatedByUserId  String?                @db.Uuid
  requiredForScope Boolean                @default(false)
  createdAt        DateTime               @default(now())
  updatedAt        DateTime               @updatedAt
  tenant           Tenant                 @relation(fields: [tenantId], references: [id])
  company          Company                @relation(fields: [companyId], references: [id])
  employee         Employee               @relation(fields: [employeeId], references: [id])
  certificate      Attachment?            @relation("EmployeeTrainingCertificate", fields: [attachmentId], references: [id])
  createdBy        User                   @relation("EmployeeTrainingCreatedBy", fields: [createdByUserId], references: [id])
  updatedBy        User?                  @relation("EmployeeTrainingUpdatedBy", fields: [updatedByUserId], references: [id])

  @@unique([tenantId, companyId, employeeId, trainingCode], map: "EmployeeTrainingRecord_tenantId_companyId_employeeId_trainingCo")
  @@index([tenantId, companyId, employeeId, status, validUntil], map: "EmployeeTrainingRecord_tenantId_companyId_employeeId_status_val")
  @@index([tenantId, companyId, status, validUntil])
}

model EmployeeComplianceDocument {
  id                 String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String                 @db.Uuid
  companyId          String                 @db.Uuid
  employeeId         String                 @db.Uuid
  documentType       EmployeeDocumentType
  documentNumber     String?
  status             EmployeeDocumentStatus @default(ACTIVE)
  issuedAt           DateTime?
  expiryAt           DateTime?
  issuedBy           String?
  attachmentId       String?                @db.Uuid
  isMandatoryForRole Boolean                @default(false)
  notes              String?
  createdByUserId    String                 @db.Uuid
  updatedByUserId    String?                @db.Uuid
  createdAt          DateTime               @default(now())
  updatedAt          DateTime               @updatedAt
  tenant             Tenant                 @relation(fields: [tenantId], references: [id])
  company            Company                @relation(fields: [companyId], references: [id])
  employee           Employee               @relation(fields: [employeeId], references: [id])
  attachment         Attachment?            @relation("EmployeeComplianceDocumentAttachment", fields: [attachmentId], references: [id])
  createdBy          User                   @relation("EmployeeDocumentCreatedBy", fields: [createdByUserId], references: [id])
  updatedBy          User?                  @relation("EmployeeDocumentUpdatedBy", fields: [updatedByUserId], references: [id])

  @@index([tenantId, companyId, employeeId, documentType, status], map: "EmployeeComplianceDocument_tenantId_companyId_employeeId_docume")
  @@index([tenantId, companyId, status, expiryAt], map: "EmployeeComplianceDocument_tenantId_companyId_status_expiryAt_i")
}

model WorkforceSchedule {
  id                 String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String                  @db.Uuid
  companyId          String                  @db.Uuid
  brandId            String?                 @db.Uuid
  locationId         String                  @db.Uuid
  departmentId       String?                 @db.Uuid
  costCenterId       String?                 @db.Uuid
  publicReference    String
  scheduleDate       DateTime
  shiftType          WorkforceShiftType
  status             WorkforceScheduleStatus @default(DRAFT)
  plannedHeadcount   Int                     @default(0)
  assignedHeadcount  Int                     @default(0)
  coverageGapCount   Int                     @default(0)
  plannedMinutes     Int                     @default(0)
  notes              String?
  reason             String?
  evidenceReference  String?
  approvalInstanceId String?                 @unique @db.Uuid
  idempotencyKey     String?
  sourceEventKey     String
  createdByUserId    String                  @db.Uuid
  submittedByUserId  String?                 @db.Uuid
  approvedByUserId   String?                 @db.Uuid
  publishedByUserId  String?                 @db.Uuid
  cancelledByUserId  String?                 @db.Uuid
  submittedAt        DateTime?
  approvedAt         DateTime?
  publishedAt        DateTime?
  cancelledAt        DateTime?
  createdAt          DateTime                @default(now())
  updatedAt          DateTime                @updatedAt
  tenant             Tenant                  @relation(fields: [tenantId], references: [id])
  company            Company                 @relation(fields: [companyId], references: [id])
  brand              Brand?                  @relation(fields: [brandId], references: [id])
  location           Location                @relation(fields: [locationId], references: [id])
  department         Department?             @relation(fields: [departmentId], references: [id])
  costCenter         CostCenter?             @relation(fields: [costCenterId], references: [id])
  createdBy          User                    @relation("WorkforceScheduleCreator", fields: [createdByUserId], references: [id])
  submittedBy        User?                   @relation("WorkforceScheduleSubmitter", fields: [submittedByUserId], references: [id])
  approvedBy         User?                   @relation("WorkforceScheduleApprover", fields: [approvedByUserId], references: [id])
  publishedBy        User?                   @relation("WorkforceSchedulePublisher", fields: [publishedByUserId], references: [id])
  cancelledBy        User?                   @relation("WorkforceScheduleCanceller", fields: [cancelledByUserId], references: [id])
  approvalInstance   ApprovalInstance?       @relation(fields: [approvalInstanceId], references: [id])
  lines              WorkforceScheduleLine[]

  @@unique([companyId, publicReference])
  @@unique([tenantId, companyId, sourceEventKey])
  @@unique([tenantId, companyId, idempotencyKey])
  @@index([tenantId, companyId, locationId, scheduleDate, status], map: "WorkforceSchedule_tenantId_companyId_locationId_scheduleDate_st")
  @@index([tenantId, companyId, brandId, scheduleDate, status], map: "WorkforceSchedule_tenantId_companyId_brandId_scheduleDate_statu")
}

model WorkforceScheduleLine {
  id                  String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId            String                      @db.Uuid
  companyId           String                      @db.Uuid
  workforceScheduleId String                      @db.Uuid
  locationId          String                      @db.Uuid
  brandId             String?                     @db.Uuid
  departmentId        String?                     @db.Uuid
  costCenterId        String?                     @db.Uuid
  employeeId          String?                     @db.Uuid
  lineNumber          Int
  stationCode         String
  roleLabel           String
  plannedStartAt      DateTime
  plannedEndAt        DateTime
  plannedMinutes      Int
  status              WorkforceScheduleLineStatus @default(PLANNED)
  coverageGapReason   String?
  evidenceReference   String?
  createdByUserId     String                      @db.Uuid
  updatedByUserId     String?                     @db.Uuid
  createdAt           DateTime                    @default(now())
  updatedAt           DateTime                    @updatedAt
  tenant              Tenant                      @relation(fields: [tenantId], references: [id])
  company             Company                     @relation(fields: [companyId], references: [id])
  schedule            WorkforceSchedule           @relation(fields: [workforceScheduleId], references: [id])
  location            Location                    @relation(fields: [locationId], references: [id])
  brand               Brand?                      @relation(fields: [brandId], references: [id])
  department          Department?                 @relation(fields: [departmentId], references: [id])
  costCenter          CostCenter?                 @relation(fields: [costCenterId], references: [id])
  employee            Employee?                   @relation(fields: [employeeId], references: [id])
  createdBy           User                        @relation("WorkforceScheduleLineCreator", fields: [createdByUserId], references: [id])
  updatedBy           User?                       @relation("WorkforceScheduleLineUpdater", fields: [updatedByUserId], references: [id])

  @@unique([workforceScheduleId, lineNumber])
  @@index([tenantId, companyId, workforceScheduleId, status], map: "WorkforceScheduleLine_tenantId_companyId_workforceScheduleId_st")
  @@index([tenantId, companyId, locationId, plannedStartAt, status], map: "WorkforceScheduleLine_tenantId_companyId_locationId_plannedStar")
  @@index([tenantId, companyId, employeeId, plannedStartAt], map: "WorkforceScheduleLine_tenantId_companyId_employeeId_plannedStar")
}

model AttendanceImportBatch {
  id                  String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId            String                      @db.Uuid
  companyId           String                      @db.Uuid
  brandId             String?                     @db.Uuid
  locationId          String                      @db.Uuid
  publicReference     String
  businessDate        DateTime
  sourceType          String
  sourceReference     String
  sourceFileReference String?
  sourceTimezone      String                      @default("Asia/Manila")
  status              AttendanceImportBatchStatus @default(DRAFT)
  rowCount            Int                         @default(0)
  acceptedCount       Int                         @default(0)
  exceptionCount      Int                         @default(0)
  duplicateCount      Int                         @default(0)
  validationSummary   Json?
  evidenceReference   String?
  rejectionReason     String?
  voidReason          String?
  approvalInstanceId  String?                     @unique @db.Uuid
  idempotencyKey      String
  createdByUserId     String                      @db.Uuid
  reviewedByUserId    String?                     @db.Uuid
  voidedByUserId      String?                     @db.Uuid
  importedAt          DateTime?
  reviewedAt          DateTime?
  voidedAt            DateTime?
  createdAt           DateTime                    @default(now())
  updatedAt           DateTime                    @updatedAt
  tenant              Tenant                      @relation(fields: [tenantId], references: [id])
  company             Company                     @relation(fields: [companyId], references: [id])
  brand               Brand?                      @relation(fields: [brandId], references: [id])
  location            Location                    @relation(fields: [locationId], references: [id])
  createdBy           User                        @relation("AttendanceImportBatchCreator", fields: [createdByUserId], references: [id])
  reviewedBy          User?                       @relation("AttendanceImportBatchReviewer", fields: [reviewedByUserId], references: [id])
  voidedBy            User?                       @relation("AttendanceImportBatchVoider", fields: [voidedByUserId], references: [id])
  approvalInstance    ApprovalInstance?           @relation(fields: [approvalInstanceId], references: [id])
  lines               AttendanceImportLine[]

  @@unique([companyId, publicReference])
  @@unique([tenantId, companyId, idempotencyKey])
  @@index([tenantId, companyId, locationId, businessDate, status], map: "AttendanceImportBatch_tenantId_companyId_locationId_businessDat")
  @@index([tenantId, companyId, brandId, businessDate, status], map: "AttendanceImportBatch_tenantId_companyId_brandId_businessDate_s")
}

model AttendanceImportLine {
  id                      String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId                String                     @db.Uuid
  companyId               String                     @db.Uuid
  attendanceImportBatchId String                     @db.Uuid
  locationId              String                     @db.Uuid
  brandId                 String?                    @db.Uuid
  employeeId              String?                    @db.Uuid
  sourceRowNumber         Int
  employeeCodeRaw         String?
  employeeNameRaw         String?
  punchInAt               DateTime?
  punchOutAt              DateTime?
  workMinutes             Int                        @default(0)
  status                  AttendanceImportLineStatus @default(ACCEPTED)
  exceptionCode           String?
  exceptionMessage        String?
  sourcePayload           Json?
  createdAt               DateTime                   @default(now())
  updatedAt               DateTime                   @updatedAt
  tenant                  Tenant                     @relation(fields: [tenantId], references: [id])
  company                 Company                    @relation(fields: [companyId], references: [id])
  batch                   AttendanceImportBatch      @relation(fields: [attendanceImportBatchId], references: [id])
  location                Location                   @relation(fields: [locationId], references: [id])
  brand                   Brand?                     @relation(fields: [brandId], references: [id])
  employee                Employee?                  @relation(fields: [employeeId], references: [id])

  @@unique([attendanceImportBatchId, sourceRowNumber], map: "AttendanceImportLine_attendanceImportBatchId_sourceRowNumber_ke")
  @@index([tenantId, companyId, attendanceImportBatchId, status], map: "AttendanceImportLine_tenantId_companyId_attendanceImportBatchId")
  @@index([tenantId, companyId, locationId, punchInAt, status], map: "AttendanceImportLine_tenantId_companyId_locationId_punchInAt_st")
  @@index([tenantId, companyId, employeeId, punchInAt], map: "AttendanceImportLine_tenantId_companyId_employeeId_punchInAt_id")
}

model BankAccount {
  id                  String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId            String               @db.Uuid
  companyId           String               @db.Uuid
  locationId          String?              @db.Uuid
  ledgerAccountId     String               @db.Uuid
  publicReference     String
  bankName            String
  bankBranchCode      String?
  maskedAccountNumber String
  accountType         BankAccountType
  status              BankAccountStatus    @default(DRAFT)
  currencyCode        String               @default("PHP")
  activeFrom          DateTime?
  activeTo            DateTime?
  evidenceReference   String?
  createdByUserId     String               @db.Uuid
  createdAt           DateTime             @default(now())
  updatedAt           DateTime             @updatedAt
  tenant              Tenant               @relation(fields: [tenantId], references: [id])
  company             Company              @relation(fields: [companyId], references: [id])
  location            Location?            @relation(fields: [locationId], references: [id])
  ledgerAccount       ChartOfAccount       @relation(fields: [ledgerAccountId], references: [id])
  createdBy           User                 @relation("BankAccountCreator", fields: [createdByUserId], references: [id])
  deposits            BranchCashDeposit[]
  statements          BankStatement[]
  reconciliations     BankReconciliation[]
  bankStatementLines  BankStatementLine[]
  paymentReleases     PaymentRelease[]
  cashAdvanceRequests CashAdvanceRequest[]

  @@unique([companyId, publicReference])
  @@index([tenantId, companyId, status])
  @@index([tenantId, companyId, locationId, status])
  @@index([ledgerAccountId])
}

model BranchCashDeposit {
  id                    String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId              String                    @db.Uuid
  companyId             String                    @db.Uuid
  locationId            String                    @db.Uuid
  bankAccountId         String                    @db.Uuid
  publicReference       String
  depositDate           DateTime
  amountPhp             Decimal                   @db.Decimal(18, 6)
  status                BranchCashDepositStatus   @default(DRAFT)
  depositSlipNumber     String?
  sourceEventKey        String
  evidenceReference     String?
  notes                 String?
  declaredByUserId      String                    @db.Uuid
  verifiedByUserId      String?                   @db.Uuid
  verifiedAt            DateTime?
  clarificationReason   String?
  cancellationReason    String?
  createdAt             DateTime                  @default(now())
  updatedAt             DateTime                  @updatedAt
  tenant                Tenant                    @relation(fields: [tenantId], references: [id])
  company               Company                   @relation(fields: [companyId], references: [id])
  location              Location                  @relation(fields: [locationId], references: [id])
  bankAccount           BankAccount               @relation(fields: [bankAccountId], references: [id])
  declaredBy            User                      @relation("BranchCashDepositDeclarer", fields: [declaredByUserId], references: [id])
  verifiedBy            User?                     @relation("BranchCashDepositVerifier", fields: [verifiedByUserId], references: [id])
  reconciliationMatches BankReconciliationMatch[]

  @@unique([companyId, publicReference])
  @@unique([tenantId, companyId, sourceEventKey])
  @@index([tenantId, companyId, locationId, status, depositDate], map: "BranchCashDeposit_tenantId_companyId_locationId_status_depositD")
  @@index([tenantId, companyId, bankAccountId, status])
}

model BankStatement {
  id               String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId         String               @db.Uuid
  companyId        String               @db.Uuid
  bankAccountId    String               @db.Uuid
  publicReference  String
  statementFrom    DateTime
  statementTo      DateTime
  statementDate    DateTime
  sourceType       String
  sourceReference  String
  sourceEventKey   String
  status           BankStatementStatus  @default(DRAFT)
  currencyCode     String               @default("PHP")
  openingBalance   Decimal              @default(0) @db.Decimal(18, 6)
  closingBalance   Decimal              @default(0) @db.Decimal(18, 6)
  validationErrors Json?
  importedAt       DateTime?
  createdByUserId  String               @db.Uuid
  createdAt        DateTime             @default(now())
  updatedAt        DateTime             @updatedAt
  tenant           Tenant               @relation(fields: [tenantId], references: [id])
  company          Company              @relation(fields: [companyId], references: [id])
  bankAccount      BankAccount          @relation(fields: [bankAccountId], references: [id])
  createdBy        User                 @relation("BankStatementCreator", fields: [createdByUserId], references: [id])
  lines            BankStatementLine[]
  reconciliations  BankReconciliation[]

  @@unique([companyId, publicReference])
  @@unique([tenantId, companyId, sourceEventKey])
  @@index([tenantId, companyId, bankAccountId, status, statementDate], map: "BankStatement_tenantId_companyId_bankAccountId_status_statement")
  @@index([sourceReference])
}

model BankStatementLine {
  id              String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId        String                    @db.Uuid
  companyId       String                    @db.Uuid
  bankStatementId String                    @db.Uuid
  bankAccountId   String                    @db.Uuid
  transactionDate DateTime
  valueDate       DateTime?
  bankReference   String
  description     String
  debitAmount     Decimal                   @default(0) @db.Decimal(18, 6)
  creditAmount    Decimal                   @default(0) @db.Decimal(18, 6)
  netAmount       Decimal                   @db.Decimal(18, 6)
  status          BankStatementLineStatus   @default(UNMATCHED)
  sourceEventKey  String
  notes           String?
  matchedAmount   Decimal                   @default(0) @db.Decimal(18, 6)
  createdAt       DateTime                  @default(now())
  updatedAt       DateTime                  @updatedAt
  tenant          Tenant                    @relation(fields: [tenantId], references: [id])
  company         Company                   @relation(fields: [companyId], references: [id])
  statement       BankStatement             @relation(fields: [bankStatementId], references: [id])
  bankAccount     BankAccount               @relation(fields: [bankAccountId], references: [id])
  matches         BankReconciliationMatch[]

  @@unique([tenantId, companyId, sourceEventKey])
  @@index([tenantId, companyId, bankStatementId, transactionDate], map: "BankStatementLine_tenantId_companyId_bankStatementId_transactio")
  @@index([tenantId, companyId, bankAccountId, status])
}

model BankReconciliation {
  id                 String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String                    @db.Uuid
  companyId          String                    @db.Uuid
  bankAccountId      String                    @db.Uuid
  accountingPeriodId String                    @db.Uuid
  bankStatementId    String                    @db.Uuid
  publicReference    String
  status             BankReconciliationStatus  @default(OPEN)
  preparedByUserId   String                    @db.Uuid
  reviewedByUserId   String?                   @db.Uuid
  approvedByUserId   String?                   @db.Uuid
  preparedAt         DateTime
  reviewedAt         DateTime?
  approvedAt         DateTime?
  closedAt           DateTime?
  reopenedAt         DateTime?
  reason             String?
  varianceAmount     Decimal                   @default(0) @db.Decimal(18, 6)
  createdAt          DateTime                  @default(now())
  updatedAt          DateTime                  @updatedAt
  tenant             Tenant                    @relation(fields: [tenantId], references: [id])
  company            Company                   @relation(fields: [companyId], references: [id])
  bankAccount        BankAccount               @relation(fields: [bankAccountId], references: [id])
  accountingPeriod   AccountingPeriod          @relation(fields: [accountingPeriodId], references: [id])
  statement          BankStatement             @relation(fields: [bankStatementId], references: [id])
  preparedBy         User                      @relation("BankReconciliationPreparer", fields: [preparedByUserId], references: [id])
  reviewedBy         User?                     @relation("BankReconciliationReviewer", fields: [reviewedByUserId], references: [id])
  approvedBy         User?                     @relation("BankReconciliationApprover", fields: [approvedByUserId], references: [id])
  matches            BankReconciliationMatch[]

  @@unique([companyId, publicReference])
  @@index([tenantId, companyId, bankAccountId, status])
  @@index([tenantId, companyId, accountingPeriodId, status], map: "BankReconciliation_tenantId_companyId_accountingPeriodId_status")
}

model BankReconciliationMatch {
  id                     String                        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId               String                        @db.Uuid
  companyId              String                        @db.Uuid
  reconciliationId       String                        @db.Uuid
  statementLineId        String                        @db.Uuid
  branchCashDepositId    String?                       @db.Uuid
  paymentReleaseId       String?                       @db.Uuid
  sourceType             BankReconciliationSourceType
  sourceDocumentId       String                        @db.Uuid
  sourceDocumentSnapshot Json?
  matchedAmount          Decimal                       @default(0) @db.Decimal(18, 6)
  status                 BankReconciliationMatchStatus @default(PROPOSED)
  idempotencyKey         String
  matchedByUserId        String?                       @db.Uuid
  matchedAt              DateTime?
  reason                 String?
  evidenceReference      String?
  createdAt              DateTime                      @default(now())
  updatedAt              DateTime                      @updatedAt
  tenant                 Tenant                        @relation(fields: [tenantId], references: [id])
  company                Company                       @relation(fields: [companyId], references: [id])
  reconciliation         BankReconciliation            @relation(fields: [reconciliationId], references: [id])
  statementLine          BankStatementLine             @relation(fields: [statementLineId], references: [id])
  branchCashDeposit      BranchCashDeposit?            @relation(fields: [branchCashDepositId], references: [id])
  paymentRelease         PaymentRelease?               @relation(fields: [paymentReleaseId], references: [id])
  matchedBy              User?                         @relation("BankReconciliationMatchMatcher", fields: [matchedByUserId], references: [id])

  @@unique([tenantId, companyId, idempotencyKey])
  @@unique([reconciliationId, statementLineId, sourceType, sourceDocumentId], map: "BankReconciliationMatch_reconciliationId_statementLineId_source")
  @@index([tenantId, companyId, reconciliationId, status], map: "BankReconciliationMatch_tenantId_companyId_reconciliationId_sta")
  @@index([tenantId, companyId, sourceType, sourceDocumentId], map: "BankReconciliationMatch_tenantId_companyId_sourceType_sourceDoc")
  @@index([tenantId, companyId, branchCashDepositId], map: "BankReconciliationMatch_tenantId_companyId_branchCashDepositId_")
  @@index([tenantId, companyId, paymentReleaseId])
}

model ReleaseReadinessGate {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId          String    @db.Uuid
  companyId         String    @db.Uuid
  gateKey           String
  category          String
  title             String
  description       String
  ownerRole         String
  status            String    @default("PENDING")
  requiredByPolicy  Boolean   @default(true)
  evidenceReference String?
  decisionNote      String?
  blockerSummary    String?
  targetDate        DateTime?
  signedOffAt       DateTime?
  signedOffByUserId String?   @db.Uuid
  sourceDecisionId  String    @default("DEC-0036")
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt
  tenant            Tenant    @relation(fields: [tenantId], references: [id])
  company           Company   @relation(fields: [companyId], references: [id])

  @@unique([companyId, gateKey])
  @@index([tenantId, companyId, category, status])
}

model DeploymentEvidenceRecord {
  id                 String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String    @db.Uuid
  companyId          String    @db.Uuid
  evidenceType       String
  title              String
  evidenceReference  String
  environment        String
  performedAt        DateTime
  performedBy        String
  verificationStatus String    @default("RECORDED")
  verifiedAt         DateTime?
  verifiedByUserId   String?   @db.Uuid
  rejectedAt         DateTime?
  rejectedByUserId   String?   @db.Uuid
  notes              String?
  sourceDecisionId   String    @default("DEC-0036")
  createdByUserId    String    @db.Uuid
  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt
  tenant             Tenant    @relation(fields: [tenantId], references: [id])
  company            Company   @relation(fields: [companyId], references: [id])
  createdByUser      User      @relation("DeploymentEvidenceCreatedBy", fields: [createdByUserId], references: [id])
  verifiedByUser     User?     @relation("DeploymentEvidenceVerifiedBy", fields: [verifiedByUserId], references: [id], onDelete: Restrict)
  rejectedByUser     User?     @relation("DeploymentEvidenceRejectedBy", fields: [rejectedByUserId], references: [id], onDelete: Restrict)

  @@index([tenantId, companyId, evidenceType, verificationStatus], map: "DeploymentEvidenceRecord_tenantId_companyId_evidenceType_verifi")
  @@index([tenantId, companyId, performedAt])
}

model EnablementEvidenceRecord {
  id                     String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId               String    @db.Uuid
  companyId              String    @db.Uuid
  evidenceType           String
  title                  String
  audienceRole           String
  evidenceReference      String
  ownerName              String
  completedAt            DateTime
  knownLimitAcknowledged Boolean   @default(false)
  supportRouteConfirmed  Boolean   @default(false)
  verificationStatus     String    @default("RECORDED")
  verifiedAt             DateTime?
  verifiedByUserId       String?   @db.Uuid
  rejectedAt             DateTime?
  rejectedByUserId       String?   @db.Uuid
  notes                  String?
  sourceDecisionId       String    @default("DEC-0036")
  createdByUserId        String    @db.Uuid
  createdAt              DateTime  @default(now())
  updatedAt              DateTime  @updatedAt
  tenant                 Tenant    @relation(fields: [tenantId], references: [id])
  company                Company   @relation(fields: [companyId], references: [id])
  createdByUser          User      @relation("EnablementEvidenceCreatedBy", fields: [createdByUserId], references: [id])
  verifiedByUser         User?     @relation("EnablementEvidenceVerifiedBy", fields: [verifiedByUserId], references: [id], onDelete: Restrict)
  rejectedByUser         User?     @relation("EnablementEvidenceRejectedBy", fields: [rejectedByUserId], references: [id], onDelete: Restrict)

  @@index([tenantId, companyId, evidenceType, verificationStatus], map: "EnablementEvidenceRecord_tenantId_companyId_evidenceType_verifi")
  @@index([tenantId, companyId, completedAt])
}

model UatEvidenceRecord {
  id                 String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId           String    @db.Uuid
  companyId          String    @db.Uuid
  evidenceType       String
  title              String
  workflowArea       String
  testerName         String
  environment        String
  evidenceReference  String
  result             String
  executedAt         DateTime
  policyVersion      String?
  defectReference    String?
  verificationStatus String    @default("RECORDED")
  verifiedAt         DateTime?
  verifiedByUserId   String?   @db.Uuid
  rejectedAt         DateTime?
  rejectedByUserId   String?   @db.Uuid
  notes              String?
  sourceDecisionId   String    @default("DEC-0036")
  createdByUserId    String    @db.Uuid
  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt
  tenant             Tenant    @relation(fields: [tenantId], references: [id])
  company            Company   @relation(fields: [companyId], references: [id])
  createdByUser      User      @relation("UatEvidenceCreatedBy", fields: [createdByUserId], references: [id])
  verifiedByUser     User?     @relation("UatEvidenceVerifiedBy", fields: [verifiedByUserId], references: [id], onDelete: Restrict)
  rejectedByUser     User?     @relation("UatEvidenceRejectedBy", fields: [rejectedByUserId], references: [id], onDelete: Restrict)

  @@index([tenantId, companyId, evidenceType, verificationStatus], map: "UatEvidenceRecord_tenantId_companyId_evidenceType_verificationS")
  @@index([tenantId, companyId, workflowArea, result])
  @@index([tenantId, companyId, executedAt])
}

model ReleaseBoardDecision {
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId          String   @db.Uuid
  companyId         String   @db.Uuid
  decision          String
  decisionNote      String
  evidenceReference String
  participants      Json
  decidedAt         DateTime
  chairUserId       String   @db.Uuid
  sourceDecisionId  String   @default("DEC-0036")
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  tenant            Tenant   @relation(fields: [tenantId], references: [id])
  company           Company  @relation(fields: [companyId], references: [id])
  chairUser         User     @relation("ReleaseBoardDecisionChair", fields: [chairUserId], references: [id])

  @@index([tenantId, companyId, decision, decidedAt])
  @@index([tenantId, companyId, createdAt])
}

model ApprovalRule {
  id              String             @id @default(uuid()) @db.Uuid
  tenantId        String             @db.Uuid
  companyId       String?            @db.Uuid
  transactionType String
  scopeFilters    Json?
  priority        Int                @default(100)
  isActive        Boolean            @default(true)
  createdAt       DateTime           @default(now())
  tenant          Tenant             @relation(fields: [tenantId], references: [id])
  company         Company?           @relation(fields: [companyId], references: [id])
  steps           ApprovalRuleStep[]
  instances       ApprovalInstance[]
}

model ApprovalRuleStep {
  id              String       @id @default(uuid()) @db.Uuid
  approvalRuleId  String       @db.Uuid
  stepOrder       Int
  approverType    String
  roleId          String?      @db.Uuid
  userId          String?      @db.Uuid
  required        Boolean      @default(true)
  escalationHours Int?
  approvalRule    ApprovalRule @relation(fields: [approvalRuleId], references: [id])

  @@unique([approvalRuleId, stepOrder])
}

model ApprovalInstance {
  id                     String                  @id @default(uuid()) @db.Uuid
  tenantId               String                  @db.Uuid
  companyId              String                  @db.Uuid
  documentType           String
  documentId             String                  @db.Uuid
  approvalRuleId         String                  @db.Uuid
  status                 ApprovalStatus          @default(PENDING)
  currentStepOrder       Int?
  createdAt              DateTime                @default(now())
  tenant                 Tenant                  @relation(fields: [tenantId], references: [id])
  company                Company                 @relation(fields: [companyId], references: [id])
  approvalRule           ApprovalRule            @relation(fields: [approvalRuleId], references: [id])
  steps                  ApprovalInstanceStep[]
  paymentRequest         PaymentRequest?
  expenseRequest         ExpenseRequest?
  employeeLeaveRequest   EmployeeLeaveRequest?
  employeeOvertimeRecord EmployeeOvertimeRecord?
  workforceSchedule      WorkforceSchedule?
  attendanceImportBatch  AttendanceImportBatch?
  cashAdvanceRequest     CashAdvanceRequest?
  pettyCashRequest       PettyCashRequest?
  paymentRelease         PaymentRelease?

  @@index([tenantId, documentType, documentId])
}

model ApprovalInstanceStep {
  id                  String             @id @default(uuid()) @db.Uuid
  approvalInstanceId  String             @db.Uuid
  stepOrder           Int
  assignedUserId      String?            @db.Uuid
  assignedRoleId      String?            @db.Uuid
  status              ApprovalStepStatus @default(WAITING)
  actedAt             DateTime?
  actedByUserId       String?            @db.Uuid
  remarks             String?
  delegatedFromUserId String?            @db.Uuid
  approvalInstance    ApprovalInstance   @relation(fields: [approvalInstanceId], references: [id])

  @@unique([approvalInstanceId, stepOrder])
  @@index([actedByUserId], map: "ApprovalInstanceStep_actedByUserId_idx")
}

model Attachment {
  id                           String                         @id @default(uuid()) @db.Uuid
  tenantId                     String                         @db.Uuid
  storageProvider              String
  objectKey                    String
  originalFilename             String
  mimeType                     String
  sizeBytes                    Int
  checksum                     String?
  uploadedByUserId             String?                        @db.Uuid
  status                       RecordStatus                   @default(ACTIVE)
  createdAt                    DateTime                       @default(now())
  tenant                       Tenant                         @relation(fields: [tenantId], references: [id])
  projectLinks                 ProjectAttachment[]
  controlledEvidenceLinks      ControlledEvidenceAttachment[]
  employeeTrainingCertificates EmployeeTrainingRecord[]       @relation("EmployeeTrainingCertificate")
  employeeComplianceDocuments  EmployeeComplianceDocument[]   @relation("EmployeeComplianceDocumentAttachment")
}

model ControlledEvidenceAttachment {
  id                String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId          String       @db.Uuid
  companyId         String       @db.Uuid
  sourceType        String
  sourceRecordId    String       @db.Uuid
  sourceLineId      String?      @db.Uuid
  sourceKey         String
  attachmentId      String       @db.Uuid
  purpose           String       @default("EVIDENCE")
  caption           String?
  requiredForAction String?
  status            RecordStatus @default(ACTIVE)
  archivedAt        DateTime?
  archivedByUserId  String?      @db.Uuid
  archiveReason     String?
  createdByUserId   String       @db.Uuid
  createdAt         DateTime     @default(now())
  updatedAt         DateTime     @updatedAt
  tenant            Tenant       @relation(fields: [tenantId], references: [id])
  company           Company      @relation(fields: [companyId], references: [id])
  attachment        Attachment   @relation(fields: [attachmentId], references: [id])
  createdBy         User         @relation("ControlledEvidenceAttachmentCreator", fields: [createdByUserId], references: [id])
  archivedBy        User?        @relation("ControlledEvidenceAttachmentArchiver", fields: [archivedByUserId], references: [id])

  @@unique([tenantId, companyId, sourceType, sourceKey, attachmentId], map: "ControlledEvidenceAttachment_sourceKey_attachment_key")
  @@index([tenantId, companyId, sourceType, sourceRecordId, status], map: "ControlledEvidenceAttachment_sourceRecord_status_idx")
  @@index([tenantId, companyId, sourceLineId, status], map: "ControlledEvidenceAttachment_sourceLine_status_idx")
  @@index([attachmentId, status])
  @@index([createdByUserId, createdAt])
}

model AuditEvent {
  id          String   @id @default(uuid()) @db.Uuid
  tenantId    String   @db.Uuid
  companyId   String?  @db.Uuid
  actorUserId String?  @db.Uuid
  eventType   String
  entityType  String
  entityId    String   @db.Uuid
  occurredAt  DateTime @default(now())
  requestId   String?
  ipAddress   String?
  beforeData  Json?
  afterData   Json?
  metadata    Json?
  tenant      Tenant   @relation(fields: [tenantId], references: [id])
  company     Company? @relation(fields: [companyId], references: [id])
  actor       User?    @relation(fields: [actorUserId], references: [id])

  @@index([tenantId, entityType, entityId, occurredAt])
}

model Notification {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId         String    @db.Uuid
  companyId        String?   @db.Uuid
  locationId       String?   @db.Uuid
  recipientUserId  String    @db.Uuid
  notificationType String
  priority         String    @default("NORMAL")
  channel          String    @default("IN_APP")
  title            String
  body             String
  deepLink         String
  entityType       String
  entityId         String?   @db.Uuid
  sourceEventKey   String
  recipientBasis   String?
  status           String    @default("UNREAD")
  generatedAt      DateTime  @default(now())
  readAt           DateTime?
  archivedAt       DateTime?
  metadata         Json?
  tenant           Tenant    @relation(fields: [tenantId], references: [id])
  company          Company?  @relation(fields: [companyId], references: [id])
  location         Location? @relation(fields: [locationId], references: [id])
  recipient        User      @relation(fields: [recipientUserId], references: [id])

  @@unique([tenantId, recipientUserId, sourceEventKey])
  @@index([tenantId, recipientUserId, status, generatedAt])
  @@index([tenantId, entityType, entityId])
  @@index([tenantId, companyId, locationId])
}
