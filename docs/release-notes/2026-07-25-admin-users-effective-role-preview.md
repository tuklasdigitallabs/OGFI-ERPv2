# Users registry current-access preview

Core Administration Users now shows a bounded `Current effective roles` preview. Future, ended, inactive, and foreign-tenant roles are excluded from the current summary; users with more than eight current roles see an explicit `8+ roles` marker. Scope assignments remain available through the authorized User Access detail route.
