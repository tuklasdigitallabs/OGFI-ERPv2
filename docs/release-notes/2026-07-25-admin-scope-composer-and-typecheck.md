# Administration: scope controls and typecheck repair

Assigned Scopes now uses one selected-record deactivation composer with preserved filters, page context, safe stale-selection handling, and the service-required five-character reason minimum. Recent option-catalog, PO lookup, quote filter, and Admin projection boundary typing is also corrected; the web TypeScript gate is green.
