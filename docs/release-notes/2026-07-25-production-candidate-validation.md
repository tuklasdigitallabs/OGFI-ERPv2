# Production candidate validation

The current web candidate builds successfully and passes lint, TypeScript, Core Admin, Purchase Order, and module-preview checks. Database-backed authorization evidence remains separately gated until the disposable database/authorization sentinels are available.
