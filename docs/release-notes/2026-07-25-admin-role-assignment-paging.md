# Administration: role assignment paging

Role access details now use bounded, searchable assignment pages with exact counts and limited scope previews, keeping large tenant role assignments responsive while preserving server-side authorization and audit controls.
