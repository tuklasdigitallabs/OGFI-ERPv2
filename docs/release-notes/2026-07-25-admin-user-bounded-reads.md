# Administration User Access bounded reads

Non-Requests User Access sections no longer load controlled-request histories; Scope and Role request views load only their selected request kind. Actor-scoped Audit searches now retain exact user isolation when a text filter is applied.

Database query-count/isolation, responsive browser, hosted recovery, and UAT evidence remain pending.
