# Purchase Request multi-line lookup continuity

The draft editor now keeps selected item, UOM, and budget options visible when switching between lines, while retaining bounded server lookups and server-side validation.
