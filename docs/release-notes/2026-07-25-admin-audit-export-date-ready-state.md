# Core Administration Audit export date-ready state

The Audit Trail now explains why CSV export is unavailable until valid From and To
dates are entered. Once the range is valid, the existing server-authorized export
continues to enforce the configured date-span and row limits.
