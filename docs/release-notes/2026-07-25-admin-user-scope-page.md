# Administration: bounded User Access scopes

Assigned Scopes now uses a tenant/company-filtered server page across all supported scope types, with search, type filtering, deterministic pagination, and generic omission of foreign or unknown targets. Mutation and audit controls remain unchanged.
