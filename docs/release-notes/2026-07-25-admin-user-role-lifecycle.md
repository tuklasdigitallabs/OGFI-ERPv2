# Assigned Role Lifecycle clarity

User Access now labels its role list as Assigned Role Lifecycle. Active assignment records can include scheduled (FUTURE) and ended (EXPIRED) dates, and each row separates assignment state from role status.

The lifecycle register is not the user’s current effective permission set. Use the effective-permission register for current authority review; role deactivation remains server-authorized and audited.
