# Administration: User Access role paging

User Access details now provide searchable, paginated assigned roles with exact counts and role context. Effective permissions remain complete and existing server-side mutation and audit controls are unchanged.
