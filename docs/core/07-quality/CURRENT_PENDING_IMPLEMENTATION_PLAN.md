# OGFI ERP — Current Pending Implementation Plan

**As of:** July 26, 2026
**Status:** Active implementation register  
**Scope:** Production-readiness implementation remaining outside formal user UAT execution and final owner signoff

## 1. Purpose

This document is the working register for implementation that remains before OGFI ERP workspaces may be certified as production-ready. It separates shared platform work from workspace-specific completion and distinguishes required production controls from intentionally deferred future scope.

The delivery approach is:

1. Stabilize and verify the shared production foundation.
2. Complete one workspace at a time in dependency order.
3. Apply the full workspace completion gate before moving to the next workspace.
4. Stabilize Phase I and Phase 1.5 before certifying later phases for production.

Formal UAT execution, evidence collection with real users, owner signoff, and the final GO / NO-GO decision remain separate release activities.

## 2. Shared Production Foundation

These items must be completed once at platform level. A workspace must not be certified when it depends on an incomplete shared control.

| ID | Pending implementation | Required outcome | Status |
|---|---|---|---|
| SPF-001 | CI and verification baseline | Secret review, release-tool self-tests, lint, typecheck, production build, unit/integration tests, access-control tests, and production-mode authenticated desktop/mobile E2E pass against the same release candidate | In progress; branch protection and production-authenticated `next start` E2E remain pending |
| SPF-002 | Migration and data-safety verification | Review all pending migrations, deploy them to a disposable environment, compare pre/post snapshots, and verify rollback considerations | Complete; corrective migration `20260721200000_project_requirement_attachment_context` independently reviewed and exact-SHA PostgreSQL 17 predecessor rehearsal, fail-closed negative checks, idempotent redeploy, and schema-drift check pass |
| SPF-003 | Authentication and privileged access | Confirm production identity provider or login path, privileged MFA enforcement, session invalidation, and break-glass runtime behavior | Implementation complete; production enablement remains blocked on hosted restore/locking evidence and accountable-owner policy signoff |
| SPF-004 | Authorization regression gate | Verify tenant, company, brand, location, department, project membership, restricted-project, and direct-route/API enforcement | Reopened — exact-candidate revalidation pending; prior closure remains historical evidence for SHA `60690ddd41c12fa31dbdeb3fdec6ebfc8a90f170` under `DEC-0188` |
| SPF-005 | Controlled evidence uploads | Complete the hybrid evidence model described in Section 3 | In progress: the same-VPS broker, private scanning, upload/download, quarantine, quota/lease, retention-register, recovery-tooling, deployment-overlay, authorization-test foundations, and dormant policy-neutral qualification boundary are implemented. Qualification PostgreSQL execution, `DEC-0047` policy values, real adapters/UI, hosted verification, backup/key custody, paired restore, and production activation remain open |
| SPF-006 | Audit and activity integrity | Verify important create, update, transition, approve, post, reverse, cancel, archive, upload, download, export, and denied actions produce the required immutable history; bound or aggregate repeated denied-event writes so enumeration attempts cannot amplify audit storage without limit | In progress; local DEC-0049 append-only/role separation and DEC-0050 bounded denial, atomic authentication admission, bounded role-scoped approval routing, 18-type backfill/detail descriptors, normalized inbox SQL, representative shared-engine action races, and monitoring checkpoints are independently accepted with normalized routing disabled. Hosted exact-release/restore/load/alert proof, canonical specialized/Finance Close action preflight, all-18 action and feature-gated page activation evidence, remaining request/reversal and audit-semantic gaps, broader behavioral races, and the cross-workspace audit matrix remain pending |
| SPF-007 | Shared UX states | Standardize loading, empty, error, denied, disabled-with-reason, validation, conflict, retry, and mobile states | Pending workspace verification |
| SPF-008 | Operational list behavior | Verify server-backed search, filters, pagination, export, responsive tables/cards, and selected-record task flows | Pending workspace verification |
| SPF-009 | Deployment and recovery | Complete staging deployment, production-mode authenticated desktop/mobile E2E, backup, isolated restore, rollback rehearsal, smoke checks, monitoring, and hypercare procedures | Pending environment execution and production-authenticated `next start` E2E |
| SPF-010 | Security and dependency review | Run the approved security/dependency checks and resolve release-blocking findings without exposing secrets or internal errors | Pending final release-candidate review |

### SPF-001 implementation evidence — July 21, 2026

- Confirmed `DEC-0038`: augment the baseline and keep SPF-001 open until hosted exact-SHA and branch-protection evidence are accepted.
- Confirmed `DEC-0044`: development-mode fixture browser E2E may support SPF-004 closure, but SPF-001 remains open until authenticated production-mode `next start` desktop/mobile E2E passes with ephemeral password/MFA fixtures and a loopback HTTPS proxy.
- CI provisions PostgreSQL 17, migrates and deterministically seeds the isolated database, typechecks E2E, builds the production artifact, runs database-backed authorization tests without skip fallback, and retains Playwright/release evidence on failure or success. Its current browser runner is explicitly development-mode fixture evidence, not production-authenticated runtime evidence.
- Local candidate verification passed: secret review, release-tool self-test, lint, application typecheck, E2E typecheck, production build, 628 web tests across 64 files plus database/worker tests, two executed access-control integration tests, and all 14 desktop/mobile E2E tests.
- Hosted run [`29774246308`](https://github.com/tuklasdigitallabs/OGFI-ERPv2/actions/runs/29774246308) passed for exact implementation SHA `c5e3969b176e271470ca66d3bef607803cb76c3e`, including the `checks` job and CI verification artifact upload.
- Remaining closure blockers: configure protection that requires the `CI / checks` gate, provision secret-safe ephemeral local-auth password/MFA fixtures, and pass hosted desktop/mobile E2E through loopback HTTPS against production-mode `next start`. Demo authentication and weakened origin/cookie/session checks are prohibited.
- Knowledge-base and glossary assessment: no update required for this slice because it changes internal engineering verification only and introduces no end-user term or workflow behavior.

### SPF-002 implementation evidence — July 21, 2026

- Confirmed `DEC-0039`: SPF-002 requires an exact-SHA populated predecessor upgrade rehearsal on disposable PostgreSQL 17 databases, with complete migration review, data preservation, recovery, idempotency, and schema-drift evidence.
- Reconciled the Prisma schema and migration history through reviewed migration `20260721120000_reconcile_schema_drift`; the hosted datasource-to-schema comparison reports zero drift.
- Inventoried all 108 migrations. Fifty-five migrations requiring explicit review are bound to exact SHA-256 hashes and marked `APPROVED_FOR_REHEARSAL`; none remain pending. The migration safety register records transaction behavior, failure point, reversibility, recovery trigger, owner, verification, and expected recovery time.
- Hosted run [`29784132306`](https://github.com/tuklasdigitallabs/OGFI-ERPv2/actions/runs/29784132306) passed for exact candidate SHA `623927ac36949362822edf9e0737454087543c59`, using successful predecessor CI run `29774246308` at SHA `c5e3969b176e271470ca66d3bef607803cb76c3e` and `deploy_to_staging=false`.
- Evidence run `gh-29784132306-1` proves: a populated predecessor baseline; pre-migration checksummed backup; atomic rollback on an intentional cross-company scope violation; successful first deploy; no-pending idempotent second deploy with identical migration journals and zero data delta; zero schema drift; 14 business/scope invariants before, after, and after restore; and exact row-count/content-digest equivalence across all 158 Prisma application tables after isolated restore.
- Artifact `ogfi-release-evidence-spf002-rehearsal-623927a` (`8477937081`) contains 31 checksum-indexed evidence files. The backup checksum and final manifest checksum were independently reverified after download.
- The same hosted candidate also passed secret review, release-tool self-tests, lint, application and E2E typechecks, production build, database/worker/UI tests, 626 web tests, access-control integration tests, and desktop/mobile Playwright E2E.
- Boundary retained: SPF-002 closure does not close SPF-009; staging deployment, application rollback, smoke checks, monitoring, and hypercare remain pending.
- Knowledge-base and glossary assessment: no update required because this phase changes internal engineering, migration, and recovery controls only; it introduces no end-user workflow, navigation, permission, or product term.

### SPF-003 implementation evidence — July 21, 2026

- Confirmed `DEC-0040`: production authentication uses tenant-qualified local username/password credentials, runtime TOTP, opaque revocable PostgreSQL sessions, controlled activation/recovery, and a one-time first-administrator bootstrap. Demo authentication fails closed in production.
- Added Argon2id password verification, account/source throttling, MFA challenge and recovery-code controls, idle and absolute session expiry, assurance rotation, administrator/user revocation, privilege-epoch invalidation, same-origin mutation protection, secure SMTP delivery, encryption-key rotation, and hash-only activation/recovery token storage.
- Authentication administration is tenant/company scoped. Active role and scope assignments are time bounded, scope types are explicit, and sensitive target mutations transactionally revalidate current company scope before changing credentials, MFA, recovery, or sessions.
- All authentication numeric controls fail startup or utility execution when malformed or outside conservative bounds. Production configuration requires secure cookie, key, SMTP TLS, and HTTPS settings.
- Migration `20260721150000_production_application_authentication` is bound to SHA-256 `b0eb3415d510b832997e9c504d02562f6edc5c78f2b3d84af7a6c0093f86be14` and independently approved for rehearsal. The populated predecessor rehearsal preserved 10 tenants, 100 companies, and 5,000 users with zero drift; 10 database-backed authentication integrity/race tests pass.
- Added the sign-in, activation, MFA challenge, account-security/session-management, activation-delivery, and controlled recovery surfaces. Shared modal triggers now remain disabled until hydration, preventing lost first-click actions on desktop and mobile.
- Local candidate verification passed: secret review, release-tool self-test, migration inventory with zero pending dispositions, lint, typecheck, production build, 643 web tests, database/worker tests, 10 authentication database tests, two access-control database tests, and all 14 desktop/mobile Playwright tests on a fresh migrated and seeded PostgreSQL database.
- Independent Database review approved the exact migration for rehearsal. Independent Security re-review found no remaining code-level production blocker.
- Remaining production-GO gates are external and are not represented as complete: hosted production-like migration locking and backup/restore/RTO/RPO evidence, accountable-owner confirmation of the open authentication policies in `DEC-0040`, role-based UAT, and final Security/QA/DevOps/Release approval.
- Knowledge-base and glossary updated for tenant login code, activation, MFA, recovery codes, challenge locks, sessions, session invalidation, first-administrator bootstrap, and administrator support workflows.

### SPF-004 implementation evidence — July 21, 2026

- Confirmed `DEC-0041`, `DEC-0042`, `DEC-0043`, and `DEC-0044`: executable database-backed boundary coverage is required for high-risk authorization surfaces; workforce access is location-primary; tenant-global role administration requires explicit `core.tenant_role_administer` plus selected-company target eligibility; and SPF-004 may close from exact-SHA production-build, database/manifest, and isolated development-fixture desktop/mobile E2E evidence.
- The regenerated authorization baseline contains 964 protected pages, server actions, service entrypoints, route handlers, and evidence downloads. It classifies 745 high-risk surfaces, including 442 high-risk service entrypoints registered as `COVERED`; every high-risk service, action, route, and evidence download binds to a verified executable boundary case, with zero missing, stale, duplicate, invalid, uncovered, or unbound registry entries.
- Manifest discovery fails closed for unknown server-action files and app routes, direct or dynamic database/service import escape paths, unresolved imported or re-exported service delegation, callback/alias escape paths, transitive synchronous database writers, baseline drift, skipped or missing registered tests, and empty no-mutation controls.
- Live PostgreSQL regression coverage verifies tenant/company/brand/location/department/project boundaries, restricted-project membership, live permission and scope revocation, authoritative workforce record locations, finance/evidence brand and department scope, self-approval segregation, invalid state transitions, route denial without export bytes, confidential workforce redaction, cross-tenant permission-detail filtering, non-enumerating evidence denials, private evidence authorization, and denied-action no-mutation behavior.
- Role administration now uses `core.tenant_role_administer`, grants it only to configured administrator/super-user roles by default, requires active/effective selected-company membership for target users, preserves MFA and no-self-action controls, invalidates active assignees after permission changes, serializes concurrent direct grants and sensitive permission promotion, freezes role permission changes while a sensitive-role request is pending, reloads role permissions under lock during approval, and compare-and-swaps sensitive-role review. Sensitive roles require controlled approval for grants but remain revocable by an authorized independent administrator. Company-bound role-assignment schema work remains explicitly deferred.
- Migration `20260721160000_tenant_role_administration_permission` is atomic, conflict-preflighted, checksum-bound in the migration safety register, and locally rehearsed on PostgreSQL 17. Positive deployment, target-only grants, idempotent redeploy, and atomic reserved-UUID/mismatched-metadata failure fixtures passed. Hosted migration/restore evidence remains pending.
- The final local authorization candidate attestation passed 666 unit tests (10 database-authentication cases intentionally skipped there and executed under their dedicated gate), 6 access-control cases, 8 adapter cases, 33 admin/platform cases, 11 finance cases, 18 procurement/inventory cases, 15 projects/operations cases, 10 workforce cases, 2 protected-route cases, and 11 controlled-evidence cases. All 14 serial desktop/mobile E2E cases passed on the post-remediation candidate in 8 minutes 18 seconds. The regenerated manifest records 964 authorization surfaces, including 745 high-risk surfaces and complete 442/442 high-risk service coverage, on disposable PostgreSQL database `ogfi_authz_spf004_final_20260721_09`, run ID `local-spf004-authz-final-20260721-09`, and source HEAD `9853a1b6dfc80945d689cb0b70842ee0065c1d2b`. The same blank candidate applied all 110 migrations, seeded successfully, and reported no pending migrations on redeploy. Independent review additionally identified and drove remediation of tenant-global session-invalidation visibility, expired/future company-admin assignment acceptance, and concurrent provider double completion; database coverage proves company-only isolation, live tenant-role authority, non-enumerating denial, and exactly-once completion/audit behavior. Post-remediation secret review, release-tool self-test, migration review, lint, application and E2E typechecks, production build, authorization manifest checks, and independent Security/Evidence re-reviews all passed; the hosted clean exact-SHA closure is recorded below.
- Hosted runs `29824012949` and `29825094297` for implementation SHAs `020c334fa692d2e8a2957df5e7705918fb90a02e` and `d2a9981a1cb59a56d1ab6e31abdd3238e98e0f21` passed migration, seed, secret, release-tool, lint, typecheck, build, package-test, and authorization steps, then failed after 60 and 120 seconds respectively while attempting to start production-mode E2E; manifest generation was consequently skipped. Exact CI-mode reproduction proved that `next start` correctly fails closed without the required production authentication configuration and that the existing browser fixtures intentionally use development-only demo authentication. Under `DEC-0044`, the isolated fixture runner now uses `next dev`; its complete desktop/mobile suite passed 14/14 locally with `CI=true` in 7.3 minutes.
- Hosted run `29827747809` for SHA `9097981f4d601bc231a3a6d59be4716c04329fdc` passed the production build, complete database-backed authorization suite, and all 14 desktop/mobile E2E cases in 6.6 minutes. Its final manifest step correctly rejected a dirty CI worktree because Next.js rewrote the tracked generated `apps/web/next-env.d.ts` route-type reference from the production build directory to the isolated E2E directory. CI now restores only that known framework-generated file after E2E; the manifest's clean-worktree assertion remains responsible for rejecting every other tracked or untracked mutation.
- Hosted run [`29829530328`](https://github.com/tuklasdigitallabs/OGFI-ERPv2/actions/runs/29829530328) passed the complete SPF-004 gate for exact SHA `60690ddd41c12fa31dbdeb3fdec6ebfc8a90f170`: migration/seed, secret and release-tool checks, lint, application and E2E typechecks, production build, package tests, database-backed authorization, 14/14 desktop/mobile development-fixture E2E cases, clean-worktree authorization manifest, and artifact upload. Artifact `ci-verification-29829530328-60690ddd41c12fa31dbdeb3fdec6ebfc8a90f170` (`8495001647`) is retained for 90 days with digest `sha256:493b4a7fa295682ee1357acb897985272fd000251192f9b8ddf6196026aefb64`.
- Production-mode authenticated `next start` E2E is not waived: `DEC-0044` assigns that unresolved gate to SPF-001 and SPF-009. It requires ephemeral tenant-qualified password/MFA fixtures and loopback HTTPS and may not be satisfied by production demo auth or weakened trusted-origin, secure-cookie, session, or authorization checks.
- Knowledge-base, glossary, Core Administration helper text, roles/permissions, security/audit model, data dictionary, decision index, and changelog were aligned with the implemented behavior.
- SPF-004 is complete. Its closure does not close SPF-001 or SPF-009 and does not authorize production release; production-authenticated `next start` E2E, branch protection, deployment, recovery, and final release evidence remain under their assigned gates.

## 3. Controlled Evidence Upload and Storage

Evidence capture will use a hybrid model:

- structured evidence notes;
- verified external references where policy permits them; and
- private file uploads where the workflow or risk policy requires an artifact.

Text alone must not satisfy high-risk evidence requirements unless an approved policy explicitly permits a verified external reference.

### Implemented foundation and pending activation

- The `DEC-0046` dedicated internal minimal storage broker is implemented for the same Hostinger VPS topology. Only the broker is configured to own the validated absolute private evidence bind mount and versioned AES-256-GCM key; hosted proof of that isolation remains required.
- Use separate mounts, encryption keys/versions, broker identities, scanner boundaries, credentials, and environment configuration for development, staging, and production.
- Use server-generated opaque non-repeating keys; keep tenant/company/source scope in PostgreSQL and do not encode original filenames or sensitive business data in storage keys.
- Store file metadata and source-record links in PostgreSQL; do not store file bytes in PostgreSQL.
- Enforce source-record authorization on upload, list, preview, download, archive, and replacement.
- Enforce location/department scope, confidential workforce access, and restricted-project membership where applicable.
- Validate configured size limits, allowed extensions, detected content type, and checksum.
- Upload into quarantine and prevent evidence use until malware scanning succeeds.
- Bounded upload and download streams are proxied through the authorized application and internal broker; filesystem paths, broker/scanner access, and permanent public URLs are not exposed.
- Audit upload, scan result, download, archive, replacement, denial, and retention actions.
- Preserve important evidence through archival/versioning rather than normal-user hard deletion.
- Application retention/legal-hold services and an administrative retention register, company quota reservations, upload leases/rate limits, disk high-water controls, and an evidence-root single-broker process lock are implemented. Recovery creation, isolated staging, and verification tools exist, but an independently recoverable encrypted backup, key escrow/custody, and hosted paired database/evidence restore proof remain activation blockers. Do not represent same-VPS filesystem storage as WORM or Object Lock.
- Provide one reusable attachment uploader/viewer for workspace adoption.
- Define the attachment requirement matrix by workflow, action, risk, value, and evidence purpose.
- Add cross-tenant, cross-company, cross-location, unauthorized-source, and restricted-project denial tests.

Direct web-mounted `local-private` storage remains suitable for local development and controlled demonstration only. The production design is the isolated broker contract in `DEC-0046`; confirmation of that architecture does not satisfy its implementation or activation gates.

### SPF-005 decision status — July 21, 2026

- `DEC-0046` supersedes `DEC-0045` and confirms a dedicated internal minimal broker on the same Hostinger VPS. Only the broker owns the absolute private evidence mount and versioned AES-256-GCM key; web, Caddy, and ClamAV have no evidence mount. The application proxies bounded streams, and the server issues opaque immutable keys/versions.
- PostgreSQL is authoritative for source-record authorization, idempotency, quota, quarantine, exact-version compare-and-set availability, retention/legal hold, and audit. ClamAV scans private streams through `INSTREAM`; only a conclusive `CLEAN` result for the exact version and accepted signature freshness becomes available. A bounded systemd timer handles reconciliation without detached post-response work, Redis, or a queue.
- Same-VPS filesystem storage is not WORM/Object Lock and retains root-compromise and same-disk-loss risks. SPF-005 is not production-ready. Activation remains blocked on actual VPS size/utilization and headroom, evidence quota/high-water values, encryption-key custody/recovery, Hostinger daily-backup entitlement/location/encryption/retention/restore granularity, approved RPO/RTO, retention/legal-hold policy, pinned ClamAV image/resources/signature freshness, implementation verification, and hosted paired database/evidence restore proof.
- External object storage is deferred until capacity, multi-host deployment, stronger RPO/RTO, legal WORM, or tenant scale triggers a new material decision and controlled copy-verify-cutover migration.

### SPF-005 implementation evidence — July 21, 2026

- Implemented the isolated encrypted broker and provider-neutral storage boundary, private ClamAV `INSTREAM` adapter, streamed application upload/download routes, opaque immutable key/version handling, quarantine-by-default lifecycle, and exact-version clean-release checks.
- Implemented PostgreSQL-backed idempotency, company quota reservation/consumption, upload leases, request-rate limits, bounded scan reconciliation, compare-and-set availability, legal-hold backend controls, preservation-aware workspace states, server-paginated evidence history, and the restricted administrative retention register. Authorization coverage includes upload/download, source-scope denial, legal-hold, retention-register, and non-enumerating evidence cases. Filename/MIME matching is enforced both when the upload intent is issued and before an exact version becomes available; active-script and spreadsheet extensions cannot pass as plain text.
- Added the Hostinger deployment overlay, systemd reconciliation and recovery-stage/verification units, configuration/readiness contracts, and recovery create/verify tooling. These are implementation artifacts, not proof that the real Hostinger environment, independent backup destination, encryption-key custody, or paired restore meets approved objectives.
- `DEC-0048` corrects a discovered project-requirement evidence mismatch: a project attachment may be task-only, comment-only, requirement-only, or task plus its matching task-bound requirement. The corrective migration validates predecessor rows, replaces the obsolete task/comment-only check, prevents duplicate active requirement links, and enforces task/requirement agreement in both write directions. This keeps project-level requirements usable without artificial tasks and preserves the atomic controlled-evidence/project-activity design.
- Migrations `20260721170000_controlled_evidence_storage_contract`, `20260721180000_evidence_legal_hold_controls`, `20260721190000_controlled_evidence_policy_alignment`, and `20260721200000_project_requirement_attachment_context` are checksum-bound and independently database-reviewed as `APPROVED_FOR_REHEARSAL`, not production `APPROVED`. They require the recorded writer quiescence, exact-SHA populated-predecessor migration/redeploy checks, schema-drift and report/export comparison, and isolated restore equivalence before release use.
- Current local candidate verification passes: web lint and typecheck, production build, 761 web unit/contract tests, 20 authorization-manifest tests, 15 database package tests, all 19 controlled-evidence database authorization scenarios, eight Hostinger-isolation contract tests, seven evidence-recovery contract tests, release-tool self-tests, secret review, migration inventory covering 114 migrations with zero pending dispositions, and `git diff --check`. The database authorization suite now proves confidential workforce evidence is unavailable through generic `workforce.view`, denied downloads create one denial audit event, and a taskless project requirement creates the correctly scoped project link. Hosted authorization/E2E and paired restore gates remain separate evidence and are not implied by these local results.
- Exact candidate `3bf902bdf03c77dfdfd6c70d982c30e55726b5e3` passed the PostgreSQL 17 corrective-migration rehearsal after the populated predecessor run: the existing matched task/requirement link was preserved; task mismatch, parentless context, and later requirement-task drift were rejected; a second deploy reported no pending migration; Prisma reported no schema difference; and the checksum-bound migration-review artifact passed in release mode. Hosted authorization/E2E and paired restore gates remain separate evidence and are not implied by this local rehearsal.
- SPF-005 remains open and must not be described as production-ready or complete. External activation is blocked on real Hostinger capacity/configuration values, independently recoverable encrypted off-VPS backup and key escrow, approved retention/legal-hold/disposition policy, paired PostgreSQL/evidence/key restore proof, hosted isolation/scanner/failure tests, and final Security/QA/DevOps/Release acceptance. `DEC-0047` also remains open for the exact finance evidence matrix and explicit attachment-selection/attestation workflow; mapped high-risk finance actions remain non-production and legacy text is supplemental only.
- `DEC-0077` implements a policy-empty qualification boundary without enabling a workflow: immutable policy versions and activation events, a compare-and-swap current pointer, immutable transaction-bound qualification/selection facts, database-authoritative canonical hashes/cardinality/readiness checks, commit-time evidence-link revalidation, least-privilege role contracts, and a closed empty adapter registry behind a constant-disabled runtime. No policy row, publisher, real adapter, call site, endpoint, UI, report, seed, or default was added. Focused validation passes 8 qualifier tests and 7 database contracts; the full candidate passes lint, typecheck, production build, 1,252 web tests, 34 database tests, one worker test, the 20-case authorization manifest after registering the dormant boundary, 8 database-role tool tests, secret review, release-tool self-tests, and the 127-migration inventory with the new migration approved only for rehearsal. Twenty-three PostgreSQL behavioral cases are authored but unexecuted because `DISPOSABLE_DATABASE_ADMIN_URL` is unavailable. Architecture, Security, and QA approve only this dormant source checkpoint. PostgreSQL acceptance, `DEC-0047`, real action/UI integration, hosted recovery, and activation remain **NO-GO**.
- Dunong’s controlled-evidence handoff is complete for the implemented foundation: the glossary, upload/support guide, legal-hold administration guide, training module, release note, and indexes cover controlled evidence, quarantine/scan wait, rejection, quota/rate denial, legal hold, retention, download, recovery/support, and the absence of any current AWS integration. These documents continue to label hosted activation and the finance evidence matrix as pending.

### SPF-006 implementation checkpoint — July 22, 2026

### DEC-0050 initial procurement notification parity checkpoint — July 24, 2026

- The five legacy initial procurement submission paths (`Purchase Request`, `Quotation Recommendation`, `Purchase Order`, `Purchase Order balance closure`, and `Purchase Order amendment`) now use the shared step-ready notification contract. A direct-user first step creates one deterministic notification for the persisted assigned user; a role-scoped first step creates no personal notification. The active approval step remains the authority, but role-scoped work has no complete actionable queue while the normalized Approval Inbox is disabled. The existing permissioned manual reminder scan may create current-user reminders for eligible due or overdue work, but it is partial and its approval links remain unavailable. The quotation path intentionally links the notification to the related Purchase Request. Existing transaction coupling, idempotency keys, scope/permission checks, and no-self-approval controls remain in force. No historical notification rows are rewritten.
- Focused local approval-routing coverage passes 48/48 tests. The initial-notification file discovers 20 PostgreSQL cases, including the three new `DEC-0078` source-serialization race specifications, but skips all 20 without the guarded database environment. The exact disposable command fails closed with `DISPOSABLE_DATABASE_ADMIN_URL_REQUIRED`. The authored matrix covers direct/role flag-off behavior, exact recipient/entity/key metadata, zero role fanout, rollback, retry, scope/role revocation, and one-winner source serialization; authored or skipped specifications are not production evidence.
- The pre-`DEC-0078` review found concurrent initial-submission risk in Purchase Request, Quotation Recommendation, and Purchase Order balance closure. `DEC-0078` subsequently implemented source compare-and-set/row locking for those handlers; the three exact PostgreSQL races remain unexecuted. Purchase Order submission and amendment retain their existing compare-and-set coverage. Production, normalized activation, and Workspace 1 completion remain **NO-GO** pending executable database acceptance and a production-ready visible role-scoped work surface; no parallel flag-off Inbox is authorized.

### DEC-0078 source serialization checkpoint — July 24, 2026

- Confirmed `DEC-0078`: Purchase Request and Quotation Recommendation now claim the scoped `DRAFT` source row with a transactional status compare-and-set before creating approval graph rows. Purchase Order balance-closure requests lock the scoped authoritative Purchase Order row with `FOR UPDATE` before rechecking pending closures. A lost claim fails closed and rolls back all approval, audit, and notification work.
- Current source-control validation passes web lint and typecheck, E2E typecheck, production build, secret review, 48/48 focused approval-routing tests, and the full non-database web suite with 1,389 passed, 305 skipped, and one existing TODO across 127 passed/11 skipped files. Final independent Security/QA and Product/enablement re-reviews found no remaining Critical, High, or Medium issue after the specs required each losing race to return its exact family conflict and the docs reconciled the partial flag-off reminder scan; both return GO only for this source-control checkpoint. The three exact concurrent one-winner PostgreSQL specifications are included in the 20 discovered initial-notification cases but remain unexecuted because the environment lacks `DISPOSABLE_DATABASE_ADMIN_URL`; they are not production evidence. Normalized routing, Workspace 1 completion, and production promotion remain **NO-GO** until those cases execute successfully and role-scoped work has an approved visible destination after controlled activation. Requested Code Spark and exact GPT-5.4 reviewers were unavailable, so the closest permitted GPT-5.6 fallbacks were used without relaxing any gate.

### SPF-008 ordinary Stock Balances pagination checkpoint — July 24, 2026

- `Stock Balances` now uses a server-backed page query with bounded 10-row pages, deterministic `id` tie-breaking, scoped search/tab predicates, query-aware counts for all/positive/expiring views, and safe page clamping. The existing full-result export path remains separate and is not limited to one page.
- Inventory service coverage passes 24/24 focused tests and web typecheck passes. Full suite, production build, responsive browser evidence, and PostgreSQL query-plan/volume evidence remain required before Workspace 1 or SPF-008 completion.

### Shared authorization and disposable-PostgreSQL checkpoint — July 24, 2026

- The disposable PostgreSQL runner now supports Docker containers mapped to a non-default loopback host port while preserving in-container `psql` transport. The PostgreSQL role-contract verifier now checks `PUBLIC` execute grants through ACL expansion, matching PostgreSQL’s accepted privilege semantics.
- Inventory posting now performs a tenant/company-scoped public location preflight before duplicate-return lookup, preserves primary-location scope checks while locking all related transfer locations, and uses an ID-keyed location map so unordered database results cannot change authorization. Inventory dashboard aggregation explicitly casts scoped UUID parameters, and reconciliation grouping uses explicit lot-key expressions accepted by PostgreSQL.
- Empty brand context is normalized at maintenance dashboard/read boundaries without weakening the exact nullable-brand contract for an explicit `NULL` scope. This preserves company/location task visibility for context adapters that emit an empty brand while retaining the existing null-brand unit contract.
- Evidence: disposable auth database 14/14; focused Procurement/Inventory authorization 32/32; Projects/Operations authorization 17/17; complete authorization unit/manifest leg 1,260 passed with 301 intentionally skipped; role-contract, seed-repeatability, authentication, access-control (6/6), adapters (8/8), admin/platform (35/35), finance (11/11), procurement/inventory (32/32), projects/operations (17/17), workforce (10/10), protected routes (4/4), controlled evidence (19/19), and denial buckets (7/7) all pass on fresh disposable PostgreSQL databases. The first chained evidence attempt hit a Windows temp-directory `ENOTEMPTY` teardown race after 19/19 assertions; the independent rerun passed cleanly.
- Parent-led inventory review used the closest available fallback model (GPT-5.6) because the requested Code Spark/GPT-5.4 choices were unavailable; it required public non-enumerating location errors, primary-location scope enforcement, complete transfer lock coverage, and reauthorization before duplicate returns. Hosted production-authenticated E2E, build, backup/restore, and branch-protection gates remain open.

### SPF-008 ordinary Receiving register pagination checkpoint — July 24, 2026

- The ordinary Receiving register now queries server-side by selected tab (`All`, `Draft`, `Posted`, or `Discrepancies`), clamps page/page-size inputs, uses deterministic `createdAt DESC, id DESC` ordering, returns server tab counts, and no longer filters or slices the complete receipt set in the page. The existing follow-up profile remains independently paginated and read-only.
- Focused Receiving coverage passes 19/19 tests; lint and web typecheck pass. The register’s broader workflow filters (supplier, PO, item, receiver, and value refinements), query-plan evidence, responsive browser proof, full suite, production build, and hosted gates remain pending.

### Receiving register query/export parity checkpoint — July 24, 2026

- Added the server-owned `q` filter for ordinary Receiving: case-insensitive GRN, Purchase Order reference, and supplier legal/trading-name matching, capped at 120 characters. Tab links, page links, search form, and ordinary CSV export retain the active query and tab; dashboard follow-up filtering remains isolated.
- Focused Receiving coverage remains green at 20/20; full suite, production build, and hosted gates remain required. Exact status/date filters now use strict lifecycle allow-lists and operational `Asia/Manila` receipt-date windows; item/receiver/value selectors remain deferred pending a larger option-loading/query-plan slice. Tab counts share the active query/status/date scope, while the legacy Posted tab intentionally remains `status != DRAFT` for backward compatibility.
- The ordinary Receiving page projection now uses Prisma `_count.lines` instead of hydrating every receipt line solely to display the line count. The mapper retains a compatibility fallback for detail/list callers that still provide line arrays; focused Receiving coverage remains 20/20 and web typecheck passes. This closes one bounded-list projection gap but does not close the remaining filter, query-plan, browser, production-build, or hosted gates.
- `DEC-0094` implements Phase A of the Receiving register filter contract: exact server-owned Supplier and Purchase Order selectors are validated as UUIDs, constrained to the selected tenant/company/location, and applied to ordinary page rows, tab counts, pagination links, and CSV export. Option lists are receipt-derived, capped at 100 recent rows, include selected values, and disclose when capped; the active search can narrow the option population. Item, receiver, and accepted-value filters remain intentionally deferred because item relation plans, historical receiver option loading, and value cost-visibility/currency/null-cost semantics still require separate evidence. Focused Receiving service coverage passes 23/23, visible-surface coverage passes 5/5, web typecheck and web lint pass; disposable PostgreSQL filter assertions are authored but the rerun was not executed because the isolated-container approval timed out. PostgreSQL EXPLAIN/volume, responsive browser, full suite, production build, and hosted gates remain open.

### DEC-0095 Receiving receiver-filter checkpoint — July 24, 2026

- `DEC-0095` implements Phase B receiver filtering for the ordinary Receiving register. The exact receiver selector is server-owned, scoped to the selected tenant/company/location through receipt rows, preserved through tabs, pagination, and ordinary CSV export, and uses bounded receipt-derived options. Historical inactive receivers remain selectable and are labeled `(inactive)`; malformed identifiers fail safely. Item and accepted-value filters remain deferred: item CSV context semantics and accepted-value cost visibility/currency/null handling still need product and query-plan evidence. Focused service, visible-surface, typecheck, and lint validation are required for this checkpoint; disposable PostgreSQL execution, EXPLAIN/volume, responsive browser, full suite, production build, and hosted gates remain open.

### DEC-0096 Receiving item-filter deferral checkpoint — July 24, 2026

- `DEC-0096` keeps the Receiving item selector and operative `itemId` query parameter deferred. `DEC-0100` confirms the same boundary after independent workflow/data/reporting review: activating it without a reporting decision would leave CSV row semantics ambiguous because receipt-level matching can export nonmatching lines while matching-line export changes row grain and receipt context. Before activation, the product/reporting owner must choose and document the CSV contract; data engineering must evidence the scoped `GoodsReceiptLine.itemId` relation, bounded historical option loading, foreign-item isolation, and representative PostgreSQL `EXPLAIN (ANALYZE, BUFFERS)` volume behavior. The future item predicate must be shared by rows, tab counts, pagination, and ordinary CSV. Accepted-value filtering remains a separate Phase C decision. No visible control was added, so current UI behavior remains truthful; browser, disposable PostgreSQL, query-plan, full-suite, build, and hosted gates remain open.
- The next source-control verification passed after registering the new server-owned option reader in the authorization baseline: the authorization manifest is 20/20, root lint and typecheck pass, and the web suite reaches 1,271 passed tests with 304 skipped and one TODO. Twelve database-backed authorization files fail closed locally with `AUTHORIZATION_DATABASE_SENTINEL_REQUIRED`; this is an environment gate, not behavioral evidence. The baseline registration is not a production-readiness closure: disposable PostgreSQL, responsive browser, production build, and hosted release/recovery gates remain open.

### DEC-0101 Receiving create-task mode checkpoint — July 24, 2026

- `DEC-0101` replaces the crowded Receiving create modal with a dedicated `/receiving/new` full-page task mode. The register now links to `Create Draft Receipt`; the existing server action, input schemas, idempotency, authorization/scope checks, audit behavior, and draft-only semantics are preserved. The task editor has a bounded responsive layout and sticky full-width mobile draft action; inventory remains unchanged until the separately authorized receipt-detail posting action.
- Focused Receiving coverage passes 45/45 executed tests with one database test intentionally skipped; the full repository suite passes with 1,268 tests passed, 301 skipped, and one TODO; authorization-manifest coverage passes 20/20; web typecheck, web lint, and `git diff --check` pass. The UI audit severity is reduced from Critical to High because the modal defect is remediated, while responsive browser proof, disposable PostgreSQL, query-plan/volume evidence, production build, and hosted deployment/recovery gates remain open.

### DEC-0102 Supplier Quote create-task mode checkpoint — July 24, 2026

- Supplier quote capture now has a dedicated `/quotes/new` route with explicit company/location context, quote-only/no-PO-commitment copy, scoped empty/configuration states, and the existing server-authorized `createSupplierQuote` action. The Quotes register uses a navigational `Record Supplier Quote` action rather than a repeated inline TaskSheet trigger; recommendation and Purchase Order actions remain separate.
- Focused quotation service coverage passes 10/10 after the new route-surface assertion; the authorization manifest passes 20/20 after registering the new protected route/action; web typecheck, lint, and `git diff --check` pass. This does not complete the Quotes workspace: selected-record comparison detail, repeated inline comparison/recommendation content, commercial/evidence fields, responsive browser, disposable PostgreSQL, production-build, and hosted gates remain pending.
- The approved-request Quotes queue now uses a server-owned 25-row page with an exact scoped count, deterministic request ordering, clamped page input, and a shared `PaginationBar`; the capture route continues to load its focused selection options separately. Focused quotation coverage passes 11/11, typecheck/lint pass, and the remaining comparison/detail, commercial/evidence, idempotency, browser, PostgreSQL, build, and hosted gates remain open.
- The post-policy-correction repository suite passes 1,272 tests with 301 skipped and one TODO; the authorization manifest remains 20/20. This local regression does not close the Quotes production-build, responsive-browser, disposable-PostgreSQL, or hosted gates.
- Recommendation submission now reloads the current purchasing policy and approved Purchase Request lines, derives the live comparison threshold, and revalidates minimum quote/shortfall justification before creating an approval route. Focused quotation coverage passes 12/12; stale drafts can no longer bypass a tightened quote policy through stored creation-time fields. An executable policy-change integration case and the quote-create idempotency migration remain open.
- Quote creation now accepts a client-generated retry key, hashes the scoped canonical request, and posts the header, lines, and first audit event transactionally. Exact retries return the original quote; changed payloads, actors, or scope fail closed, and a unique-key race fallback replays only the targeted idempotency conflict. Focused quotation coverage passes 13/13; the full web suite passes 1,273 tests with 301 skipped and one TODO; web typecheck/lint and Prisma validation pass. Migration `20260724150000_supplier_quote_create_idempotency`, disposable PostgreSQL exact-retry/concurrency evidence, responsive browser, production-build, and hosted release/recovery gates remain open.
- `DEC-0104` replaces the vertically repeated Quotes cards with a master-detail workspace: a server-paginated approved-request queue, one selected-request comparison detail, and one recommendation composer. The selected surface shows recorded totals, line availability, lead time, terms, supplier accreditation, requester, due date, and recommendation state; mobile collapses the master queue before detail and keeps recommendation controls at usable touch sizes. Focused quotation coverage remains 13/13 and web typecheck/lint pass. Tax/discount/freight/other-charge breakdowns and binary quote attachments remain explicitly unavailable pending a policy/schema/evidence decision; responsive browser, disposable PostgreSQL, production-build, and hosted gates remain open.
- The comparison checkpoint full web suite passes 1,273 tests with 301 skipped and one TODO; the authorization manifest remains 20/20, and an isolated `NEXT_DIST_DIR=.next-quality-quotes` production build passes compile, type validation, static generation, route optimization, and trace collection. Generated route metadata was restored afterward. Responsive browser, disposable PostgreSQL comparison/recommendation concurrency and scope evidence, and hosted release/recovery gates remain open.
- `DEC-0105` adds additive Decimal quote commercial components: subtotal, tax, discount, freight, other charges, and supplier-accreditation snapshot. Existing quote totals are preserved by migration `20260724160000_supplier_quote_commercial_fields`; server-side six-decimal arithmetic rejects negative totals, and the comparison/editor now display and capture these values. Focused quotation coverage passes 14/14; Prisma validation, web typecheck, and lint pass. Disposable PostgreSQL migration/backfill, Decimal/export parity, scope/concurrency, responsive browser, production-build, and hosted gates remain open. Binary quote attachments remain deferred pending the controlled-evidence requirement matrix.
- The supplier-quotes CSV now repeats the persisted Decimal commercial components and supplier-accreditation snapshot at its existing line-grain export, with legacy zero-charge fallback parity. Focused quotation coverage is extended by the export contract assertion; disposable PostgreSQL Decimal/export parity and volume evidence remain open.
- The export-parity checkpoint passes focused quotation coverage 15/15, web typecheck/lint, and isolated `NEXT_DIST_DIR=.next-quality-quotes-export` production build (compile, type validation, static generation, optimization, and trace collection). Generated route metadata was restored afterward. Disposable PostgreSQL Decimal/export parity, export-volume, responsive browser, and hosted release/recovery gates remain open.
- The full non-database web suite now passes 1,275 tests with 301 skipped and one TODO; the authorization manifest passes 20/20. This regression evidence does not close disposable PostgreSQL export parity/volume, responsive browser, or hosted release/recovery gates.
- `SPF-007` Quotes state coverage now includes route-level loading and retryable error boundaries. The loading skeleton preserves the master-detail shape; the error state explains that quote, recommendation, and Purchase Order data were not changed. Focused quotation coverage remains 15/15; web typecheck/lint and isolated `NEXT_DIST_DIR=.next-quality-quotes-state` production build pass. Generated route metadata was restored afterward. Responsive browser and hosted gates remain open.
- The post-state full non-database web suite remains green at 1,275 passed tests, 301 skipped, and one TODO; the authorization manifest remains 20/20. This does not close responsive browser, disposable PostgreSQL, or hosted release/recovery gates.
- `SPF-007` now has a shared `(app)` loading skeleton and retryable error boundary for operational routes without a more specific state boundary. The generic error explicitly says no operational record or workflow action was changed; Dashboard, Evidence, Expansion, and Quotes retain their more specific boundaries. Shared-state contract coverage passes 2/2, web typecheck/lint pass, and the isolated production build passed in the preceding state-boundary checkpoint. Full suite, responsive browser, and hosted gates remain applicable.
- The shared-state increment full non-database suite passes 1,277 tests with 301 skipped and one TODO across 120 passed files; the authorization manifest remains 20/20. Responsive browser and hosted release/recovery evidence remain open.
- `DEC-0106` adds optional controlled quotation evidence to the comparison workspace through shared source type `SUPPLIER_QUOTATION`. The server derives tenant/company and Purchase Request location scope, requires quote-management permission for list/link/upload, and preserves scan quarantine, availability gating, download audit, and recommendation/PO independence. The controlled-evidence PostgreSQL authorization fixture now includes a scoped Supplier Quotation source in its location-denial, assignment, list, and download matrix. Focused quotation coverage remains 14/14; attachments unit coverage passes 13/13; web typecheck/lint pass. The evidence matrix could not execute locally because the required authorization database sentinel is not configured (`AUTHORIZATION_DATABASE_SENTINEL_REQUIRED`). Mandatory evidence requirements remain open pending the controlled-evidence matrix. Responsive browser, production-build, and hosted release/recovery gates remain open.
- The commercial-field checkpoint full web suite passes 1,274 tests with 301 skipped and one TODO; the authorization manifest remains 20/20, and an isolated `NEXT_DIST_DIR=.next-quality-quotes-commercial` production build passes compile, type validation, static generation, route optimization, and trace collection. Generated route metadata was restored afterward. Disposable PostgreSQL migration/backfill and export/concurrency evidence, responsive browser, and hosted release/recovery gates remain open.
- A fresh isolated production build for source SHA `7ec94be` passed with `NEXT_DIST_DIR=.next-quality` (compile, type validation, static generation, route optimization, and build traces). Generated `tsconfig`/route metadata was restored to the repository’s tracked baseline afterward. This closes only the local build check; authenticated production-mode browser, disposable PostgreSQL, and hosted release/recovery evidence remain open.
- Release-tool self-test and secret review passed for the current candidate; evidence was written under `release-evidence/self-tests/` and `release-evidence/secret-review/`. These checks do not replace hosted deployment, recovery, database, or authenticated browser gates.
- The owner handoff checklist was refreshed at `release-evidence/pending-evidence-checklist/pending-evidence-checklist-20260724T012157Z.txt`. It confirms the remaining critical evidence is external/environment-owned: populated migration snapshots, backup/restore and rollback artifacts, provider/security proof, pilot/UAT execution, enablement signoff, and final release metadata. No local command can legitimately mark those artifacts complete.

### DEC-0098 Stock Count immutable recount recovery decision — July 24, 2026

- `DEC-0098` confirms the next implementation contract for Count Variance prerequisites: an additive immutable child attempt/version model with the existing count session retained as the stable case and attempt 1; original cutoff reuse only under continuously active movement freeze and no post-cutoff movement under the canonical inventory-location lock; otherwise a controlled close/new cutoff; explicit MFA/reason/evidence/idempotent void-for-recount for approved-but-unposted generated adjustments while preserving approval history; and full-document reversal before any posted adjustment correction. Count Variance remains inactive until the schema migration/backfill, recovery actions, authorization/segregation, notification, concurrency, blind-count, and responsive browser evidence gates pass.

### DEC-0098 immutable attempt foundation implementation checkpoint — July 24, 2026

- Added the additive Prisma foundation and reviewed migration `20260724140000_stock_count_immutable_attempt_foundation`: `StockCountAttempt`/`StockCountAttemptLine`, session current-attempt pointer, adjustment source lineage, lossless attempt-1 backfill, scoped indexes, transaction lock/statement timeouts, and database-enforced terminal history guards. Existing legacy count services remain authoritative during cutover; no Count Variance surface was activated.
- `prisma validate`, database package typecheck, migration safety-register review, and diff hygiene pass for this slice. The migration is authored but not executed locally because no disposable PostgreSQL admin sentinel is configured; live backfill/trigger behavior, rollback rehearsal, concurrency, authorization, browser/mobile, hosted deployment/recovery, UAT, and enablement gates remain open. Workspace 1 and Phase I remain in progress.

- First-pass lifecycle dual-write is now implemented in one existing location-locked transaction: start creates/links attempt 1 when needed and mirrors snapshot lines, entry save mirrors counted facts with line CAS, and submit/review mirror status and actor timestamps. The legacy session/line path remains the compatibility read path, and Count Variance/recount recovery remains inactive. Focused Stock Count coverage passes 32/32; web lint and typecheck pass. The isolated production-build attempt reached Next.js optimized-build initialization but produced no terminal success, so the current production-build gate remains open. Disposable PostgreSQL migration/backfill/trigger and concurrency execution, responsive browser, hosted recovery, and UAT gates remain open.

- The next read-safety increment requires every scoped Stock Count read and lock to have a non-null `currentAttemptId`; records missing the additive lineage fail closed instead of silently using an unlinked mutable case. Legacy line projections remain the compatibility projection until attempt-line parity and redaction evidence are completed. Focused coverage remains 32/32; disposable migration execution and attempt-line parity/query evidence remain open.

- The read-safety increment now validates current-attempt lineage at the service boundary: scoped Prisma reads require a non-null attempt with matching tenant/company/location context, and the location-locked raw path joins the attempt to the same session and scope before returning a lockable record. Missing or cross-scope lineage therefore fails closed as `STOCK_COUNT_NOT_FOUND`; legacy line projections remain the compatibility projection and Count Variance/recount recovery remain inactive. Focused Stock Count coverage passes 25/25 for the affected service suite and web typecheck passes. Disposable PostgreSQL migration/backfill/trigger, cross-scope negative, contention, browser, hosted recovery, and UAT gates remain open.

- Cancellation now mirrors the selected attempt's terminal state, cancellation timestamp, and reason under the same location lock and session CAS as the compatibility session. A missing attempt or zero-row attempt CAS fails the transaction before audit, preventing a stale immutable attempt from surviving a cancelled case. Focused Stock Count workflow/foundation coverage passes 35/35; web typecheck passes. Header/status parity, adjustment attempt-line lineage, disposable PostgreSQL trigger/concurrency evidence, browser, hosted recovery, and UAT gates remain open; operational reads still use the legacy compatibility projection.

- Corrective safety increment: scheduling now creates and links immutable attempt 1 in the same transaction as the Stock Count session and scheduling audit, so scheduled counts are not invisible to the current-attempt read/lock gate and can reach `Start Count`. Count Variance dashboard cards/exception tasks and the detail-page generation action are disabled, and the server generator fails closed with `STOCK_COUNT_VARIANCE_DISABLED` before any database mutation until DEC-0098 recovery and lineage gates close. Focused workflow/foundation tests, web typecheck/lint, and an isolated production build pass; disposable PostgreSQL migration/backfill/trigger, pointer/adjustment lineage, recovery, browser, hosted, and UAT gates remain open.

- Header/status parity now extends the existing current-attempt line digest guard across lifecycle status, blind/freeze flags, actor lineage, cutoff/start/submit/review/cancel timestamps, review notes, and cancellation reason. Any mismatch fails closed as `STOCK_COUNT_ATTEMPT_HEADER_PARITY_FAILED`; operational reads remain on the legacy projection until an attempt-authoritative mapper is separately verified. The dormant variance bridge now carries the selected current-attempt and attempt-line lineage fields and rejects mismatched attempt lines, but generation remains server-disabled. Focused Stock Count tests pass 37/37; PostgreSQL parity/constraint evidence, recovery authority, browser, hosted, and UAT gates remain open.

- Migration review found and corrected a missing PL/pgSQL `END IF` in `20260724140000_stock_count_immutable_attempt_foundation`; the migration safety register now carries the corrected SHA-256. Prisma validation, database typecheck, web typecheck/lint, focused tests, production build, and diff hygiene pass. The corrected migration remains authored but unexecuted because disposable PostgreSQL is unavailable; no migration deployment or production-readiness credit is claimed.

- Authored additive migration `20260724170000_stock_count_attempt_scope_lineage_guards` with preflight duplicate checks, current-attempt/session scope triggers, adjustment-header and attempt-line lineage triggers, and attempt-scoped partial uniqueness. It deliberately preserves Count Variance as disabled and does not rewrite historical rows. The migration is hash-registered but unexecuted; disposable PostgreSQL preflight, cross-scope/lineage negatives, redeploy, drift, restore, contention, and hosted evidence remain mandatory.

- Stock Count My Tasks now requires the immutable `currentAttempt` relation to match the assigned counter and actionable status for Start, Enter, and Submit predicates; task status is projected from that attempt. A stale legacy session assignment/status can no longer create a task. Focused Stock Count coverage remains 37/37, with PostgreSQL parity/concurrency, browser, hosted, and UAT gates still open.

- Stock Count detail and CSV adjustment context now reads through the selected current attempt rather than the newest session-linked adjustment, preventing a prior-attempt adjustment from being presented as current. Focused Stock Count coverage passes 38/38; disposable PostgreSQL lineage/concurrency, browser, hosted, and UAT gates remain open.

- The legacy `RECOUNT` review action now fails closed before scope lookup, locks, audit, or mutation with `STOCK_COUNT_RECOUNT_DISABLED`; the detail form visibly disables the option and explains that immutable recovery safeguards are pending. Focused Stock Count coverage passes 39/39 across workflow/foundation tests, while migration, recovery, browser, hosted, and UAT gates remain open.

- Validated current-attempt identity is now included in Stock Count list/page/detail projections, and detail visibly labels the current immutable attempt number. This is read-only lineage context; legacy line compatibility and Count Variance/recount activation remain gated. Focused Stock Count coverage remains 39/39; migration, recovery, browser, hosted, and UAT gates remain open.

- Stock Count CSV now appends the validated current attempt number to each row, preserving existing line-grain columns and blind-count redaction. It is lineage context only; Count Variance/recount activation and full attempt-authoritative export remain gated. Focused Stock Count coverage remains 39/39; PostgreSQL, browser, hosted, and UAT gates remain open.

- The accumulated non-database web regression initially exposed authorization-baseline drift for the removed disabled Count Variance page action and the dashboard service’s current permission expression. Regenerating the source baseline removed only the stale action and recorded the dashboard permission change; the authorization manifest now passes 20/20. PostgreSQL authorization, responsive browser, production build, hosted, and recovery gates remain separate and open.

- After baseline reconciliation, the complete non-database web suite passes 1,290 tests across 120 files, with 301 environment-gated skips and one existing TODO; the authorization manifest passes 20/20. This closes local regression evidence only and does not close disposable PostgreSQL, production build, authenticated browser, hosted deployment, recovery, or UAT gates.

- A clean isolated `NEXT_DIST_DIR=.next-quality-current5` production build now completes successfully: compilation, TypeScript validation, static generation, route optimization, and build traces all pass. Generated route metadata was restored afterward. This closes only the local production-build gate; authenticated production-mode browser, disposable PostgreSQL, hosted deployment/recovery, and UAT gates remain open.

- Added a reversible dual-read parity guard for Stock Count detail and CSV export. A scoped raw PostgreSQL projection compares current attempt-1 and legacy line cardinality plus deterministic line digests (identity, scope, lot/expiry, quantities, notes, counter, and timestamps); missing or divergent attempt evidence fails with `STOCK_COUNT_ATTEMPT_LINE_PARITY_FAILED` rather than falling back. Legacy data remains the compatibility projection and blind redaction is applied after the guard. Focused Stock Count coverage passes 33/33; list/page/dashboard parity, disposable PostgreSQL execution, query-plan/volume, migration/restore, browser, hosted, and Count Variance recovery gates remain open.

- The same parity guard now runs for ordinary Stock Count list and paginated-page rows before mapping the legacy compatibility projection. No dashboard or Count Variance authority changed; dashboard attempt aggregates and full attempt-only read cutover remain pending. Focused coverage remains 33/33, with live PostgreSQL/query-plan evidence still required.

- Independent dashboard review rejects per-candidate parity calls: they would add N+1 raw digest scans and still leave the unbounded `varianceCount` aggregate unverified. A single server-owned attempt-only aggregate/predicate contract is now implemented and reused for total and bounded candidates, with current-attempt fail-closed scope, status × blind-count × actor-lineage filtering, deterministic ordering, and no dashboard mutation. Focused dashboard/workflow tests pass 34/34; web typecheck and lint pass. PostgreSQL `EXPLAIN (ANALYZE, BUFFERS)`/representative-volume evidence, disposable-database execution, and hosted/browser gates remain open before production enablement or Count Variance activation; the dashboard is not yet an authoritative Count Variance source.
- The authorization-surface baseline now declares the exported parity guard, and the authorization manifest passes 20/20. This closes baseline drift only; it does not replace the pending disposable-database, query-plan, browser, or hosted production gates.
- The complete non-database web suite now passes 1,267 tests with 301 skipped and one TODO after the baseline correction. An isolated `NEXT_DIST_DIR=.next-quality-dashboard` production build also passes compile, type validation, static generation, route optimization, and trace collection; generated route metadata was restored afterward. These local gates do not close disposable PostgreSQL, authenticated responsive-browser, or hosted release/recovery evidence.
- The current `pnpm test:e2e` attempt fails closed before launching because `DISPOSABLE_DATABASE_ADMIN_URL` is not configured (`DISPOSABLE_DATABASE_ADMIN_URL_REQUIRED`). No browser or database readiness credit is claimed from that attempt.

### DEC-0097 Receiving accepted-value filter deferral checkpoint — July 24, 2026

- `DEC-0097` keeps accepted-value filtering deferred as a Phase C decision. Independent workflow and data/security review found that an operative value filter would otherwise expose an undefined financial contract: receipt versus line grain, nullable `GoodsReceiptLine.unitCost` treatment (unknown is not zero), PO-level currency and mixed-currency handling, confidential-cost authorization, CSV context, and aggregate query-plan/volume behavior. No accepted-value URL parameter or visible control is added. Any future implementation must use exact Decimal server-side predicates shared by rows, tab counts, pagination, and ordinary CSV, with non-enumerating denial and explicit zero-versus-NULL tests. Browser, disposable PostgreSQL, query-plan, hosted, and final release gates remain open.

### SPF-008 ordinary Inventory Ledger pagination checkpoint — July 24, 2026

- The ordinary Inventory Ledger now uses a server-backed page query with bounded 25-row pages, exact scoped counts, deterministic `occurredAt DESC, id DESC` ordering, and filter-preserving pagination for search and movement type. The existing exact reconciliation trace remains on its dedicated server-paginated service, and CSV export remains a separately authorized full filtered export.
- Inventory-focused service coverage passes 24/24 plus the updated static ledger-surface assertion; web typecheck passes. Full lint, production build, responsive browser evidence, PostgreSQL query-plan/volume evidence, and hosted gates remain required before SPF-008 or Workspace 1 completion.

### SPF-008 ordinary Wastage register pagination checkpoint — July 24, 2026

- The ordinary Wastage register now uses a server-backed 25-row page query with exact selected-scope counts, deterministic `createdAt DESC, id DESC` ordering, and page links. The closed `wastage-exceptions-v1` dashboard profile remains a separate read-only contract with its own pagination and predicate.
- Focused Wastage coverage, authorization-manifest registration, web typecheck, and `git diff --check` remain required evidence before this slice is complete; full lint, production build, responsive browser evidence, PostgreSQL query-plan/volume evidence, and hosted gates remain open.

### DEC-0063/0066 My Tasks cursor contract checkpoint — July 24, 2026

- Signed My Tasks cursors now include an explicit registered-source contract version (`my-tasks-registry-v6`) and the selected module, priority, source-qualified status, and normalized native due bounds in their scope hash. Any future adapter predicate, ordering, enrollment, projection, or queue-filter change must bump this version so old cursors fail closed instead of traversing a changed queue contract. `v6` invalidates pre-correction cursors after Branch Operations and Food Safety status filters were changed from permission-dependent array positions to named review/correction predicates.
- Focused My Tasks coverage, full web lint/typecheck/test, and the authorization-manifest gate remain required evidence; browser, PostgreSQL, and hosted production gates remain open.

### DEC-0107 My Tasks filter contract checkpoint — July 24, 2026

- Independent workflow and data/security reviews confirmed that the dashboard's required My Tasks filters cannot be shipped as client-side or post-merge filtering. The current adapters expose heterogeneous projections, source-native statuses, fixed `HIGH`/no-due semantics for most sources, no common assigned-by field, and only the already-selected location scope.
- The confirmed implementation contract is server-owned module, canonical priority, source-qualified status, selected-location context, and native `dueAt` only. Every filter must be applied inside each adapter's count and page predicate and bound into the signed cursor; `assignedBy` and arbitrary multi-location selectors remain deferred. The existing partial-source continuation risk must be resolved before filtered pagination is called complete.
- Module, canonical priority, source-qualified status, and native due-bucket filters are now live across every enrolled adapter. Filters are validated at the server boundary, intersect each source's existing authorized action predicate for both count and page reads, reject status without a selected module, use Asia/Manila day boundaries, exclude fixed/no-due sources from date reads, and bind normalized bounds into signed cursors. Assignment and arbitrary-location filters remain absent rather than inert; they require separate authority and data contracts. See `docs/core/00-governance/decisions/DEC-0107-MY-TASKS-FILTER-CONTRACT.md`.
- Focused My Tasks coverage passes 15/15; the full non-database web suite passes 1,283 tests with 301 skipped and one todo; web typecheck/lint, the 20/20 authorization manifest, isolated production build, and `git diff --check` pass. PostgreSQL boundary/count-page parity, query-plan/index evidence, responsive authenticated browser proof, and hosted gates remain open.
- Partial-source pagination now fails closed: when any selected enrolled source is unavailable, the page withholds both the total and `nextCursor`. This prevents a recovered source from being permanently skipped after a partial-page anchor; users must reload page one after source availability returns. Focused regression coverage passes for this behavior. All enrolled adapters now accept the shared module, priority, source-qualified status, and native due-bucket predicate contract; PostgreSQL boundary/count-page parity, query-plan/index evidence, responsive authenticated browser proof, and hosted gates remain open.

### SPF-008 ordinary Stock Counts pagination checkpoint — July 24, 2026

- The ordinary Stock Counts register now uses a server-backed 25-row page query with exact selected-scope counts, deterministic `createdAt DESC, id DESC` ordering, and page links. Blind-count redaction and review permissions remain enforced by the shared mapper; export remains a separately authorized full report.
- Focused Stock Counts coverage, authorization-manifest registration, web typecheck, lint, and full web tests remain required evidence; production build, responsive browser, PostgreSQL query-plan/volume, and hosted gates remain open.

### SPF-008 ordinary Transfers pagination checkpoint — July 24, 2026

- The ordinary Transfers register now queries the selected tab server-side (`All`, `Draft`, `Dispatch`, `Receive`, or `Completed`) with bounded 25-row pages, scoped tab counts, deterministic `createdAt DESC, id DESC` ordering, and filter-preserving links. The closed Transfer Follow-up dashboard profile remains separate and read-only.
- Focused Transfer coverage and authorization-manifest registration pass; full lint, typecheck, web suite, production build, responsive browser, PostgreSQL query-plan/volume, and hosted gates remain required.

### SPF-008 ordinary Stock Adjustments pagination checkpoint — July 24, 2026

- The ordinary Stock Adjustments register now uses a server-backed 25-row page query with exact selected-scope counts, deterministic `createdAt DESC, id DESC` ordering, and page links. The closed Adjustment Exceptions dashboard profile remains a separate read-only contract.
- Focused Stock Adjustment coverage and authorization-manifest registration pass; full lint, typecheck, web suite, production build, responsive browser, PostgreSQL query-plan/volume, and hosted gates remain required.

### SPF-008 ordinary Purchase Requests pagination checkpoint — July 24, 2026

- The ordinary Purchase Requests register now uses the existing server-owned filter contract with bounded 25-row pages, exact scoped counts, deterministic `createdAt DESC, id DESC` ordering, and filter-preserving navigation. The closed Open PR dashboard profile remains separate and uses its own source-owned population.
- Focused Purchase Request coverage, authorization-manifest registration, web typecheck, lint, and full web tests remain required evidence; production build, responsive browser, PostgreSQL query-plan/volume, and hosted gates remain open.

### SPF-008 ordinary Purchase Orders pagination checkpoint — July 24, 2026

- The ordinary Purchase Orders register now uses the existing server-owned filter contract with bounded 25-row pages, exact scoped counts, deterministic `createdAt DESC, id DESC` ordering, and filter-preserving navigation. The closed Open PO dashboard profile remains separate and uses its own source-owned population.

### SPF-008 ordinary Branch Operations pagination checkpoint — July 24, 2026

- The authoritative Branch Operations register now uses a server-owned 25-row page query with exact tenant/company/optional-brand/location counts, deterministic `businessDate DESC, createdAt DESC, id DESC` ordering, and server-side search across checklist, actor, line, evidence, and notes fields plus shift, status, and business-date filters. Page links preserve the active filters; the bounded dashboard aggregate remains separate from the list and export remains a separately authorized full report.
- Existing direct detail, correction, review, return, close, audit, and source-record boundaries remain unchanged. Focused Branch Operations tests and web typecheck are required; responsive browser, disposable PostgreSQL, hosted, and production-build gates remain open.

### SPF-008 ordinary Food Safety pagination checkpoint — July 24, 2026

- The ordinary Food Safety register now uses a server-owned 25-row page query with exact tenant/company/optional-brand/location counts, deterministic `businessDate DESC, createdAt DESC, id DESC` ordering, and server-side search across log, actor, reading, corrective-action, and evidence fields plus type, status, and business-date filters. Page links preserve active filters; dashboard aggregates remain separate and CSV export remains separately authorized.
- Existing source detail, review, correction, close, evidence, audit, and no-inventory-mutation controls remain unchanged. Focused Food Safety tests and web typecheck are required; responsive browser, disposable PostgreSQL, hosted, export-parity, and production-build gates remain open.
- The Food Safety detail summary now performs one exact scoped record lookup rather than materializing the full dashboard population. Full export/report paths remain intentionally separate and still require volume and parity evidence.
- The ordinary Food Safety page projection now returns reading counts rather than hydrating reading payloads; reading detail remains on the focused record route.
- Food Safety detail now exposes a read-only Activity section sourced from immutable tenant/company-scoped audit events for the selected compliance log; the focused Food Safety suite passes 15/15 and web typecheck passes. Browser, disposable-PostgreSQL, export-parity, hosted, and production-build gates remain open.
- The corrected full non-database web regression reaches 1,259 passed tests across 119 files, with 10 PostgreSQL/environment-gated files skipped, 301 skipped tests, and one existing TODO. The first run exposed authorization-surface baseline drift for the changed Incident detail service; the generated baseline was refreshed and the dedicated manifest now passes 20/20. This closes only source-control regression evidence; PostgreSQL, browser, hosted, and production-build gates remain open.

### SPF-008 ordinary Incidents pagination checkpoint — July 24, 2026

- The ordinary Incidents register now uses a server-owned 25-row page query with exact tenant/company/optional-brand/location counts, deterministic `incidentDate DESC, createdAt DESC, id DESC` ordering, validated incident-date/status/severity filters, and server-side text search across incident, corrective-action, evidence, and source-link fields. Page links preserve active filters; dashboard aggregates remain separate and export remains separately authorized.
- Existing source-link scope checks, owner/resolver segregation, evidence, idempotency, cancellation, correction, audit, and source-record non-mutation controls remain unchanged. Focused Incident tests and web typecheck are required; cancellation metric/UI defects, responsive browser, PostgreSQL, export-parity, hosted, and production-build gates remain open.
- Cancelled incidents are now excluded from overdue metrics and their detail view presents a terminal cancellation state instead of an unresolved/open follow-up.
- Incident detail now performs a direct tenant/company/brand/location-scoped lookup instead of loading the full dashboard collection, and exposes a read-only Activity section sourced from immutable `AuditEvent` rows for the selected incident. The focused Incident suite passes 21/21; browser, disposable-PostgreSQL, export-parity, hosted, and production-build gates remain open.

### SPF-008 ordinary Maintenance pagination checkpoint — July 24, 2026

- The ordinary Maintenance register now uses a server-owned 25-row page query with exact tenant/company/null-aware-brand/location counts, deterministic `requestedAt DESC, createdAt DESC, id DESC` ordering, validated requested-date/status/priority filters, and server-side text search across ticket, asset, corrective-action, evidence, and source-incident fields. Page links preserve active filters; dashboard aggregates remain separate and export remains separately authorized.
- Existing completion, cancellation, correction, reporter-lineage, owner, idempotency, audit, evidence, and incident-link controls remain unchanged. Focused Maintenance tests and web typecheck are required; responsive browser, PostgreSQL, export-parity, hosted, and production-build gates remain open.
- Maintenance search now preserves reporter/owner-name matching through tenant-scoped actor resolution, and the register distinguishes an empty dataset from a no-match filter result.
- Maintenance completion-date defaults now use the operational timezone helper, and malformed requested-date filters reuse the existing user-safe validation code.
- Maintenance and Incident cancelled rows now use a neutral terminal-state badge rather than success-green styling.
- Full non-database web regression after the register and terminal-state changes passes 1,258 tests across 119 files, with 10 environment-gated files skipped; authorization-manifest coverage passes all 20 checks. PostgreSQL, browser, hosted deployment/recovery, and production-build gates remain open.
- Repository typecheck caught and closed a UUID-filter typing defect in the new Incident/Maintenance server predicates; exact UUID source-link search remains supported, while non-UUID text uses source-type/incident fields.
- Incident pagination behavior now has a runtime contract test covering count/page predicate parity, actor-name search, page clamping, bounded `skip/take`, and deterministic ordering.
- Maintenance detail now exposes a read-only Activity section sourced from immutable, tenant/company-scoped audit events for the selected ticket; the focused Maintenance suite passes 28/28. Browser, disposable-PostgreSQL, export-parity, hosted, and production-build gates remain open.
- Focused Purchase Order coverage, authorization-manifest registration, web typecheck, lint, and full web tests remain required evidence; production build, responsive browser, PostgreSQL query-plan/volume, and hosted gates remain open.

- The current approval-engine checkpoint uses one transaction, scoped row locks, live authority revalidation, and compare-and-set transitions. Intermediate approval keeps the source record pending and advances only the actionable step. Direct-user initial and normalized next-step assignments create at most one idempotent personal notification, while role-scoped work creates no per-user step-ready notification rows. All five initial procurement paths implement this contract, but role-scoped work has no complete actionable queue while `APPROVAL_ROUTING_V1_ENABLED=false`; the active step is authoritative, no parallel flag-off Inbox is authorized, and the existing current-user due/overdue reminder scan is only a partial non-actionable signal. Final approve, return, or reject writes one retry-safe outcome notification for the requester or responsible owner in the terminal-decision transaction. This bounded behavior does not enable broad external fanout.
- Goods Receipt reference allocation now uses a transactional company/document-type/year sequence row (`DEC-0092`) instead of `count + 1`; the existing unique index remains the backstop and failed receipt/audit transactions roll the sequence increment back. Focused receiving serialization coverage passes 13/13 and web typecheck passes.
- `DEC-0093` implements retry-safe Goods Receipt draft creation with an additive nullable tenant/company-unique idempotency key and canonical SHA-256 request hash. Exact retries return the original draft only after live authorization plus actor, PO, location, and payload checks; changed or cross-scope reuse fails closed without returning the existing receipt ID. The UI generates a fresh key and rotates it when the selected PO changes. Focused receiving unit coverage remains green. The service retains a transaction-local raw SQL compatibility path for the new columns because the checked-in generated client must be regenerated before typed access is adopted.
- After restoring normal native Prisma Client generation (`copyEngine: true` with the Windows query engine), the disposable PostgreSQL lifecycle applied all 129 migrations, seeded successfully, passed the seed-repeatability check, authentication-throttle probes, role-contract probes, and the receiving serialization integration. The receiving database test passed 1/1, including authority revocation, same-key replay, changed-payload conflict, concurrent draft serialization, and PO-closure barriers. This closes the local disposable migration/concurrency/idempotency evidence for `DEC-0093`; hosted, browser, and final release gates remain open.
- Goods Receipt reference retries classify only the company/reference uniqueness target; unrelated uniqueness errors and idempotency conflicts are not swallowed or retried.
- Receiving filter validation now has user-safe action-feedback messages for invalid status, invalid dates, and reversed date ranges; focused action-feedback coverage passes 10/10. This improves the visible error contract but does not close the receiving database/browser/hosted gates.
- The Receiving error contract now also maps discrepancy conflicts, stale line mismatches, unsupported/overlong dashboard profiles, unavailable follow-up reasons, and overlong searches to user-safe messages. Focused action-feedback coverage passes 11/11, including a specific-message regression guard; this closes only the visible error-copy gap.
- Current root validation passes repository lint and typecheck across database, UI, web, and worker packages. The required `pnpm test:e2e` gate remains unexecuted because the disposable lifecycle fails closed with `DISPOSABLE_DATABASE_ADMIN_URL_REQUIRED`; no browser or database production evidence is claimed.
- The corrected full non-database web regression now passes 1,260 tests across 119 files, with 10 PostgreSQL/environment-gated files and 301 tests skipped plus one existing TODO. The 20-case authorization manifest passes within that suite. This is source-control evidence only; E2E, disposable PostgreSQL, production build, browser, and hosted gates remain open.
- The root `pnpm test` gate also passes the database package (34 tests, 18 database-gated skips), worker package (1 test), and the same 1,260-test web suite; the UI package has no test files and exits successfully. These remain non-database/source-control evidence only.
- Release helper self-test passes and writes its evidence artifact. Prisma client generation was retried after the root test gate but still hung after schema load; the idempotency schema slice remains uncommitted and unverified until generation can complete.
- A fresh `pnpm --dir apps/web build` attempt again stalled after Next.js initialization without a pass/fail result and was terminated. The production-build gate remains open; no build success is claimed.
- The same checkpoint adds canonical user-lock ordering, post-lock authority/MFA revalidation, same-transaction MFA denial auditing, and explicit concurrency-conflict mapping for sensitive Core Administration permission mutations; cancellation locking and compare-and-set protection for pending wastage and Stock Adjustment approvals; and consistent Purchase Order/line/receipt locking for receipt creation, posting, reversal, and remaining-balance closure. Receiving rejects stale shortage/discrepancy snapshots and revalidates live authority and quantities so concurrent actions fail without overwriting, over-receiving, reopening a closed PO, or mutating the ledger.
- Disposable PostgreSQL evidence passes 35/35 admin/platform cases and 31/31 procurement/inventory cases. It covers multi-step advance, direct and role-assigned next-recipient segregation, recipient and authority revocation while lock-blocked, atomic final-outcome notification, approve-versus-reject/cancel races, stale receipt discrepancies, receipt post/reversal versus balance closure, administrative actor/MFA revocation, and duplicate cancellation. Full lint, typecheck, production build, release-tool self-test, secret review, migration review, 823 web tests, 27 database-package tests, and the worker test pass. Independent Security and QA re-reviews returned GO for this explicitly in-progress checkpoint and NO-GO for SPF-006 completion or production promotion.
- The DEC-0049 database control is implemented locally at migration hash `52cccdce8596f9d1b287bc771c219ccf82cbe8b5c43f438ce407ba08bd671efa`. One unconditional `ENABLE ALWAYS` statement trigger rejects `UPDATE`, `DELETE`, and `TRUNCATE` on each of `AuditEvent`, `ProjectActivityEvent`, and `InventoryMovement` with SQLSTATE `55000`; ordinary `SELECT` and `INSERT` remain available. Corrections continue through new audit/activity events or authorized linked inventory reversal movements. The migration contains no row rewrite or backfill.
- Seed and test lifecycle code no longer clears protected history. Ordinary seeding is additive and idempotent, demo reset rebuilds only a loopback, positively marked disposable demo database, and database integration execution owns a uniquely named and marked disposable database. Administrative setup credentials and marker secrets are scrubbed before the application-under-test receives its per-run runtime credential.
- The Hostinger-only database role contract is implemented as packaging-neutral PostgreSQL SQL, controlled migration/verification tools, and systemd credential templates. Staging and production separate a non-login owner, controlled migrator, and membership-free runtime role. The runtime is limited to `SELECT` and `INSERT` on the three protected tables and cannot own protected objects, read `_prisma_migrations`, create schemas/databases, disable guards, alter the guard function, assume the owner/migrator role, or receive an administrative/migrator URL through the application environment. This implementation does not select between a private Hostinger host service and a dedicated private container.
- Local PostgreSQL 17.10 guard evidence preserves identical non-empty pre/post snapshots for all three protected tables, keeps inventory movement and balance-cache totals equal, passes 17/17 real-database mutation/positive-path checks, proves a forced lock timeout leaves no partial guard objects, and reports no pending redeploy or Prisma schema drift. The exact migration is independently reviewed at hash `52cccdce8596f9d1b287bc771c219ccf82cbe8b5c43f438ce407ba08bd671efa` and is `APPROVED_FOR_REHEARSAL`, not production `APPROVED`.
- The disposable lifecycle passes 11/11 local contract tests and the database-role tools pass 8/8. Controlled and adversarial identities are bound to the expected per-run database before mutation, and cross-run identity or role substitutions are rejected. All eight adversarial role/ownership/grant/function/trigger drift cases failed closed, were repaired by the controlled reconciliation path, and left zero per-run database roles after teardown; the PostgreSQL 17.10 mismatch-rejection rehearsal likewise left zero adversarial roles. Independent database/security review returned GO for the local DEC-0049 checkpoint. The complete candidate also passes 27 database tests, 823 web tests, one worker test, lint, typecheck, production build, release-tool self-tests, secret review, migration review in release mode, and `git diff --check`.
- Requested deliberation-model fallback could not be followed literally because `GPT-5.3-Codex-Spark` and the exact `gpt-5.4` identifiers were unavailable in the active toolset. The closest permitted inherited specialist roles were used. This process fallback does not weaken or waive any hard gate.
- DEC-0050 now bounds repeated denied writes into tenant-qualified 5/15/60-minute buckets with exact first/final immutable evidence, fixed closed dimensions, one-way finalization, bounded redacted persistence-failure logging, and PostgreSQL deletion/tamper guards. Authentication admission now uses fixed-cardinality PostgreSQL windows, a database-authoritative ACTIVE/PAUSED key-policy generation, domain-separated signed one-time reservations, canonical locks, success-audit coupling, a bounded Argon2 work gate, exact-source Caddy limits, and aggregate-only monitoring. Fresh PostgreSQL 17 evidence applies all 122 migrations and passes denial 7/7, throttle 9/9, authentication 14/14, approval backfill 3/3, append-only 17/17, route authorization 3/3, and an actual replication-role approval immutability probe.
- The normalized Approval Inbox implementation is server-paginated, resolves direct and active-role eligibility from live permission/scope/resource state, excludes prohibited actors and self-approval, creates at most one notification for a direct-user assignment, creates zero per-user notifications for role-scoped activation, and removes passive Returned/Audit tabs. It remains behind `APPROVAL_ROUTING_V1_ENABLED=false`. A fresh database with all 122 migrations and deterministic seed passes a serial combined 13/13 PostgreSQL matrix: all 18 registry types dry-run without writes, apply exact permission/due/scope/prohibited-actor descriptors, rerun idempotently, retain exactly one backfill audit, and hydrate authorized details with the expected kind, reference, location/company wording, requester, and status. Representative data-service cases prove direct/role eligibility, notification-independent role eligibility, `ANY` and multi-location `ALL`, prohibited actors, live permission/role/assignment/effective-date/scope revocation, exact pagination/count, and inclusive due cutoff. This does not make role-scoped work visibly discoverable while the flag is off: the public page service fails closed when disabled and exposes no legacy or parallel Inbox. It rejects incomplete current-company routing, matches the lower-level eligible page only after readiness, and ignores an unrelated tenant's blocker. The shared-database files run serially because the production coordinator lock intentionally excludes concurrent backfill workers; the lock behavior itself remains directly tested. Targeted lint, root typecheck, and 37/37 routing unit tests pass. Independent Database and QA review returned GO to commit this feature-disabled evidence slice after the helper-generated ID type contract was corrected.
- An independent Approvals workspace UX review confirms that the normalized path already has URL-backed tabs, server pagination, and one decision composer, while the required flag-off posture still falls back to the unbounded legacy `listPendingApprovals()` plus client slicing. No passive Returned/History/Audit tabs or second approval authority will be added. The workspace remains not production-ready until normalized-routing cutover readiness, bounded eligibility, and the exact candidate PostgreSQL/authorization/browser gates are accepted. The requested Code Spark/GPT-5.4 identifiers were unavailable; the closest permitted fallback specialist was used without relaxing any hard gate.
- The approval detail surface now explicitly labels comments as read-only for non-Purchase Request approval types and explains the authoritative source workspace, and it renders a truthful empty state when no audit events are recorded. This closes the minor visible-surface ambiguity without adding a second comment authority or changing approval semantics. Web typecheck and lint pass; normalized cutover, disposable PostgreSQL, responsive browser, and hosted gates remain open.
- Normalized inbox hydration now handles a revoked or concurrently changed row through a user-safe `APPROVAL_AUTHORITY_STALE` feedback redirect and re-render instead of throwing a generic error boundary. The detail/action service remains independently authorized; the fallback legacy list remains explicitly unbounded and is not production-ready. Focused approval coverage passes 42/42, web typecheck and lint pass; normalized cutover, disposable PostgreSQL, responsive browser, and hosted gates remain open.
- The isolated production-build retry for the latest approval candidate did not launch because the Windows/WSL command bridge failed with `UtilBindVsockAnyPort`; no build credit is claimed for this attempt. The prior successful candidate build remains historical evidence only, and a fresh exact-SHA build is still required.
- The workspace audit now reflects the current Approval surface accurately: severity is reduced from Critical to High because fake/passive tabs and repeated decision-form concerns are remediated; the unbounded flag-off compatibility queue remains the explicit High production-readiness blocker.
- The action-runtime follow-up separates exhaustive operator cutover inspection from the tenant/company structural guard used on the caller's existing transaction, removing the nested coordinator transaction that could self-block behind action row locks. The steady-state guard now runs before mutation and not inside intentional multi-step transition state. The shared action engine deterministically anchors a role-assigned next step to one eligible actor, locks that actor with the current actor in canonical UUID order, and revalidates the exact anchor after approval locks. A fresh all-122-migration database passes the combined 23/23 matrix: focused malformed/inactive source failures with atomic no-write assertions; inbox cases; permission/role/scope revocation with zero mutation; approve-versus-reject, approve-versus-return, and competing-approver exactly-once outcomes; both action/operator-dry-run start orders; and a coherent two-step advance to a distinct eligible next-role actor with one activation audit and no premature outcome notification. Routing/approval units pass 38/38 and 41/41, the authorization manifest accepts the new reviewed internal guard, and lint/typecheck/production build pass. Independent review accepts only this feature-disabled shared-engine checkpoint and keeps activation at NO-GO: ten specialized finance/workforce handlers and Finance Close still require the same canonical acting-session/authority/next-anchor preflight, forced revocation races, scoped compare-and-set transitions, and the heterogeneous all-18 action matrix.
- The canonical action preflight now covers the ten specialized finance/workforce handlers and Finance Close as well as the shared seven families. It uses explicit approval-instance/current-step/next-step lock ordering, tenant/company-wide structural readiness, live actor/session/permission/scope/prohibited-actor revalidation, deterministic next-role anchoring, scoped compare-and-set transitions, and Finance Close source-version/pending-action revalidation. Finance Close sensitive requests serialize on the close-run row, and exported lock/reopen services now request approval rather than directly changing the accounting period. Corrective migration `20260722200000_approval_routing_step_order_guard` (SHA-256 `792c04eee347c96cc5a1d4c2e9422ee1ccbabd5f659b6baf51d787af2ef2773b`) adds the readiness index and an `ENABLE ALWAYS` v1 step-order guard. A fresh seeded 123-migration PostgreSQL database passes 25/25 combined routing cases, including exact SQLSTATE `55000`, unchanged-data, trigger-mode, and index evidence. Full lint, typecheck, 929 web tests, 27 database-package tests, one worker test, production build, 20/20 authorization-manifest tests, release-tool self-test, secret review, release-mode migration inventory, and `git diff --check` pass. Independent Database review returned GO to commit this disabled checkpoint. Security and Correctness keep activation at NO-GO because live source-workspace decision paths still bypass canonical approval actions, terminal future-step cleanup lacks exact invariant enforcement, AuthSession/forced-race breadth is incomplete, and the public action suite still executes only Purchase Request rather than all 18 families.
- The next feature-disabled approval-integrity checkpoint converges source-workspace decisions on strict family commands, shared terminal/cancellation primitives, live family-permission detail checks, and atomic specialized source outcomes while retaining `APPROVAL_ROUTING_V1_ENABLED=false`. Petty Cash request approval now exposes no amount override and rejects any explicit amount differing from the current proposal in both modes under the reconciled `DEC-0051`/`DEC-0052` policy hold. Canonical Payment Request approval fails closed pending its Finance policy matrix, while the flag-off legacy path avoids the unresolved snapshot-capacity rule. Migration `20260722210000_approval_integrity_petty_cash_intents` (SHA-256 `926779eba192e1d6e113eda5691294bc8bef93ed6d606734ef6b61576c3b723f`) adds pending-instance uniqueness and rollback-compatible typed Petty Cash proposal/intent schema; `20260722220000_purchase_order_amendment_pending_status` (`e715b261a9eb5983d6ae1132b7d09e3a6c7a6d4e117c6b20b00c31674f54ac3d`) corrects the PO status constraint; and `20260722230000_budget_revision_atomic_step_activation` (`c1caa9dcd936f633b8196c3cc9bf1cace48e6a358b70a7d60c99a00ea09353af`) makes Budget first-step `dueAt` activation atomic. Hostinger reconciliation limits the intent table to runtime `SELECT`/`INSERT`, and verification binds exact function bodies and both exact `ENABLE ALWAYS` trigger OIDs. A fresh PostgreSQL 17 disposable lifecycle applies all 126 migrations and passes 17/17 append-only checks plus 172/172 executable approval cases; Petty Cash and Payment Request approve remain two explicit policy TODOs. Independent Database and Security review return GO only for a source-control checkpoint with the flag disabled; Correctness and QA keep activation at NO-GO. Full unchanged-tree repository/release gates remain required before this checkpoint is committed.
- Authorization hardening for this checkpoint standardizes authentication mutation locks as `User` then `AuthSession` then `MfaAuthenticator`, moves controlled-evidence source authorization ahead of metadata/binary work, and verifies DEC-0050 bounded denial effects across access, administration, finance, procurement/inventory, projects/operations, workforce, protected routes, and evidence. Operator throttle-control transitions use a maximum of three retries only for reviewed transient `P2034`, `P2028`, `40001`, and `40P01` conditions and never retry the explicit generation-conflict compare-and-set result. The real PostgreSQL PAUSE/reservation probe now repeats 25 races. Standalone denial recording/finalization uses a bounded 10-second acquisition/execution budget so the 24-way same-bucket exact-count contract does not inherit Prisma's two-second acquisition default; focused PostgreSQL evidence passes 7/7 and awaits every concurrent outcome. Hosted same-key hostile-burst, connection-starvation, and unrelated-query latency evidence remains mandatory before production activation.
- The final post-correction unchanged working-tree gate passed 1,052 web unit/contract tests with two documented approval-policy TODOs, lint, typecheck, production build, all 126 migrations, the 25-race throttle control probe, runtime throttle rollback, 17/17 append-only checks, seed repeatability, eight adversarial role-contract repair cases, and 159/159 database-backed authorization cases. The final denial suite included a passing 24-way same-bucket exact-count race. A separate evidence-broker capacity regression was made deterministic without changing production storage: it now calibrates its midpoint watermark only after the first reservation is held and always releases and awaits the gated writer. Three simultaneous focused storage suites and the full parallel web suite passed. This local evidence does not replace exact-commit CI or hosted Hostinger load/recovery acceptance.
- Commit-blocking review corrections now preserve flag-off Payment Request approval while normalized Payment approval remains policy-blocked. Decision-time text is stored only as explicitly named supplemental audit context: it cannot satisfy the Expense, Cash Advance, or Petty Cash authoritative source-evidence gate and cannot overwrite source evidence on approve, return, or reject. A fresh 126-migration PostgreSQL run passes 175/175 approval cases with two normalized-policy TODOs, including atomic rollback when Expense or Cash Advance has no source evidence and only decision text, source-evidence non-overwrite assertions, intermediate supplemental-audit semantics, and an executable flag-off Payment approval that atomically commits the source, approval, audit, and requester notification after the paired normalized rejection leaves the fixture unchanged. Focused unit coverage passes 71/71. Activation follow-up remains mandatory: execute and accept the implemented Petty Cash intent/proposal-CAS PostgreSQL matrix; transactionally qualify controlled attachments under `DEC-0047`; lock and revalidate each remaining exact source record/version inside canonical decisions; execute and accept the five-path initial-procurement parity matrix including the three authored `DEC-0078` races; provide an approved visible role-scoped work surface through controlled normalized-Inbox activation; and complete Payment Request approve/return/reject policy and PostgreSQL parity. Finance workspace completion also remains blocked by visible Petty Cash gaps recorded by Dunong: fund setup has no activation action, returned requests have no correction/resubmission action, and returned liquidations have no in-place correction/resubmission action.
- Budget Revision lifecycle follow-up is complete as a feature-disabled checkpoint: normalized `SUBMITTED` cancellation now requires and atomically terminates the genuine all-`WAITING` route; review activation, cancellation, approve, and reject share the deterministic authority → approval graph → source lock order; and fresh 126-migration PostgreSQL evidence passes 22/22 cancellation cases, including flag-off start-review/cancellation preservation, both cancellation/start-review and cancellation/first-step-approve orders, a forced pre-lock first-step-anchor revocation, source-only stale-snapshot rollback, and idempotent post-advance review retry. Full repository tests pass (1,070 web, 27 database, one worker); lint, typecheck, build, release-tool self-test, secret review, and 126-migration review pass. Authorization manifest/boundary baselines were updated for the reviewed lifecycle helpers, while the release authorization evidence correctly fails closed without a current exact-candidate authorization-test attestation. `APPROVAL_ROUTING_V1_ENABLED` remains `false`; activation still requires terminal final-step decision-versus-cancellation and start-review-versus-approval barrier races, actor/session/permission/scope revocation barriers, real-submit end-to-end coverage, all-family parity, unresolved finance policy, evidence qualification, cutover/drain, current-candidate authorization/E2E evidence, and hosted release gates.
- `DEC-0073` is implemented as a bounded, feature-disabled Expense Request and Cash Advance Request approval-integrity checkpoint. Canonical decisions re-lock the exact approval graph and tenant/company source row, validate actionable status, approval linkage, expected version, scope, and prohibited actors from the locked source, and use source-version/linkage compare-and-swap for terminal approve, return, and reject. Intermediate approval holds the same source lock through canonical step advance. Expense approval-material lines are locked in deterministic ID order and their identity, tenant/company ownership, and `updatedAt` snapshot are revalidated; locked-source evidence, budget, scope, and audit values replace stale pre-transaction values. PostgreSQL specifications now cover edit-first and decision-first ordering across approve/return/reject, two-step intermediate approval, source/linkage mismatch, Expense child-line drift and malformed scope, otherwise-authorized requester/beneficiary denials, authoritative evidence, atomic rollback, and exact audit/notification cardinality. These new PostgreSQL specifications are **implemented but locally unexecuted** because `DISPOSABLE_DATABASE_ADMIN_URL` is unavailable and therefore are not production evidence. Web lint, typecheck, the isolated-output production build, 45 focused approval/Expense tests, the approval-parity non-PostgreSQL run (59 passed; 78 PostgreSQL skipped; two existing policy TODOs), and the full non-database web suite (1,233 passed; 168 skipped; two TODOs across 117 passed/seven skipped files) pass; the root suite also passes 27 database-package tests and one worker test, and a broader worker-focused run passed 55/55. Independent Architecture and Database corrected re-reviews report no remaining Critical/High finding, and Security accepted the corrections after initially identifying the same two High issues; all return GO only for this disabled source-control checkpoint and activation remains **NO-GO** pending executable PostgreSQL and live-revocation evidence, all-family source-lock/action parity, Petty Cash/Payment and evidence policy, notification parity, cutover/drain, exact-candidate authorization/E2E, and hosted release/recovery gates. Dunong assessed no knowledge-base, glossary, training, or user-facing release-note change because the flag remains false and visible behavior/policy is unchanged.
- `DEC-0074` is implemented as a feature-disabled shared next-step notification parity checkpoint. Purchase Request, Quotation Recommendation, Purchase Order, Purchase Order Balance Closure, Purchase Order Amendment, Wastage Report, and Stock Adjustment now use `recordApprovalStepReadyNotification` inside the same transaction as step decision, activation, and audit. Direct-user work uses the deterministic `approval:<instance>:step:<order>:ready` key and exact step/recipient/entity metadata; closed optional routing context preserves assigned role, required permission, and location-context scope without overriding helper-owned core fields. Role-scoped activation calls no personal writer and creates zero role-member rows. The PostgreSQL specifications cover all seven direct/role variants, retry/cardinality, the corrected Quotation-to-related-Purchase-Request entity mapping, representative flag-off Purchase Order behavior, a concurrent same-step one-winner/one-row outcome, and atomic rollback under a runtime-permitted notification-write failure. They are **implemented but locally unexecuted** because `DISPOSABLE_DATABASE_ADMIN_URL` is unavailable and are not production evidence; the command fails only with `DISPOSABLE_DATABASE_ADMIN_URL_REQUIRED`. Focused validation passes 112 tests with 95 PostgreSQL cases skipped and two existing policy TODOs. The unchanged-candidate root suite passes 1,233 web tests with 185 skipped and two TODOs across 117 passed/seven skipped files, 27 database-package tests with 18 skipped, and one worker test; root typecheck, web lint, and the isolated-`NEXT_DIST_DIR` production build pass. Final Architecture review reports no Critical/High finding and GO only for this disabled source-control checkpoint; Security reports no introduced Critical/High but requires executable PostgreSQL before accepting the checkpoint as behavioral evidence. The later five-path initial-procurement source change is implemented under `DEC-0050` and `DEC-0078`, with three exact source-serialization specifications authored but unexecuted. Activation remains **NO-GO** pending executable PostgreSQL, visible role-scoped Inbox activation, broader live-revocation/prohibited-actor and all-family parity, unresolved policy, cutover/drain, exact-candidate authorization/E2E, and hosted release/recovery gates. Dunong assessed no knowledge-base, glossary, training, or user-facing release-note change while the flag remains false and visible behavior is unchanged.
- `DEC-0075` is implemented as PostgreSQL specifications for the next feature-disabled authorization-evidence checkpoint. The capability-derived prohibited matrix covers exactly 49 preflight-executable commands (17 approve, 14 return, 18 reject), excluding only Payment Request approve because its policy hold rejects before prohibited-actor preflight. Petty Cash no-amount approve, Petty Cash return/reject, and Payment Request return/reject are negative authority tests only and do not resolve positive Petty Cash intent/source-lock/amount or Payment approval policy. Each case establishes otherwise-eligible baseline metadata with no prior prohibition, persists and snapshots the active-step prohibition, expects `APPROVAL_AUTHORITY_STALE`, proves the setup prohibition remains unchanged, and asserts no decision-side source, approval, audit, notification, or broad family-collateral write. A separate 12-spec revocation suite covers acting User, two real AuthSession changes, compositional permission fencing tied to the registered production `updateRolePermissions` privileged-MFA concurrency gate, actual production role/scope deactivation commands with privilege-epoch/session/audit effects, direct next-recipient loss, an Expense specialized-adapter smoke, and decision-first actor/recipient/session outcomes. Query-aware `User`/`AuthSession`/`ApprovalInstance` observers verify exact PostgreSQL PID blocking without sleeps or production hooks; all new PostgreSQL specs have explicit bounded timeouts. Focused local execution passes 60 tests with 156 PostgreSQL cases skipped and two existing policy TODOs; the full candidate passes 1,234 web tests with 246 skipped and two TODOs, 27 database-package tests with 18 skipped, one worker test, focused lint, root typecheck, production build, and `git diff --check`. Both exact disposable commands fail only with `DISPOSABLE_DATABASE_ADMIN_URL_REQUIRED`, so no behavioral or production evidence is claimed. Final Architecture, Security, and QA re-reviews report no Critical/High finding and GO only for this feature-disabled source checkpoint; Dunong found no glossary, knowledge-base, training, release-note, or new knowledge-gap change because visible behavior and policy remain unchanged. The flag remains false and behavioral acceptance/activation are **NO-GO** pending both PostgreSQL suites on the same candidate, remaining positive policies/source locks, cutover, exact-candidate, and hosted gates.
- `DEC-0076` implements the feature-disabled normalized Petty Cash immutable-step-intent and proposal-CAS checkpoint. Petty Cash alone opts into canonical `FULL_GRAPH` preflight: the transaction locks/reuses the parent and all steps in deterministic `(stepOrder, id)` order, locks current-step scope metadata and the exact active fund/location-backed source, reconciles source scope inside the transaction, and skips terminal future steps from that locked graph without a second re-lock. Every acted current-step approve, return, or reject appends exactly one immutable intent with unchanged six-decimal amounts, typed action, normalized reason/supplemental context, stable server-derived SHA-256 hash/idempotency key, and consecutive proposal versions. An exact Decimal source/proposal-version compare-and-swap increments once; terminal actions clear the proposal but retain the new version, while skipped or cancelled steps receive no intent. Intent, source, graph, audit, notification, and bounded no-financial-effect state are atomic; there is no amount authority, Payment Request, bank, journal, fund, ledger, disbursement, settlement, inventory, purchasing, or receiving effect. Fifteen PostgreSQL specifications are authored/discovered but unexecuted because the exact database command fails only with `DISPOSABLE_DATABASE_ADMIN_URL_REQUIRED`; focused execution passes 70 tests with 159 skipped and one unrelated TODO, and the full root candidate passes 1,244 web tests with 261 skipped/one TODO, 27 database tests with 18 skipped, one worker test, lint, typecheck, production build, and `git diff --check`. Architecture's initial three High findings were corrected; final Architecture, Data, QA, and Security reviews report no remaining Critical/High finding and GO only for the feature-disabled checkpoint. Requested Spark and GPT-5.4 specialists were unavailable, so the closest permitted GPT-5.6 fallback was used without relaxing gates. Dunong assessed no visible glossary, KB, release-note, or training behavior change while the flag is false; its existing Petty Cash article must only correct the now-stale statement that the writer is pending. PostgreSQL behavioral acceptance and activation remain **NO-GO**.
- Knowledge-base and glossary assessment: the Petty Cash user article was added for the implemented feature-disabled workspace behavior and continues to label activation/correction gaps. The glossary describes the normalized direct-user versus role-scoped notification target. The initial-procurement source behavior is implemented for all five paths, but enablement must state that its PostgreSQL race specifications remain unexecuted and role-scoped work has no complete actionable queue while the normalized Inbox is disabled; the manual due/overdue reminder scan is partial and non-actionable in that state.
- Final candidate validation passes lint, typecheck, production build, 1,052 web tests, 27 database-package tests, one worker test, all 126 disposable PostgreSQL migrations, 17/17 append-only checks, 175/175 executable approval cases with two normalized-policy TODOs, the 25-race throttle probe and runtime rollback, deterministic seed, eight adversarial role-contract cases, 159/159 database-backed authorization cases, release-tool self-tests, secret review, migration inventory, and `git diff --check`. The release authorization manifest correctly fails closed with `AUTHORIZATION_TEST_ATTESTATION_INVALID:e2e` because production-mode E2E evidence has not been regenerated for the exact candidate SHA; the older attestation is not accepted as current evidence. Independent Database, Security, and QA review permit commit and push only as a feature-disabled source-control checkpoint and report no remaining commit blocker. Normalized-routing activation and production promotion remain NO-GO pending the recorded implementation and exact-candidate hosted gates.
- SPF-006 remains open and Hostinger production activation is **NO-GO**. The exact hosted release must execute the disposable lifecycle and demo-reset safety contract, prove owner/migrator/runtime effective privileges and escalation denials from the real credentials, validate root-owned systemd credential and health-token isolation plus private database/network publication, preserve populated protected-table and report/export evidence through migration, prove idempotent redeploy and role reconciliation after an isolated restore, measure restore and recovery against approved RPO/RTO, calibrate Argon2/edge/pressure thresholds under staged hostile load, prove alert delivery/acknowledgement/escalation and Caddy image provenance/SBOM/scan/signature, and receive Security/QA/DevOps/Release acceptance. Approval activation still requires the shared canonical pre-mutation authority/session/next-actor contract across the ten specialized handlers and Finance Close; all-18 approve/return/reject and prohibited-actor breadth; forced acting/next-actor revocation races; deterministic operator/action barriers; activation notification/audit retry behavior; remaining pagination/due guards and provenance/source no-mutation assertions; usable action destinations; production-volume query-plan evidence; and production-mode desktop/mobile browser evidence. Remaining request/reversal and audit-semantic gaps, broader service-to-service races, the receiving reference allocator gap, and the cross-workspace audit/action matrix also remain required before SPF-006 completion.

## 4. Workspace Completion Sequence

After the shared baseline is stable, complete workspaces in this dependency order:

| Order | Workspace | Key completion focus | Status |
|---:|---|---|---|
| 1 | Overview and application shell | Default landing, navigation, scope context, role-aware summaries, responsive behavior, and safe drilldowns | In progress; `DEC-0053` action-first queue preview, explicit queue metadata, shell-control integrity, dashboard loading/error handling, bounded scoped source reads, `DEC-0054` feature-disabled Approval Inbox posture, closed Open PO/PR/Transfer/Adjustment/Wastage/Receiving Follow-up/Ledger Variance/Positive Stock drilldowns (`DEC-0055`–`DEC-0059`, `DEC-0069`, `DEC-0070`, `DEC-0231`), `DEC-0226` Branch Operations, `DEC-0227` Food Safety Reviews/Exceptions, `DEC-0228` Incident, and `DEC-0229` Maintenance profile destinations, mobile shell, and signed/paginated `My Tasks` coverage through controlled Stock Count, Branch Operations, Food Safety, Incident, and Maintenance adapters are implemented locally. `DEC-0066` adds the v2 priority/due/age cursor needed for risk- and due-aware sources. `DEC-0060` server-side blind-count redaction and reviewer-only Count Variance access are implemented; Count Variance remains inactive pending the `DEC-0061` immutable recount-attempt decision and its two open recovery/cutoff policies. `DEC-0071` implements the safe Food Cost fallback: no unresolved Overview projection or new exception reminder, with the source workspace retained. `DEC-0072` adds truthful per-source observation/partial states and bounded technical deadlines without inventing a freshness SLA, and `DEC-0230` applies the same bounded-settlement, retained-admission, and redacted-telemetry safety properties to My Tasks. Normalized approval cutover, Food Cost source-definition correction/reactivation, remaining browser/staging evidence, representative PostgreSQL reconciliation-plan/concurrency proof, hosted load/cancellation/recovery gates, and UAT remain pending |
| 2 | Administration | Companies, brands, locations, users, roles, scopes, policies, approval rules, security controls, and audit access | In progress; bounded registries/details and the `DEC-0225` company Approval Rule lifecycle are implemented, while disposable-PostgreSQL, responsive-browser, hosted deployment/recovery, UAT, and remaining visible-surface gates stay open |
| 3 | Master data | Suppliers, items, categories, units, locations, eligibility/deactivation, duplicate controls, and scoped access | Pending production-readiness review |
| 4 | Approval engine and inbox | Configurable routes, thresholds, pending steps, no self-approval, return/reject, escalation visibility, and audit | Pending production-readiness review |
| 5 | Purchase Requests | Create/edit lines, warehouse-first replenishment decision, submission, approval, correction/cancellation, evidence, and exports | Pending production-readiness review |
| 6 | Quotations | Quote capture, comparison, minimum quote policy, recommendation, exception justification, approval, and evidence | Pending production-readiness review |
| 7 | Purchase Orders | Controlled conversion, issuance, approval, cancellation, outstanding quantities, supplier/location scope, and exports | Pending production-readiness review |
| 8 | Receiving | Ordered/delivered/accepted/rejected/damaged/short/outstanding quantities, discrepancies, partial receipts, posting, reversal, evidence, and PO integrity | Pending production-readiness review |
| 9 | Inventory and immutable ledger | Exact-once movements, derived balances, location scope, lot/expiry behavior, drilldown, reconciliation, and exports | Pending production-readiness review |
| 10 | Transfers | Request, approval where required, dispatch, receipt confirmation, discrepancies, idempotency, reversal policy, and no duplicate stock | Pending production-readiness review |
| 11 | Stock Counts | Freeze/snapshot rules, entries, review, variance, generated adjustment, concurrency, and audit | Pending production-readiness review |
| 12 | Wastage | Reason, evidence policy, approval, posting, reversal, immutable movement, and reporting | Pending production-readiness review |
| 13 | Stock Adjustments | Reason/evidence, approval, separate posting, full reversal, concurrency, and audit | Pending production-readiness review |
| 14 | Reports, exports, notifications, and audit | Scope-safe filters, source links, trust notices, export metadata/audit, in-app notification behavior, and pagination | Pending production-readiness review |
| 15 | Projects & Implementation Tracker | Visibility, membership, tasks, blockers, evidence, requirements, milestones, risks, linked-record redaction, activity, and mobile completion | Pending production-readiness review |

### Workspace 2 Administration implementation checkpoint — July 24, 2026

- `DEC-0108` selects a bounded Users-registry pilot as the first Administration slice. Core Administration now applies server-owned name/email and status predicates, exact count/page parity, deterministic `displayName ASC, id ASC` ordering, bounded URL-backed pagination, and an explicit pre-query restricted state when `core.tenant_role_administer` is absent. The existing service guard and DEC-0043 target-company rules are unchanged; roles, organization, audit, request, permission, and detail pagination remain separate follow-up slices.
- Focused Core Administration coverage passes 15/15 after the pilot source/visible-surface assertions; web typecheck and lint pass. The full web regression passes 1,291 tests across 120 files (301 skipped, 1 todo), the authorization manifest passes 20/20 after recording the new protected service surface, and the isolated production build passes. Responsive browser, disposable PostgreSQL boundary/count parity, and hosted gates remain open. Glossary, Users/Roles UI specification, administrator guide, release note, formal decision record, and decision index are aligned.

### DEC-0110 Administration authorization/read-boundary hardening checkpoint — July 24, 2026

- Before further role or organization pagination, Administration now requires both `core.tenant_role_administer` and active selected-company `MANAGE` scope for overview and Users-registry reads. Company creation requires tenant-role authority before duplicate lookup or mutation. Overview organization reads are constrained to the selected company, with tenant-wide approval rules retained where applicable.
- User detail now verifies active selected-company membership before loading the target record and returns a non-enumerating not-found result when membership is absent. Cross-company scope assignments are redacted from the selected-company detail response. These changes preserve tenant-global role semantics and existing sensitive-role/MFA/audit controls; role-library pagination remains the next deferred slice.
- Focused Core Administration coverage passes 16/16 and web typecheck passes. Responsive browser, disposable PostgreSQL authorization/no-mutation, full regression, production build, and hosted gates remain open. Users/Roles UI specification, administrator knowledge-base guidance, glossary, release note, formal decision record, and decision index are aligned.

### DEC-0111 Role Library pagination implementation checkpoint — July 24, 2026

- The Role Library now uses a server-owned bounded page contract: name/code query and status filters are URL-backed, counts and rows share the same tenant predicate, ordering is deterministic by `name ASC, id ASC`, and each row returns an explicit permission count plus a three-permission preview. Role detail now repeats the tenant-role and selected-company Manage guard with a direct-route restricted state. User onboarding uses a separate active-role option catalog capped at 100 and discloses when the full library is larger.
- Focused Core Administration coverage passes 17/17; full web regression passes 1,293 tests across 120 files (301 skipped, 1 todo); web lint and typecheck pass; authorization manifest passes 20/20 after registering the new protected role-page service; and the isolated production build passes. Responsive browser, disposable PostgreSQL authorization/query-parity, export/detail-volume, and hosted gates remain open. Users/Roles specification, administrator guide, glossary, release note, formal decision record, and decision index are aligned.

### DEC-0110 direct-route boundary remediation checkpoint — July 24, 2026

- Administration detail services and direct pages now require both `core.tenant_role_administer` and selected-company `MANAGE` before loading company, location, permission, audit-event, or user-detail data. Missing authority redirects to the explicit restricted Administration state; user-detail denial remains non-enumerating. Core Admin audit export authorization now requires the same tenant-role boundary.
- Focused Core Administration coverage remains 17/17 after the correction; web typecheck and lint pass. Full regression, production build, responsive browser, disposable PostgreSQL no-query authorization, audit redaction/pagination, and hosted gates remain open. Audit and organization registries remain pending independently reviewed bounded contracts; no phase or workspace completion is claimed.

### DEC-0113 Audit Trail redaction and bounded pagination decision — July 24, 2026

- Parent-led deliberation with independent Architecture and Security review selected Audit Trail redaction plus deterministic keyset pagination as the next Administration slice. The current raw audit detail projection and silent `take: 500` list/export cap are treated as confidentiality and evidence-integrity blockers; Organization Scope pagination is deferred until this contract is implemented and gated.
- The implementation must use one server-owned tenant/company/filter resolver for list, count, detail, and export; require tenant-role authority plus selected-company `MANAGE` before queries or export-start writes; use `occurredAt DESC, id DESC` cursor ordering; bound filters/page size; recursively redact policy-sensitive values without mutating immutable rows; and prove export parity. No audit or workspace completion is claimed until these controls and responsive visible-surface tests pass.

### DEC-0113 Audit Trail implementation checkpoint — July 24, 2026

- Audit list/detail/export now share the authorized tenant/company filter resolver. The registry uses bounded cursor pages (default 25, maximum 100), exact matching totals, filter-bound cursors, and deterministic `occurredAt DESC, id DESC` ordering. Legacy callers iterate bounded pages rather than using a silent `take: 500` cap.
- Audit detail and CSV output use projection redaction for actor contact/IP and sensitive nested credential, token, email, storage-key, and URL fields. Stored immutable JSON is not mutated. Direct export performs selected-company Manage preflight before recording export-start activity and omits contact/IP columns.
- Focused Core Admin tests pass 18/18; report tests pass 11/11; authorization manifest passes 20/20; web typecheck and lint pass; full regression and isolated production build remain the final gates for this checkpoint, along with responsive browser and disposable PostgreSQL export/no-query evidence.

### DEC-0114 Organization Scope Locations pagination decision — July 24, 2026

- Independent Architecture and Security reviews selected a Locations-first registry as the next Administration slice. It will use selected-company server predicates, bounded query/status/type/brand filters, deterministic `name ASC, id ASC` ordering, exact count/page parity, and separate bounded active option catalogs for dependent forms.
- Company, Brand, Department, and broader Organization Scope completion remain deferred. The visible Organization tab must retain truthful pending/read-only wording until its remaining registries have their own contracts and production evidence.

### DEC-0114 Organization Scope Locations implementation checkpoint — July 24, 2026

- The Administration overview now requests a bounded selected-company Locations page rather than an unbounded location list. Filters cover name/code/brand search, status, and type; rows use explicit safe projections and deterministic `name ASC, id ASC` ordering with exact totals. Initial-location onboarding uses a separate active-only catalog capped at 100 with overflow disclosure.
- The Organization surface labels Company, Brand, and Department registries as separate pending contracts; Locations alone is not treated as completion of the Organization workspace. Focused Core Admin coverage passes 19/19; full web regression passes 1,295 tests with 301 skipped and one TODO; authorization manifest passes 20/20; web typecheck, lint, and isolated production build pass. Responsive browser, disposable PostgreSQL authorization/count parity, selector overflow, and hosted recovery gates remain open.

### DEC-0115 Organization Scope Brands pagination decision — July 24, 2026

- Independent Architecture and Security reviews selected Brands-first as the next registry because Brand is the direct Company → Brand → Location dependency. The slice will use selected-company server predicates, bounded name/code/status filters, deterministic ordering, exact totals, and a separate active Brand option catalog.
- Company remains a single read-only selected-company summary. Departments remain deferred for their own finance/workforce dependency-count contract; the Organization workspace must not be called complete while Departments remain pending.

### DEC-0115 Organization Scope Brands implementation checkpoint — July 24, 2026

- The overview now delegates Brands to a bounded selected-company page contract instead of an unbounded brand list. Name/code and status filters, exact totals, deterministic `name ASC, id ASC` ordering, and a separate active-brand option catalog capped at 100 are implemented. Location creation no longer treats paginated rows as selector authority.
- Focused Core Admin coverage passes 20/20; web typecheck and lint pass. Full regression, authorization manifest, isolated production build, responsive browser, disposable PostgreSQL count/authorization, selector overflow, and hosted recovery gates remain open for this checkpoint. Departments remain pending and Organization Scope is not complete.

### DEC-0116 Organization Scope Departments pagination decision — July 24, 2026

- Accepted selected-company, server-paginated Departments registry with bounded name/code search, status filter, exact totals, deterministic `name ASC, id ASC`, and pre-query Core Admin + tenant-role + selected-company `MANAGE` authorization.
- Retain the three dependency counts already promised by the visible Department cards (budgets, budget lines, cost centers) as selected-company-scoped read-only summaries; defer employee-assignment count until a visible consumer and disclosure contract exist. No Department option catalog is needed yet.
- Decision record: `docs/core/00-governance/decisions/DEC-0116-ADMIN-DEPARTMENTS-REGISTRY-PAGINATION.md`. Implementation is the next in-scope slice; Organization Scope remains incomplete until Department surfaces and applicable production gates pass.

### DEC-0116 Organization Scope Departments implementation checkpoint — July 24, 2026

- Replaced the unbounded Department overview query with a selected-company, server-paginated registry. Search/status inputs are bounded; totals and active counts share the tenant/company predicate; rows sort by `name ASC, id ASC` and expose only identity/scope/status plus the approved budget, budget-line, and cost-center related-record counts. Employee-assignment count remains deferred.
- Organization UI now provides Department search, status filtering, empty-state copy, responsive record summaries, and pagination while Company remains a single selected-company summary. The visible Organization Scope header no longer describes Brand/Department registries as pending contracts.
- Core Admin focused coverage passes 21/21; full web regression passes 1,297 tests with 301 skipped and one TODO; authorization-manifest tests pass 20/20; web typecheck, lint, and isolated production build pass. Responsive browser, disposable PostgreSQL authorization/count/query-plan evidence, and hosted recovery/deployment gates remain open. Organization Scope and Phase I remain in progress.

### DEC-0117 Administration Approval Rules registry pagination decision — July 24, 2026

- Accepted a bounded Approval Rules registry with transaction-type search, active/inactive filtering, exact totals, page sizes 10–100, and deterministic `isActive DESC, priority ASC, id ASC` ordering.
- Preserve tenant-wide plus selected-company visibility under the existing Core Admin, tenant-role, and selected-company Manage guards. Rows retain exact step count and an allowlisted first-three step/approver-type preview because the current visible cards promise that context; full steps and policy details remain on the independently authorized detail route.
- Decision record: `docs/core/00-governance/decisions/DEC-0117-ADMIN-APPROVAL-RULES-REGISTRY-PAGINATION.md`. Implementation is the next Administration slice; no Administration or Phase I completion is claimed.

### DEC-0117 Administration Approval Rules registry implementation checkpoint — July 24, 2026

- Replaced the eager Approval Rules overview query with a bounded selected-company plus tenant-wide page contract. Transaction-type search and active/inactive filters share the exact tenant/company-or-null predicate for counts and rows; ordering is deterministic by `isActive DESC, priority ASC, id ASC`.
- The visible registry retains exact step counts and only the first three ordered step/approver-type labels, disclosing additional steps and routing users to the independently authorized detail route. Full step payloads and mutation authority remain excluded from the registry.
- Core Admin focused coverage passes 22/22; full web regression passes 1,298 tests with 301 skipped and one TODO; authorization-manifest tests pass 20/20; web typecheck, lint, and isolated production build pass. Responsive browser, disposable PostgreSQL scope/query-plan evidence, and hosted deployment/recovery gates remain open. Administration and Phase I remain in progress.

### DEC-0118 Administration user-detail option-catalog decision — July 24, 2026

- Accepted separate capped catalogs for active selected-company locations and active unassigned tenant roles on user-detail assignment forms. Catalogs are capped at 100 with deterministic name/ID order and `hasMore` disclosure; referenced locations used by current assignments and controlled requests remain separately loaded for display.
- Existing service guards and mutation-time scope/role revalidation remain authoritative. No selector row, overflow flag, or client-supplied ID grants authority. Decision record: `docs/core/00-governance/decisions/DEC-0118-ADMIN-USER-DETAIL-OPTION-CATALOGS.md`.

### DEC-0118 Administration user-detail option-catalog implementation checkpoint — July 24, 2026

- User-detail reads now use bounded, searchable active-location and active-role catalogs (`take: 100`, deterministic name/ID ordering) with exact `hasMore` indicators. Locations are fixed to the selected company; roles retain tenant-global semantics and no longer serialize permission-code payloads into selector options. Existing scope/request references are loaded separately for history and display.
- The assignment UI provides server-side location/role refinement controls and explains when the first 100 results are not complete. Existing assignment and controlled-request mutation services continue to revalidate live target membership, scope, sensitive-role eligibility, MFA, segregation, and audit requirements.
- Core Admin focused coverage passes 23/23; full web regression passes 1,299 tests with 301 skipped and one TODO; authorization-manifest tests pass 20/20; web typecheck, lint, and isolated production build pass. Responsive browser, disposable PostgreSQL selector-scope/no-query evidence, and hosted deployment/recovery gates remain open. Administration and Phase I remain in progress.

### DEC-0119 Administration user request-history pagination decision — July 24, 2026

- Accepted separate bounded page contracts for Controlled Scope Requests and Controlled Role Requests. Both retain the target-user selected-company boundary, allowlisted lifecycle status filters, exact totals, and deterministic `createdAt DESC, id DESC` ordering; silent `take: 20` caps are removed.
- Existing row review context and action controls remain source-owned and independently reauthorized. Decision record: `docs/core/00-governance/decisions/DEC-0119-ADMIN-USER-REQUEST-HISTORY-PAGINATION.md`.

### DEC-0119 Administration user request-history pagination implementation checkpoint — July 24, 2026

- Controlled Scope Requests and Controlled Role Requests now use separate URL-backed status-filtered pages with exact totals and deterministic `createdAt DESC, id DESC` ordering. The prior silent `take: 20` cap is removed; target-user, tenant, selected-company, and no-self-review boundaries remain unchanged.
- User Detail shows `shown of total` pagination for both histories while preserving contextual review actions and lifecycle labels. No list-side mutation or authority expansion was introduced.
- Core Admin focused coverage passes 24/24; full web regression passes 1,300 tests with 301 skipped and one TODO; authorization-manifest tests pass 20/20; web typecheck, lint, and isolated production build pass. Responsive browser, disposable PostgreSQL count/scope/CAS evidence, and hosted deployment/recovery gates remain open. Administration and Phase I remain in progress.

### Administration shared visible-state checkpoint — July 24, 2026

- Added route-level Core Administration loading and retryable error surfaces so slow or failed overview reads are explicit and user-safe. This does not change authorization or record data and does not close the external responsive/hosted gates.
- Focused Core Admin coverage passes 25/25; full web regression passes 1,301 tests with 301 skipped and one TODO; web typecheck, lint, and isolated production build pass. Authorization manifest remains 20/20. Responsive browser, disposable PostgreSQL, hosted deployment/recovery, and UAT gates remain open.

### DEC-0120 Administration request-history redacted registry — July 24, 2026

- Historical approved/rejected request rows now expose safe lifecycle summaries plus reason/evidence-recorded indicators; pending rows retain review context required for active decisions. A separately authorized detail path remains required before historical narrative is re-exposed.
- Focused Core Admin coverage passes 26/26 and web typecheck passes. Responsive browser, disposable PostgreSQL, hosted deployment/recovery, and UAT gates remain open.

### DEC-0121 Administration high-risk scope review CAS — July 24, 2026

- High-risk scope approval/rejection now locks the target user, revalidates active/effective selected-company membership inside the transaction, and claims only a still-`PENDING` request before creating an assignment or terminal audit event.
- Focused Core Admin coverage passes 27/27; web typecheck and lint pass. Disposable PostgreSQL concurrency/revocation evidence, responsive browser, hosted deployment/recovery, and UAT gates remain open.

### DEC-0122 Administration sensitive-role review membership CAS — July 24, 2026

- Sensitive-role approval and rejection now lock the target user, revalidate active/effective selected-company membership inside the transaction, and claim only a still-pending request before role assignment or terminal audit posting.
- Focused Core Admin coverage passes 28/28; web typecheck and lint pass. Disposable PostgreSQL concurrency/revocation evidence, responsive browser, hosted deployment/recovery, and UAT gates remain open.

### DEC-0123 Administration audit bounded synchronous export — July 24, 2026

- Admin Audit CSV now requires a valid UTC date range, enforces configurable maximum date-span/row policies, applies a pre-start watermark, counts once, and returns truthful 400/413 errors without partial CSV output. Async delivery remains deferred until queue, private artifact, expiry, and recovery controls exist.
- Focused Core Admin coverage passes 29/29; web typecheck and lint pass. Disposable PostgreSQL volume/query-plan evidence, responsive browser, hosted deployment/recovery, and UAT gates remain open.

### DEC-0124 Administration Reason Codes registry pagination — July 24, 2026

- Reason Codes now use a selected-company, server-paginated/filterable registry with exact counts, deterministic ordering, bounded URL inputs, and a scoped read-only selected-code detail surface. Create and active-to-inactive deactivation remain the only lifecycle actions.
- Focused Reason Codes coverage passes 1/1 source-contract test; web typecheck and lint pass. Responsive browser, disposable PostgreSQL authorization/count/query-plan evidence, hosted deployment/recovery, and UAT gates remain open.

### DEC-0125 Administration Policy Settings registry pagination — July 24, 2026

- Admin Settings now provides server-owned category/search/page inputs, bounded visible policy rows, exact matching totals/category counts, and global override/default summaries. Existing Configure and Use Recommended actions remain audited and server-authorized.
- Focused policy-settings coverage passes 6/6; web typecheck and lint pass. Responsive browser, disposable PostgreSQL authorization/query-plan evidence, selected-record drawer hardening, hosted deployment/recovery, and UAT gates remain open.

### DEC-0126 Administration Readiness gate pagination — July 24, 2026

- Release Readiness now uses a server-authorized, URL-backed category/status/search/page contract with exact matching totals for its gate register. Gate updates re-read the target by tenant, company, and gate key inside the transaction; existing evidence and release-board controls remain unchanged.
- This is a bounded gate-register checkpoint, not Readiness completion. Evidence registers, security aggregate reads, GO/NO-GO history, selected-gate detail/drawer behavior, responsive browser proof, disposable PostgreSQL authorization/count/query-plan evidence, hosted deployment/recovery, and UAT gates remain open.

### DEC-0127 Administration UAT evidence pagination — July 24, 2026

- The UAT evidence register now has a selected-company, server-owned bounded page contract with search, evidence type/result/status/workflow/environment filters, exact totals, deterministic ordering, and stale-page clamping. The visible UAT list uses this contract while preserving existing audited evidence mutations.
- The legacy full-register summary/export paths, selected-record detail drawer, deployment/enablement evidence bounds, security aggregate bounds, GO/NO-GO surface, responsive browser, disposable PostgreSQL, hosted recovery, and UAT execution gates remain open.
- The UAT list now supports a scoped selected-record read-only detail panel with generic unavailable behavior for malformed or foreign IDs. Verify/Reject actions remain on the existing audited controls; a dedicated action drawer and bounded aggregate summary are still required.

### DEC-0128 Administration UAT aggregate summary — July 24, 2026

- UAT readiness summaries and UAT gate-transition checks now use tenant/company-scoped aggregate counts and existence checks for verification status, required evidence types, unresolved results, and Phase 3 workflow-area coverage. The page no longer hydrates the full UAT register to render its summary.
- UAT export remains separate, and deployment/enablement/security/GO-NO-GO evidence, selected action drawer behavior, browser/database/hosted recovery, and UAT execution gates remain open.

### DEC-0129 Administration UAT selected review actions — July 24, 2026

- UAT rows are now read-only list records with distinct UAT filter/page URL state. Verify and Reject are selected-record actions with creator self-review disclosure, minimum rejection reason, server-side management authorization, `RECORDED` compare-and-set, and audit-preserving retries.
- The selected detail remains a compact panel rather than a completed evidence drawer/activity workspace. Deployment/enablement/security/GO-NO-GO surfaces, export bounds, responsive browser, disposable PostgreSQL, hosted recovery, and UAT execution gates remain open.

### DEC-0130 Administration deployment evidence pagination — July 24, 2026

- Deployment evidence now uses selected-company bounded search/type/status/environment pagination with exact totals, deterministic ordering, selected-record detail, read-only rows, and server-authorized review actions. Deployment gate readiness uses scoped aggregate verified-type checks for the five migration/restore types and monitoring/hypercare.
- Deployment export/date-range bounds, activity detail, enablement/security/GO-NO-GO surfaces, responsive browser, disposable PostgreSQL, hosted recovery, and UAT execution gates remain open.

### DEC-0131 Administration enablement evidence pagination — July 24, 2026

- Enablement evidence now uses selected-company bounded search/type/status/audience pagination with exact totals, deterministic ordering, selected-record detail, read-only rows, and server-authorized review actions. Training and KB/release-note dependencies use aggregate verified evidence checks with the documented acknowledgement/support semantics.
- Enablement export/activity bounds, security/GO-NO-GO surfaces, responsive browser, disposable PostgreSQL, hosted recovery, and UAT execution gates remain open.

### DEC-0132 Administration security readiness projection — July 24, 2026

- Security Readiness now requires selected-company management scope before reads and narrows user, role-permission, MFA, authenticator, and break-glass projections to the fields required for its counters and attention sample. Local/provider MFA semantics and server-side gate blocking remain unchanged.
- This is an intermediate projection hardening checkpoint, not Security completion. SQL aggregate/EXPLAIN evidence, paginated attention detail, freshness/source status, authoritative workspace links, responsive browser, disposable PostgreSQL, hosted recovery, and UAT gates remain open.

### DEC-0133 Administration Release Board decision register — July 24, 2026

- The GO / NO-GO category now uses a selected-company, management-authorized bounded decision register with search, outcome filtering, exact totals, deterministic ordering, stale-page clamping, and a selected read-only decision detail panel. Malformed or foreign IDs return a generic unavailable state; decisions remain append-only and cannot be edited or hard-deleted.
- Existing audited decision creation and gate-transition controls remain authoritative: GO still requires the readiness summary to allow proceeding, and READY/CONDITIONAL_GO/WAIVED gate statuses still require the latest matching board outcome. This slice does not complete the separate Release Board workspace, composer, date filters, bounded export, audit/activity detail, responsive browser proof, disposable PostgreSQL evidence, hosted recovery, or UAT execution.

### DEC-0134 Administration security readiness SQL projection — July 24, 2026

- Security Readiness counters now use one parameterized, materialized PostgreSQL snapshot with exact tenant/company scope, active assignment windows, sensitive-role membership, deterministic latest MFA state, bounded three-user attention sample, explicit `LIVE_SNAPSHOT` source status, and UTC `asOf` metadata. The selected attention identity is a separate scoped, read-only detail query that exposes no secrets or mutation authority and returns generic unavailable behavior for malformed, foreign, inactive, or non-privileged users.
- This closes the unbounded application-memory projection gap but not the Security gate. Disposable PostgreSQL isolation/latest-status/concurrency execution, representative `EXPLAIN (ANALYZE, BUFFERS)`, external provider/vault evidence, responsive browser proof, hosted deployment/recovery, and UAT remain open. Authoritative User Detail and session-control workflows remain the mutation surfaces.

### DEC-0135 Administration enablement aggregate semantics — July 24, 2026

- Enablement readiness summaries now use bounded tenant/company-scoped counts and evidence-type grouping plus a targeted acknowledged-training count. The documented rule is preserved: a verified training signoff with both known-limit and support-route flags can satisfy those two requirements; otherwise standalone verified evidence remains required. The prior unbounded verified-row hydration is removed.
- Query-plan/volume, responsive browser, hosted recovery, and UAT gates remain open; this does not complete Enablement or Phase I.

### DEC-0136 Administration Readiness export bounds — July 24, 2026

- Readiness CSV and checksum exports now require strict UTC `from`/`to` dates, enforce the shared reporting date-span and row policies, count evidence/decision rows before bounded fetches, and fail with stable 400/413 responses before emitting bytes. The UI supplies a default 31-day UTC window and discloses the bound. Core Admin plus selected-company Manage scope is required before export audit start or data access.
- Async private-artifact delivery remains deferred. Database volume/query-plan, responsive browser, hosted recovery, and UAT gates remain open.

### DEC-0137 Administration Release Board workspace — July 24, 2026

- Added a separate `/admin/readiness/release-board` workspace with latest decision, readiness blocker counts, full audited append-only composer, selected read-only detail, searchable history, and server pagination. The existing GO / NO-GO register links to it; gate status remains source-owned and decision creation does not mutate gates.
- Date filters, bounded workspace export, richer audit/activity history, responsive browser proof, disposable PostgreSQL isolation/query-plan evidence, hosted deployment/recovery, and UAT execution remain open. This is a controlled workspace slice, not Readiness or Phase I completion.

### DEC-0138 Master Data Item registry pagination — July 24, 2026

- The company-scoped Item Master registry now uses server-side validated search, status filtering, deterministic ordering, bounded page size, exact totals, and shared pagination. Core Administration plus selected-company management scope is enforced in the service.
- Categories, UOMs, and Conversions remain pending bounded-read and option-catalog review; the Master Data workspace is not complete. Disposable PostgreSQL query/authorization evidence, responsive browser verification, hosted recovery, and UAT gates remain open.

### DEC-0139 Master Data conversion scope fence — July 24, 2026

- Conversion list, duplicate detection, and update reads now scope the item and both UOM relations to the active tenant/company; updates also require active related master records. Distinct-UOM, positive-factor, audit, and non-destructive lifecycle controls remain server-authoritative.
- Conversion option-catalog pagination and browser/database isolation evidence remain open; this does not complete Master Data.

### DEC-0140 Master Data option-catalog foundation — July 24, 2026

- Added a server-authorized, tenant/company-scoped option-catalog contract for category, UOM, and item selectors with bounded query/page inputs, exact totals, `hasMore`, active-option semantics, and selected-ID retention for edit context.
- Item create/edit selectors now consume the contract and disclose/disable when the bounded catalog is insufficient. Items, Categories, UOMs, and Conversions tabs now use URL-backed server filters and pagination; selected-record drawer/composer restructuring and conversion option migration remain open. Browser, disposable PostgreSQL, query-plan/load, hosted recovery, and UAT gates remain open.

### DEC-0141 Master Data supplier registry pagination — July 24, 2026

- The company-scoped Supplier Register now uses validated search, lifecycle/accreditation filters, deterministic ordering, bounded pages, exact totals, and selected-company Manage authorization. Supplier catalog detail remains separately scoped and paginated.
- Focused action composers, supplier option-catalog review, disposable PostgreSQL/query-plan evidence, responsive browser verification, hosted recovery, and UAT remain open.

### DEC-0142 Supplier selected action composer — July 24, 2026

- Supplier accreditation and deactivation now use a selected supplier URL state and focused action composer in the supplier catalog workspace. Repeated registry-row forms were removed; item-link deactivation remains in selected catalog detail.
- Item/category/UOM/conversion composers, cross-company browser/deep-link evidence, disposable PostgreSQL, hosted recovery, and UAT remain open.

### DEC-0143 Item selected action composer and base-UOM guard — July 24, 2026

- Item edit/deactivation now use a selected item URL state, scoped detail read, focused composer, and context-preserving success/error redirects. Repeated row mutation forms were removed.
- Base-UOM changes are blocked after posted InventoryMovement history with a transactional row lock and stable user-safe feedback; unchanged base-UOM edits remain valid. Disposable PostgreSQL race/isolation, browser/mobile, hosted recovery, and UAT gates remain open.
- Conversion creation uses the bounded active item/UOM option catalogs and remains disabled only when the catalog reports overflow; conversion edit now uses the selected conversion composer with scoped detail and preserved context.

Categories and UOMs now expose selected URL-backed detail/action composers with context-preserving redirects; legacy repeated row forms are disabled with explicit guidance. Conversion edit actions and external readiness gates remain open.

Conversion edit now uses the selected conversion composer with scoped detail and preserved query/page context; legacy row edit controls are disabled with guidance. Master Data still requires external database/browser/recovery/UAT evidence.

### DEC-0144 Approval Inbox feature-disabled state — July 24, 2026

- `/approvals` now checks normalized routing before any list read. When routing is disabled, it presents an explicit unavailable state and no false zero count or legacy client-sliced queue. Normalized server pagination remains unchanged when enabled.
- If the flag is enabled before runtime backfill readiness is complete, the known backfill-readiness failure renders the same unavailable state; unrelated database and authorization faults remain errors. Focused approval tests (42), typecheck, lint, and diff checks pass for this checkpoint.
- Approval routing activation remains NO-GO pending PostgreSQL parity/isolation, live authorization revocation, all-family policy/cutover, hosted recovery, browser, and UAT gates.

### DEC-0145 Supplier-item link duplicate race — July 24, 2026

- Supplier-item link creation now maps the unique-key race on supplier, item, and purchase UOM to the stable `DUPLICATE_SUPPLIER_ITEM_LINK` outcome while preserving atomic price-history and audit creation.
- The global link composer gap was superseded by DEC-0146; supplier-item creation now uses the selected-supplier bounded lookup composer. Browser/database/recovery/UAT evidence remains open.

### DEC-0146 Supplier-item bounded lookup composer — July 24, 2026

- Supplier-item creation now uses the selected-supplier catalog with server-owned active item and purchase-UOM lookup pages, bounded search, deterministic ordering, exact totals, selected-option context, out-of-range page clamping, empty-state handling, and independent pager links. Global unbounded selectors and their render-time full-catalog query were removed.
- Register/catalog/lookup URL context and validated create error/success return paths are preserved. Focused supplier tests (4), web typecheck, lint, and diff checks pass; responsive browser, disposable PostgreSQL isolation/query-plan, hosted recovery, and UAT gates remain open.

### DEC-0147 Purchase Request catalog-UOM relationship guard — July 24, 2026

- Purchase Request draft creation now validates each catalog item/UOM pair against the item's base, purchase, issue, and configured conversion endpoints. Emergency free-text lines remain allowed; invalid catalog pairs fail with stable user-safe feedback before any draft side effects.
- Focused Purchase Request tests (12), web typecheck, and lint pass. The visible draft editor still needs bounded searchable item/UOM lookups and an explicit budget-line overflow/lookup contract before the PR workspace can pass its list and visible-surface gates.

### DEC-0148 Purchase Request bounded draft lookups — July 24, 2026

- Purchase Request draft entry now uses a server-authorized, tenant/company/location-scoped lookup endpoint for catalog items, item-valid UOMs, and active budget lines. Inputs validate query, page, page size, item context, and selected-ID retention; responses provide exact totals, deterministic ordering, and bounded pages.
- The editor preserves selected item/UOM/budget context across searches, exposes independent 44px item/UOM/budget page controls with exact page counts, shows loading/error/empty states, and merges selected IDs into visible pages when they fall outside the requested page. Emergency free-text lines remain available.
- The create sheet now keeps Company, Brand, Location, and Requester context visible, uses 44px primary controls, groups lookup paging to active searches, and offers an explicit retry action after lookup failure. Department and cost-center values are not present in the session context and are not invented by the editor.
- Focused Purchase Request tests (13), web typecheck, lint, and diff checks are the checkpoint evidence. Authenticated responsive-browser evidence, disposable-PostgreSQL authorization/query evidence, hosted recovery, and UAT remain open; the Purchase Request workspace is not complete.

### DEC-0149 Master Data conversion-create composer — July 25, 2026

- Conversion creation now uses a focused bounded composer backed by the shared server-authorized Item Master option catalog. Item, source-UOM, and target-UOM selectors each provide search, selected-ID retention, exact page counts, independent paging, loading/error/empty feedback, retry, and mobile-safe 44px controls.
- The previous capped 100-option arrays and disabled-when-overflow behavior were removed. Conversion creation still delegates all active tenant/company, distinct-UOM, positive-factor, duplicate, reason, audit, and no-inventory-mutation rules to the server mutation.
- Focused Item Master tests (8), web typecheck, lint, and diff checks pass. Disposable PostgreSQL isolation/query-plan, responsive browser, hosted recovery, and UAT evidence remain open; Master Data is not complete.

### DEC-0150 Supplier Quote queue search — July 25, 2026

- The approved Purchase Request quote queue now supports server-side search across PR reference, requester, and supplier code/name plus required-date From/To filters. Predicates remain within the existing tenant/company/selected-location/APPROVED boundary, preserve request selection and pagination context, and provide truthful no-match states.
- Focused Quotes tests (15), web typecheck, lint, and diff checks pass. Amount filtering remains open pending commercial-field/export policy; responsive browser, disposable PostgreSQL, hosted recovery, and UAT evidence remain open; Quotations are not complete.

### DEC-0151 Supplier quote line comparison matrix — July 25, 2026

- The selected approved request now shows a read-only line-aligned matrix across recorded suppliers. It displays requested quantity/UOM beside each quote's quantity/UOM, unit price, line total, availability, and lead time, and explicitly marks missing quoted lines. Existing commercial cards, controlled evidence, recommendation policy, and approval actions remain authoritative.
- Focused Quotes tests (15), web typecheck, lint, and diff checks pass. Substitution semantics, mandatory evidence policy, responsive browser, disposable PostgreSQL, hosted recovery, and UAT remain open; Quotations are not complete.

### DEC-0152 Administration Permission Access bounded detail — July 25, 2026

- Permission Access detail no longer hydrates every tenant role, assignment, and user scope in one nested read. It now uses a server-owned role page with search, exact totals, deterministic ordering, selected-company active/effective-date scope predicates, and at most five safe current-company user previews per role.
- Existing Core Administration, tenant-role administration, and selected-company Manage guards remain before all reads. The page explicitly states tenant-global role semantics, read-only purpose, selected-company context, and preview limits.
- Focused Core Admin tests (30), web typecheck, lint, and diff checks pass. Disposable PostgreSQL authorization/count/query-plan evidence, responsive browser, hosted recovery, and UAT remain open; Administration is not complete.

### DEC-0225 versioned company Approval Rule lifecycle checkpoint — July 26, 2026

- The previously passive Approval Rules register now provides a bounded company-owned Create, Revise, Activate, and Deactivate workflow. New definitions and revisions are complete inactive immutable versions; activation atomically replaces the exact selected-company transaction/route slot for future submissions, while retained approval instances keep their original rule and copied steps. Tenant-wide, unsupported future-type, and legacy named-user rules remain explicitly read-only.
- Every write revalidates the live actor/session, Core Administration and tenant-role permissions, active/effective selected-company `MANAGE`, strict privileged-MFA proof, supported server-owned route, eligible scoped role approvers, idempotency, and lifecycle/active-slot compare-and-set state inside a serializable company-locked transaction. PostgreSQL enforces one active slot, scoped successor/intent references, root/successor lineage, sealed immutable rule and step definitions, append-only lifecycle intents, and no rule/step deletion or truncation. Seed reruns use immutable no-op upserts and seal fresh definitions in one transaction.
- The glossary, approval matrix, security/audit model, data dictionary, Users/Roles UI specification, administrator knowledge base, release note, decision index, and open-policy register are aligned. Requested Code Spark and exact GPT-5.4 subagent models were unavailable in the active toolset; the closest permitted GPT-5.6 product, architecture, security, and QA specialists were used and recorded without relaxing gates.
- Prisma validation and client generation, web/database typechecks, web lint, 58 focused web tests, 34 database-package tests, the full 1,357-test web regression, the 20-case authorization manifest, E2E typecheck, an isolated production build, secret review, release-tool self-tests, migration inventory, and diff hygiene pass. Migration `20260726180000_approval_rule_versioning` is bound to SHA-256 `02d4ecf9f2da7bb51abab0c0c669701916e4fb4a3034109ff3ca1d28c1271f2f` and independently approved only for disposable rehearsal. The existing PostgreSQL approval fixtures now construct and seal rule definitions through one shared atomic helper so the new append-only contract does not invalidate their database gates. A disposable-PostgreSQL lifecycle suite is authored and registered for real create/revise/activate/deactivate, idempotency/CAS, live authority/MFA/scope revocation, immutable/sealed triggers, scoped constraints, audit/intent evidence, and pinned Approval Instances, but it cannot execute locally because `DISPOSABLE_DATABASE_ADMIN_URL` is unavailable.
- Independent Security and QA re-reviews return GO for this coherent source checkpoint with no remaining Critical, High, or Medium source defect; independent Database review approves the exact migration only for rehearsal. Administration and Phase I remain in progress: populated/negative migration rehearsal, restricted-runtime trigger/search-path/ACL and restore-state verification, writer quiescence, fresh/repeat seed digest, concurrency and submit-versus-activation evidence, authenticated desktop/tablet/mobile verification, hosted deployment/recovery, and UAT remain required before production readiness or workspace completion.

### DEC-0226 Branch Operations dashboard-profile checkpoint — July 26, 2026

- Branch Operations Reviews and Exceptions now use the allowlisted, versioned `branch-checklist-reviews-v1` and `branch-checklist-exceptions-v1` destinations. Each card and destination share the dashboard read's exact selected tenant, company, optional-brand, and location predicates. Raw status, shift, and business-date inputs cannot widen a profile; bounded search may only narrow it, and invalid or stale identifiers fail visibly instead of falling back to an ordinary list.
- Reviews preserves the complete scoped `SUBMITTED` plus `MANAGER_REVIEW` oversight population without claiming actor-actionable work. Exceptions preserves its line-grain card count and separately reports affected checklist rows. Both destinations are read-only entry points: record detail and every available review/correction/close action independently reauthorize the live actor, source status, and scope. Profile, search, page, and safe return context remain intact through detail navigation.
- Independent Product, Security, and QA challenge review accepted the bounded profile design subject to exact predicate parity, truthful count grain, fail-closed identifiers, independent detail authorization, and navigation/parity tests. Requested Code Spark and exact GPT-5.4 models were unavailable in the active toolset, so the closest permitted GPT-5.6 specialists were used and recorded without relaxing gates.
- Focused Branch Operations/dashboard tests pass 54/54; the authorization manifest passes 20/20 with the list adapter classified as a high-risk protected read and bound to a real PostgreSQL permission-denial/no-mutation case; the complete non-database web suite passes 1,365 tests across 127 files (302 skipped, 1 todo); web and E2E typecheck, lint, production build, secret review, and `git diff --check` pass. Independent final review returned GO with no Critical, High, or Medium source finding. Requested Code Spark and exact GPT-5.4 models were unavailable, so the closest permitted inherited GPT-5.6 fallback was used and recorded. This checkpoint does not complete Overview, Branch Operations, Workspace 1, or Phase I: disposable-PostgreSQL scope/count/query-plan execution, authenticated desktop/tablet/mobile evidence, hosted deployment/recovery, production-authenticated E2E, and UAT remain open.

### DEC-0227 Food Safety dashboard-profile checkpoint — July 26, 2026

- Food Safety Reviews and Exceptions use the allowlisted, versioned `food-safety-reviews-v1` and `food-safety-exceptions-v1` destinations. Both use the exact session tenant, selected company, optional-brand, and selected-location predicates. Raw type, status, and business-date inputs cannot widen a profile; normalized search is capped at 120 characters and may only narrow it; invalid or stale identifiers fail visibly.
- Reviews preserves the complete scoped `SUBMITTED` plus `EXCEPTION_REVIEW` oversight population without claiming actor-actionable work. Exceptions preserves the card's summed exception-reading count while the destination separately reports affected logs with `exceptionCount > 0` across all statuses and retained history. Profile mode hides create/export, preserves canonical profile-only return context, and grants no workflow authority; source detail and actions independently reauthorize.
- Independent Product, Security, and QA challenge review accepted the bounded profile design subject to exact predicate parity, truthful reading/log grains, all-status history, fail-closed identifiers, hidden create/export, independent detail authorization, and navigation/parity coverage. Requested Code Spark and exact GPT-5.4 models were unavailable, so the closest permitted GPT-5.6 specialists were used without relaxing gates.
- The existing Phase II workflow policy catalog and live Food Safety service differ in their review, return, close, and evidence contracts. This read-only profile checkpoint does not resolve that discrepancy; policy-owner confirmation and controlled reconciliation remain open.
- Focused Food Safety/dashboard/export/authorization-manifest coverage passes 99/99, including the 20/20 manifest gate with `listFoodSafetyLogPage` classified as a high-risk protected read and bound to the real PostgreSQL restaurant-operations permission-denial/no-mutation case. The complete non-database web suite passes 1,372 tests across 127 files (302 skipped, 1 todo); web and E2E typecheck, lint, production build, secret review, and `git diff --check` pass. Independent QA and Security re-reviews returned GO with no Critical, High, or Medium finding after present-empty/duplicate profile fail-closed handling, 44px profile-path controls, and retained ordinary-workspace knowledge-base guidance were corrected.
- Overview, Food Safety, Workspace 1, and Phase I remain in progress. Disposable-PostgreSQL scope/parity/query-plan execution, authenticated desktop/tablet/mobile evidence, hosted deployment/recovery, production-authenticated E2E, UAT, and the catalog/service policy reconciliation remain open.

### DEC-0228 Incident dashboard-profile checkpoint — July 26, 2026

- Incident Open, Critical, Pending Review, and Overdue use the allowlisted, versioned `incident-open-v1`, `incident-critical-v1`, `incident-pending-review-v1`, and `incident-overdue-v1` destinations. All use exact session tenant, selected company, nullable selected brand, and selected location scope. Raw status, severity, and incident-date inputs cannot redefine a profile; normalized search is capped at 120 characters and may only narrow it; missing, empty, duplicate, invalid, and stale profile context fails visibly before data access.
- Open contains `OPEN`, `IN_PROGRESS`, and `PENDING_REVIEW`. Critical is an all-status historical severity lens. Pending Review is scoped oversight, not actor-assigned work. Overdue requires the dashboard's captured non-future `asOf` operating date and uses `dueAt` before that cutoff, `resolvedAt` null, and status other than `CANCELLED`. The cutoff remains stable through navigation, but the destination reads current records and is explicitly not a historical snapshot. The four incident-record populations may overlap and are not additive.
- Profile mode is read-only, hides create and ordinary export, rejects direct dashboard-profile export requests, uses a minimal triage projection, and preserves only canonical profile/search/page/applicable-cutoff return context through detail and actions. Correction, resolve, and cancel independently reauthorize the live actor, status, and exact scope.
- Independent Product, Workflow, and Security deliberation accepted the bounded contract subject to exact nullable-brand scope, all-status Critical semantics, oversight labeling, captured-cutoff/current-record disclosure, minimal projection, fail-closed parameters, export rejection, independent action authorization, and parity/navigation tests. Requested Code Spark and exact GPT-5.4 models were unavailable, so the closest permitted GPT-5.6 specialists were used without relaxing gates.
- The existing correction-permission mismatch, absent active transition into `PENDING_REVIEW`, assignment language without a command, and unenforced terminal-status/`resolvedAt` consistency remain open under the core decision register. This checkpoint does not resolve or conceal them.
- Focused Incident/dashboard/export coverage passes 84/84; combined focused coverage with the authorization manifest passes 104/104. The authorization manifest passes 20/20 with `listIncidentPage` classified as a high-risk protected read and bound to the real PostgreSQL restaurant-operations permission-denial/no-mutation case. The complete non-database web suite passes 1,379 tests across 127 files (302 skipped, 1 todo); web and E2E typecheck, lint, production build, secret review, and `git diff --check` pass. Independent QA and Security re-reviews returned GO with no remaining Critical, High, or Medium finding after exact status/count parity, hidden-field inference, truthful cutoff/count presentation, neutral action language, and reporter/resolution/source-context gaps were corrected. The coherent commit is the checkpoint commit containing this record; its exact SHA is verified after push.
- The projects/operations authorization runner was attempted and failed closed before database creation with `DISPOSABLE_DATABASE_ADMIN_URL_REQUIRED`; no PostgreSQL execution credit is claimed. Overview, Incident Management, Workspace 1, and Phase I remain in progress. Policy/data-integrity reconciliation, disposable-PostgreSQL scope/parity/query-plan execution, authenticated desktop/tablet/mobile evidence, hosted deployment/recovery, production-authenticated E2E, and UAT remain open.

### DEC-0229 Maintenance dashboard-profile checkpoint — July 26, 2026

- Maintenance Follow-up, Critical, Pending Vendor, and Overdue use the allowlisted, versioned `maintenance-follow-up-v1`, `maintenance-critical-v1`, `maintenance-pending-vendor-v1`, and `maintenance-overdue-v1` destinations. All use exact session tenant, selected company, nullable selected brand, and selected location scope. Raw status, priority, and requested-date inputs cannot redefine a profile; normalized search is capped at 120 characters, searches visible metadata only, and may only narrow; missing, empty, duplicate, invalid, and stale profile context fails visibly before data access.
- Follow-up contains active `OPEN`, `IN_PROGRESS`, and `PENDING_VENDOR` tickets. Critical is an all-status historical priority lens. Pending Vendor is scoped oversight, not actor-assigned work. Overdue requires the dashboard's captured non-future `asOf` operating date and uses `targetDueAt` before that cutoff, `completedAt` null, and the active-status whitelist. This intentionally excludes cancelled and malformed completed/null-completion rows from active overdue work without rewriting stored history. The cutoff remains stable through navigation, but the destination reads current records and is not a historical snapshot. The four ticket populations may overlap and are not additive.
- Profile mode is read-only, hides create and ordinary export, rejects direct dashboard-profile export requests, uses a minimal visible-metadata triage projection, and preserves only canonical profile/search/page/applicable-cutoff return context through detail and actions. Source Incident ID is hidden from profile rows; any detail source link is permission-gated and independently reauthorized. Correction, completion, and cancellation independently reauthorize the live actor, status, and exact scope.
- Parent-led specialist deliberation accepted the bounded contract subject to exact nullable-brand scope, all-status Critical semantics, Pending Vendor oversight labeling, active-only captured-cutoff overdue semantics, visible-field search, minimal projection, fail-closed parameters, export rejection, independently authorized source links/actions, and parity/navigation tests. Requested Code Spark and exact GPT-5.4 models were unavailable, so the closest permitted GPT-5.6 specialists were used without relaxing gates.
- Assignment/reassignment authority, vendor-state transitions, terminal reopen, terminal-status/date integrity and recovery, and authoritative operating-timezone policy remain open under the core decision register. This checkpoint does not resolve or conceal them.
- Independent QA and Security re-reviews returned GO with no remaining Critical, High, or Medium finding after the profile source-Incident UUID was removed from row projections, exact permission-and-scope source reauthorization was added, all profile-reachable controls were raised to 44px, and the real company-level empty-brand session sentinel was normalized to database `null` in both Maintenance and the linked Incident destination. The source-disclosure matrix proves resolve-only access, no-Incident-authority redaction, out-of-scope redaction, and history redaction. Requested Code Spark and exact GPT-5.4 models were unavailable, so the closest permitted GPT-5.6 specialists were used without relaxing gates.
- Combined Maintenance, Incident regression, dashboard, export, and authorization-manifest coverage passes 167/167, including the 20/20 manifest gate. The complete non-database web suite passes 1,389 tests across 127 files (302 skipped, 1 todo); web and E2E typecheck, web lint, production build, secret review, and `git diff --check` pass. The projects/operations disposable-PostgreSQL runner was attempted and failed closed before database creation with `DISPOSABLE_DATABASE_ADMIN_URL_REQUIRED`; no database execution credit is claimed. The coherent commit is the checkpoint commit containing this record; its exact SHA is verified after push. No authenticated desktop/tablet/mobile, hosted deployment/recovery, production-authenticated E2E, or UAT credit is claimed. Overview, Maintenance Management, Workspace 1, and Phase I remain in progress.

### Workspace 1 implementation checkpoint — July 23, 2026

- Confirmed `DEC-0053` after independent UX, correctness, and security review. The overview now places compact scope/freshness context before `Today’s work`, then compact KPI/supporting content. The earlier KPI-first wording in the Phase I dashboard screen specification was aligned with the higher-authority Dashboard Rules and the existing workspace audit.
- The server-derived approval and exception queue contracts now expose a bounded preview with deterministic priority ordering, location, owner, timing, and severity labels. Existing server-side permission and selected-scope behavior remain the data boundary; the UI does not supply scope, source type, record ID, or route values. The separate My Tasks workspace now provides exact totals and signed cross-source seek pagination for every enrolled source; totals and continuation are withheld whenever an enrolled source is unavailable. Enterprise-wide coverage remains intentionally incomplete because Approval Inbox, Count Variance, Food Cost, and sources without an approved action adapter are excluded.
- The Overview visibly labels the queue as a bounded priority preview and shows `shown of total` whenever its cap excludes matching records. Existing knowledge-base wording already describes `Today’s work` as a bounded preview, so no glossary or enablement change was required.
- A Playwright desktop/Pixel-7 check now verifies the Overview priority-preview disclosure, source-workspace headings, and no horizontal overflow. The test and web application typechecks/lint pass; execution remains pending because this environment does not provide `DISPOSABLE_DATABASE_ADMIN_URL` for the required seeded disposable-database E2E harness.
- `DEC-0063` confirms the path from a bounded preview to a complete cross-source queue: Overview remains a preview, while a separate server-owned `My Tasks` workspace may enroll only source-owned, authorized, seek-paginated task adapters with one tested ordering/predicate contract. Exact totals are withheld whenever a registered source is unavailable; Approval, Count Variance, and Food Cost remain excluded under their existing gates. Implementation begins with source task-contract parity, not by increasing preview limits.
- Mobile navigation now uses a fixed four-action bar (`Overview`, the user's primary operational workspace, `Alerts`, and `More`) and a role-filtered workspace sheet. This replaces the prior multi-row fixed grid that could obscure the active workspace. Web typecheck and lint pass; seeded browser execution remains pending on `DISPOSABLE_DATABASE_ADMIN_URL`. The glossary needs no change because no new user-facing term or control was introduced.
- Dashboard queue dates and due-state ranking now use the documented `Asia/Manila` operating day instead of UTC date slicing. A Manila-midnight regression test passes with the focused dashboard suite (19 tests), web typecheck, and lint. This corrects existing display/ranking behavior; it does not create a new user-facing term, workflow, or policy, so no glossary or knowledge-base edit is required.
- The first `DEC-0063` source-task adapter is now implemented for Wastage: it exposes only `SUBMITTED` reports to users with the existing review permission and `APPROVED` reports to users with the existing posting permission, with the same scoped predicate for exact count and bounded seek paging. Draft and returned reports are intentionally excluded because their current detail surface has no focused correction editor; exposing them as a task would falsely promise a completable action. Focused Wastage tests (17), web typecheck, and lint pass. This is a server-only queue foundation, so no glossary or knowledge-base update is required yet.
- Stock Adjustments now provide the second `DEC-0063` source-task adapter: only unposted `APPROVED` adjustments are returned, and only to users who already have the existing posting permission. The count and bounded seek page share that exact scoped predicate. Draft/correction work and approval work remain excluded until their authoritative focused surfaces are included; Approval Inbox remains the approval authority. Focused Stock Adjustment tests (18), web typecheck, and lint pass. This is a server-only queue foundation, so no glossary or knowledge-base update is required yet.
- Transfers now provide a third `DEC-0063` source-task adapter. It returns `REQUESTED` transfers only to dispatch-authorized users in the selected source location, and dispatched/partially received/disputed transfers only to receipt-authorized users in the selected destination location who are not the dispatcher. Dispute settlement remains excluded until its stricter independent-actor eligibility can be represented and parity-tested. The count and bounded seek page share this direction- and actor-aware predicate. Focused Transfer tests (22), web typecheck, and lint pass. This is a server-only queue foundation, so no glossary or knowledge-base update is required yet.
- The registered source adapters now share one deterministic cross-source cursor tuple: creation timestamp, fixed source rank, then source record ID. Same-timestamp tests prove that a later-ranked source remains eligible after a prior source page, while the current source advances only after its record ID; this prevents cross-source skips and duplicates. The first registry intentionally contains only high-priority, no-due-date task projections. Any source with different priority/due semantics must extend and test the common tuple before enrollment. Focused task-ordering and source tests (59), web typecheck, and lint pass. This internal contract introduces no user-facing term, so no glossary or knowledge-base update is required.
- The initial `My Tasks` workspace is now live in the Overview navigation. It server-merges only the enrolled requester-owned Purchase Request, Purchase Order, Transfer, Wastage, Stock Adjustment, and Receiving task adapters, reuses each authoritative service’s scoped action predicate, and opens the source record for the actual controlled action. The Purchase Order adapter returns only `DRAFT` orders to users with the existing submit permission and `APPROVED` orders to users with the existing supplier-send permission; it does not treat all open POs as tasks. Purchase Request submission now server-enforces that the draft’s recorded requester is the acting user before it creates routing, audit, or notification effects; the detail screen shows the true requester and explains the denied action. The `My Tasks` adapter returns only that user’s `DRAFT` requests with the existing submit permission. Submission on behalf remains explicitly open and is not inferred from read scope. The Receiving adapter returns only `DRAFT` receipts to users with the existing post permission; discrepancy review is excluded because it has no controlled resolution action. Its opaque HMAC-signed cursor is short-lived and bound to the current user, selected scope, permission set, and registered-source version; it cannot authorize a source action. Same-timestamp ordering remains gap-free across pages. A failed enrolled source is disclosed as unavailable and causes the total to be withheld rather than reported as zero. The page explicitly states its current source coverage, so it is an initial controlled queue rather than a claim of complete enterprise task coverage. Focused My Tasks, Purchase Request, Purchase Order, authentication, and source tests, web typecheck, and lint pass. Browser execution remains pending on `DISPOSABLE_DATABASE_ADMIN_URL`. Knowledge-base and glossary now explain this distinction.
- `DEC-0064` enrolls Branch Operations with two exact source-owned task predicates: independently reviewable `SUBMITTED`/`MANAGER_REVIEW` checklists with complete actor lineage, and role-pooled `RETURNED` correction work for scoped creators. Final `REVIEWED`/`EXCEPTION_OPEN` close work remains excluded pending confirmed self-action/final-signoff policy. Count and page reuse the same tenant/company/optional-brand/location and action predicate; the adapter selects no lines, evidence, notes, or audit data. Direct review and return now also fail closed when opener or latest-submitter lineage is missing, and the Return control is shown only with the existing correction permission. Independent post-implementation QA accepted the remediation with no remaining blocker. Focused Branch Operations/My Tasks/task-ordering tests pass 22/22; web typecheck, lint, and `git diff --check` pass. Browser, disposable-database authorization, and hosted evidence remain pending under the workspace gate. The glossary and dashboard/My Tasks knowledge-base guide now distinguish personal work from role-pooled work.
- July 26 filter-parity correction: a correction-only Branch Operations or Food Safety actor no longer loses authorized pooled `RETURNED` work when selecting the matching My Tasks status. Both adapters use named permission predicates, review-only actors remain excluded from returned correction rows, and signed queue cursors advance to registry `v6`. Focused Branch Operations, Food Safety, and My Tasks coverage passes 48/48; the full non-database web suite passes 1,344 tests across 124 files (301 skipped, 1 todo); typecheck, lint, the 20/20 authorization manifest, and production build pass. Independent GPT-5.6 QA identified the High-severity false-empty state and returned GO after remediation with no remaining Critical, High, or Medium finding; requested Spark/GPT-5.4 models were unavailable. Disposable PostgreSQL authorization/filter-parity, authenticated responsive-browser, hosted recovery/deployment, production-authenticated E2E, and UAT remain open; Workspace 1 and Phase I are not complete. Existing knowledge-base and glossary wording already describes the intended behavior and remains accurate; the user-facing correction is recorded in release notes.
- `DEC-0065` enrolls Food Safety with independently reviewable `SUBMITTED`/`EXCEPTION_REVIEW` logs whose recorder is known and is not the acting reviewer, plus role-pooled `RETURNED` correction work for scoped creators. Final `REVIEWED`/`EXCEPTION_OPEN` close work remains excluded pending final-signoff/self-action policy. Count and page reuse the same tenant/company/optional-brand/location predicate; the minimal projection excludes readings, measurements, corrective actions, evidence, notes, and audit history. Direct review and return fail closed when recorder lineage is missing, and the visible Return control now requires the existing correction permission. Independent post-implementation review accepted the slice with no blocking or high finding; its noted cursor and projection test gaps were added before final validation. Focused Food Safety/My Tasks/task-ordering tests, web typecheck, lint, and `git diff --check` pass. Browser, disposable-database authorization, and hosted evidence remain pending under the workspace gate. Glossary and knowledge-base guidance now cover Food Safety independent review and pooled correction.
- `DEC-0066` upgrades the signed My Tasks cursor to v2 and orders tasks by explicit critical/high/medium/low priority, dated-before-undated absolute due date, creation time, source rank, and record ID. Existing sources retain fixed `HIGH`/no-due semantics; v1 cursors fail safely and restart. Incident joins as one role-pooled resolution obligation per active record, using four bounded severity streams and exact tenant/company/null-aware-brand/location scope. High/critical incidents require known non-self reporter lineage; low/medium follow current resolver policy. Cancellation remains a destination exception and ordinary correction is excluded because it has no correction-needed state and its documented permission remains inconsistent with implementation. Direct resolve/cancel now fail closed for missing high-risk lineage, the UI mirrors actor eligibility, and Incident detail preauthorizes the exact null-aware brand scope before returning narrative/evidence. Independent review found the detail-scope gap, then accepted its remediation with no remaining blocker. Focused Incident/My Tasks/ordering tests pass 32/32; web typecheck, lint, and `git diff --check` pass. Browser, disposable-database authorization, representative backlog monitoring, and hosted evidence remain pending at their workspace/release gates. Glossary and user guidance describe role-pooled resolution and v2 ordering.
- `DEC-0067` enrolls one role-pooled Maintenance completion obligation per eligible active ticket through four bounded native-priority streams. The adapter enforces exact tenant/company/null-aware-brand/location scope, `completedAt = null`, and known non-self reporter lineage for critical/high tickets; medium/low tickets retain the current scoped completion rule. Cancellation and ordinary correction remain destination-only and excluded from the queue. The destination now fails closed for missing high-risk reporter lineage, the UI mirrors that rule, Maintenance dashboard/detail/history/source-Incident reads use exact null-aware brand scope, and correction now consistently requires `restaurant.maintenance.correct`. Completion, cancellation, and correction use `updatedAt` compare-and-swap guards so a concurrent correction cannot bypass the risk decision or overwrite stale history. Independent review found this concurrency gap and accepted the remediation with no remaining High/Critical finding. Focused service/policy/documentation tests pass 73/73 and focused visible-surface tests pass 6/6; web typecheck, lint, and `git diff --check` pass. Browser/disposable-database concurrency evidence, representative backlog monitoring, and hosted gates remain required before Workspace 1 completion. The glossary, workflow, UAT permission rule, and user guidance are aligned.
- `DEC-0068` enrolls exactly one assigned first-pass Stock Count obligation per eligible session: on-date `DRAFT` start, non-empty incomplete `IN_PROGRESS` entry, or non-empty complete `IN_PROGRESS` submission. It uses fixed `HIGH`/no-due ordering and excludes future starts, empty snapshots, `RECOUNT_REQUESTED`, submitted review/recount, cancellation, reviewed variance generation, and all Count Variance activation. Direct actions now enforce the recorded counter, post-lock live permission, first-pass state, lineage, and transaction/CAS parity; blind protected facts and entered-count exports are actor/state-redacted even when the counter also holds review permission. Count start and all receiving, transfer, wastage, and adjustment posting/reversal paths share one complete, sorted, scoped Inventory Location lock set before source locks, so the balance snapshot/cutoff and movement freeze have one serialization boundary; the cutoff uses post-lock PostgreSQL wall-clock time. Independent reviews found and accepted remediation of entered-fact export leakage, review-control actor parity, transaction-start timestamp drift, and authorization-evidence overclaiming, with no remaining Critical/High code finding. Focused backend/posting tests pass 154/154, focused page tests pass 11/11, the authorization-manifest gate passes 20/20 with reviewed boundary bindings for `/my-tasks`, `getMyTasksPage`, and both delegated Inventory Location lock exports, and web typecheck, lint, production build, and `git diff --check` pass. The corrected disposable-PostgreSQL tenant/company/exact-lock-set and downstream selected-location rollback case is present but could not execute locally because `DISPOSABLE_DATABASE_ADMIN_URL` is unavailable; fresh database execution, real contention/opposing-lock-order evidence, and authenticated browser/hosted gates remain pending. Workspace 1 and Stock Counts therefore remain in progress; Count Variance remains inactive under `DEC-0061`. Glossary, workflow, UI specification, and user guidance are aligned.
- `DEC-0069` replaces the misleading `Receiving Variance` card with a closed `Receiving Follow-up` profile. One source-owned predicate includes clean `DRAFT`/`POSTING`, every `POSTED_WITH_DISCREPANCY`, and discrepancy-flagged `DRAFT`/`POSTING`/`POSTED`/`POSTED_WITH_DISCREPANCY`/`REVERSING` receipts in the selected receiving location while excluding terminal and future statuses. Dashboard count/candidates, authorized total, constrained GRN/PO/supplier search, 25-row server pages, inclusion reasons, and the header-only CSV export reuse that boundary. The profile hides create and inline post controls, exposes detail navigation only, and states that it neither resolves discrepancies nor grants posting, reversal, Purchase Order, or inventory authority. Independent review found and drove remediation of a duplicate lifecycle predicate, incomplete terminal/future truth-table evidence, unsafe overlength-search failure, generic invalid-profile fallback, and lost/stale detail return context; final Workflow, Security, and UX re-reviews accepted the checkpoint with no remaining Critical/High finding. Focused backend/dashboard/export/visible-surface tests pass 43/43; web typecheck, lint, production build, the 20/20 authorization-manifest gate, and `git diff --check` pass. Receiving and Workspace 1 remain in progress because disposable-database, responsive-browser, query-plan/volume, and hosted evidence remain pending. Glossary, dashboard/Receiving specifications, user guidance, decision record, and changelog are aligned.
- `DEC-0070` closes the Ledger Variance drilldown through a dedicated `/inventory/reconciliation?dashboard=ledger-variance-v1` diagnostic profile. It requires both balance-view and ledger-view authority and uses one parameterized PostgreSQL statement to union balance-cache and movement keys for the exact tenant/company/selected active location, aggregate ledger deltas, round cache-minus-ledger variance to six decimals, and return only non-zero rows. The same query contract drives the dashboard count and three candidates, additive max-120 search, deterministic 25-row server pages, full filtered diagnostic export, and exact independently authorized ledger traces; no balance edit, Stock Adjustment, posting, approval, or reversal action is exposed. Trust-gate `block` suppresses the numeric dashboard value while retaining a visibly untrusted diagnostic path for authorized investigation. Independent Workflow, Security, and UX review first rejected the exact trace because it was ledger-only, silently capped at 100 movements, did not identify resolved keys, and was misclassified in generated authorization evidence. Remediation now requires both permissions for exact traces, rejects trace filters from the generic ledger reader, provides deterministic 50-row trace pages with exact totals and preserved return context, displays current cache/ledger/variance quantities plus a resolved/stale warning, classifies reconciliation surfaces as the explicit permission conjunction, and adds an executable export boundary matrix for neither, either permission alone, and both. The UX re-review additionally identified and closed sub-44px controls and missing route states: affected controls and paging now have 44px minimum targets, and both reconciliation and ledger routes provide scoped loading skeletons plus user-safe retry boundaries. Security and static UX re-reviews returned GO. The requested Spark/GPT-5.4 models were unavailable, so the closest permitted GPT-5.6 specialist roles were used and recorded. The complete non-database web unit suite passes, including 23 Inventory service tests, 12 reconciliation/ledger route UI tests, and the 20-test authorization manifest; web typecheck, lint, and production build pass. The new disposable-PostgreSQL route boundary case is implemented but remains locally unexecuted until a disposable database is available. Integration also corrected invalid dashboard-profile CSV requests so Purchase Order, Purchase Request, Transfer, Wastage, Adjustment, and Receiving exports fail closed through shared no-store validation responses instead of returning generic data. Workspace 1 and Inventory remain in progress because representative PostgreSQL `EXPLAIN (ANALYZE, BUFFERS)`, concurrent-posting snapshot evidence, route-matrix execution, export-volume policy, authenticated responsive-browser evidence, and hosted gates remain pending. Glossary, permission/reporting/dashboard/Inventory specifications, user guide, release note, decision record, and changelog are aligned.
- `DEC-0071` implements the `DEC-0062` safe fallback instead of leaving known-ambiguous Food Cost signals live. Overview no longer imports or calls the Food Cost analysis, and it emits no Food Cost card, KPI, exception candidate, unavailable-source item, source-health claim, readiness badge, or aggregate contribution. Authorized recipe/cost users retain only neutral `Source workspace` navigation to the independently authorized `/recipes/analysis` route. Restaurant Operations scans no longer read Food Cost, create or refresh `FOOD_COST_EXCEPTION`, or accept recipe-only eligibility; Branch Operations, Food Safety, Incident, and Maintenance reminders remain enrolled. Existing Food Cost reminders remain stored, visible, readable, and manually archivable with a non-mutating legacy-definition warning, and they do not contribute to the page's current actionable count. Independent review exposed and drove remediation of an existing tenant-only notification boundary: list, unread count, read, and archive now share an exact tenant/recipient/selected-company and null-or-selected-location predicate, require an active selected location and a live effective company-or-location assignment, hide company-less rows, and silently deny forged or revoked-scope mutations. Notification list/count are now explicitly high-risk disclosures in the authorization manifest, and all four entrypoints have executable live-context bindings. The disposable-PostgreSQL matrix covers cross-company/location/null-company/forged/revoked cases, company-wide rows, immutable legacy Food Cost history, and two authorized idempotent scans yielding exactly the six non-Food-Cost reminders; it is implemented but locally unexecuted because `DISPOSABLE_DATABASE_ADMIN_URL` is unavailable. Final independent QA and Security re-reviews returned GO with no remaining Critical/High finding. The complete non-database web suite passes 1,216 tests, including focused 12/12 notification tests; the authorization manifest passes 20/20; typecheck, lint, and `git diff --check` pass. The preceding checkpoint production build passed, but the latest rerun stalled during Next.js compilation and was terminated without a pass/fail result. The requested Spark/GPT-5.4 models were unavailable, so GPT-5.6 reporting, workflow, UX, backend, QA, and security fallbacks were used and recorded. Workspace 1 remains in progress because a fresh production-build result, `DEC-0062` definition/reactivation work, normalized approval cutover, authenticated responsive-browser proof, disposable-PostgreSQL execution, and hosted release/recovery gates remain pending. Decision record, security/permission/reporting/UI/UAT sources, glossary, user guidance, release note, and changelog are aligned.
- The application shell no longer displays a decorative search affordance. Its notification control now opens the existing Notification Center. The dashboard adds layout-preserving loading and safe retry error states.
- Seven source services now provide dashboard-specific, server-authorized, selected-scope reads. Purchase Requests, Receiving, Transfers, Stock Counts, Wastage, and Stock Adjustments return an exact scoped count and no more than eight narrow header/exception candidates; they do not expose line, comment, audit, or evidence collections. Purchase Orders use scoped count/amount aggregates, a narrow fulfillment-line projection needed for the existing exact open/received-value calculation, and no more than eight overdue header candidates instead of materializing their related workspace records. Transfer candidates put `DISPUTED` items ahead of ordinary follow-up items before the bounded cap. The source workspace still reauthorizes every opened record.
- Branch Operations now follows the same dashboard-read boundary: Overview uses selected-scope status/exception aggregates, a critical-exception aggregate, and no more than three narrow review candidates plus three narrow exception candidates. The full checklist/line query remains confined to the authoritative Branch Operations workspace.
- Food Safety now follows the same dashboard-read boundary: Overview uses selected-scope log/reading/exception aggregates and no more than three narrow review candidates plus three narrow exception candidates. Full safety-log and reading detail remains confined to the authoritative Food Safety workspace.
- Incidents now follow the same dashboard-read boundary: Overview uses selected-scope status/severity/overdue aggregates and no more than three narrow follow-up candidates. Full incident detail and correction/evidence context remains confined to the authoritative Incident workspace.
- Maintenance now follows the same dashboard-read boundary: Overview uses selected-scope status/priority/overdue/downtime aggregates and no more than three narrow follow-up candidates. Full maintenance ticket detail, history, and operational evidence remains confined to the authoritative Maintenance workspace.
- `DEC-0062` leaves Food Cost dashboard-read replacement open. Independent reporting review found mixed default-date source data, missing ledger valuation represented as zero, and an `AWAITING_ACTUALS` definition mismatch. Do not add a bounded Food Cost dashboard projection or call the current analytical result equivalent until those source definitions are confirmed, corrected, and parity-tested; hiding the summary is the safe fallback.
- `DEC-0054` confirms the feature-disabled Approval Inbox posture. While normalized approval routing remains deliberately disabled, Overview no longer calls the unbounded legacy approval list or substitutes a direct approval query. It explicitly directs authorized users to the controlled Approval Inbox and does not represent its unavailable preview as zero assignments, a clear indicator, or an alert count. The eventual bounded approval adapter remains conditional on approved normalized-routing cutover and a minimal source-authorized projection.
- `DEC-0072` replaces eager shared dashboard-source mutation with lazy, authorized descriptors whose reads return immutable patches. Every attempted authorized source settles independently as available or unavailable with an ISO-UTC check time; unauthorized and suppressed sources are omitted. The response records assembly time, preserves a documented native `Data as of` only for sources that supply one, and never calls observation time fresh, stale, live, complete, or real-time. A deployment-configurable technical deadline defaults to 2,500 ms and rejects values above 3,000 ms; late results cannot change the returned response. A process-wide admission controller defaults to 32 and caps at 64 executing source reads, retains timed-out slots until the underlying work actually settles, and emits safe timeout/saturation/late-settlement/assembly telemetry. Composite queue contracts expose contributor provenance and make the complete total unavailable when any contributor fails, while the UI labels remaining rows as a partial view and links generically to the authoritative workspace. Trust-policy degradation fails closed for reconciliation values, and the feature-disabled Approval preview is explicitly unavailable. The dashboard Inventory Balance input is now a bounded aggregate rather than an unpaginated full-row list, with an executable disposable-PostgreSQL aggregate plus public-dashboard scope/value case registered under the procurement/inventory authorization gate. Initial Architecture and Security review found the trust, availability, telemetry, database-evidence, and unbounded-late-work gaps; final re-reviews accepted their remediation with no remaining Critical/High finding. The complete non-database web suite passes 1,233 tests, including 54 focused dashboard/inventory tests and 8 dashboard UI tests; web typecheck, lint, the 20/20 authorization manifest, isolated-output production build, and `git diff --check` pass. The PostgreSQL case is implemented but locally unexecuted until `DISPOSABLE_DATABASE_ADMIN_URL` is available. Workspace 1 remains in progress pending authenticated responsive-browser evidence, database-side cancellation/query-plan proof, staging fault injection against the four-second NFR, and the existing approval/Food Cost/hosted gates.
- `DEC-0055` confirms closed, source-owned dashboard destinations. The first activated destination is `Open POs`: the dashboard card, selected-scope count, server-paginated list, and CSV export share the exact `DRAFT`, `PENDING_APPROVAL`, `APPROVED`, `ISSUED`, `AMENDMENT_PENDING`, and `PARTIALLY_RECEIVED` lifecycle set. The destination accepts no client scope, record, status, or generic filter override; the target list, export, record view, and actions reauthorize the current session independently. Invalid dashboard-profile values do not activate a profile.
- `DEC-0056` adds the second closed, source-owned destination: `Open PRs`. Its card, selected-scope count, server-paginated list, and CSV export share the exact `DRAFT`, `PENDING_APPROVAL`, `APPROVED`, and `RETURNED` lifecycle set. The profile ignores raw status and search values, accepts no client scope or record authority, and each source operation reauthorizes the current session independently. Its user-facing wording explicitly includes approved requests.
- `DEC-0057` adds `Transfer Follow-up` as the third closed, source-owned destination. Its card, selected-scope count, server-paginated list, and CSV export share exactly `REQUESTED`, `DISPATCHED`, `PARTIALLY_RECEIVED`, and `DISPUTED` transfers where the selected location is the source or destination. The profile ignores raw tab/status/scope/filter overrides and grants no dispatch, receipt, settlement, or inventory capability; record actions independently reauthorize the current session.
- `DEC-0058` adds `Adjustment Exceptions` as a closed, source-owned destination. Its count, server-paginated list, and CSV export share exactly `PENDING_APPROVAL`, `APPROVED`, `POSTING`, and `RETURNED` records in the selected inventory location. It is read-only and preserves source authorization, no-self-approval, MFA, immutable-ledger, posting, and reversal controls.
- `DEC-0059` adds `Wastage Exceptions` as a closed, source-owned destination, sharing the selected inventory location's existing status-or-missing-required-evidence exception predicate across its count, server-paginated list, and CSV export. It remains read-only and preserves evidence policy, approval, audit, idempotent immutable posting, and reversal controls. Count Variance remains deliberately inactive: independent workflow and security review found that non-review users can currently derive frozen system quantity from the returned/displayed variance and that recount overwrites earlier evidence. Do not activate a variance KPI, queue, drilldown, or sensitive export until server-side redaction, review-only access, immutable recount history, and controlled adjustment recovery are implemented and verified.
- `DEC-0060` closes the immediate blind-count disclosure: non-review count reads and UI now redact system/variance/reviewer-derived fields, and Count Variance dashboard reads are review-permission gated. Count Variance remains inactive pending immutable recount history and controlled adjustment recovery; this security fix does not change count posting, adjustment generation, approval, or ledger behavior.
- Focused dashboard/adapter tests (including the closed PO/PR/Transfer profiles), web lint, web typecheck, the authorization-manifest gate (20), the isolated production build, release-tool self-test, and `git diff --check` pass. The refreshed authorization baseline classifies the new Branch Operations, Food Safety, Incident, and Maintenance dashboard reads as standard server-scoped workspace reads. This is not Workspace 1 completion: the Food Cost source still needs its own analytical dashboard-read design; remaining typed filtered destination contracts, database-side boundary/query-plan/concurrency evidence, authenticated desktop/tablet/mobile browser evidence, and hosted gates remain pending. My Tasks cross-source totals and pagination are complete only for the explicitly enrolled sources and fail closed on partial-source availability.
- The reusable live PostgreSQL authorization regression is registered for the new PO and PR profile reads and proves a missing read permission must deny them without mutation when it executes. It could not be run locally in this checkpoint because `DISPOSABLE_DATABASE_ADMIN_URL` is not configured; run it on the disposable/hosted gate before production readiness is claimed.
- Knowledge-base and glossary guidance now describes `Today’s work` as a read-only, scoped priority view. It does not claim that an item is complete, approved, posted, or otherwise changed merely because it appears on the overview.
- Knowledge-base and glossary impact was assessed for this internal read-path hardening. No user-visible term, navigation, permission, or workflow changed, so their existing `Today’s work` guidance remains accurate without an additional edit.

### Workspace 1 approval-unavailable surface correction — July 26, 2026

- Overview now treats the flag-off approval source as an intentionally non-actionable unavailable surface. The approval source-status row, assigned-approvals header, unavailable queue state, and approval-alert summary no longer direct users to `Open approvals`, `Open Approval Inbox`, or a generic approval source that exposes no queue or actions. The available-state `Open approvals` action remains conditional and unchanged.
- The unavailable state explicitly says pending work may still exist. It accurately describes `Scan Approvals` as a partial current-user reminder scan for eligible due or overdue work whose approval links remain unavailable until controlled Inbox activation. No approval read, list, count, notification, permission, scope, route, or decision authority changed; no legacy or parallel flag-off queue was added.
- Focused dashboard model and presentation coverage passes 38/38. The complete non-database web suite passes 1,389 tests with 305 skipped and one existing TODO across 127 passed/11 skipped files; web and E2E typecheck, web lint, production build, secret review, and `git diff --check` pass. The source-of-truth dashboard UI specification, existing dashboard/approval knowledge-base guidance, and the user-facing release note are aligned. Requested Code Spark and exact GPT-5.4 specialists were unavailable, so the closest permitted GPT-5.6 fallbacks were used without relaxing any gate.
- Workspace 1 and normalized-routing activation remain **NO-GO**. Independent reconciliation identified a separate High production-availability gap in unbounded My Tasks source settlement; `DEC-0230` is now confirmed and implemented in the following checkpoint. Disposable PostgreSQL, authenticated responsive-browser, hosted recovery/deployment, production-authenticated E2E, Food Cost/Count Variance policy, and UAT gates remain open.

### DEC-0230 My Tasks bounded source settlement — July 26, 2026

- Every enrolled My Tasks adapter now settles at the presentation boundary within a validated technical deadline. The default is 2,500 ms and the accepted maximum is 3,000 ms. Timeout, saturation, and exception name the affected source as unavailable; healthy-source rows remain visible, while the total and continuation cursor are withheld so recovered work cannot be skipped or represented as zero.
- A process-wide admission controller defaults to 32 and caps at 64 active source reads. A timed-out read retains its slot until the underlying promise actually fulfills or rejects, and late rejection is observed. Redacted telemetry records only closed source/outcome values, timestamps/duration, and aggregate counts; it excludes users, record identifiers, query text, payloads, stack traces, and database errors. This checkpoint does not claim database cancellation or leak-free termination: permanently stuck reads can retain capacity until process/read recovery.
- The visible partial-empty state now says `No actions shown from available sources` and warns that it is not confirmation no work is waiting. Complete empty pages retain the positive all-clear state. Task eligibility, ordering, cursor binding, permissions, scope, source action authority, and the underlying workflow services are unchanged.
- Focused My Tasks service and visible-state coverage passes 25/25; the authorization manifest passes 20/20; the complete non-database web suite passes 1,395 tests with 305 skipped and one existing TODO across 127 passed/11 skipped files. Web typecheck and lint, E2E typecheck, production build, secret review, and `git diff --check` pass. The authenticated disposable-database E2E entry point was attempted and failed closed before database creation with `DISPOSABLE_DATABASE_ADMIN_URL_REQUIRED`; no browser or PostgreSQL execution credit is claimed.
- The source-of-truth dashboard UI specification, knowledge-base guide, decision record, and user-facing release note are aligned. No glossary term, permission document, data dictionary, migration, or training-content change is required because this correction adds no user term, authority, field, status, or workflow policy. Requested Code Spark and exact GPT-5.4 specialists were unavailable, so the closest permitted GPT-5.6 specialist fallbacks were used without relaxing gates.
- Independent final Security/QA and Product/UX reviews both returned source-checkpoint GO with no Critical, High, or Medium finding. Workspace 1 and Phase I remain **NO-GO** pending hosted load/threshold/alert/recovery evidence, database statement-timeout or cancellation posture, authenticated desktop/tablet/mobile evidence, production-authenticated E2E, UAT, and the existing normalized-approval, Food Cost, Count Variance, PostgreSQL query-plan/concurrency, deployment, and recovery gates.

### DEC-0231 Positive Stock dashboard profile — July 26, 2026

- The Overview Positive Stock metric and Active stock rows signal now open the allowlisted `positive-stock-v1` Inventory profile instead of the broader register. One canonical predicate fixes tenant, company, selected location, active Inventory Location, tenant/company parity across its Location, Item, Item Category, and base UOM relations, and `qtyOnHand > 0` across the dashboard no-search count, exact profile total/page, and profile CSV. Bounded search may only narrow it; only `dashboard`, `q`, and `page` are accepted in profile mode, so ordinary, unknown, or future filter inputs cannot silently redefine it.
- Missing, empty, duplicate, invalid, stale, or contradictory profile inputs fail visibly before the balance read. The profile is explicitly read-only current data rather than a historical dashboard snapshot, provides an intentional `Open all stock balances` escape, preserves profile/search/page context through the independently authorized ledger handoff, and adds scoped loading and retryable error states with 44px controls. No posting, adjustment, balance-edit, ledger, or export authority is granted by the URL.
- Profile CSV reauthorizes the existing export permission, uses the same canonical predicate and deterministic ID-tied order, enforces the validated `reporting.export.max_rows` policy with an exact count preflight and `maxRows + 1` race guard, and returns a safe `413` without a partial file. Audit metadata is aggregate-only: profile, configured cap, outcome, and completed row count; search text, row payloads, identifiers, and internal errors are excluded.
- Focused Inventory/dashboard/export/visible-surface coverage passes 72/72; the complete non-database web suite passes 1,405 tests with 305 skipped and one existing TODO across 129 passed/11 skipped files. Web and E2E typecheck, web lint, production build, secret review, and `git diff --check` pass. The 20/20 authorization manifest registers the new protected page/export reads and binds them to the expanded disposable-PostgreSQL inventory aggregate/profile parity case. That case proves denial, selected-scope positive row grain, zero, negative, inactive-location, mismatched-related-record, and adjacent-scope exclusion, dashboard/list/export parity, and unchanged balance/audit snapshots when executed. The procurement/inventory runner and authenticated E2E runner were attempted and both failed closed before database creation with `DISPOSABLE_DATABASE_ADMIN_URL_REQUIRED`; no PostgreSQL, query-plan, browser, or hosted execution credit is claimed.
- The Inventory and dashboard UI specifications, reporting/export source, Stock Balances knowledge-base article, glossary, decision record, and user-facing release note are aligned. No permission, data dictionary, schema/migration, or training-workflow change is required. Requested Code Spark and exact GPT-5.4 specialists were unavailable; the closest permitted GPT-5.6 Product, Security/QA, Backend, and documentation fallbacks were used without relaxing gates.
- Independent final Security/QA and Product/UX source re-reviews returned **GO**, each with C0/H0/M0 after closing relation-scope, strict-allowlist, authorization-evidence, export-guidance, touch-target, and mobile-label findings. Workspace 1 and Phase I remain **NO-GO** pending representative PostgreSQL query-plan/volume evidence, authenticated desktop/tablet/mobile verification, hosted export/load/alert/recovery evidence, production-authenticated E2E, UAT, and all previously open normalized-approval, Food Cost, Count Variance, deployment, and recovery gates.

### DEC-0232 Zero Stock dashboard profile — July 26, 2026

- The Overview Zero Stock metric now opens the allowlisted `zero-stock-v1` Inventory profile. The exhaustive profile dispatcher handles both `positive-stock-v1` and `zero-stock-v1`; unknown or future values cannot fall through to ordinary Inventory behavior. One canonical predicate fixes tenant, company, selected location, active Inventory Location and related-record ownership, and exact `qtyOnHand = 0` row membership across dashboard count, profile count/page, and bounded CSV. Positive, negative, inactive, malformed, and cross-scope rows are excluded; multiple valid zero rows for the same item remain separate balance rows.
- Profile mode requires strict exact-cardinality `dashboard`, optional `q`, and optional `page`; normalized search is capped at 120 characters and may only narrow. Raw tab, scope, and filter overrides fail visibly. Copy now says `Zero-stock balance rows` instead of the false `Items configured`. The profile is current live, read-only data—not a historical snapshot, catalog-completeness report, or replenishment action—and preserves canonical page/search/profile context through independently authorized ledger navigation.
- Profile CSV reauthorizes existing export permission, shares the canonical predicate and deterministic ID-tied order, enforces validated `reporting.export.max_rows` through exact count preflight and a `maxRows + 1` race guard, and returns safe `413` with no partial file. Audit metadata is aggregate-only and excludes search text, row payloads, identifiers, and internal errors.
- The strengthened disposable-PostgreSQL specification includes two zero rows for one item plus positive, negative, inactive-location, mismatched-related-record, cross-scope, and no-mutation fixtures. It is authored but unexecuted and earns no PostgreSQL or query-plan credit. Focused relevant coverage passes 74/74: Inventory 29, dashboard 30, Inventory page 4, ledger 7, and export 4. The broader Inventory-filter run passes 127/127; web typecheck, web lint, and the 20/20 authorization manifest pass.
- UX initially found 36px PaginationBar controls; the profile now supplies `controlClassName` with `min-h-11` and a regression test, and UX re-review returned **GO, C0/H0/M0**. Security initially found three Medium evidence/consistency gaps; remediation adds zero-valued adversarial PostgreSQL relation rows, stale cached-permission denial, `id`/quantity/version plus movement/audit no-mutation snapshots, authorization registry/baseline alignment, and corrected broad-register wording. Security re-review returned **GO, C0/H0/M0**. Final source review is complete and GO.
- The complete non-database web suite passes 1,407 tests with 305 skipped and one existing TODO across 129 passed/11 skipped files; production build, E2E typecheck, secret review, and final diff hygiene pass. The disposable procurement/inventory runner was attempted and failed closed before database creation with `DISPOSABLE_DATABASE_ADMIN_URL_REQUIRED`; no PostgreSQL, query-plan, browser/mobile, hosted, recovery, or UAT credit is claimed. No schema, migration, permission, or training-workflow change was introduced. Requested Code Spark and exact GPT-5.4 specialists were unavailable, so the closest permitted GPT-5.6 fallbacks were used without relaxing gates. Workspace 1 and Phase I remain **NO-GO** pending representative PostgreSQL query-plan/volume evidence, authenticated desktop/tablet/mobile verification, hosted export/load/alert/recovery evidence, production-authenticated E2E, UAT, and all previously open normalized-approval, Food Cost, Count Variance, deployment, and recovery gates.

### DEC-0233 Lot / Expiry Data dashboard profile — July 26, 2026

- Overview now labels the group `Stock balance signals`; its truthful `Rows with lot or expiry data` card opens the exact `lot-expiry-data-v1` Inventory profile. One static parameterized `Prisma.sql` scope/membership predicate is shared by dashboard count, profile count/page, and bounded export: current tenant/company/selected location, active Inventory Location and Location/Item/Category/base-UOM ownership, plus trimmed nonblank lot or recorded expiry. Positive, zero, and negative quantities are included; blank or whitespace-only lot without expiry is excluded.
- The profile is current live, read-only data presence—not a historical snapshot or tracking-compliance, coverage, accountability, or traceability claim. It requires strict exact-cardinality `dashboard`, optional `q`, and optional `page`; normalized search is capped at 120 characters and may only narrow. It displays absent fields as `Not recorded`, distinguishes negative quantities, preserves canonical page/search/profile context through independently authorized ledger navigation, and provides bounded redacted export with deterministic ID-tied order, configured count preflight, `maxRows + 1` race guard, safe `413`, and no partial file.
- Future `postInventoryMovement` writes store movement and balance lot values as trimmed text or null. Existing legacy rows are not rewritten; blank-lot data quality remains a separate follow-up. No schema, migration, index, permission, or training-workflow change was introduced.
- The authored PostgreSQL fixture covers lot-only, expiry-only, both, positive/zero/negative quantities, whitespace exclusion, zero-valued cross-scope/inactive/mismatched relations, stale cached-permission denial, and no mutation. It is unexecuted and earns no PostgreSQL or query-plan credit. Directly relevant service/dashboard/page/ledger/export/dashboard-page coverage passes 86/86 (32 + 30 + 4 + 7 + 5 + 8). The complete non-database web suite passes 1,411 tests with 305 skipped and one existing TODO across 129 passed/11 skipped files; web typecheck, lint, E2E typecheck, production build, secret review, the 20/20 authorization manifest, and `git diff --check` pass.
- Final independent Security and UX re-reviews returned **GO**, each with C0/H0/M0 after closing scope-safe hydration, whitespace-race membership, future-write normalization evidence, touch-target, and blank-display findings. The disposable procurement/inventory runner was attempted and failed closed before database creation with `DISPOSABLE_DATABASE_ADMIN_URL_REQUIRED`; no PostgreSQL, query-plan, authenticated E2E, responsive-browser, hosted-recovery, or UAT credit is claimed. The confirmed challenge round resolved the blank-lot disagreement in favor of trim-and-exclude under legacy-preservation, future-normalization, adversarial-evidence, and separate-data-quality safeguards. Requested Code Spark and exact GPT-5.4 specialists were unavailable, so the closest permitted GPT-5.6 fallbacks were used without relaxing gates. Workspace 1 and Phase I remain **NO-GO** pending representative PostgreSQL query-plan/volume, authenticated desktop/tablet/mobile, hosted export/load/alert/recovery, production-authenticated E2E, UAT, and previously open normalized-approval, Food Cost, Count Variance, deployment, and recovery gates.

### DEC-0234 Inventory balance-cache recency suppression — July 26, 2026

- Parent-confirmed `DEC-0234` suppresses Overview `Updated this week` and recent-stock-updates presentation and removes `recentlyUpdatedRows` from the bounded Inventory dashboard contract/query. Mutable `InventoryBalance.updatedAt` is cache lifecycle metadata, not authoritative inventory-event time; generic `/inventory` also fails the closed count/page/export destination requirement. No replacement count, candidates, card, export, or generic drilldown is authorized.
- Independent Reporting/Product, Security, and UX reviewers unanimously selected suppression (Option C, 4.25/5) over a rolling seven-day balance-cache profile (Option A, 2.95/5) or calendar-week cache calculation (Option B, 2.65/5). A future immutable-movement profile is preferred but remains unapproved pending `createdAt` versus `occurredAt` authority, rolling/calendar period and week/timezone boundary, movement/document grain, movement-type inclusion, ledger permission, and closed profile/export/audit decisions.
- Implementation is complete locally: the DTO field, query predicate, card, icon mapping, label/detail, and generic drilldown are removed without changing Positive Stock, Zero Stock, Lot / Expiry Data, ordinary Inventory, or independently authorized Ledger contracts. The remaining three cards use a deliberate one-column mobile and three-column tablet/desktop layout. No schema, migration, index, permission, or training-workflow change was introduced; the KB and end-user release note explain that suppression does not mean zero activity and that no replacement signal is approved.
- Focused final-review coverage passes 79/79; the complete non-database web suite passes 1,412 tests with 305 skipped and one existing TODO across 129 passed/11 skipped files. Web typecheck, lint, E2E typecheck, production build, secret review, the 20/20 authorization manifest, and `git diff --check` pass. Independent Security and UX re-reviews returned **GO**, each with C0/H0/M0. The disposable procurement/inventory runner was attempted and failed closed before database creation with `DISPOSABLE_DATABASE_ADMIN_URL_REQUIRED`; no PostgreSQL credit is claimed. Requested Code Spark and exact GPT-5.4 specialists were unavailable, so the closest permitted GPT-5.6 Reporting/Product, Security, UX, and documentation fallbacks were used without relaxing gates. Workspace 1 and Phase I remain **NO-GO** pending authenticated responsive-browser/E2E, hosted recovery, UAT, and all previously open PostgreSQL query-plan/volume, normalized-approval, Food Cost, Count Variance, deployment, and recovery gates.

### DEC-0235 Purchase Order monetary-signal suppression — July 26, 2026

- Parent-confirmed `DEC-0235` suppresses Overview `PO commitment`, `Open PO exposure`, and `Received value` and removes `committedValue`, `openValue`, `receivedValue`, and `primaryCurrency` from `PurchaseOrderDashboardRead` and its broad queries. The rollups combine all-status header/line values, can retain approval-rejected subtype remainder, do not reconcile header versus line basis, and can label mixed currencies using the newest PO's currency. Child-lineage, snapshot consistency, live authorization, and closed destination parity are also incomplete. No replacement monetary metric is authorized.
- The exact non-monetary `po-open-v1` count/profile and selected-scope overdue PO queue remain unchanged. Their lifecycle, scope, deterministic paging/order, export, and independently authorized source behavior must not be widened by this suppression.
- Reporting initially proposed one narrow open-line metric. The challenge round classified it **Blocking** until one Decimal-safe, snapshot-consistent, relation-safe, single-currency query defines exact lifecycle/cancellation-subtype semantics and is shared by a closed dashboard/profile/page/bounded-export contract with live authorization. Relabeling or retaining the existing rollups was rejected.
- Implementation is complete locally: the DTO fields, broad header/line/newest-currency reads, formatting, three cards, icon mappings, labels, and monetary drilldowns are removed. The exact `po-open-v1` count/profile and bounded overdue queue remain. Final UX review first returned **NO-GO**, C0/H1/M0, because the Reports tab still advertised `Purchase Order Exposure`; remediation replaced it with a neutral `Purchase Order Register` source-workspace link using record/status/currency wording and removed the page subtitle's commitment implication. UX re-review and independent Security review then returned **GO**, each with C0/H0/M0. No schema, migration, index, permission, currency conversion, inventory, approval, or training-workflow change was introduced; source-of-truth guidance, dashboard/purchasing/reporting specifications, data dictionary, KB, decision record, index, and release note are aligned. The glossary was assessed and requires no new or changed term.
- Evidence: focused adapter/service coverage passes 70/70; the final dashboard-focused selection passes 93/93; the complete non-database web suite passes 1,413 tests with 305 skipped and one existing TODO across 129 passed/11 skipped files. Web typecheck, lint, E2E typecheck, production build, secret review, the 20/20 authorization manifest, and `git diff --check` pass. The disposable procurement/inventory runner was attempted and failed closed before database creation with `DISPOSABLE_DATABASE_ADMIN_URL_REQUIRED`; no PostgreSQL relation/snapshot/query-plan credit is claimed. Requested Code Spark and exact GPT-5.4 specialists were unavailable, so the closest permitted GPT-5.6 Reporting, Product, Security, UX, and documentation fallbacks were used without relaxing gates. Authenticated responsive-browser/E2E, hosted recovery/deployment, UAT, and all previously open approval, Food Cost, Count Variance, PostgreSQL, and recovery controls remain open. Workspace 1 and Phase I remain **NO-GO**.

### DEC-0236 Branch critical-exceptions dashboard profile — July 26, 2026

- Parent-confirmed `DEC-0236` adds only the versioned, read-only `branch-checklist-critical-exceptions-v1` profile. Its primary metric is the exact count of retained lines with `result = EXCEPTION` and `severity = CRITICAL` across every checklist status; the destination separately reports that line total and the number of affected checklists. Dashboard and destination must share relation-safe tenant, selected-company, optional-brand, and selected-location predicates, bounded narrowing search, and server-owned pagination. Profile membership grants no action authority.
- Overview suppresses `branch-manager-review-count` and `branch-reviewed-count`. The standalone Manager Review signal duplicates the retained combined Reviews card/profile and its `waiting` copy conflicts with started-review semantics. Reviewed is intermediate, excludes `CLOSED`, and has no confirmed period, owner, or close-policy definition. The combined `SUBMITTED` + `MANAGER_REVIEW` Reviews profile remains unchanged.
- The implemented profile renders rows as cards below `lg`, labels reviewer attribution `Reviewed by`, shows the selected-brand badge, hides create and ordinary export, preserves canonical profile/search/page return context, and independently reauthorizes every source action. Invalid, duplicate, stale, or widening profile inputs fail visibly. All child-line projections and searches enforce duplicated selected scope in addition to parent relation scope. A failed Branch dashboard source withholds the signal and recovers to `/branch-operations`. No schema, permission, workflow, action, notification, or export contract changed.
- Implementation is complete locally. Focused service coverage passes 57/57; the complete non-database web suite passes 1,416 tests with 305 skipped and one existing TODO across 129 passed/11 skipped files. Web typecheck, lint, E2E typecheck, production build, secret review, the 20/20 authorization manifest, and `git diff --check` pass. Independent Data/Security and UX re-reviews returned **GO**, each with C0/H0/M0/L0. The disposable Projects/Operations authorization runner was attempted and failed closed before database creation with `DISPOSABLE_DATABASE_ADMIN_URL_REQUIRED`; no PostgreSQL execution credit is claimed. Authenticated responsive-browser/E2E, hosted deployment/recovery, UAT, and the pre-existing wider Branch Activity/Audit visibility gate remain open. This checkpoint does not complete Overview, Branch Operations, Workspace 1, or Phase I. The exact requested Code Spark and GPT-5.4 subagent models were unavailable; the closest permitted GPT-5.6 specialist fallbacks were used for deliberation, independent evaluation, documentation, and enablement without relaxing hard gates.

### DEC-0237 Food Safety critical-exceptions dashboard profile — July 26, 2026

- Parent-confirmed `DEC-0237` adds only the versioned, read-only `food-safety-critical-exceptions-v1` profile. Its primary metric is the exact count of retained readings with `result = EXCEPTION` and `severity = CRITICAL` across every Food Safety log status; the destination must separately report that reading total and the number of affected logs. Dashboard and destination must share exact relation-safe parent-and-child tenant, selected-company, optional-brand, and selected-location predicates, bounded narrowing search, and server-owned pagination. Profile membership grants no action authority.
- Overview must suppress `food-safety-exception-review-count` and `food-safety-reviewed-count`. The standalone Exception Review signal duplicates the retained combined Reviews card/profile and its `waiting` copy conflicts with started-review semantics. Reviewed is intermediate, excludes `CLOSED`, and has no confirmed period, owner, or close-policy definition. The combined `SUBMITTED` + `EXCEPTION_REVIEW` Reviews profile and the all-severity Exceptions profile remain unchanged.
- The implemented local profile is all-status and read-only, hides create and ordinary export, preserves canonical profile/search/page return context, independently reauthorizes every source action, and fails visibly for invalid, duplicate, empty, stale, or widening inputs. It does not resolve the known Phase II Food Safety workflow-catalog/live-service discrepancy. No schema, permission, workflow, action, notification, or export contract changed. Focused Food Safety/dashboard coverage passes 57/57; the complete non-database web suite passes 1,418 tests with 305 skipped and one existing TODO across 129 passed/11 skipped files. Web typecheck, lint, E2E typecheck, production build, secret review, the 20/20 authorization manifest, and `git diff --check` pass. Data/Security final review is **GO** C0/H0/M0/L0. UX initially found a profile-badge grain mismatch (C0/H0/M1); profile-aware narrowed totals remediated it and final UX review is **GO** C0/H0/M0/L0. The disposable Projects/Operations runner failed closed before database creation with `DISPOSABLE_DATABASE_ADMIN_URL_REQUIRED`, so no PostgreSQL credit is claimed. Authenticated responsive-browser/E2E, hosted deployment/recovery, UAT, and the catalog/live-service policy reconciliation remain open. This checkpoint does not complete Overview, Food Safety, Workspace 1, or Phase I. Requested Code Spark and exact GPT-5.4 reviewers were unavailable; closest permitted GPT-5.6 fallbacks were used without relaxing any gate.

### DEC-0238 Truthful operational Source views — July 26, 2026

- The compatible Overview `reports` route/view now presents `Source views`: a typed directory that distinguishes versioned, read-only exact scoped populations from authorized source workspaces. It does not claim to be a generic report surface or imply a report definition, aggregate reconciliation, data completeness, or source availability.
- Each operational destination is enrolled only when its authorized dashboard source was attempted. Exact views use their existing service-owned profile href helpers; source workspaces retain independent authorization. The presentation labels each attempted source available or unavailable and does not substitute failed/unavailable reads with zero records. It separates Wastage Exceptions, Stock Adjustment Exceptions, Branch Checklist Exceptions, Open Incidents, and Maintenance Follow-up rather than using broad combined labels, and shows an explicit no-destination state where appropriate. No schema, permission, workflow, export, report-definition, inventory, financial, approval, or audit behavior changed.
- Focused dashboard page/service coverage passes 46/46; the complete non-database web suite passes 1,423 tests with 305 skipped and one existing TODO across 129 passed/11 skipped files. Web typecheck/lint, E2E typecheck, production build, the 20/20 authorization manifest, secret review, and `git diff --check` pass. Final Data/Security and UX reviews each return **GO** C0/H0/M0/L0. Disposable PostgreSQL, authenticated responsive-browser/E2E, hosted deployment/recovery, and UAT remain open and no external readiness credit is claimed. This checkpoint does not complete Overview, Workspace 1, or Phase I. Requested Code Spark and exact GPT-5.4 reviewers were unavailable; the closest permitted GPT-5.6 fallback was used without relaxing any gate.

## 5. Workspace Completion Gate

Every workspace must satisfy all applicable items before it is marked complete:

- All visible tabs, panels, actions, and navigation destinations work or clearly explain their intentional read-only/disabled state.
- Create, edit, submit, approve, post, receive, dispatch, complete, cancel, archive, reverse, export, and configuration actions work where the UI implies them.
- Server-side tenant and scope authorization is enforced; UI hiding is not treated as security.
- Valid transitions succeed and invalid transitions are rejected with stable, user-safe errors.
- Financial and inventory segregation-of-duties rules, including no self-approval, are enforced.
- Retried posting actions are idempotent and concurrency-sensitive actions detect stale versions.
- Important changes preserve audit/activity history and do not hard-delete controlled records.
- Required reasons, evidence, attachments, and independent review are policy-driven and enforced.
- Lists provide the required search, filters, pagination, export, and mobile presentation.
- Empty, loading, error, denied, returned, rejected, conflict, and retry states are usable.
- Desktop, tablet, and mobile task flows are verified.
- Relevant unit, integration, access-control, and focused E2E tests pass.
- Workflow, permission, data, UI, knowledge-base, release-note, and training documentation impacts are assessed and updated where behavior changed.

## 6. Phase I and Phase 1.5 Deferred Controlled Transitions

The following remain pending implementation or require a separate confirmed release slice. They must not be represented as available merely because adjacent foundations exist:

- full post-receiving PO amendment for supplier, location, lines, substitutions, and payment terms;
- partial receiving-line reversal;
- transfer dispatch reversal and automated replacement/financial settlement;
- backdated operational corrections outside approved controlled paths;
- opening-balance and inventory cutover execution after the final cutover policy is confirmed;
- automated notification scheduling and external email/chat/SMS delivery;
- time-limited attachment links where direct signed URLs are adopted;
- formal PDF summaries where required;
- broader task dependency enforcement, drag/drop concurrency, and automation beyond the approved Phase 1.5 boundary.
- Phase 1.5 Project Board still has implementation blockers before its workspace gate: the visible board needs the documented project context header, linked-record/assignee indicators, complete status controls, mobile filtered-list mode, and server pagination; task-key allocation must replace the concurrent `count + 1` pattern; project/task dates must render in the company or user timezone; and the enabled task-transition policy needs an explicit lifecycle matrix. These are recorded for the later Projects & Implementation Tracker workspace and must not be hidden behind the current Phase I stabilization work.
- The same Phase 1.5 audit found that `/projects` reports all active members and open risks but renders only `members.slice(0, 10)` and `risks.slice(0, 8)` from unbounded service reads. Replace those truncations with server-owned, scoped pagination and truthful page/empty/error states before the project workspace can pass SPF-008.
- Administration remains the next Phase I workspace after Overview stabilization. Its previously identified unbounded/hard-capped users, roles, organization, audit, request, and permission reads have received bounded URL-backed contracts and focused visible-state hardening through the DEC-0162–DEC-0218 checkpoints. Administration is still not complete: disposable PostgreSQL no-query/isolation/query-plan evidence, responsive browser/mobile verification, hosted recovery/deployment, production-authenticated E2E, and UAT remain open, and no server guard may be relaxed.

## 7. Later-Phase Boundaries

### Phase 2 — Restaurant Operations and Food Cost

Pending production work includes full regression, mobile operational verification, trusted sales/POS source validation, attachment adoption, training impact, and resolution of any formula or workflow defects found during the workspace pass. Recursive sub-recipe flattening and bulk mutation remain deferred unless separately approved.

### Phase 3 — Finance and Workforce

The current implementation is a controlled foundation. Production implementation remains pending for supplier-credit application, AP settlement, payment execution, bank effects/integration, official accounting consequences, cash-advance and petty-cash settlement, budget hard-block rollout/backfill, broader reconciliation exceptions, production close resolution, full workforce transfer approval, and production document retention.

Phase 3 must not be presented as the official accounting book of record until these controls and the required finance-owner validations are complete.

### Phase 4 — Expansion Projects

The workspace foundation remains subject to the shared attachment/storage, authorization, release-evidence, and workspace completion gates. Contractor portals, public links, advanced Gantt calculations, workload balancing, and custom automation builders remain intentionally out of scope unless separately approved.

## 8. Production Configuration Still Required

- Approval thresholds and authorized approvers
- Emergency-purchase ceiling and post-review deadline
- Evidence and attachment requirements by workflow, action, value, and risk
- Lot/expiry-controlled categories
- Stock-count frequency by location/category
- Opening inventory date and valuation method
- Supplier accreditation and blocked-supplier rules
- Retention, privacy, backup, and restore ownership
- Production evidence-storage provider, limits, scanning, and recovery policy
- Monitoring, incident response, and hypercare ownership

## 9. Related Registers and Source Documents

- `docs/core/07-quality/PHASE1_PHASE1_5_DEFERRED_GO_LIVE_BLOCKERS_FOR_UAT.md`
- `docs/core/07-quality/PHASE2_DEFERRED_GO_LIVE_BLOCKERS_FOR_UAT.md`
- `docs/core/07-quality/PHASE3_DEFERRED_GO_LIVE_BLOCKERS_FOR_UAT.md`
- `docs/core/00-governance/OPEN_DECISIONS_AND_ASSUMPTIONS.md`
- `docs/core/02-controls/SECURITY_AND_AUDIT_MODEL.md`
- `docs/core/03-data/ERP_DATA_DICTIONARY.md`
- `docs/core/04-design/UI_UX_WORKSPACE_AUDIT.md`
- `docs/core/05-technical/DEPLOYMENT_AND_ENVIRONMENT.md`
- `docs/core/07-quality/TEST_STRATEGY_AND_UAT_PLAN.md`

## 10. Maintenance Rule

Update this register only when implementation state, release scope, a confirmed production control, or a material blocker changes. Do not mark an item complete based only on data display, backend scaffolding, local demonstration, or unverified tests. Link completion evidence to the relevant workspace, test run, migration, release artifact, or decision record.
### DEC-0153 Purchase Order approved-recommendation lookup — July 25, 2026
- Replaced the eager all-record recommendation selector with a server-authorized lookup API and responsive composer. Search covers PR reference, supplier, and quote; results are deterministic, exact-count, 25-row pages with selected-option retention.
- Mutation remains protected by the existing transactional `createPurchaseOrderFromRecommendation` revalidation for tenant/company/location, approved status, approved PR, supplier status, and duplicate PO prevention.
- Evidence: `purchaseOrders.test.ts` 36/36 passed; web TypeScript check passed. Browser responsive, database query-plan, hosted deployment, and UAT evidence remain open. PO amendment TaskSheet and remaining visible/detail history gates are still pending.

### DEC-0154 Purchase Order amendment TaskSheet — July 25, 2026
- Moved the multi-line amendment request from `EntryModal` to the shared workspace `TaskSheet`, adding visible PO/company/location/supplier and approval/audit context while retaining the existing server action, eligibility guards, line ordering, and audit controls.
- Evidence: web TypeScript check passed. Responsive browser, database query-plan, hosted deployment, and UAT evidence remain open; denied/unavailable explanatory state and interactive unsaved-error coverage remain follow-up work.

### DEC-0155 Administration role assignment paging — July 25, 2026
- Role detail no longer hydrates every active assignment and every user scope. It now uses server-owned name/email search, exact-count deterministic 25-row paging, and at most eight active scope previews per returned user.
- Core Administration and selected-company Manage authorization remains before all reads and role mutation paths are unchanged.
- Evidence: Core Admin tests 30/30 and web typecheck passed. PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT gates remain open; Administration is not complete.

### DEC-0156 Administration User Access assigned-role paging — July 25, 2026
- User Access detail now uses distinct URL-backed assigned-role search/page state, exact totals, deterministic active-assignment ordering, safe tenant/system-role projections, role context, and pagination. Effective permissions are projected separately from the complete active-role graph, so paging cannot alter authority.
- Existing tenant-role, selected-company Manage, target-user, self-protection, mutation CAS, and audit controls remain authoritative. Scope assignments and request/audit subworkspaces remain separate follow-up contracts.
- Evidence: Core Admin tests 31/31 and web typecheck passed. PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT gates remain open; Administration is not complete.

### DEC-0157 Administration User Access scope page foundation — July 25, 2026
- Added a parameterized PostgreSQL CTE/UNION scope read contract joining each polymorphic target to tenant and selected-company ownership before type/search filtering, exact count, deterministic ordering, and bounded paging. The User Access page now consumes the bounded scope result with search/type controls and page state; existing mutation services remain authoritative.
- The visible scope register now uses one URL-selected deactivation composer, preserves filter/page return context, enforces the service’s five-character reason minimum in the UI, and shows a safe stale-selection message when the selected row leaves the filtered page.
- Evidence: Core Admin tests 32/32, web typecheck, and diff check pass. PostgreSQL isolation/query-plan execution, responsive browser, hosted recovery, and UAT remain open; Administration is not complete.
- Scope rows now expose explicit CURRENT/FUTURE/EXPIRED effective-state labels and end dates from the bounded projection. Stale out-of-range requests now clamp from a shared exact count before row retrieval; live PostgreSQL execution remains open.
- The detail service no longer eagerly hydrates all scope assignments; the bounded scope projection is the sole assigned-scope read, with action metadata derived per returned row and mutation revalidation preserved. PostgreSQL isolation/query-plan, responsive browser, hosted recovery, and UAT gates remain open.

### DEC-0159 Web lint gate repair — July 25, 2026
- Removed obsolete overflow state from the bounded Purchase Request line lookup editor. Web lint and TypeScript now both pass; focused Core Admin tests remain 32/32. Full browser, database, hosted, and UAT gates remain open.

### DEC-0160 Production candidate validation — July 25, 2026
- Next.js production build passes; web lint and TypeScript pass; Core Admin, Purchase Order, and module-preview focused suites pass (32/32, 36/36, and 16/16). The module-preview assertion now tracks the bounded Admin Settings page contract.
- PostgreSQL/authorization integration suites remain gated by required disposable authorization/database sentinels; responsive browser, hosted recovery, and UAT evidence remain open.

### DEC-0161 Administration User Access Requests subworkspace — July 25, 2026
- Added URL-backed `Requests` navigation with separate scope/role request kinds. The detail service loads only the active request kind, preserving tenant/company/target-user predicates, status filtering, exact totals, deterministic ordering, and bounded paging.
- Replaced repeated per-row approval/rejection modals with one selected-request composer. It preserves return context, shows pending-only narrative/evidence context, keeps historical rows read-only, and leaves existing authorization, MFA, segregation, CAS, and audit mutation services authoritative.
- Evidence: Core Admin focused tests 32/32, web TypeScript, lint, and diff checks pass. PostgreSQL authorization/isolation/query-plan, responsive browser, hosted recovery, and UAT remain open; User Access Overview/Roles/Scopes/Audit information architecture and Administration are not complete.

### DEC-0162 Administration User Access Audit subworkspace — July 25, 2026
- Added a real URL-backed Audit section using an exact actor-scoped wrapper around the existing redacted/keyset Admin Audit service. Target-user membership is checked before querying; Core Administration, tenant-role, selected-company Manage, cursor binding, deterministic ordering, exact totals, and sensitive-field redaction remain authoritative.
- Removed the unsafe eager ten-row `user.auditEvents` projection, added bounded search/cursor state and separately authorized event-detail links, and kept the User Access Audit surface read-only with no browser-supplied actor authority.
- Evidence: Core Admin tests 33/33, web TypeScript, lint, and diff checks pass. PostgreSQL no-query authorization/isolation/query-plan, responsive browser, hosted recovery, and UAT remain open; Overview/Roles/Scopes/Requests section isolation remains follow-up and Administration is not complete.

### DEC-0163 Administration User Access tab isolation — July 25, 2026
- User Access now renders Overview, Roles, Scopes, Requests, and Audit as mutually exclusive URL-backed visible sections. Overview retains identity/status, role summary, and effective permissions; Roles and Scopes retain their bounded registers and contextual actions; Requests and Audit remain the dedicated DEC-0161/0162 workspaces.
- Evidence: Core Admin tests 34/34, web TypeScript, lint, production build, and diff checks pass. Full removal of non-active-section server reads, PostgreSQL query-plan/no-query authorization evidence, responsive browser, hosted recovery, and UAT remain open; Administration and Phase I are not complete.

### DEC-0164 Administration User Access bounded request reads — July 25, 2026
- Non-Requests sections now pass `requestKind: "none"`, skipping both controlled-request count/list queries; Requests scope and role sections continue to load only their selected dataset. Existing callers without a kind retain compatibility behavior.
- Corrected Audit predicate composition so exact actor scoping survives free-text search and cannot leak another same-company actor’s event.
- Evidence: Core Admin tests 34/34, web TypeScript, lint, production build, and diff checks pass. Query-count/isolation, PostgreSQL, responsive browser, hosted recovery, and UAT remain open; role/scope/catalog non-active reads remain a separate follow-up.

### DEC-0165 Administration User Access section-aware catalogs — July 25, 2026
- Added an allowlisted section projection for assignment catalogs: Overview/Audit skip both, Roles loads roles, Scopes loads locations, and Requests loads only the catalog matching its selected kind. Omitted projection callers retain compatibility behavior. Assign Role is now visible only in Roles; selector surfaces remain section-specific.
- Evidence: Core Admin tests 34/34, web TypeScript, lint, production build, and diff checks pass. Query-count/isolation, PostgreSQL, responsive browser, hosted recovery, and UAT remain open; scope-page and role-page reads remain a separate follow-up.

### DEC-0166 Administration User Access scope-read selection — July 25, 2026
- The page now invokes the bounded polymorphic scope projection only for Overview, Scopes, and scope Requests. Roles, role Requests, and Audit use an explicit empty projection and display an unavailable scope KPI marker instead of running an irrelevant scope read.
- Evidence: Core Admin tests 34/34, web TypeScript, lint, production build, and diff checks pass. PostgreSQL query-count/isolation, responsive browser, hosted recovery, and UAT remain open; role-page/effective-permission shaping remains separate.

### DEC-0167 Administration User Access role-read selection — July 25, 2026
- The detail service now loads assigned-role rows, role totals, and effective-permission projections only for Overview and Roles. Role identifiers remain available for Roles and role Requests so the assignable-role catalog can exclude active assignments; non-role sections receive an explicit empty role projection.
- Overview/Roles retain their role and permission KPIs; Scopes, Requests, and Audit render an unavailable marker rather than implying a loaded count. Role mutation and authorization services remain unchanged.
- Evidence: Core Admin focused tests 34/34, web TypeScript, lint, production build, and diff checks pass. PostgreSQL query-count/isolation/query-plan, responsive browser, hosted recovery, and UAT remain open; Administration and Phase I are not complete.

### DEC-0168 Administration User Access role action composer — July 25, 2026
- Replaced repeated per-row role deactivation forms with one URL-selected role controls composer. Selection preserves the bounded assigned-role search/page context, shows role/status/consequence context, requires a five-character reason, and returns to the same filtered page after success or error.
- Stale, filtered-out, or non-mutable selections are explicitly read-only; the existing server mutation remains authoritative for tenant/company scope, self-protection, sensitive-role and approval-rule restrictions, CAS/locking, privilege epoch, and audit.
- Evidence: Core Admin focused tests 34/34, web TypeScript, lint, production build, and diff checks pass. PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT remain open; Administration and Phase I are not complete.

### DEC-0169 Administration User Access scope URL continuity — July 25, 2026
- Scope search, type filters, paging, selected controls, and deactivation return paths now use one canonical URL builder that retains `section=scopes` and active query/type/page context. Scope actions no longer fall back to Overview after navigation or mutation.
- Selection remains bounded to the current page and all authorization, selected-company, reason, concurrency, self-protection, and audit controls remain server-authoritative.
- Evidence: Core Admin focused tests 34/34, web TypeScript, lint, production build, and diff checks pass. PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT remain open; Administration and Phase I are not complete.

### DEC-0170 Administration Settings policy action composer — July 25, 2026
- Admin Settings now uses one URL-selected policy action composer instead of repeated per-row Configure and Use Recommended forms. The selected policy context preserves category, search, page, and page-size state, shows current/recommended values and type, and returns to the same bounded registry context after update or reset.
- `settingKey` is allowlisted display/navigation state only; existing policy services remain authoritative for selected-company Manage authorization, definition lookup, value validation, transaction, and audit. Update reasons require five characters; reset uses a separate short audited form.
- Evidence: Policy Settings focused tests 6/6, web TypeScript, lint, production build, and diff checks pass. PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT remain open; Administration and Phase I are not complete.

### DEC-0171 Administration Readiness selected UAT review sheet — July 25, 2026
- Selected UAT evidence Verify/Reject actions now use one focused TaskSheet with selected-record context, explicit status/outcome consequences, preserved filter/page fields, 44px controls, and a visible five-character rejection reason requirement. Inline competing review forms were removed.
- Existing selected-company authorization, creator self-review protection, `RECORDED` CAS, reason validation, audit, and generic unavailable behavior remain server-authoritative.
- Evidence: Release Readiness focused tests 15/15, web TypeScript, lint, production build, and diff checks pass. PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT execution evidence remain open; Readiness, Administration, and Phase I are not complete.

### DEC-0174 Administration Readiness enablement review sheet — July 25, 2026
- Selected enablement evidence Verify/Reject actions now use one focused TaskSheet with explicit training, knowledge-base, and release-note readiness consequences, preserved filter/page context, 44px controls, and visible rejection-reason validation.
- Existing selected-company authorization, creator self-review protection, `RECORDED` CAS, reason validation, audit, and no-direct-gate-mutation semantics remain server-authoritative.
- Evidence: Release Readiness focused tests 15/15, web TypeScript, lint, production build, and diff checks pass. PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT execution evidence remain open; Readiness, Administration, and Phase I are not complete.

### DEC-0175 Administration Readiness gate update sheet — July 25, 2026
- Readiness gate updates now use one URL-selected workspace TaskSheet with preserved category/search/status/page/page-size context, 44px controls, and safe unavailable handling for stale or foreign keys. Repeated per-row Update Gate modals were removed.
- Existing selected-company Core Administration Manage authorization, transactional re-read/CAS, status-transition and required evidence/decision-note/blocker validation, audit, and no-direct-GO/NO-GO mutation semantics remain server-authoritative. Deployment and Enablement capture-sheet parity remains the immediate follow-up; Readiness is not complete.
- Evidence: Release Readiness focused tests 15/15, web TypeScript, lint, production build, and diff checks pass. PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT execution evidence remain open; Readiness, Administration, and Phase I are not complete.

### DEC-0176 Administration Readiness deployment and enablement capture parity — July 25, 2026
- Deployment and Enablement Record Evidence workflows now use workspace TaskSheets with contained scrolling, 44px controls, and preserved filter/page context through normalized success and validation-error redirects. The previous long centered modals were removed; UAT capture remains on the same pattern.
- Existing selected-company/Core Administration Manage authorization, UTC/value/required-field validation, reason/audit behavior, and no-direct-gate-mutation semantics remain server-authoritative.
- Evidence: Release Readiness focused tests 15/15, web TypeScript, lint, production build, and diff checks pass. PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT execution evidence remain open; Readiness, Administration, and Phase I are not complete.

### DEC-0177 Administration Readiness Release Board canonical authority — July 25, 2026
- The main Readiness register no longer renders a duplicate Release Board decision creation modal. It now links to the dedicated `/admin/readiness/release-board` composer as the single GO/NO-GO decision authority; selected decision detail remains read-only and context-preserving.
- Dedicated Release Board authorization, readiness eligibility, UTC/required validation, append-only audit, and no-direct-gate-mutation semantics remain authoritative. Administrator training now points to the dedicated workspace.
- Evidence: Release Readiness focused tests 15/15, web TypeScript, lint, production build, and diff checks pass. PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT execution evidence remain open; Readiness, Administration, and Phase I are not complete.

### DEC-0178 Administration Release Board UTC date filters — July 25, 2026
- Release Board history now supports inclusive UTC `decidedFrom`/`decidedTo` filters with strict date validation, reversed/over-366-day fail-safe handling, exact count/page parity, and preserved search/decision/page/page-size/selected-detail/create-action context.
- Existing selected-company Core Administration Manage authorization, append-only decision semantics, deterministic ordering, and no-direct-gate-mutation behavior remain authoritative. Export and richer activity history remain later slices.
- Evidence: Release Readiness focused tests 15/15, web TypeScript, lint, production build, and diff checks pass. PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT execution evidence remain open; Readiness, Administration, and Phase I are not complete.

### DEC-0179 Core Administration active-tab projection and compact shell — July 25, 2026
- Core Administration now passes an allowlisted active-tab profile to the overview service. Users, Roles, Organization, Approval Rules, and Audit load only the selected register and required option catalogs; inactive sections no longer hydrate their registries or audit events.
- The card-heavy workspace selector is replaced by shared URL-backed `WorkspaceTabs` with 44px targets and an explicit inactive-view disclosure. Existing authorization, pagination, filters, create actions, and audit keyset service remain authoritative.
- Evidence: Core Admin focused tests 35/35, web TypeScript, lint, production build, and diff checks pass. PostgreSQL no-query/query-plan, responsive browser, hosted recovery, and UAT evidence remain open; Administration and Phase I are not complete.

### DEC-0172 Administration Readiness selected Deployment review sheet — July 25, 2026
- Selected Deployment Evidence Verify/Reject actions now use one focused TaskSheet with explicit migration/restore/rollback/monitoring readiness consequences, preserved filter/page context, 44px controls, and visible rejection-reason validation. Inline competing review forms were removed.
- Existing selected-company authorization, creator self-review protection, `RECORDED` CAS, reason validation, audit, generic unavailable behavior, and no-direct-gate-mutation semantics remain server-authoritative.
- Evidence: Release Readiness focused tests 15/15, web TypeScript, lint, production build, and diff checks pass. PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT execution evidence remain open; Readiness, Administration, and Phase I are not complete.

### DEC-0173 Administration Readiness UAT evidence capture sheet — July 25, 2026
- The long Record UAT Evidence workflow now uses a shared workspace TaskSheet with 44px controls instead of a centered modal. It retains all evidence fields and preserves UAT filter/page context through normalized success and error redirects.
- Existing selected-company/Core Admin Manage authorization, UTC/value/required-field validation, audit, and no-direct-gate-transition semantics remain authoritative; hidden context is navigation state only. Enablement selected review parity remains next.
- Evidence: Release Readiness focused tests 15/15, web TypeScript, lint, production build, and diff checks pass. PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT execution evidence remain open; Readiness, Administration, and Phase I are not complete.

### DEC-0180 Item Master active-tab projection and compact shell — July 25, 2026
- Item Master now passes an allowlisted active-tab profile to its service. Items load only the item register and required catalogs; Categories, UOMs, and Conversions load only their selected register and counts, with selected-record reads scoped to the active tab.
- The card-heavy register selector is replaced by shared URL-backed `WorkspaceTabs` with 44px targets and an explicit inactive-register disclosure. Existing company authorization, pagination, filters, selected composers, and mutation services remain authoritative.
- Evidence: Item Master focused tests 9/9, web TypeScript, lint, production build, and diff checks pass. PostgreSQL no-query/query-plan, responsive browser, hosted recovery, and UAT evidence remain open; Master Data, Administration, and Phase I are not complete.

### DEC-0181 Supplier catalog selected item-link action composer — July 25, 2026
- Active supplier-item catalog rows now use one URL-selected TaskSheet for deactivation. The selected link context preserves supplier/catalog filters and page, shows item/UOM/status context, validates a five-character reason, and gives a generic unavailable state for stale or foreign selections.
- The existing server mutation remains authoritative for tenant/company scope, active-link re-read, non-destructive status change, audit, and history retention; no procurement or inventory state is changed.
- Evidence: Supplier focused tests, web TypeScript, lint, production build, and diff checks must pass before this slice is committed. PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT evidence remain open; Supplier Master Data, Administration, and Phase I are not complete. Supplier workspace tabs and broader visible IA remain pending.

### DEC-0182 Supplier selected workspace tabs and active projection — July 25, 2026
- Selected Supplier work now uses mutually exclusive URL-backed Overview, Catalog, Accreditation, and Audit sections. Only Catalog loads the paginated item-link register; Catalog retains DEC-0181 controls, Accreditation owns supplier review actions, and Audit is an explicit bounded Admin Audit handoff rather than raw event output.
- Supplier register filters/page, catalog filters/page, selected IDs, and action return context remain preserved. Existing tenant/company authorization, reason/audit, non-destructive status changes, and no-procurement/inventory-side-effect controls remain authoritative.
- Evidence: Supplier focused tests 4/4, web TypeScript, lint, production build, and diff checks must pass before commit. PostgreSQL query-plan/authorization, responsive browser, hosted recovery, and UAT evidence remain open; Supplier Master Data, Administration, and Phase I are not complete. A supplier-specific Audit projection remains a follow-up.

### DEC-0183 Supplier Audit handoff correctness — July 25, 2026
- Supplier Audit now links to the real `/admin?tab=audit` workspace with an exact UUID `entityId` filter. Core Admin applies the entity predicate together with existing tenant/company/authorization predicates; cursor hashes, pagination, CSV export, and event-detail return links preserve the filter.
- The handoff remains read-only and redacted; no supplier-local duplicate audit authority or procurement/inventory mutation was introduced. Invalid or foreign entity IDs cannot widen the result set.
- The supplier-local projection remains intentionally deferred: the exact Core Admin handoff is the approved audit source of truth, and a second projection would duplicate cursor, redaction, export, and authorization semantics before UAT identifies a concrete local-context need.
- Evidence: Core Admin and Supplier focused tests 39/39, web TypeScript, lint, production build, and diff checks pass. PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT evidence remain open; Supplier Master Data, Administration, and Phase I are not complete.

### DEC-0184 Administration User Access controlled TaskSheets — July 25, 2026
- Evidence-heavy controlled Scope Request, controlled Role Request, and selected request review now use contained-scroll `TaskSheet`s with 44px controls, explicit target/request/status/evidence context, stale/unavailable handling, and preserved request section/kind/page return context.
- Existing server-side authorization, no-self-approval, MFA/SOD, pending-only CAS, reason/evidence, audit, and mutation semantics remain authoritative. Short assignment/deactivation forms remain separate.
- Evidence: Core Admin focused tests 35/35, web TypeScript, lint, production build, and diff checks pass. PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT evidence remain open; Administration and Phase I are not complete.

### DEC-0185 Core Administration duplicate Audit read cleanup — July 25, 2026
- Removed the unused `recentAuditEvents` query from the active Audit overview profile; the bounded keyset Audit register is now the sole visible Audit read while the compatibility field returns an empty projection.
- Evidence: Core Admin focused tests 35/35, web TypeScript, lint, production build, and diff checks pass. PostgreSQL no-query/query-plan, responsive browser, hosted recovery, and UAT evidence remain open; Administration and Phase I are not complete.

### DEC-0186 Core Administration Organization Scope tabs — July 25, 2026
- Organization Scope now uses URL-backed Companies / Summary, Brands, Departments, and Locations sections. Only the selected registry is rendered and its bounded page read is loaded; inactive organization registers are not hydrated.
- Existing tenant/company authorization, deterministic pagination, filters, bounded location brand catalog, create actions, and scope predicates remain authoritative. Nested section/filter/page URLs preserve context.
- Evidence: Core Admin focused tests 35/35, web TypeScript, lint, production build, and diff checks pass. PostgreSQL no-query/query-plan, responsive browser, hosted recovery, and UAT evidence remain open; Administration and Phase I are not complete.

### DEC-0187 Shared authorization manifest lookup-route checkpoint — July 25, 2026
- Classified the protected Item Master option-catalog, approved-recommendations, and purchase-request draft-lookup GET routes as bounded, service-enforced, tenant/company(/location) scoped read surfaces. The Item Master route records stable 400/401/403/503 denial behavior, private no-store delivery, selected-company Manage/Core Administration authorization, and no Item/Category/UOM/Audit mutation controls.
- Regenerated `scripts/authorization-surface-baseline.json` so the lookup routes and newly exported bounded read services are represented. No user-facing workflow behavior changed; DEC-0140 and existing Item Master enablement remain authoritative.
- Evidence: authorization manifest 20/20 tests pass after adding the four explicit admin-platform boundary bindings and extending the PostgreSQL integration case to invoke the user-audit and three release-readiness page readers under live permission revocation. A dedicated API lookup-route test now covers live permission denial, malformed input, stable errors, and no audit mutation. The full web suite reports 1,321 passing, 301 skipped, and 1 todo across 131 files. The executable PostgreSQL gates remain open because `DISPOSABLE_DATABASE_ADMIN_URL` is not configured in this environment; API isolation/no-mutation execution and browser/hosted/UAT gates remain required. Administration and Phase I are not complete.

### DEC-0188 SPF-004 exact-candidate revalidation — July 25, 2026
- Reopened SPF-004 for the current release candidate. The previous hosted closure remains historical evidence for exact SHA `60690ddd41c12fa31dbdeb3fdec6ebfc8a90f170`; later protected API classifications, lookup behavior, and Administration/Readiness reader bindings require exact-candidate reruns.

### DEC-0189 Supplier catalog category option paging — July 25, 2026
- Replaced the unbounded supplier-category option read with a tenant/company/supplier-scoped, deterministically ordered, bounded service page. The Catalog UI preserves selected-category scope, exposes refinement and previous/next option controls, and keeps item-link pagination independent. No schema or public API migration is required; focused UI/type/test validation and PostgreSQL high-cardinality evidence remain part of the open shared gates.

### DEC-0190 Supplier catalog option contract gate — July 25, 2026
- Added executable supplier service/UI assertions for bounded category page size, refinement length, deterministic ordering, selected-category retention, option pagination controls, preserved URL context, and removal of the prior unbounded category-count materialization. Focused supplier tests, typecheck, lint, and production build pass; disposable PostgreSQL query-plan, responsive browser, hosted recovery, and UAT evidence remain open.

### DEC-0191 Role Detail permission matrix paging — July 25, 2026
- Replaced the unbounded tenant/global permission catalog on Role Detail with a bounded server page, code/action search, and allowlisted Sensitive/Overrides/Recommended Drift filters. Complete enabled permission codes remain separately available for hidden-input preservation during mutation, while visible group counts are labeled page-local. Follow-up hardening keeps role-level drift/sensitive/user totals invariant across matrix pages and preserves assignment/filter return context; filtered empty states are explicit. Core Administration tests 36/36, full web suite 1,323 passing, typecheck, lint, and production build pass; PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT evidence remain open.

### DEC-0192 Permission Access summary truth — July 25, 2026
- Permission Access now uses the exact server `rolesPage.totalRoles` for the Granting roles KPI and explicitly labels the bounded five-user-per-role view as Preview users shown on this page. Filtered-empty search states, clear-search recovery, and bounded 120-character query handling are explicit. The UI no longer implies an exhaustive effective-user total; the existing tenant/company scope and active-date preview predicates remain authoritative. Focused Core Administration tests, typecheck, lint, and production build remain required; PostgreSQL, browser, hosted, and UAT gates remain open.
- Role-level user previews are now collapsed by default behind keyboard-accessible disclosures, preserving granting-role context while removing the expanded nested-card stack. A separate exhaustive Preview Users list remains deferred until an access-review policy defines deduplication and effective-access semantics.
- Current status is **Reopened — exact-candidate revalidation pending**. Disposable PostgreSQL permission/isolation/no-mutation execution, hosted exact-SHA build/database/manifest evidence, and accepted fixture browser evidence remain required. Production-mode `next start` E2E stays assigned to SPF-001/SPF-009.
### DEC-0193 Break-Glass bounded queue and selected actions — July 25, 2026
- Replaced the silent 50-row emergency-access register cap with a tenant/company-authorized exact-count page, deterministic ordering, status/reason/evidence/target-user/location filters, clamped URL state, and bounded target catalogs. Target-user mutation rechecks active/effective selected-company or requested-location membership server-side.
- Replaced repeated per-row lifecycle modals with a selected-record TaskSheet for approve, reject, revoke, and post-review. Lifecycle and expiry transactions claim expected status before side effects to prevent duplicate assignment/audit transitions.
- Evidence: focused Break-Glass test 1/1, web TypeScript, lint, and production build pass. Disposable PostgreSQL authorization/query-plan, responsive browser, hosted recovery, and UAT evidence remain open; Administration and Phase I are not complete.

### DEC-0194 Session Invalidation bounded queue and selected completion — July 25, 2026
- Replaced the silent 100-row Session Invalidation cap with exact-count server paging, deterministic ordering, status/search/inclusive UTC date filters, stale-page clamping, explicit projections, and truthful tenant-wide versus selected-company scope labels.
- Replaced repeated completion modals with a separately authorized selected-record TaskSheet. Only pending provider records expose completion; foreign, stale, and terminal selections are read-only/unavailable while existing pending-only CAS, no-self completion, provider evidence, and audit controls remain authoritative.
- Evidence: focused auth-invalidation test 1/1, web TypeScript, lint, and production build pass. PostgreSQL isolation/query-plan, responsive browser, hosted recovery, and UAT evidence remain open; Administration and Phase I are not complete.

### DEC-0195 MFA Enrollment effective bounded queue — July 25, 2026
- Replaced full privileged-user role/permission/enrollment hydration with effective company-scope derivation, deterministic bounded paging, exact database-derived status totals, bounded target options, search, and `NOT_RECORDED`/pending/verified/revoked filters. Future, expired, inactive, and foreign-tenant role/permission links are excluded.
- Replaced repeated row actions with a separately authorized selected-record TaskSheet. Evidence recording is transactional with audit; verify/revoke use expected-status CAS while no-self and privileged-target checks remain server-authoritative.
- Evidence: focused MFA test 1/1, web TypeScript, lint, and production build pass. PostgreSQL effective-date/isolation/query-plan, responsive browser, hosted recovery, and UAT evidence remain open; Administration and Phase I are not complete.

### DEC-0196 Reason Codes selected deactivation composer — July 25, 2026
- Reason Codes retains the DEC-0124 bounded selected-company register, while active rows now open one URL-selected TaskSheet for deactivation. Filter/search/page context is preserved and stale or foreign selections remain unavailable/read-only.
- Deactivation now claims the expected `ACTIVE` state with a scoped atomic update before the audit event, preventing duplicate concurrent transitions while retaining historical references. Authentication Recovery is next.
- Evidence: focused Reason Codes test, web TypeScript, lint, and production build pass. PostgreSQL authorization/concurrency/query-plan, responsive browser, hosted recovery, and UAT evidence remain open; Administration and Phase I are not complete.

### DEC-0197 Authentication Recovery bounded queue — July 25, 2026
- Recovery is now the active Authentication section with server-owned status/search/date filters, exact-count paging, deterministic safe projections, selected-request detail, and one TaskSheet for independent approve/reject actions. Terminal history remains visible and read-only; sibling activation/account sections are not hydrated in this slice.
- Request and approval fail closed for targets without an active local identity. Approval retains current-scope revalidation and all existing MFA, separation, CAS, session/MFA revocation, activation-delivery, and audit controls; rejection is non-granting cleanup for company-scoped pending records.
- Evidence: web TypeScript pass; focused Authentication contract test, lint, and production build are required before commit. PostgreSQL authorization/concurrency/query-plan, responsive browser, hosted recovery, and UAT evidence remain open; Administration and Phase I are not complete.
- Hardening checkpoint: the request selector now uses a bounded selected-company active-local-identity catalog with search, overflow disclosure, selected retention, and an in-transaction identity recheck. Invalid queue filters fail closed with a visible explanation; selected rows expose company context, timezone-rendered creation/review timestamps, and an audit handoff. Focused contract test 1/1, TypeScript, lint, and production build pass. The request composer remains inline and should move behind a focused TaskSheet before UAT; PostgreSQL, responsive browser, hosted recovery, and UAT gates remain open.
- Composer checkpoint: request creation now uses a focused TaskSheet trigger; the queue remains a separate paginated/read-only history surface. The selected review TaskSheet and request composer both retain server authorization, CSRF, identity/scope checks, evidence requirements, and audit-backed mutations. Focused contract test 1/1, TypeScript, lint, and production build pass; PostgreSQL, responsive browser, hosted recovery, and UAT gates remain open.
- Shared manifest checkpoint: regenerated `scripts/authorization-surface-baseline.json` for the three selected Authentication Recovery read services and removed stale server-action entries. The focused authorization-manifest suite passes 20/20. The full web suite currently reports 1,323 passing, 311 skipped, and 13 integration files blocked by the missing disposable authorization database / localhost PostgreSQL; Authentication Recovery remains conditional and Phase I is not complete.
- UX-state checkpoint: unknown recovery intents now fail closed, known action failures map to safe actionable feedback, invalid/filtered-empty states expose recovery paths, and the route includes an accessible loading skeleton. Selected detail timestamps render in `Asia/Manila`; browser pending/denied/error and production authenticated E2E evidence remain open.
- Gate execution checkpoint: disposable PostgreSQL lifecycle safety passes 15/15, but the actual disposable authorization suites cannot start because this environment has no `DISPOSABLE_DATABASE_ADMIN_URL` and Docker is unavailable in WSL. This is an execution blocker only; no database gate is waived.

### DEC-0198 User Access truth and continuity hardening — July 25, 2026
- User Access detail now shows exact assigned-role totals from the server page count, renders assignment/request/audit timestamps in `Asia/Manila`, distinguishes filtered-empty role/scope results, and includes selected-company context in the detail header.
- Controlled pending-role permission previews are bounded to seven server-hydrated permissions; the UI states that the preview is capped. Pending rows expose the truthful next action (eligible second-admin review or terminal outcome), while historical sensitive permission details remain redacted.
- Assignment and controlled-request forms preserve the selected section/filter/page return path through validated hidden navigation state. Controlled request TaskSheet triggers now use non-interactive spans to avoid nested buttons, and action targets meet the 44px mobile minimum.
- Evidence: Core Admin focused tests 36/36, web TypeScript, lint, and production build pass. High-cardinality exclusion catalogs, exhaustive effective-permission presentation, responsive browser/mobile, PostgreSQL authorization/query-plan, hosted recovery, and UAT evidence remain open; User Access, Administration, and Phase I are not complete.

### DEC-0199 User Access high-cardinality eligibility catalogs — July 25, 2026
- Parent-led deliberation selected the smallest reversible server-owned catalog contract: independent bounded direct-role, controlled-role, low-risk-location, and high-risk-location predicates with exact totals, deterministic ordering, active-assignment/pending-request anti-filters, and explicit overflow refinement guidance. The alternative of page-local UI exclusion was rejected because it exposes invalid options after the first history page; full catalog subworkspaces remain deferred.
- User Access now replaces page-local location/role exclusion with relational or SQL `NOT EXISTS` predicates before `LIMIT 100`; direct and controlled location catalogs are separate, direct and sensitive role catalogs are separate, custom permission codes containing `approve`, `post`, or `reverse` match the existing sensitivity fallback, and role-assignment ID lists plus nested catalog permission hydration are removed. Effective permissions use authorization-parity predicates (active role, assignment status, and effective start/end dates), a deterministic bounded preview with an exact total, and each preview badge links to the existing authorized Permission Access detail route for the complete definition; controlled request previews expose exact permission totals for truthful caps.
- Request history pages clamp stale page numbers to filtered totals. Catalog searches preserve section/request-kind context, controlled catalog overflow is disclosed, and status-filtered empty histories offer clear-filter recovery. Core Administration role-permission policy remains authoritative; the catalog now excludes roles referenced by active tenant approval-rule steps using the same role-id/active-rule predicate enforced by mutation services, despite the absence of a Prisma relation for that join.
- Evidence: Core Admin focused tests 36/36 and the non-disposable web suite 1,324 passed / 301 skipped / 1 todo across 122 passed and 10 skipped files; web TypeScript, lint, and production build pass. Disposable PostgreSQL authorization, duplicate/pending/high-cardinality fixtures, approval-rule parity execution, effective-permission complete-detail UAT, responsive browser/mobile, hosted recovery, and final UAT evidence remain open. User Access, Administration, and Phase I are not complete.

### DEC-0200 User Access complete effective-permission register — July 25, 2026
- Parent-led deliberation with independent Product and Architecture review selected a read-only, URL-backed effective-permission register inside User Access → Roles. The Overview remains a bounded preview; Roles now provides the complete selected-user union with server-owned search, exact filtered totals, deterministic `code ASC, id ASC` ordering, stale-page clamping, clear-search recovery, and 10–100 row bounds. The union uses active tenant/global roles and effective assignment dates, while the Assigned Roles lifecycle register remains separate and can still show scheduled/ended active assignments.
- Each register permission links to the authorized read-only Permission Access definition/granting-role view. No register control mutates role assignments or permissions, and non-Roles sections do not load the register query. Overview-to-Roles navigation discloses the preview cap and preserves selected-user/company context.
- Focused Core Admin tests pass 36/36; web typecheck, lint, and production build pass. The full non-disposable web run reports 1,324 passing, 311 skipped, and 1 todo; its 12 integration suites remain environment-blocked by the missing authorization database sentinel/local PostgreSQL. Disposable PostgreSQL >25-permission overlap/deduplication, future/expired/inactive/cross-tenant fixtures, query-plan/count-page parity, responsive browser/mobile, hosted recovery, and UAT evidence remain open. User Access, Administration, and Phase I are not complete.
- Requested Code Spark/GPT-5.4 deliberators were unavailable; the closest permitted GPT-5.6 fallback was used and recorded. Glossary, Users/Roles UI specification, administration knowledge-base guidance, and release notes now describe the complete register and preserve the Permission Access distinction.

### DEC-0202 User Access effective-register PostgreSQL fixture coverage — July 25, 2026
- Added a disposable-PostgreSQL authorization fixture covering 32 effective permissions across overlapping tenant roles and a global role, plus future, expired, inactive-assignment, inactive-role, and cross-tenant exclusions. The test traverses deterministic pages, verifies deduplication, separates unfiltered `permissionTotal` from filtered page totals, exercises search/no-match/clear behavior, and clamps stale pages. It is wired into the existing Admin Platform authorization command and performs relation cleanup after execution.
- The test file is discovered and skipped safely when the authorization integration sentinel is absent; in this environment it cannot provide behavioral evidence because `AUTHORIZATION_DATABASE_INTEGRATION`/disposable PostgreSQL are unavailable. Typecheck, lint, production build, and the skipped-test discovery pass; no PostgreSQL gate is waived.

### DEC-0201 User Access effective-register context continuity — July 25, 2026
- Overview’s `Open effective permission list` handoff now carries the current assigned-role page and search context into Roles, while effective-permission search and pagination retain that context through URL state. This keeps the selected-user access review navigable without changing server authority or permission semantics.
- Focused Core Admin tests, web typecheck, lint, production build, and diff check remain the applicable local gates; PostgreSQL, responsive browser/mobile, hosted recovery, and UAT evidence remain open.

### DEC-0203 Administration Location Detail assigned-access paging — July 25, 2026
- Parent-led deliberation with independent Product and Architecture review selected a bounded, read-only, assignment-grain Assigned Access register for Location Context. The service preserves Core Administration, tenant-role administration, selected-company Manage, and tenant/company location authorization, validates malformed IDs as not-found, applies active/effective assignment and active current-tenant user predicates, and limits nested role labels to active/effective tenant or global roles.
- Location Context now reports an exact request-time assignment total and URL-backed 10–100 row pages with finite/capped inputs, stale-page clamping, deterministic `userId ASC, assignmentId ASC` ordering, explicit assignment-grain labeling, safe projection, empty state, and responsive pagination. It does not mutate access; User Access remains authoritative for grants/revocations.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0203-ADMIN-LOCATION-DETAIL-ASSIGNED-ACCESS-PAGING.md`. Users/Roles and Location Context UI guidance, glossary, administration knowledge-base guidance, and release note were updated. Requested Code Spark/GPT-5.4 deliberators were unavailable; closest permitted GPT-5.6 fallback was used and recorded.
- Evidence: focused Core Admin contract tests, web typecheck, lint, production build, and diff checks are the applicable local gates. Disposable PostgreSQL tenant/foreign-user/effective-date/query-plan execution, responsive browser/mobile, hosted recovery/deployment, and UAT evidence remain open; Administration and Phase I are not complete.

### DEC-0204 Administration Company Context bounded access and registry handoff — July 25, 2026
- Parent-led deliberation with independent Product and Architecture review selected a read-only Company Context contract that removes hidden relation hydration and bounds the visible Company Access register. The service preserves Core Administration, tenant-role administration, selected-company Manage, and tenant/company authorization, validates malformed IDs as not-found, and applies active/effective company-assignment plus active current-tenant user predicates with active/effective tenant or global role previews.
- Company Context now derives explicit brand/location relation counts, reports assignment-grain Company Access totals, supports bounded name/email search, finite 10–100 URL-backed pages, deterministic `userId ASC, assignmentId ASC` ordering, stale-page clamping, timezone-rendered assignment timestamps, and filtered-empty/empty states. Brands and Locations are a truthful handoff to Organization Scope; Approval Rules are not silently hydrated or represented as an incomplete panel. User Access, Organization Scope, and Approval Rules remain authoritative for mutations and detailed registers.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0204-ADMIN-COMPANY-CONTEXT-BOUNDED-ACCESS.md`. Tenant Admin UI guidance, glossary, administration knowledge-base guidance, and release note were updated. Requested Code Spark/GPT-5.4 deliberators were unavailable; closest permitted GPT-5.6 fallback was used and recorded.
- Evidence: Core Admin contract tests pass 38/38 and web typecheck passes. Lint, production build, diff checks, disposable PostgreSQL tenant/foreign-user/effective-date/query-plan evidence, responsive browser/mobile, hosted recovery/deployment, and UAT remain applicable gates; Administration and Phase I are not complete.

### DEC-0205 Administration Approval Rule detail bounded registers — July 25, 2026
- Parent-led deliberation with independent Product and Architecture review selected separate bounded, read-only Approval Steps and Related Audit Activity registers. The existing Core Administration, selected-company Manage, tenant role authority for tenant-wide rules, and tenant/company audit scope remain authoritative; malformed rule IDs fail closed.
- Approval Steps now use exact totals, URL-backed 10–100 pages, deterministic `stepOrder ASC, id ASC`, current-page-only tenant/global role and tenant-user projections, and explicit missing/inactive assignee status. Related Audit Activity now uses exact totals, URL-backed 10–100 pages, deterministic `occurredAt DESC, id DESC`, selected-company scope, and allowlisted actor/entity fields. Both panels render truthful totals, empty states, responsive pagination, and company-timezone timestamps; no mutation authority was added.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0205-ADMIN-APPROVAL-RULE-DETAIL-BOUNDED-REGISTERS.md`. Approval Rules UI guidance, glossary/KB guidance, and release note were updated. Requested Code Spark/GPT-5.4 deliberators were unavailable; closest permitted GPT-5.6 fallback was used and recorded.
- Evidence: Core Admin contract tests pass 39/39 and web typecheck passes. Lint, production build, diff checks, disposable PostgreSQL tenant/global-rule/audit isolation and query-plan evidence, responsive browser/mobile, hosted recovery/deployment, and UAT remain applicable gates; Administration and Phase I are not complete.

### DEC-0206 Administration Audit Event bounded detail projection — July 25, 2026
- Parent-led deliberation with independent Product and Architecture review selected a bounded, read-only Audit Event detail projection. Existing Core Administration, tenant-role audit authority, selected-company Manage, and tenant/company/global audit predicates remain authoritative; malformed event IDs fail closed before the database lookup.
- Audit detail now uses explicit tenant-safe actor/company projections, retains recursive sensitive-key redaction, and applies depth, node, and serialized-byte budgets with a visible truncation notice. Stored immutable JSON is never changed. Event timestamps render in company timezone, JSON panels have contained scrolling, and audit IDs/correction guidance remain visible.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0206-ADMIN-AUDIT-EVENT-BOUNDED-DETAIL.md`. Audit UI guidance, glossary/KB guidance, and release note were updated. Requested Code Spark/GPT-5.4 deliberators were unavailable; closest permitted GPT-5.6 fallback was used and recorded.
- Evidence: Core Admin contract tests pass 40/40; web typecheck, lint, production build, and diff checks pass. Disposable PostgreSQL tenant/cross-company/no-query and query-plan evidence, responsive browser/mobile, hosted recovery/deployment, and UAT remain applicable gates; Administration and Phase I are not complete.

### DEC-0207 Administration Role Detail effective assignment previews — July 25, 2026
- Parent-led deliberation with independent Product and Architecture review selected effective-date hardening for the bounded Role Detail Assigned Users register. Malformed role IDs fail closed; assignment count/page predicates now require active/effective assignments, active users in the current tenant, and active tenant-local/global roles. The existing tenant-role administration and selected-company Manage guards remain authoritative.
- Nested scope context is a selected-company, active/effective preview with explicit 8-row per-user cap, bounded 1,001-ID catalog reads, deterministic ordering, and truthful capped-preview wording. It is contextual only; User Access and source authorization remain authoritative. Assignment timestamps render in the selected company timezone.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0207-ADMIN-ROLE-DETAIL-EFFECTIVE-PREVIEWS.md`. Role UI guidance, glossary/KB guidance, and release note were updated. Requested Code Spark/GPT-5.4 deliberators were unavailable; closest permitted GPT-5.6 fallback was used and recorded.
- Evidence: Core Admin contract tests pass 41/41; web typecheck, lint, production build, and diff checks pass. Disposable PostgreSQL effective-date/tenant/foreign-scope/high-cardinality and query-plan evidence, responsive browser/mobile, hosted recovery/deployment, and UAT remain applicable gates; Administration and Phase I are not complete.

### DEC-0208 User Access Assigned Role Lifecycle truthfulness — July 25, 2026
- Parent-led deliberation with independent Product and Architecture review confirmed that User Access → Assigned Roles intentionally remains a status-active lifecycle register, distinct from the current effective-permission register. Scheduled and ended assignments remain visible for revocation and audit context; no date predicate was added to this lifecycle query.
- The UI now labels the surface Assigned Role Lifecycle, explains that FUTURE and EXPIRED records remain visible and are not current effective permissions, and separates `Assignment: CURRENT/FUTURE/EXPIRED` from `Role: ACTIVE/INACTIVE`. End dates remain visible and existing server-authorized deactivation/CAS controls are unchanged.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0208-ADMIN-USER-ROLE-LIFECYCLE-TRUTH.md`. User Roles UI guidance, `reviewing-user-assigned-roles.md`, glossary, and release note were updated. Requested Code Spark/GPT-5.4 deliberators were unavailable; closest permitted GPT-5.6 fallback was used and recorded.
- Evidence: Core Admin contract tests pass 42/42 and web typecheck passes. Lint, production build, diff checks, disposable PostgreSQL lifecycle/effective-permission isolation, responsive browser/mobile, hosted recovery/deployment, and UAT remain applicable gates; Administration and Phase I are not complete.

### DEC-0209 Core Administration Users effective-role preview — July 25, 2026
- Parent-led deliberation with independent Product and Architecture review selected a bounded, read-only current-access preview for the Users registry. Role assignments are filtered by one captured effective timestamp, active assignment status, active role status, and tenant-local/global role ownership. Inactive users do not present roles as current.
- The registry now selects only explicit user fields and a per-user role preview bounded to eight names plus one cap probe, shows `8+ roles` when capped, clamps stale requested pages, and no longer hydrates unvalidated scope IDs. Full scope review remains on User Access detail; no authority was added.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0209-ADMIN-USERS-EFFECTIVE-ROLE-PREVIEW.md`. Users UI guidance, glossary/KB guidance, and release note were updated. Requested Code Spark/GPT-5.4 deliberators were unavailable; closest permitted GPT-5.6 fallback was used and recorded.
- Evidence: Core Admin contract tests pass 42/42 and web typecheck passes. Lint, production build, diff checks, disposable PostgreSQL tenant/high-cardinality/query-plan evidence, responsive browser/mobile, hosted recovery/deployment, and UAT remain applicable gates; Administration and Phase I are not complete.

### DEC-0210 Core Administration Role Library tenant-safe preview — July 25, 2026
- Parent-led deliberation with independent Product and Architecture review selected a bounded Role Library hardening slice before Organization Scope. The three-permission informational preview and per-role permission count now use explicit tenant-local/global permission predicates; high-access counts use the same predicate. The Role Detail route remains authoritative for complete permissions and mutations.
- Filtered stale role pages now clamp to the current last page while preserving deterministic name/ID ordering, bounded search/status filters, tenant-role authority, and selected-company Manage guards. No new mutation authority or public API was added.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0210-ADMIN-ROLE-LIBRARY-TENANT-SAFE-PREVIEW.md`. The existing Users/Roles UI specification already documents the preview as informational; the decision index and release evidence were updated. Requested Code Spark/GPT-5.4 deliberators were unavailable; closest permitted GPT-5.6 fallback was used and recorded.
- Evidence: Core Admin contract tests pass 42/42 and web typecheck passes. Lint, production build, diff checks, disposable PostgreSQL permission-isolation/query-plan evidence, responsive browser/mobile, hosted recovery/deployment, and UAT remain applicable gates; Administration and Phase I are not complete.

### DEC-0211 Core Administration Organization Scope selected-company context — July 25, 2026
- Parent-led deliberation with independent Product and Architecture review selected a reversible visible-context hardening slice. Organization Scope now names the selected company, labels the summary tab `Selected company summary`, and clearly states that Brand, Department, and Location registries and create actions are selected-company scoped rather than a tenant-wide company directory.
- Existing tenant, selected-company Manage, bounded registry, mutation, and User Access scope-authority rules are unchanged. No new API, schema, or permission behavior was added.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0211-ADMIN-ORGANIZATION-SELECTED-COMPANY-CONTEXT.md`. Users/Roles UI guidance, administration knowledge-base guidance, glossary, and release note were updated. Requested Code Spark/GPT-5.4 deliberators were unavailable; closest permitted GPT-5.6 fallback was used and recorded.
- Evidence: Core Admin contract tests pass 42/42 and web typecheck passes. Lint, production build, diff checks, disposable PostgreSQL selected-company/query-plan evidence, responsive browser/mobile, hosted recovery/deployment, and UAT remain applicable gates; Administration and Phase I are not complete.

### DEC-0212 Core Administration Organization Scope registry paging — July 25, 2026
- Parent-led deliberation with independent Product and Architecture review selected a selected-company registry consistency pass. Brand, Department, and Location pages now clamp stale URL pages, preserve exact totals/active counts and deterministic ordering, and use narrow explicit relation projections; Department’s approved database-derived budget, budget-line, and cost-center counts remain intact.
- Existing tenant/company predicates, selected-company Manage authorization, filters, mutations, and User Access scope authority are unchanged. No cross-company rows or new authority were introduced.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0212-ADMIN-ORGANIZATION-REGISTRY-PAGING.md`. Users/Roles UI guidance, administration KB, glossary, and release evidence remain aligned with selected-company scope. Requested Code Spark/GPT-5.4 deliberators were unavailable; closest permitted GPT-5.6 fallback was used and recorded.
- Evidence: Core Admin contract tests pass 42/42 and web typecheck, lint, production build, and diff checks pass. Disposable PostgreSQL selected-company/query-plan evidence, responsive browser/mobile, hosted recovery/deployment, and UAT remain applicable gates; Administration and Phase I are not complete.

### DEC-0213 Core Administration Approval Rules registry paging — July 25, 2026
- Parent-led deliberation with independent Product and Architecture review selected the remaining small registry consistency gap. Approval Rules now clamps stale filtered pages from the exact tenant/current-company total while preserving its explicit safe projection, exact step count, capped first-three-step preview, deterministic ordering, and read-only authority.
- Existing tenant-wide/selected-company predicates, status/search filters, detail authorization, and approval routing semantics are unchanged. No approval authority or mutation path was added.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0213-ADMIN-APPROVAL-RULE-REGISTRY-PAGING.md`. Existing Users/Roles UI, glossary, KB, and release guidance remain authoritative; the decision index and release note were updated. Requested Code Spark/GPT-5.4 deliberators were unavailable; closest permitted GPT-5.6 fallback was used and recorded.
- Evidence: Core Admin contract tests pass 42/42 and web typecheck, lint, production build, and diff checks pass. Disposable PostgreSQL tenant/company/query-plan evidence, responsive browser/mobile, hosted recovery/deployment, and UAT remain applicable gates; Administration and Phase I are not complete.

### DEC-0214 Core Administration Role Detail permission-link integrity — July 25, 2026
- Parent-led deliberation with independent Product and Architecture review selected canonical tenant-local/global permission parity for Role Detail. The relation projection, matrix, counts, drift indicators, and hidden enabled-code preservation now derive from the same predicate. Unsupported RolePermission links produce a truthful read-only integrity state rather than being silently omitted or deleted.
- Role Detail hides permission mutation composers when integrity is unavailable, and direct manual/recommended mutations reject with `ROLE_PERMISSION_SCOPE_CORRUPTED` before audit or writes; the transaction rechecks after locking the role. Existing tenant-role, selected-company Manage, MFA, segregation, and CAS controls remain authoritative.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0214-ADMIN-ROLE-PERMISSION-INTEGRITY.md`. Role UI guidance, glossary/KB, and release note were updated. Requested Code Spark/GPT-5.4 deliberators were unavailable; closest permitted GPT-5.6 fallback was used and recorded.
- Evidence: Core Admin contract tests pass 42/42 and web typecheck passes; lint, production build, diff checks, disposable PostgreSQL foreign-permission/no-mutation/query-plan evidence, responsive browser/mobile, hosted recovery/deployment, and UAT remain applicable gates; Administration and Phase I are not complete.

### DEC-0215 Purchase Request draft lookup selected-scope hardening — July 25, 2026
- Parent-led deliberation with independent Product and Architecture review selected a narrow hardening checkpoint for the already-bounded draft lookup contract. Selected budget-line retention now reuses the active-budget, selected-location, and selected-brand predicates used for page results, preventing a forged selected ID from exposing sibling-branch budget metadata. Draft creation’s transactional ID/scope/UOM validation remains authoritative and budget classification remains optional.
- Removed the unused unbounded `listPurchaseRequestDraftOptions` relation-hydration path so no alternate caller can reintroduce the old broad catalog read. No new draft, budget, inventory, approval, or audit authority was added.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0215-PR-DRAFT-LOOKUP-SELECTED-SCOPE-HARDENING.md`. Purchase Request source/KB/release guidance was updated; requested Code Spark/GPT-5.4 deliberators were unavailable, so the closest permitted GPT-5.6 fallback was used and recorded.
- Evidence: Purchase Request focused contract tests, web typecheck, lint, production build, and diff checks are required for this checkpoint. Disposable PostgreSQL foreign-location/brand lookup isolation/no-mutation, responsive browser/mobile, hosted recovery, and UAT remain open; Workspace 1 and Phase I are not complete.

### DEC-0216 Purchase Request draft editor multi-line continuity — July 25, 2026
- Parent-led deliberation with independent Product and Architecture review selected a bounded client-state hardening slice. Selected item, UOM, and budget options are cached by ID across draft lines, so switching lines does not leave a previously selected value invisible after a search-page change. The cache contains only authorized lookup/user-selected records and does not preload catalogs or grant authority.
- Server-side draft creation still revalidates all catalog, scope, UOM, and budget values before any write. No workflow, approval, inventory, or budget policy changed.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0216-PR-DRAFT-EDITOR-MULTILINE-CONTINUITY.md`. Purchase Request KB guidance and release evidence were updated; requested Code Spark/GPT-5.4 deliberators were unavailable, so the closest permitted GPT-5.6 fallback was used and recorded.
- Evidence: Purchase Request focused tests 37/37, web typecheck, lint, production build, and diff checks pass. Responsive browser/mobile, disposable PostgreSQL scope/no-mutation, hosted recovery, and UAT remain open; Workspace 1 and Phase I are not complete.

### DEC-0217 Administration Sensitive Role Request permission integrity — July 25, 2026
- Sensitive Role Request creation and approval now derive permission metadata from a canonical tenant-local/global snapshot and fail closed with `ROLE_PERMISSION_SCOPE_CORRUPTED` before any request, assignment, audit, or notification effect when unsupported `RolePermission` links exist. Both paths repeat the check after locking the role in the transaction; rejection remains non-granting cleanup and no longer hydrates permissions.
- Pending request previews filter permission labels to tenant-local/global permissions and mark an integrity issue without exposing foreign permission codes. Approval is unavailable until repair; rejection remains available. Existing MFA, no-self, scope, approval-rule, CAS, and audit controls remain authoritative.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0217-ADMIN-SENSITIVE-ROLE-PERMISSION-INTEGRITY.md`. Administration UI guidance, knowledge-base guidance, glossary, and release evidence require alignment before commit.
- Evidence: Core Administration focused tests 43/43 and web typecheck pass. Lint, production build, diff checks, disposable PostgreSQL foreign-permission/locked-race/no-mutation/query-plan execution, responsive browser/mobile, hosted recovery/deployment, and UAT remain open; User Access, Administration, and Phase I are not complete. Permission Access global-role provenance parity remains a separate follow-up.

### DEC-0218 Administration Permission Access global-role provenance parity — July 25, 2026
- Permission Access now includes tenant-local and tenant-global granting roles for a tenant/global permission. Granting-role rows and current-company active/effective user previews share the same tenant/global role predicate, while current-tenant user ownership, selected-company scope, deterministic ordering, exact totals, five-user caps, and read-only authority remain unchanged.
- Each row labels `Global role` or `Tenant role`. Malformed permission IDs fail closed before database reads; service-level query/page bounds and stale-page clamping prevent route-level bypasses. Global roles remain provenance-only and are not linked to tenant-owned mutation controls.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0218-ADMIN-PERMISSION-ACCESS-GLOBAL-ROLE-PARITY.md`. Administration UI guidance, knowledge-base guidance, glossary, and release evidence were updated.
- Evidence: Core Administration focused tests, web typecheck, lint, production build, and diff checks are required for this checkpoint. Disposable PostgreSQL tenant/global/foreign-role isolation, effective-date/selected-company preview, query-plan, responsive browser/mobile, hosted recovery/deployment, and UAT remain open; User Access, Administration, and Phase I are not complete.

### DEC-0219 Purchase Order amendment TaskSheet unavailable-state hardening — July 25, 2026
- Purchase Order detail no longer silently hides the amendment action when the visible PO cannot be amended. A read-only status panel explains missing amendment permission, non-issued status, receiving activity, pending amendment or balance closure, or posted receipt state; the server-side amendment eligibility and transactional approval controls remain authoritative.
- Eligible issued/unreceived POs retain the focused multi-line TaskSheet and approval/audit/receiving-pause behavior. Existing action-feedback mappings surface validation, line, stale-CAS, duplicate-pending, and receiving conflicts; the page states that no amendment was saved and must be reopened after such an error.
- Formal decision record: `docs/core/00-governance/decisions/DEC-0219-PO-AMENDMENT-TASKSHEET-UNAVAILABLE-STATES.md`. Purchasing workflow/UI specifications, knowledge-base guidance, release notes, and glossary assessment were updated; no new glossary term was required.
- Evidence: PO amendment visible-surface tests 2/2, Purchase Order focused service tests 36/36, web typecheck, lint, production build, and diff checks pass. Disposable PostgreSQL authorization/CAS/no-mutation, responsive browser/mobile, hosted recovery/deployment, and UAT remain open; Purchase Orders and Phase I are not complete.

### DEC-0220 Exact-candidate authorization baseline drift correction — July 25, 2026
- The Administration/Overview evidence re-audit found one stale authorization-baseline entry for the removed `listPurchaseRequestDraftOptions` service. The baseline was regenerated from the current source manifest; no service, route, permission, or user authority was added. This keeps the fail-closed manifest aligned with the earlier DEC-0215 removal of the unused unbounded lookup path.
- Evidence: authorization-manifest tests pass 20/20; the focused Administration/Overview/My Tasks regression passes 93/93; the full non-database web suite passes 1,334 tests across 123 files (301 skipped, 1 todo across 10 skipped files); web lint, typecheck, production build, and `git diff --check` pass. Disposable PostgreSQL authorization/no-query/isolation/query-plan/concurrency execution remains blocked by missing `DISPOSABLE_DATABASE_ADMIN_URL`; responsive authenticated browser, hosted recovery/deployment, production-authenticated E2E, and UAT remain open. Administration, Overview, and Phase I are not complete.
- Documentation assessment: no glossary, knowledge-base, workflow, or user-facing release-note change is required because this is a release-evidence registry correction with no runtime behavior change. Activation and delivery remain explicitly deferred follow-up sections under DEC-0197.

### DEC-0221 Authentication deferred-surface static contract — July 25, 2026
- Added a static route contract asserting that `/admin/authentication` renders the active Recovery section while keeping Account readiness, activation delivery, `Send activation link`, and `Retry delivery` controls unrendered. This protects the explicit DEC-0197 deferred boundary while external Administration/Overview evidence remains unavailable.
- Evidence: the new Authentication page contract passes 2/2; disposable PostgreSQL lifecycle safety passes 15/15; web lint, typecheck, and `git diff --check` pass. No runtime authority, schema, workflow, or navigation behavior changed. Both the disposable authorization suites and `pnpm test:e2e` fail closed before execution because `DISPOSABLE_DATABASE_ADMIN_URL` is unset and Docker is unavailable; responsive authenticated browser, hosted recovery/deployment, production-authenticated E2E, and UAT remain open. Administration, Overview, and Phase I are not complete.
- Documentation assessment: no glossary, knowledge-base, workflow, or release-note behavior update is required. Activation and delivery remain a later separately authorized slice.

### DEC-0222 Stock Count attempt-authoritative read mapper checkpoint — July 25, 2026
- Added a reversible attempt-authoritative detail/CSV line-selection contract behind `STOCK_COUNT_ATTEMPT_READ_V1_ENABLED = false`. The disabled path retains the legacy compatibility projection and avoids hydrating attempt lines; the enabled path requires the existing current-attempt header/line parity guard and fails closed with `STOCK_COUNT_ATTEMPT_LINE_PARITY_FAILED` rather than silently falling back. No Count Variance, recount, posting, approval, schema, or default-read authority changed.
- Evidence: Stock Count focused service tests pass 28/28, including disabled-default, enabled-selection, and missing-attempt fail-closed cases; web lint, typecheck, production build, and `git diff --check` pass. Disposable PostgreSQL migration/backfill/attempt-line parity, redaction, query-plan, contention, responsive browser, hosted recovery/deployment, authenticated E2E, and UAT remain open; the mapper must remain disabled until those gates pass.
- Documentation assessment: no glossary or user-facing knowledge-base behavior changed because the runtime flag remains disabled. The Stock Count workflow and data specifications continue to treat legacy reads as compatibility-only and Count Variance as inactive.

### DEC-0223 Core Administration Audit export date-ready state — July 25, 2026
- Parent-led deliberation with independent Architecture review selected a bounded visible-surface correction. Audit Trail now exposes `Export CSV` only when both From and To filters parse as strict date-only values in non-reversed order; otherwise it shows a disabled explanation telling the administrator to choose valid dates. The existing export route remains authoritative for maximum span, row cap, tenant/company authorization, scope, and audit events.
- No schema, permission, workflow, inventory, money, or audit-record semantics changed. The change is reversible and does not add a policy read to the page.
- Evidence: Core Administration static contract coverage passes 45/45, export-authorization coverage passes 25/25, and the full non-database web suite passes 1,338 tests across 124 files (301 skipped, 1 todo). Web typecheck, lint, production build, and `git diff --check` pass. Disposable PostgreSQL authorization/no-query/query-plan, responsive browser/mobile, hosted recovery/deployment, production-authenticated E2E, and UAT remain open; Administration, Overview, and Phase I are not complete.
- Source-of-truth UI guidance, administration knowledge-base guidance, and release note were updated. No new glossary term was required.

### OD-017 Purchase Request Department and Cost Center policy hold — July 25, 2026
- Independent Product and Architecture deliberation confirmed a real Phase I gap: purchasing workflow/UI/data specifications require Department and Cost Center on new Purchase Requests, while the current editor/service has no authorized lookup contract and legacy rows remain nullable. Requested Code Spark/GPT-5.4 deliberators were unavailable; closest permitted GPT-5.6 fallback was used and recorded.
- Implementation is intentionally held under `OPEN_DECISIONS_AND_ASSUMPTIONS.md` until requiredness/conditional policy, Department–Cost Center derivation or pair validity, legacy null/backfill treatment, approval-route/report/export impact, and effective user scope rules are confirmed. No defaults, schema tightening, or client-only selectors are permitted.
- The next approved slice, after policy confirmation, is server-owned selected-company Department and Cost Center lookup pages, transactional create validation, and consistent list/detail/export/approval snapshots with cross-tenant/company/brand/location/department/cost-center isolation tests. This is a material data/workflow decision, not a safe UI-only patch; Purchase Requests and Phase I remain incomplete.

### DEC-0224 Purchase Order amendment draft recovery — July 25, 2026
- Parent-led deliberation with independent Architecture and Product review selected a bounded in-memory recovery contract. Eligible amendment TaskSheets now retain reason, expected delivery, supplier-notice fields, and every line quantity/price/note after correctable server validation errors. Stale, receiving, pending-amendment, and similar conflict outcomes show a reload-current-PO action and disable resubmission until refresh; no values are persisted outside component memory.
- Post-commit interaction review found and corrected a controlled-dirty-state gap: line-only quantity, price, or note edits now explicitly activate the same unsaved-change discard guard as header edits, and the TaskSheet reports captured edits back to the controlled owner.
- Independent GPT-5.6 fallback review found the first static regression assertion could false-pass because it did not bind the dirty transition to the line helper. The contract now isolates `updateLine`, requires its own dirty transition, and verifies all three line controls delegate to it; re-review returned GO with no remaining Critical, High, or Medium finding. Requested Spark/GPT-5.4 models were unavailable in the active toolset.
- The existing Purchase Order amendment service remains authoritative for authorization, selected-location scope, status, line-set, evidence, CAS, approval, audit, and no-mutation-on-failure behavior. Successful requests still revalidate the detail, PO register, and approval queue.
- Evidence: Purchase Order service tests pass 36/36, amendment visible-surface tests pass 4/4, web typecheck and lint pass, production build passes, authorization-manifest coverage passes 20/20, and the full non-database web suite passes 1,340 tests across 124 files (301 skipped, 1 todo). Disposable PostgreSQL CAS/no-mutation/query-plan, responsive browser/mobile, hosted recovery/deployment, production-authenticated E2E, and UAT remain open; Purchase Orders and Phase I are not complete.
- Purchasing UI guidance, amendment knowledge-base guidance, release note, and decision index were updated. No new glossary term was required.

### Stock Balances exact-total and timezone truthfulness — July 26, 2026
- The `Balance rows` summary now uses the exact server-owned matching total for the current search and tab instead of the bounded ten-row page length. Pagination, query-aware tab counts, deterministic ordering, selected-location authorization, and full-result export behavior are unchanged.
- Updated dates now render deterministically in the selected company's server-resolved configured timezone instead of inheriting the application host locale. Missing or invalid configuration falls back to `Asia/Manila`, the current approved operational default. No inventory quantity, ledger, posting, or authorization behavior changed.
- Evidence: focused Inventory coverage passes 25/25 and configured-timezone presentation coverage passes 2/2; the full non-database web suite passes 1,347 tests across 125 files (301 skipped, 1 todo); typecheck, lint, the 20/20 authorization manifest, and production build pass. Independent GPT-5.6 QA first rejected the hardcoded-default timezone, then returned GO after server-owned selected-company resolution, IANA validation, fallback handling, and UTC-boundary tests, with no remaining Critical, High, or Medium finding. Requested Spark/GPT-5.4 models were unavailable. PostgreSQL query-plan/volume and company-timezone isolation execution, authenticated responsive-browser, hosted recovery/deployment, production-authenticated E2E, and UAT remain open; Workspace 1, Inventory, and Phase I are not complete.
- Inventory UI guidance and the Stock Balances knowledge-base article are aligned. No new glossary term or training workflow is required.
