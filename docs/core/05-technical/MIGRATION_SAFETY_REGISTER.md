# Migration Safety Register

This register provides the explicit, machine-readable review disposition required by `scripts/release-migration-review.mjs` for migrations that mutate data, drop a table, column, type, constraint, or index, truncate data, alter an enum, set a column `NOT NULL`, or add uniqueness to an existing table. Additive migrations that do not meet those conditions receive the generator's `ADDITIVE_DEFAULT` classification.

`PENDING` means the migration has been inventoried but has not been accepted for a release rehearsal. `APPROVED_FOR_REHEARSAL` authorizes only the disposable exact-SHA rehearsal for the exact migration hash; it is not production approval or data-preservation evidence. `APPROVED` is reserved for final reviewed evidence after the rehearsal. `REJECTED` blocks execution and promotion. Each row is bound to the reviewed SQL by `sha256`; a content change fails the generator until a new review updates the row.

Each row records migration-specific risk, expected effect, recovery, failure point, transaction behavior, reversibility, decision trigger, owner, verification, and the recovery-time obligation. The generator fails closed when a required row or field is absent, a destructive/data-changing migration is not dispositioned, or a row names a migration that no longer exists. Rehearsal/final approval also requires reviewer identity and UTC review time; the generated artifact binds that hash review to the exact hosted candidate SHA and evidence run ID.

Local inventory generation permits hash-bound `PENDING` rows. The hosted release rehearsal must set `RELEASE_MIGRATION_REVIEW_REQUIRE_APPROVED=yes`; in that mode every required row must be `APPROVED_FOR_REHEARSAL` or `APPROVED`, while `PENDING`, `REJECTED`, missing, or hash-mismatched rows fail closed.

<!-- MIGRATION_SAFETY_REGISTER_JSON_START -->

```json
[
  {
    "migration": "20260629213000_quotation_recommendation_foundation",
    "sha256": "061689e8ab67ba42a5637a9a025093a38acdaa4eb45ee2ac122b5271f74ae571",
    "risk": "Unique indexes on existing quotation tables can fail on duplicate scoped keys.",
    "expectedDataEffect": "No row mutation; enforce scoped quotation and active-recommendation uniqueness.",
    "recovery": "Resolve reported duplicate quotation keys with a reviewed forward-fix and retry; restore the pre-migration backup if partial DDL is unsafe.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Unique indexes on existing quotation tables can fail on duplicate scoped keys.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; enforce scoped quotation and active-recommendation uniqueness.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260630165000_wastage_posting",
    "sha256": "d026a8e23d302170e2c88779218a3e4232c525fe0bfac59cac779cb04c960e67",
    "risk": "The unique posted-movement index can fail on duplicate links, while replacing the wastage status check can weaken validation after partial DDL.",
    "expectedDataEffect": "No row mutation; enforce one posted movement link per wastage line and replace the WastageReport status check.",
    "recovery": "Reconcile duplicate ledger links and preflight statuses before retry; restore the pre-migration backup if ledger integrity or the replacement check is uncertain.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "The unique posted-movement index can fail on duplicate links, while replacing the wastage status check can weaken validation after partial DDL.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; enforce one posted movement link per wastage line and replace the WastageReport status check.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260630172000_wastage_reversal",
    "sha256": "b9c845f0cd6927e099afabe77c319b6b3e9da139833551e794798c8bf1bdc5b9",
    "risk": "The partial unique reversal index can fail on duplicate links, while replacing the wastage status check can weaken validation after partial DDL.",
    "expectedDataEffect": "No row mutation; enforce one reversal per reversed movement and replace the WastageReport status check.",
    "recovery": "Reconcile duplicate reversal links and preflight statuses before retry; restore the pre-migration backup if integrity or the replacement check is uncertain.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "The partial unique reversal index can fail on duplicate links, while replacing the wastage status check can weaken validation after partial DDL.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; enforce one reversal per reversed movement and replace the WastageReport status check.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260630233000_goods_receipt_reversal",
    "sha256": "51c88d9ce30079e3aaa5b4556ab857cffc1cb4dce7a7bf3eb8c8069dbbd3e332",
    "risk": "The unique posted-movement index can fail on duplicate receipt movement links.",
    "expectedDataEffect": "No row mutation; enforce one posted movement link per goods-receipt line.",
    "recovery": "Reconcile receipt-ledger duplicates with a reviewed forward-fix and retry; restore the pre-migration backup if the partial state is ambiguous.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "The unique posted-movement index can fail on duplicate receipt movement links.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; enforce one posted movement link per goods-receipt line.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260701001000_stock_count_variance_adjustment_bridge",
    "sha256": "306c49f5507548d11fca685401427cea34a4083d44d9255f648e5371648caef2",
    "risk": "Unique bridge indexes can fail on duplicate source links, while replacing the adjustment-type check can weaken validation after partial DDL.",
    "expectedDataEffect": "No row mutation; enforce one variance adjustment and line per stock-count source and replace the adjustment-type check.",
    "recovery": "Reconcile duplicate links and preflight adjustment types without deleting audit history; restore the pre-migration backup if the replacement is unsafe.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Unique bridge indexes can fail on duplicate source links, while replacing the adjustment-type check can weaken validation after partial DDL.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; enforce one variance adjustment and line per stock-count source and replace the adjustment-type check.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260701013000_project_tracker_foundation",
    "sha256": "749abcfa874adf04a21b5a9a25a4ad4c10c347f6f2abf6225173a012cab1eeca",
    "risk": "Scoped unique constraints on existing organizational rows can expose duplicate identifiers.",
    "expectedDataEffect": "No row mutation; enforce composite scope keys used by new project foreign keys.",
    "recovery": "Resolve scoped-key duplicates through an approved forward-fix and retry; restore the pre-migration backup if partial project DDL is unsafe.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Scoped unique constraints on existing organizational rows can expose duplicate identifiers.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; enforce composite scope keys used by new project foreign keys.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260701023000_project_task_foundation",
    "sha256": "3db3e3ceea5de52d8a937a4e6f416baeaec308857baccb2679f144b08297bb8b",
    "risk": "The composite unique key on existing Project rows can fail on duplicate scope identity.",
    "expectedDataEffect": "No row mutation; enforce project scope identity used by new task tables.",
    "recovery": "Resolve duplicate project scope keys through an approved forward-fix and retry; restore the pre-migration backup for unsafe partial DDL.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "The composite unique key on existing Project rows can fail on duplicate scope identity.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; enforce project scope identity used by new task tables.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260701033000_project_milestone_calendar_foundation",
    "sha256": "cae031fb68fc9c16148592965ca28b2d618c5303c730cb712ca1ba74481e0947",
    "risk": "Timezone-based backfill can derive an unintended date near Manila timezone boundaries.",
    "expectedDataEffect": "Backfill project and task date-only fields from timestamps using Asia/Manila; retain source timestamps.",
    "recovery": "Compare every derived date with its source timestamp and forward-fix mismatches; restore the pre-migration backup if broadly invalid.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Timezone-based backfill can derive an unintended date near Manila timezone boundaries.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Backfill project and task date-only fields from timestamps using Asia/Manila; retain source timestamps.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260701080000_transfer_discrepancy_settlement_permission",
    "sha256": "26f86d9c966a21e5709c6c5293b69187b1d26f4e1d01a280d1177edb726aadf6",
    "risk": "Permission seeding can assign unintended capability if role-code selection is wrong.",
    "expectedDataEffect": "Idempotently insert the settlement permission and intended role grants.",
    "recovery": "Correct only seeded grants by stable role and permission codes after authorization review; restore if unrelated grants changed.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Permission seeding can assign unintended capability if role-code selection is wrong.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Idempotently insert the settlement permission and intended role grants.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260701090000_purchase_order_amendments",
    "sha256": "f6254f1a20601ed1a6741ce39646666063f77b67d8b93cb736e5b775b39d031e",
    "risk": "Non-idempotent permission and approval seeds can collide or create a wrong route.",
    "expectedDataEffect": "Insert the PO amendment permission, approval rule, steps, and intended grants.",
    "recovery": "Reconcile the complete seeded ID/code set with a reviewed forward-fix and retry; restore the pre-migration backup after partial inserts.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Non-idempotent permission and approval seeds can collide or create a wrong route.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Insert the PO amendment permission, approval rule, steps, and intended grants.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260701100000_opening_inventory_balances",
    "sha256": "9b822ce5c162323c802a698822c93bded674a75824b21834aaaa1c9e344f761e",
    "risk": "PostgreSQL enum addition has no portable down-migration, and replacing the adjustment-type check can weaken validation after partial DDL.",
    "expectedDataEffect": "Add OPENING_BALANCE_IN to InventoryMovementType and replace the StockAdjustment adjustment-type check; change no rows.",
    "recovery": "Forward-fix consumers/check validation and prevent new-value use for logical rollback; restore the backup if physical enum removal or constraint recovery is required.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "PostgreSQL enum addition has no portable down-migration, and replacing the adjustment-type check can weaken validation after partial DDL.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Add OPENING_BALANCE_IN to InventoryMovementType and replace the StockAdjustment adjustment-type check; change no rows.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260701110000_operational_reason_codes",
    "sha256": "f87f0b4c154a2961b3984d9ad1fafdb5293e0d4ef44d3852f6d013a8274d7a0c",
    "risk": "Reason-code seeds can conflict with predecessor codes or encode wrong policy.",
    "expectedDataEffect": "Idempotently insert the documented operational reason-code catalog.",
    "recovery": "Correct seeded rows by stable reason code through an approved forward-fix; restore if existing rows were unexpectedly affected.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Reason-code seeds can conflict with predecessor codes or encode wrong policy.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Idempotently insert the documented operational reason-code catalog.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260701111000_normalize_stock_adjustment_reason_defaults",
    "sha256": "6e259924430eb73b37fb415f022574d40ca752536afc6fd418374489f64905fa",
    "risk": "Normalization updates can overwrite nonstandard labels or assign a wrong default.",
    "expectedDataEffect": "Normalize targeted stock-adjustment OperationalReasonCode rows.",
    "recovery": "Compare affected rows to the predecessor snapshot and forward-fix mismatches; restore if original values cannot be reconstructed.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Normalization updates can overwrite nonstandard labels or assign a wrong default.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Normalize targeted stock-adjustment OperationalReasonCode rows.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260702190000_company_business_code",
    "sha256": "9e4cb4aab1d7e66cb72ed1f5f1a1ba2fe01119ceec02e4b409f969587e1c1d27",
    "risk": "Generated company codes can be wrong or collide; NOT NULL and uniqueness then fail.",
    "expectedDataEffect": "Backfill every Company.code deterministically, then enforce non-null tenant-scoped uniqueness.",
    "recovery": "Validate the full tenant/code mapping, forward-fix invalid codes and retry; restore if accepted business codes cannot be reconstructed.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Generated company codes can be wrong or collide; NOT NULL and uniqueness then fail.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Backfill every Company.code deterministically, then enforce non-null tenant-scoped uniqueness.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260703150000_phase2_branch_food_create_permissions",
    "sha256": "2d063945ea2e4f3d191580d7763732e3b455a963a78d132cd6db281fbc79aa75",
    "risk": "The DELETE can remove intentional review grants if role policy differs.",
    "expectedDataEffect": "Insert two create permissions/grants and remove two review grants only from CONFIGURED_REQUESTER.",
    "recovery": "Capture affected grant keys; reinsert wrongly removed grants by stable codes through a reviewed forward-fix, or restore the backup.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "The DELETE can remove intentional review grants if role policy differs.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Insert two create permissions/grants and remove two review grants only from CONFIGURED_REQUESTER.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260707210000_supplier_accreditation_status",
    "sha256": "645927a4cfd1bfcb5dc7361a1ebaf3c91024b26e8c8e29ab7363c741f581b351",
    "risk": "Mapping every non-ACTIVE supplier to SUSPENDED can misclassify predecessor statuses.",
    "expectedDataEffect": "Map ACTIVE suppliers to APPROVED and all other existing suppliers to SUSPENDED.",
    "recovery": "Verify every predecessor status mapping and forward-fix from the captured snapshot; restore the pre-migration backup if needed.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Mapping every non-ACTIVE supplier to SUSPENDED can misclassify predecessor statuses.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Map ACTIVE suppliers to APPROVED and all other existing suppliers to SUSPENDED.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260707220000_phase3_finance_permissions",
    "sha256": "790ba82e183d6bc7c3ffb945497f7cb265fc2edb772b67afa7339a7bd17240bc",
    "risk": "Finance permission seeds can conflict or broaden authority.",
    "expectedDataEffect": "Idempotently insert finance permissions and intended role grants.",
    "recovery": "Reconcile seeded grants by stable codes through authorization review; restore if unrelated grants changed.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Finance permission seeds can conflict or broaden authority.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Idempotently insert finance permissions and intended role grants.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260707233000_phase3_finance_journal_foundation",
    "sha256": "50634ceb85480a63b9d1e6a8b36474be1a269cd20755538f0ad042aed4fa5d25",
    "risk": "Journal permission seeds can conflict or broaden financial authority.",
    "expectedDataEffect": "Idempotently insert journal permissions and intended role grants; change no journals.",
    "recovery": "Reconcile seeded grants by stable codes through authorization review; restore if unrelated grants changed.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Journal permission seeds can conflict or broaden financial authority.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Idempotently insert journal permissions and intended role grants; change no journals.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708000000_phase3_ap_invoice_match_foundation",
    "sha256": "6a681dd91e45a4a0ea48ea5fc61c115cf8ecb77b17ab2c3b4760bf9230a954a7",
    "risk": "Invoice-match permission seeds can conflict or create unintended grants.",
    "expectedDataEffect": "Idempotently insert invoice-match permissions and intended grants; change no AP documents.",
    "recovery": "Reconcile seeded grants by stable codes through authorization review; restore if unrelated grants changed.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Invoice-match permission seeds can conflict or create unintended grants.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Idempotently insert invoice-match permissions and intended grants; change no AP documents.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708012500_phase3_budget_commitment_foundation",
    "sha256": "d4996594dfc78b56897697b17a122f7aedba0c0f232136294d75d528c4f33131",
    "risk": "Budget permission seeds can conflict or grant unintended authority.",
    "expectedDataEffect": "Idempotently insert budget commitment permissions and intended grants; change no budgets.",
    "recovery": "Reconcile seeded grants by stable codes through authorization review; restore if unrelated grants changed.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Budget permission seeds can conflict or grant unintended authority.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Idempotently insert budget commitment permissions and intended grants; change no budgets.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708014000_phase3_expense_request_foundation",
    "sha256": "6f28056ff84de409b1d88b1c2c0633815f1e7f2c29b13901c45f96c5282d0eae",
    "risk": "Expense permission seeds can conflict or grant unintended financial authority.",
    "expectedDataEffect": "Idempotently insert expense permissions and intended grants; change no requests.",
    "recovery": "Reconcile seeded grants by stable codes through authorization review; restore if unrelated grants changed.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Expense permission seeds can conflict or grant unintended financial authority.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Idempotently insert expense permissions and intended grants; change no requests.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708020000_phase3_cash_advance_liquidation_foundation",
    "sha256": "ce25af729e34ed797308df8de18b8003f5efe9b536d3b28c4bea023fc0af823e",
    "risk": "Cash-advance permission seeds can conflict or grant unintended authority.",
    "expectedDataEffect": "Idempotently insert advance/liquidation permissions and intended grants; change no advances.",
    "recovery": "Reconcile seeded grants by stable codes through authorization review; restore if unrelated grants changed.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Cash-advance permission seeds can conflict or grant unintended authority.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Idempotently insert advance/liquidation permissions and intended grants; change no advances.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708021500_phase3_petty_cash_foundation",
    "sha256": "4151c562f6a73b76d3417d214ee1260b54b7b750e3f781f6e3109e7f5ce5850c",
    "risk": "Petty-cash permission seeds can conflict or grant unintended cash authority.",
    "expectedDataEffect": "Idempotently insert petty-cash permissions and intended grants; change no cash transactions.",
    "recovery": "Reconcile seeded grants by stable codes through authorization review; restore if unrelated grants changed.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Petty-cash permission seeds can conflict or grant unintended cash authority.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Idempotently insert petty-cash permissions and intended grants; change no cash transactions.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708022000_phase3_petty_cash_location_scope_fix",
    "sha256": "ff5dd69ce5861bd4d3206b1637283a66be5f0f60497747048b926bd53a3851e5",
    "risk": "Multi-table backfill can assign wrong branch scope or leave related records inconsistent.",
    "expectedDataEffect": "Populate location scope on petty-cash requests, ledger entries, liquidations, and lines from parents.",
    "recovery": "Verify location and scope invariants, forward-fix from authoritative parent records, or restore the pre-migration backup.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Multi-table backfill can assign wrong branch scope or leave related records inconsistent.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Populate location scope on petty-cash requests, ledger entries, liquidations, and lines from parents.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708030000_phase3_budget_revision_approval_rule",
    "sha256": "e4cbbc3fe956b95263873e00450eaea8105b3d08b743eccccabb4be7ea09674f",
    "risk": "Non-idempotent approval seeds can collide or create a wrong money-approval route.",
    "expectedDataEffect": "Insert the budget revision approval rule and ordered steps.",
    "recovery": "Validate and reconcile the complete rule/step seed set with a reviewed forward-fix; restore after partial inserts.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Non-idempotent approval seeds can collide or create a wrong money-approval route.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Insert the budget revision approval rule and ordered steps.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708031000_phase3_expense_request_approval_rule",
    "sha256": "0fa2fe5e97acb12d10e6464908650424f8993a255364af550590f1f1e1dc949a",
    "risk": "Non-idempotent approval seeds can collide or create a wrong money-approval route.",
    "expectedDataEffect": "Insert the expense request approval rule and ordered steps.",
    "recovery": "Validate and reconcile the complete rule/step seed set with a reviewed forward-fix; restore after partial inserts.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Non-idempotent approval seeds can collide or create a wrong money-approval route.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Insert the expense request approval rule and ordered steps.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708032000_phase3_cash_advance_approval_instance",
    "sha256": "51172983417dbcdc860623458e7fe9680add7be3f93569de6b8fb095ac4cf840",
    "risk": "Approval seeds can collide and unique links can fail on duplicate approval references.",
    "expectedDataEffect": "Insert the cash advance route and enforce one approval-instance link per request.",
    "recovery": "Reconcile duplicate links and the complete seed set with a reviewed forward-fix; restore after unsafe partial application.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Approval seeds can collide and unique links can fail on duplicate approval references.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Insert the cash advance route and enforce one approval-instance link per request.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708033000_phase3_petty_cash_approval_instance",
    "sha256": "26c94ceb7797983f0cfdd80ea33cf997cb30180d766620e6060edb0165b88de0",
    "risk": "Approval seeds can collide and unique links can fail on duplicate approval references.",
    "expectedDataEffect": "Insert the petty cash route and enforce one approval-instance link per request.",
    "recovery": "Reconcile duplicate links and the complete seed set with a reviewed forward-fix; restore after unsafe partial application.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Approval seeds can collide and unique links can fail on duplicate approval references.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Insert the petty cash route and enforce one approval-instance link per request.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708034000_phase3_payment_request_approval_rule",
    "sha256": "3e4245d18649d10c76b283b54e4b238202e893ff233906dcc8e48b4a03c13561",
    "risk": "Non-idempotent approval seeds can collide or create a wrong payment route.",
    "expectedDataEffect": "Insert the payment request approval rule and ordered steps.",
    "recovery": "Validate and reconcile the complete rule/step seed set with a reviewed forward-fix; restore after partial inserts.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Non-idempotent approval seeds can collide or create a wrong payment route.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Insert the payment request approval rule and ordered steps.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708035000_phase3_payment_release_approval_instance",
    "sha256": "0e3dcbddb6de7868b6511b7594a296c2a1be173b4d5df57a6ea6c2f442a83442",
    "risk": "Approval seeds can collide and unique links can fail on duplicate approval references.",
    "expectedDataEffect": "Insert the payment release route and enforce one approval-instance link per release.",
    "recovery": "Reconcile duplicate links and the complete seed set with a reviewed forward-fix; restore after unsafe partial application.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Approval seeds can collide and unique links can fail on duplicate approval references.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Insert the payment release route and enforce one approval-instance link per release.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708043000_phase3_payment_release_reconciliation_match",
    "sha256": "7675ce36886af015bc76601eae2cd7d59a231241040057f68768a2c5bc062ed0",
    "risk": "Enum expansion lacks a portable down-migration; permission inserts can collide or broaden authority.",
    "expectedDataEffect": "Add PAYMENT_RELEASE to reconciliation sources and insert associated permission grants.",
    "recovery": "Forward-fix consumers/grants and prevent value use for logical rollback; restore the backup if physical enum removal is required.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Enum expansion lacks a portable down-migration; permission inserts can collide or broaden authority.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Add PAYMENT_RELEASE to reconciliation sources and insert associated permission grants.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708044000_phase3_workforce_leave_approval_instance",
    "sha256": "38a88c91bb8b1692a3f2d61b65c09523ed4aa0913b9f65c9d4d302cd07771ebd",
    "risk": "Approval seeds can collide and unique links can fail on duplicate leave approval references.",
    "expectedDataEffect": "Insert the leave route and enforce one approval-instance link per leave request.",
    "recovery": "Reconcile duplicate links and the complete seed set with a reviewed forward-fix; restore after unsafe partial application.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Approval seeds can collide and unique links can fail on duplicate leave approval references.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Insert the leave route and enforce one approval-instance link per leave request.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708045000_phase3_workforce_overtime_approval_instance",
    "sha256": "2dbe28b60ba8b7c76e597e0385b6745cec4fea60d21f355222dd8b859dbff669",
    "risk": "Approval seeds can collide and unique links can fail on duplicate overtime approval references.",
    "expectedDataEffect": "Insert the overtime route and enforce one approval-instance link per overtime record.",
    "recovery": "Reconcile duplicate links and the complete seed set with a reviewed forward-fix; restore after unsafe partial application.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Approval seeds can collide and unique links can fail on duplicate overtime approval references.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Insert the overtime route and enforce one approval-instance link per overtime record.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708050000_phase3_workforce_schedule_approval_instance",
    "sha256": "d443d210576164fb3078005c8b8a3926c94ea2031b8963695747a2d1948dce76",
    "risk": "Approval seeds can collide and unique links can fail on duplicate schedule approval references.",
    "expectedDataEffect": "Insert the schedule route and enforce one approval-instance link per schedule.",
    "recovery": "Reconcile duplicate links and the complete seed set with a reviewed forward-fix; restore after unsafe partial application.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Approval seeds can collide and unique links can fail on duplicate schedule approval references.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Insert the schedule route and enforce one approval-instance link per schedule.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708051000_phase3_attendance_import_approval_instance",
    "sha256": "e5da58956430cde40adc53d647436e3ee689a846bac418a6d394c51faa9dbf5c",
    "risk": "Approval seeds can collide and unique links can fail on duplicate import approval references.",
    "expectedDataEffect": "Insert the attendance import route and enforce one approval-instance link per batch.",
    "recovery": "Reconcile duplicate links and the complete seed set with a reviewed forward-fix; restore after unsafe partial application.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Approval seeds can collide and unique links can fail on duplicate import approval references.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Insert the attendance import route and enforce one approval-instance link per batch.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708052000_phase3_expense_budget_commitment_source",
    "sha256": "a11545bbb76a98f334bcb1e11cc5052b32e43bf1b0146d9bb9fd2269f1116190",
    "risk": "PostgreSQL enum addition has no portable down-migration and may break old consumers.",
    "expectedDataEffect": "Add EXPENSE_REQUEST to BudgetSourceEventType; change no rows.",
    "recovery": "Forward-fix consumers and prevent new-value use for logical rollback; restore the backup if physical enum removal is required.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "PostgreSQL enum addition has no portable down-migration and may break old consumers.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Add EXPENSE_REQUEST to BudgetSourceEventType; change no rows.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708053000_phase3_period_close_reopen_attempt_action",
    "sha256": "9226cca67c58108f3132a48f9d3f8bdbf30cac169efd370792fa6ec4f9921133",
    "risk": "PostgreSQL enum addition has no portable down-migration and may break old consumers.",
    "expectedDataEffect": "Add REOPEN_PERIOD to FinanceCloseAttemptAction; change no rows.",
    "recovery": "Forward-fix consumers and prevent new-value use for logical rollback; restore the backup if physical enum removal is required.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "PostgreSQL enum addition has no portable down-migration and may break old consumers.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Add REOPEN_PERIOD to FinanceCloseAttemptAction; change no rows.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708062000_phase3_expense_handoff_unique_event",
    "sha256": "1dc15086717be9eee0dd725abfdf73194a1231b8d6bbb42a111c0f6547c669fe",
    "risk": "The unique event index can fail on duplicate expense handoff source events.",
    "expectedDataEffect": "No row mutation; enforce tenant/company uniqueness for expense handoff events.",
    "recovery": "Reconcile duplicate source events through an approved financial-data forward-fix and retry; restore if provenance is ambiguous.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "The unique event index can fail on duplicate expense handoff source events.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; enforce tenant/company uniqueness for expense handoff events.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708064000_phase3_period_close_approval_rule",
    "sha256": "d24438dd6f26942052946b424d62e3e5e951183b44a390f25613135015e2a160",
    "risk": "Non-idempotent approval seeds can collide or create a wrong period-close route.",
    "expectedDataEffect": "Insert the period close approval rule and ordered steps.",
    "recovery": "Validate and reconcile the complete rule/step seed set with a reviewed forward-fix; restore after partial inserts.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Non-idempotent approval seeds can collide or create a wrong period-close route.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Insert the period close approval rule and ordered steps.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708065000_phase3_supplier_credit_note_foundation",
    "sha256": "7a502fa30a1b01c12bfb9a321e8831cfc45d26e29d5036ea7e328cd3029d2f00",
    "risk": "Supplier-credit permission seeds can conflict or grant unintended authority.",
    "expectedDataEffect": "Idempotently insert supplier-credit permissions and intended grants; change no credit notes.",
    "recovery": "Reconcile seeded grants by stable codes through authorization review; restore if unrelated grants changed.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Supplier-credit permission seeds can conflict or grant unintended authority.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Idempotently insert supplier-credit permissions and intended grants; change no credit notes.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260708072500_phase3_supplier_credit_submit_permission",
    "sha256": "675226b0fbb6428701f6824dd5e3b1b430265e3b67aca1dd058815c60a11c1cd",
    "risk": "Submit-permission seeds can conflict or grant unintended supplier-credit authority.",
    "expectedDataEffect": "Idempotently insert the submit permission and intended grants; change no credit notes.",
    "recovery": "Reconcile seeded grants by stable codes through authorization review; restore if unrelated grants changed.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Submit-permission seeds can conflict or grant unintended supplier-credit authority.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: Idempotently insert the submit permission and intended grants; change no credit notes.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260711100000_expansion_playbook_revision_lifecycle",
    "sha256": "0366fe7ea89ce5902c8bc6e6b555d8f9fd632a25225c6d8ae1b6db700a75384e",
    "risk": "The revision-lineage unique index can fail on duplicate lineage revision numbers.",
    "expectedDataEffect": "No row mutation; enforce one revision number per company and playbook lineage root.",
    "recovery": "Reconcile duplicate lineage revisions through an approved forward-fix and retry; restore if lineage cannot be proven.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "The revision-lineage unique index can fail on duplicate lineage revision numbers.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "No automatic down migration is assumed; use reviewed inverse/forward DDL only when verified, otherwise restore the pre-migration backup.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; enforce one revision number per company and playbook lineage root.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260626015833_sync_prisma_schema",
    "sha256": "7d9896685ede4187943ea58705b2f26f04743c51fae5c81057aa6814568e749a",
    "risk": "Dropping and recreating foundational foreign keys can weaken relationships or fail on orphaned rows; dropping 19 UUID id defaults and seven updatedAt defaults can also break database-side inserts and timestamp initialization.",
    "expectedDataEffect": "No existing row mutation; replace foundational foreign keys and remove 19 id plus seven updatedAt database defaults as encoded by this historical migration.",
    "recovery": "Preflight replacement foreign keys for orphans and verify every affected default against the reviewed predecessor contract; restore missing defaults with a forward-fix or restore the pre-migration backup if constraints/defaults are partially changed.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Dropping and recreating foundational foreign keys can weaken relationships or fail on orphaned rows; dropping 19 UUID id defaults and seven updatedAt defaults can also break database-side inserts and timestamp initialization.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No existing row mutation; replace foundational foreign keys and remove 19 id plus seven updatedAt database defaults as encoded by this historical migration.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260630081500_purchase_order_approval_statuses",
    "sha256": "0c15856371a8baf97d0c1549536a53e53c3ab4d8a6e18822d4136c3ea7b96b6d",
    "risk": "Replacing the purchase-order status constraint can admit wrong states or fail on existing states outside the new set.",
    "expectedDataEffect": "No row mutation; replace the PurchaseOrder status check with the approval-status definition.",
    "recovery": "Preflight all existing status values and forward-fix the constraint definition or invalid rows; restore the pre-migration backup if the old constraint is left absent.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Replacing the purchase-order status constraint can admit wrong states or fail on existing states outside the new set.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; replace the PurchaseOrder status check with the approval-status definition.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260630093000_purchase_order_issued_status",
    "sha256": "29a586e4b2bbebab69fc5bacc1d56d90e91f8718b523df86604c1cde4f8e303f",
    "risk": "Replacing the purchase-order status constraint can admit wrong states or fail on existing states outside the new set.",
    "expectedDataEffect": "No row mutation; replace the PurchaseOrder status check to include the issued-state definition.",
    "recovery": "Preflight all existing status values and forward-fix the constraint definition or invalid rows; restore the pre-migration backup if the old constraint is left absent.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Replacing the purchase-order status constraint can admit wrong states or fail on existing states outside the new set.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; replace the PurchaseOrder status check to include the issued-state definition.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260630111500_receiving_reports_foundation",
    "sha256": "c45df0a101e379f86449d9011b77ae833f4ea11e75f6b09dba19a733f0e076ba",
    "risk": "Replacing the purchase-order status constraint can fail on predecessor states or leave status validation weakened after partial DDL.",
    "expectedDataEffect": "No row mutation; replace the PurchaseOrder status check for receiving workflow states.",
    "recovery": "Preflight current status values and forward-fix invalid rows or the replacement check; restore the pre-migration backup if partial DDL leaves the check absent.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Replacing the purchase-order status constraint can fail on predecessor states or leave status validation weakened after partial DDL.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; replace the PurchaseOrder status check for receiving workflow states.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260630133000_inventory_transfer_dispatch",
    "sha256": "f76329d5af8d4083b1f1ac648f4e8357626528652a08b747bb5c1653abc953f7",
    "risk": "Replacing the transfer status constraint can fail on existing states or weaken dispatch-state validation.",
    "expectedDataEffect": "No row mutation; replace the InventoryTransfer status check for dispatch workflow states.",
    "recovery": "Preflight transfer statuses and forward-fix invalid rows or the check definition; restore the pre-migration backup if the old constraint is left absent.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Replacing the transfer status constraint can fail on existing states or weaken dispatch-state validation.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; replace the InventoryTransfer status check for dispatch workflow states.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260630140000_inventory_transfer_receipt",
    "sha256": "d1d17558b129edb28a6128612943e349dd13251cd8d1ad9a91325834636bdaf7",
    "risk": "Replacing the transfer status constraint can fail on existing states or weaken receipt-state validation.",
    "expectedDataEffect": "No row mutation; replace the InventoryTransfer status check for receiving workflow states.",
    "recovery": "Preflight transfer statuses and forward-fix invalid rows or the check definition; restore the pre-migration backup if the old constraint is left absent.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Replacing the transfer status constraint can fail on existing states or weaken receipt-state validation.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; replace the InventoryTransfer status check for receiving workflow states.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260630161000_wastage_approval_integration",
    "sha256": "12fb630391ba836b7d46df7393d426d6025e035ba085366ace694970e4afa11d",
    "risk": "Replacing the wastage status constraint can fail on existing states or weaken approval-state validation.",
    "expectedDataEffect": "No row mutation; replace the WastageReport status check for approval integration.",
    "recovery": "Preflight wastage statuses and forward-fix invalid rows or the check definition; restore the pre-migration backup if the old constraint is left absent.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Replacing the wastage status constraint can fail on existing states or weaken approval-state validation.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; replace the WastageReport status check for approval integration.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260630190000_po_balance_closure",
    "sha256": "f8b2c4b5ace7c503b4b32fc4e8978153f2f0035b8c89cf452f6c6be22397c6ba",
    "risk": "Replacing the purchase-order status constraint can fail on existing states or weaken closure-state validation.",
    "expectedDataEffect": "No row mutation; replace the PurchaseOrder status check for balance-closure states.",
    "recovery": "Preflight PO statuses and forward-fix invalid rows or the check definition; restore the pre-migration backup if the old constraint is left absent.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Replacing the purchase-order status constraint can fail on existing states or weaken closure-state validation.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; replace the PurchaseOrder status check for balance-closure states.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260630220000_stock_adjustment_posting_reversal",
    "sha256": "2e874d45fa692e87cef640b4c1714a96b131567981ef16a7b647812b8fbe371d",
    "risk": "Replacing the stock-adjustment status constraint can fail on existing states or weaken posting/reversal validation.",
    "expectedDataEffect": "No row mutation; replace the StockAdjustment status check for posting and reversal states.",
    "recovery": "Preflight adjustment statuses and forward-fix invalid rows or the check definition; restore the pre-migration backup if the old constraint is left absent.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Replacing the stock-adjustment status constraint can fail on existing states or weaken posting/reversal validation.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; replace the StockAdjustment status check for posting and reversal states.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260630234500_transfer_receipt_events",
    "sha256": "18f0078920d938209221d797f5e9e21a804f96c8b9c0f485aad0fea18716ace8",
    "risk": "Replacing the transfer status constraint can fail on existing states or weaken receipt-event validation.",
    "expectedDataEffect": "No row mutation; replace the InventoryTransfer status check for receipt-event states.",
    "recovery": "Preflight transfer statuses and forward-fix invalid rows or the check definition; restore the pre-migration backup if the old constraint is left absent.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Replacing the transfer status constraint can fail on existing states or weaken receipt-event validation.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; replace the InventoryTransfer status check for receipt-event states.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260701053000_project_record_link_operational_adapters",
    "sha256": "9b921b8371ddbfccf0a4bc60af4b0fbd3ea7ff283a3c9d6b91dcc86af1fe8ede",
    "risk": "Replacing the record-link source-type constraint can fail on existing types or allow unsupported ERP links.",
    "expectedDataEffect": "No row mutation; replace the ProjectRecordLink source-type check for operational adapters.",
    "recovery": "Preflight every sourceType, forward-fix unsupported values or the check definition, and restore the backup if the old constraint is left absent.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Replacing the record-link source-type constraint can fail on existing types or allow unsupported ERP links.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; replace the ProjectRecordLink source-type check for operational adapters.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260701074000_project_record_link_inventory_balance",
    "sha256": "44c48b960bee8901ae0be4c5a8bbd7d1680ee44c2bff64375cb54e47965188b2",
    "risk": "Replacing the record-link source-type constraint can fail on existing types or allow unsupported inventory links.",
    "expectedDataEffect": "No row mutation; replace the ProjectRecordLink source-type check to include inventory balance links.",
    "recovery": "Preflight every sourceType, forward-fix unsupported values or the check definition, and restore the backup if the old constraint is left absent.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Replacing the record-link source-type constraint can fail on existing types or allow unsupported inventory links.",
    "transactionBehavior": "No explicit transaction wrapper; treat partial application as possible and inspect _prisma_migrations plus affected objects before recovery.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; replace the ProjectRecordLink source-type check to include inventory balance links.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260721120000_reconcile_schema_drift",
    "sha256": "4ab30dbaebc82aa0514d3bba1b20ce7cd241359ba15474ccb5ce7c9a43a51eda",
    "risk": "Replacing eight project-scope foreign keys can fail on cross-scope rows; an incorrect replacement could weaken tenant/company isolation.",
    "expectedDataEffect": "No row mutation; validate replacement composite scope foreign keys before dropping predecessors and add two review indexes in one explicit transaction.",
    "recovery": "Treat validation failure as blocking and correct cross-scope data through reviewed forward-fix; transaction rollback should retain predecessors, otherwise restore the pre-migration backup and verify all eight constraints.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Replacing eight project-scope foreign keys can fail on cross-scope rows; an incorrect replacement could weaken tenant/company isolation.",
    "transactionBehavior": "Explicit PostgreSQL transaction; any statement failure must roll back the migration as one unit.",
    "reversibility": "Not assumed reversible; use the migration-specific forward fix or the verified pre-migration backup restore path.",
    "decisionTrigger": "Stop promotion on SQL error, checksum mismatch, unexpected row/content delta, invariant failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Release Manager approval; QA independently verifies evidence.",
    "verification": "Verify migration journal/checksum, zero schema drift, full-model content snapshots, business invariants, and restore equivalence. Expected effect: No row mutation; validate replacement composite scope foreign keys before dropping predecessors and add two review indexes in one explicit transaction.",
    "expectedRecoveryTime": "Measure during the hosted rehearsal and record against the OGFI-approved RTO/RPO; final production acceptance remains blocked until that external objective and observed duration are recorded.",
    "reviewerIdentity": "Ligaya database review and Lualhati QA review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-20T21:19:24Z"
  },
  {
    "migration": "20260721150000_production_application_authentication",
    "sha256": "b0eb3415d510b832997e9c504d02562f6edc5c78f2b3d84af7a6c0093f86be14",
    "risk": "Tenant login-code backfill and unique indexes on populated tenant, company, and user tables require write/index locks; incorrect composite keys or auth-state uniqueness could weaken tenant isolation or permit competing credentials and recovery state.",
    "expectedDataEffect": "Backfill one lowercase unique login code per existing tenant, add privilegeEpoch=0 to existing users, add tenant-qualified authentication tables and constraints, and preserve all predecessor tenant/company/user rows and operational data.",
    "recovery": "The explicit transaction, five-second lock timeout, and five-minute statement timeout roll back on contention or statement failure. Do not use a destructive down migration; correct forward from the reviewed backup and preserve any authentication/audit rows if the release has begun accepting credentials.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Login-code backfill collision, blocking unique-index lock, composite foreign-key validation, or partial-unique creation can abort the transaction; production promotion must stop on any timeout, checksum mismatch, or invariant delta.",
    "transactionBehavior": "Explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='5min'; all schema and backfill changes commit or roll back together.",
    "reversibility": "Application rollback may return only to the last secure authentication build or maintenance mode while retaining additive auth tables; never restore demo authentication. Database recovery uses reviewed forward-fix or verified pre-migration restore.",
    "decisionTrigger": "Stop promotion on SQL error, lock/statement timeout, checksum mismatch, tenant/company/user count change, invalid login code, nonzero predecessor privilege epoch, unexpected initial auth rows, race-test failure, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Security and Release Manager approval; QA independently verifies tenant, race, bootstrap, and rotation evidence.",
    "verification": "Exact-SHA local populated-predecessor rehearsal preserved 10 tenants, 100 companies, and 5000 users; completed in 5.94 seconds; produced zero invalid login codes/privilege epochs and zero initial auth rows; second deploy reported no pending migrations; schema diff reported no difference; 10 database-backed auth tests passed. Hosted rehearsal and restore evidence remain required before production GO.",
    "expectedRecoveryTime": "Measure backup restore and forward-fix decision time during the hosted rehearsal against the OGFI-approved RTO/RPO; local transaction rollback is bounded by the configured five-second lock and five-minute statement timeouts.",
    "reviewerIdentity": "Ligaya database re-review approved exact-SHA migration rehearsal; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-21T01:04:56Z"
  },
  {
    "migration": "20260721160000_tenant_role_administration_permission",
    "sha256": "626655a6e8fa287a19122b336d74d3161934f14850dfd18917f320cd152c8f79",
    "risk": "This migration grants tenant-level role-administration authority to every CONFIGURED_ADMIN and CONFIGURED_SUPER_USER role. Static predecessor conflicts fail closed, and every active/effective assignee who newly receives the grant must have their privilege epoch advanced, active application sessions revoked, and migration-sourced invalidation evidence recorded. Permission, role, and session writers must remain quiesced so concurrent changes cannot evade the reviewed postcondition.",
    "expectedDataEffect": "Atomically insert exactly one global core.tenant_role_administer permission when absent; add one RolePermission grant for every previously ungranted CONFIGURED_ADMIN and CONFIGURED_SUPER_USER role visible to the transaction; and, once per active/effective assignee of those newly granted roles, increment privilegeEpoch, revoke active application sessions, and insert an APPLICATION_COMPLETED invalidation record. No existing role, assignment, scope, audit, or operational transaction row is updated or deleted.",
    "recovery": "Any metadata conflict, timeout, constraint failure, or grant failure rolls back the explicit transaction. Correct the predecessor conflict or contention and rerun the exact migration. After release use begins, preserve authorization evidence and use a reviewed forward-fix or verified pre-migration restore rather than an unreviewed destructive down migration.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Reserved UUID collision, mismatched existing permission identity or metadata, five-second lock timeout, five-minute statement timeout, foreign-key or uniqueness failure, concurrent conflicting permission insertion after preflight, or a target role created after the grant statement.",
    "transactionBehavior": "Explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='5min'; the metadata preflight, permission insert, and role-permission grants commit or roll back together.",
    "reversibility": "Before operational use, verified migration-created grants and the unreferenced permission are logically reversible through reviewed inverse DML. After role-administration activity begins, retain authorization/audit evidence and recover through a reviewed forward-fix or verified pre-migration restore.",
    "decisionTrigger": "Stop rehearsal or promotion on SQL error, timeout, checksum mismatch, permission identity or metadata mismatch, missing or extra grants, unexpected row/content delta, migration-journal inconsistency, second-deploy delta, restore non-equivalence, schema drift, or evidence of concurrent permission or role writes.",
    "owner": "Database Engineering with Security and Release Manager approval; QA independently verifies authorization scope, atomic negative cases, and retained evidence.",
    "verification": "Local PostgreSQL 17 populated-predecessor rehearsal applied migration 110 successfully. A newly granted active CONFIGURED_ADMIN assignee advanced from privilege epoch 0 to 1, its ACTIVE application session became REVOKED with the reviewed reason, exactly one APPLICATION_COMPLETED migration invalidation was recorded, and exactly one target RolePermission grant existed. Exact-checksum reserved-UUID and same-code/mismatched-metadata predecessor fixtures both failed atomically with zero target grants, zero migration invalidations, and no completed migration-journal entry. Hosted target-only grant/content-delta, idempotent redeploy, quiescence, backup/restore equivalence, and schema-drift evidence remain required before production acceptance.",
    "expectedRecoveryTime": "Local transaction rollback is bounded by the configured five-second lock and five-minute statement timeouts. Measure conflict correction, forward-fix, and isolated backup restore during the hosted rehearsal against the OGFI-approved RTO/RPO before production acceptance.",
    "reviewerIdentity": "Migration Review Tool database review with SPF-004 Security and QA confirmation; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-21T06:00:00Z"
  },
  {
    "migration": "20260721170000_controlled_evidence_storage_contract",
    "sha256": "239ddab4e8ea725926c55ac364ad5c729230f12c7e5a3870547a9e8716204ac8",
    "risk": "The migration backfills mandatory Attachment company scope, replaces four identifier-only attachment foreign keys, installs uniqueness and availability/retention/quota checks, and creates append-only scan-signal triggers. Any orphan, tenant mismatch, multi-company legacy attachment, duplicate provider/environment/object key, invalid uploader, constraint violation, or lock timeout must abort the complete transaction.",
    "expectedDataEffect": "Preserve every Attachment row, byte reference, original object key, and existing source link; derive exactly one company from existing controlled-evidence, project, training-certificate, or compliance-document links; mark all legacy rows LEGACY_UNVERIFIED and QUARANTINED; set updatedAt to the preserved createdAt; insert exactly one quota-usage row per distinct legacy tenant/company/environment with usedBytes equal to summed attachment size and no cap; create zero upload-intent and scan-signal rows. No source workflow, inventory, finance, approval, report, or export row changes.",
    "recovery": "The explicit transaction rolls back every enum, column, backfill, constraint, table, trigger, and index on any failure. The five-second lock timeout and five-minute per-statement timeout do not bound total transaction or lock duration; rehearsal must measure the full transaction and deployment must quiesce attachment/source-link writers. Correct orphaned or conflicting predecessor data only through a separately reviewed forward fix that preserves evidence history, then retry the exact migration. After controlled uploads begin, retain attachment metadata, scan signals, and source links; use a reviewed forward migration or verified pre-migration backup restore rather than destructive down DDL.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Atomic preflight exception for orphan, tenant mismatch, uploader mismatch, or multi-company derivation; duplicate legacy object-key uniqueness; composite foreign-key validation; check/index creation; append-only trigger creation; quota backfill; five-second lock timeout; or five-minute statement timeout.",
    "transactionBehavior": "Explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='5min'; every schema addition, preflight, data backfill, foreign-key replacement, quota summary, and trigger commits or rolls back as one unit.",
    "reversibility": "Not automatically reversible. Before application use, a verified restore may return to the predecessor. After use begins, rollback must keep new attachment metadata and append-only scan evidence and use a compatibility build or reviewed forward fix; object bytes and keys are never rewritten by this migration.",
    "decisionTrigger": "Stop rehearsal or promotion on SQL error, timeout, checksum mismatch, orphan or ambiguous company scope, cross-tenant/company link, attachment/source-link count delta, byte/key/checksum change, unexpected intent/signal rows, quota total mismatch, state/check/append-only failure, second-deploy delta, restore non-equivalence, report/export regression, or schema drift.",
    "owner": "Database Engineering with Security and Release Manager approval; QA independently verifies negative migration fixtures, exact-version availability, append-only behavior, quota behavior, restore equivalence, and source-link/report compatibility.",
    "verification": "The current SQL checksum was recomputed on 2026-07-21 after the upload-intent lease and request-rate controls changed. Focused database contract tests cover scope, availability, append-only scan signals, quota, and scoped request-bound upload idempotency, but the prior clean-chain rehearsal is not evidence for this revised hash. A fresh exact-SHA populated-predecessor rehearsal must assert Attachment and all four source-link counts are unchanged, every legacy row resolves to one company, sum(sizeBytes) equals quota usedBytes by company/environment, upload-intent and scan-signal counts remain zero, report/export output is unchanged, second deploy is idempotent, and backup restore is equivalent.",
    "expectedRecoveryTime": "Lock acquisition and each statement are bounded by the configured timeouts, but total transaction and lock duration are not. Measure full populated-predecessor migration, rollback/restore, and forward-fix decision time under attachment/source-link writer quiescence during hosted rehearsal against the approved evidence RTO/RPO before production acceptance.",
    "reviewerIdentity": "Independent Database re-review approved this exact hash for isolated rehearsal only; QA, Security, Release, populated-predecessor, and restore acceptance remain required for production",
    "reviewedAtUtc": "2026-07-21T16:44:19Z"
  },
  {
    "migration": "20260721180000_evidence_legal_hold_controls",
    "sha256": "d570a9c8bdf957bd77f64b2047bbd6879fec8b095b0ba615b7980bfee9c03e6a",
    "risk": "The migration replaces legal-hold coherence, creates global evidence-governance permissions, grants them to configured administrator roles, increments affected users' privilege epochs, revokes active sessions, and writes invalidation evidence. Incorrect metadata, scope assumptions, concurrent role changes, or incomplete invalidation could grant authority without a fresh authorization evaluation.",
    "expectedDataEffect": "Add legal-hold authority and case-reference columns and an index; replace the hold-coherence check; insert exactly two global permissions when absent; grant them to CONFIGURED_ADMIN and CONFIGURED_SUPER_USER roles; increment each active affected assignee's privilege epoch once; revoke their active or pending application sessions; and create one application-completed invalidation record per affected user. Existing evidence bytes, keys, scan state, source links, and source workflows are unchanged.",
    "recovery": "The explicit transaction and bounded timeouts roll back all DDL, grants, epoch changes, session revocations, and invalidation inserts on failure. Resolve only the reported metadata conflict or contention and retry the exact migration. After use begins, preserve legal-hold and authorization evidence and use a reviewed forward fix or verified pre-migration restore rather than destructive down DDL.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Reserved permission UUID/code or metadata conflict; existing legal-hold rows that do not satisfy the strengthened check; lock or statement timeout; role/grant concurrency; foreign-key or uniqueness failure; or incomplete user/session invalidation postconditions.",
    "transactionBehavior": "Explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='5min'; schema, permission grants, privilege-epoch changes, session revocations, and invalidation evidence commit or roll back together.",
    "reversibility": "Not automatically reversible after legal-hold or permission use. Before use, verified additive columns/grants may be removed only through reviewed inverse DDL/DML or restore. After use, retain preservation and authorization history and recover with a compatible build and reviewed forward fix.",
    "decisionTrigger": "Stop rehearsal or promotion on SQL error, timeout, checksum mismatch, unexpected permission/grant count, affected-user epoch delta other than exactly one, active affected session remaining, missing/duplicate invalidation evidence, legal-hold coherence failure, second-deploy delta, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Security and Release Manager approval; QA independently verifies legal-hold coherence, authorization scope, session invalidation, idempotency, and restore evidence.",
    "verification": "Checksum inventoried only. Require a fresh exact-SHA populated-predecessor rehearsal with Permission, Role, RolePermission, UserRoleAssignment, User, and AuthSession writers quiesced; valid and invalid legal-hold fixtures; reserved-ID, concurrent/same-code, and mismatched-metadata atomic failures; target-role grant counts; exactly-once affected-user epoch/session invalidation; second-deploy idempotency; authorization tests; zero unrelated data delta; schema drift; and isolated restore equivalence.",
    "expectedRecoveryTime": "Local transaction failure is bounded by the configured five-second lock and five-minute statement timeouts. Measure conflict correction and isolated restore against the approved evidence RPO/RTO during hosted rehearsal.",
    "reviewerIdentity": "Independent Database re-review approved this exact hash for isolated rehearsal only with the recorded writer-quiescence gates; Security, QA, Release, populated-predecessor, and restore acceptance remain required for production",
    "reviewedAtUtc": "2026-07-21T16:44:19Z"
  },
  {
    "migration": "20260721190000_controlled_evidence_policy_alignment",
    "sha256": "59dc20d48e588e2d7164db0ab603299c546a5c3e59b155462026b26b350c5752",
    "risk": "The migration changes the controlled-evidence default policy from the superseded external/local-waiver wording to DEC-0046. An incorrect predicate or JSON value could overwrite company-approved overrides or silently weaken hosted scan, preservation, recovery, or audit requirements.",
    "expectedDataEffect": "For security.evidence_storage.default_policy only, replace defaultValue and governance metadata for every row; replace value only when isDefault is true; preserve every non-default company override. No evidence record, byte, key, quota, legal hold, permission, source workflow, or unrelated policy row changes.",
    "recovery": "The explicit transaction rolls back the policy update on failure. If an incorrect value is discovered after deployment, stop activation and apply a reviewed forward correction from a pre-migration snapshot; do not overwrite tenant/company overrides indiscriminately.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Incorrect JSON policy content, wrong key predicate, unintended override mutation, lock contention, or a missing/duplicate expected company policy row can invalidate the intended default alignment.",
    "transactionBehavior": "Explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='5min'; the scoped CompanyPolicySetting update commits or rolls back as one unit.",
    "reversibility": "Logically reversible only with an exact pre-migration snapshot that distinguishes defaults from company overrides. Prefer a reviewed forward correction and preserve post-deployment company changes.",
    "decisionTrigger": "Stop rehearsal or promotion on checksum mismatch, update outside the target key, non-default value change, unexpected target-row count, malformed policy JSON, sourceDecisionId other than DEC-0046, second-deploy delta, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering with Product, Security, and Release Manager approval; QA independently verifies default/override preservation and runtime policy behavior.",
    "verification": "Checksum recomputed after adding bounded lock/statement timeouts and independently re-reviewed for isolated rehearsal. Require a fresh exact-SHA populated-predecessor rehearsal containing both default and overridden company rows; prove only default values change, all defaultValue/source-decision metadata aligns to DEC-0046, overrides remain byte-equivalent, unrelated policy rows are unchanged, second deploy is idempotent, and isolated restore is equivalent.",
    "expectedRecoveryTime": "Measure forward correction and isolated restore against the approved evidence RPO/RTO during hosted rehearsal; do not assume rollback by rerunning older defaults.",
    "reviewerIdentity": "Independent Database re-review approved this exact hash for isolated rehearsal only; Product, Security, QA, Release, populated-predecessor, and restore acceptance remain required for production",
    "reviewedAtUtc": "2026-07-21T16:44:19Z"
  },
  {
    "migration": "20260721200000_project_requirement_attachment_context",
    "sha256": "78097f394b1683ec91de17d9a7785d35e0ef7adab6ad34ff92822574926bb526",
    "risk": "Replacing the original task/comment-only ProjectAttachment check and adding cross-record task-context triggers can block deployment if legacy comment/requirement or mismatched task/requirement rows exist. An over-broad context rule could admit ambiguous evidence ownership, while an over-narrow rule would keep valid project-level requirement uploads unusable.",
    "expectedDataEffect": "No row update, delete, backfill, or evidence-byte change. Preserve every existing task-only, comment-only, and correctly matched task/requirement attachment; permit future requirement-only links; install a partial active requirement/attachment uniqueness index and fail-closed triggers that keep ProjectAttachment.taskId equal to ProjectRequirement.taskId.",
    "recovery": "Any invalid predecessor row, timeout, validation failure, uniqueness failure, or trigger-installation failure rolls back the explicit transaction. Correct only the reported predecessor inconsistency through a separately reviewed evidence-preserving forward fix, then retry. After requirement-only rows exist, retain the widened compatible constraint during application rollback and prefer a reviewed forward fix or paired backup restore over restoring the old constraint.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Preflight detection of an orphan, comment-mixed, or mismatched task/requirement row; check validation; partial unique-index creation; lock/statement timeout; trigger compilation; or an unreviewed direct writer that attempts an inconsistent task context.",
    "transactionBehavior": "Explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='5min'; preflight, replacement check, unique index, and both cross-record consistency triggers commit or roll back together.",
    "reversibility": "Application rollback can retain the expanded backward-compatible constraint. Database rollback to the original task/comment-only check is unsafe after requirement-only links exist and must never delete or fabricate evidence/tasks; use a reviewed forward fix or verified paired restore.",
    "decisionTrigger": "Stop rehearsal or promotion on SQL error, timeout, checksum mismatch, ProjectAttachment count/content delta, any invalid row shape, any requirement/task mismatch, unexpected duplicate active requirement link, second-deploy delta, restore non-equivalence, schema drift, or project/evidence authorization regression.",
    "owner": "Database Engineering with Architecture and Release Manager approval; QA independently verifies row-shape, authorization, idempotency, archive, and rollback evidence.",
    "verification": "Three independent reviews selected the fail-closed context migration with High confidence. Exact candidate 3bf902bdf03c77dfdfd6c70d982c30e55726b5e3 applied the migration on PostgreSQL 17 after the populated predecessor rehearsal, preserved the existing correctly matched task/requirement ProjectAttachment count and content, rejected a task mismatch, rejected an orphan context, rejected later ProjectRequirement task drift, reported no pending migrations on redeploy, and produced no Prisma schema drift. All 19 controlled-evidence database authorization scenarios passed, including a taskless project-requirement upload, along with all 15 database contract tests. Hosted paired restore equivalence and final release acceptance remain required before production use.",
    "expectedRecoveryTime": "Local failure is bounded by the configured five-second lock and five-minute statement timeouts. Measure full validation/index duration and paired restore against the approved evidence RPO/RTO during hosted rehearsal.",
    "reviewerIdentity": "Ligaya database, Hiraya architecture, and Lualhati QA independent review; confirmed by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-21T17:30:00Z"
  },
  {
    "migration": "20260722100000_append_only_history_guards",
    "sha256": "52cccdce8596f9d1b287bc771c219ccf82cbe8b5c43f438ce407ba08bd671efa",
    "risk": "Installing statement-level guards takes locks on AuditEvent, ProjectActivityEvent, and InventoryMovement and will fail every existing cleanup, reset, retention, or application path that attempts UPDATE, DELETE, or TRUNCATE. Guard-only deployment remains bypassable by an owning or elevated runtime role until the separately owned DEC-0049 role split is verified.",
    "expectedDataEffect": "Add one unconditional ENABLE ALWAYS statement-level UPDATE/DELETE/TRUNCATE rejection trigger to each of AuditEvent, ProjectActivityEvent, and InventoryMovement. Update, delete, backfill, and rewrite zero rows; preserve the exact pre-migration count and content of all three tables. INSERT and SELECT remain available, so report/export reads and append/reversal posting contracts are unchanged.",
    "recovery": "The explicit transaction and bounded timeouts roll back the function and all three triggers together on lock, compilation, or installation failure. Correct only the reported conflict and retry the exact migration. After protected rows are appended, retain the guards during application rollback and use a reviewed forward fix or a verified restore that reconciles every post-backup protected row; never recover by deleting history or re-enabling destructive runtime writes.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "failurePoint": "Lock timeout on any protected table; function or trigger name collision; missing expected predecessor table; unsupported direct-writer behavior; checksum mismatch; or a post-deploy guard that does not reject all three destructive operations with INSERT still allowed.",
    "transactionBehavior": "Explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='5min'; the shared rejection function and all three table triggers commit or roll back together, with no data statements or backfill.",
    "reversibility": "Before any post-deploy protected write, abandoning the complete DEC-0049 rollout may use the verified pre-migration restore path. After protected rows exist, dropping guards is not an acceptable rollback; retain the compatible additive controls and use a reviewed forward fix or evidence-preserving restore/reconciliation procedure.",
    "decisionTrigger": "Stop rehearsal or promotion on SQL error, timeout, checksum mismatch, any protected-table count/content delta during migration, an allowed UPDATE/DELETE/TRUNCATE, a rejected INSERT/SELECT, inventory-ledger balance invariant drift, report/export regression, second-deploy delta, restore non-equivalence, schema drift, missing role separation, or any callable bypass.",
    "owner": "Database Engineering owns the guard migration; Platform Engineering and Security separately own the non-login owner/migrator and restricted-runtime role split. QA and Release independently verify the combined DEC-0049 evidence gate.",
    "verification": "Local PostgreSQL 17.10 rehearsal of exact migration hash 52cccdce8596f9d1b287bc771c219ccf82cbe8b5c43f438ce407ba08bd671efa passed on a uniquely named database with an exact singleton ogfi_disposable_control.database_identity marker and controlled migrator session_user/non-login owner current_user. Non-empty predecessor and post-contract snapshots were identical: AuditEvent 1 row/digest e0f78b57dc69d7002fba01a592612b1c; ProjectActivityEvent 1 row/digest 08401e68f3c39721ae7eaea1721f3305; InventoryMovement 204 rows/digest d9ab3914bc2642c1dcb4f8621c3173ee; movement and balance-cache totals both 5587.900000. All 17 real-database tests passed: marker and role identity, ENABLE ALWAYS trigger state, actual-row UPDATE/DELETE and TRUNCATE CASCADE rejection on all three tables with exact Prisma P2010/meta.code 55000, INSERT/SELECT positives, proof-fixture rollback, and an unexpected-success simulation that disabled one guard transactionally and proved the destructive change plus trigger disable both rolled back. A separate fresh predecessor held ProjectActivityEvent under ACCESS EXCLUSIVE lock; deployment failed within the bounded lock window and catalog verification returned zero guard functions and zero guard triggers, proving no partial installation. Second deploy reported no pending migrations and Prisma reported no schema difference. Hosted acceptance must repeat snapshot/inventory/report/export and restore evidence plus the separate runtime 42501, role-escalation, effective-privilege, and recovery negatives. Guard-only evidence is not production approval.",
    "expectedRecoveryTime": "Local lock failure is bounded by the configured five-second lock timeout and statements by five minutes. Measure populated-table trigger-installation duration, forward-fix decision time, and protected-history reconciliation/restore against the approved RPO/RTO during hosted rehearsal.",
    "reviewerIdentity": "Ligaya independent database/security audit; approved for isolated rehearsal only by Codex Decision Chair",
    "reviewedAtUtc": "2026-07-21T21:31:37Z"
  },
  {
    "migration": "20260722130000_authorization_denial_buckets",
    "sha256": "9a7af7d9be895db844f23385f6a35eed05e56420001d630959816a1417c401de",
    "risk": "The migration adds mutable security aggregation state linked to immutable AuditEvent evidence. Incorrect uniqueness, tenant-qualified evidence links, scope foreign keys, window/count checks, transition guards, runtime column grants, deletion guards, or service concurrency could lose denial counts, create duplicate summaries, cross tenant boundaries, permit evidence rewriting, or reintroduce attacker-controlled audit amplification.",
    "expectedDataEffect": "Create four closed PostgreSQL enums, one AuditEvent tenant-qualified unique key, one empty AuthorizationDenialBucket table, indexes, tenant-qualified scope/evidence foreign keys, integrity checks, ENABLE ALWAYS evidence/update transition guards, and an ENABLE ALWAYS DELETE/TRUNCATE rejection trigger. Update, delete, backfill, and rewrite zero predecessor rows. Expected bucket and new AuditEvent counts immediately after migration are both zero; runtime counts grow only after the separately deployed DEC-0050 service records real denials.",
    "recovery": "The explicit transaction rolls back every new type, table, index, function, and trigger on failure. Before any bucket evidence exists, abandoning the slice may use the verified pre-migration restore. After recording begins, retain buckets and linked immutable events, disable only the timer/callers if necessary, and apply a reviewed forward fix that finalizes open buckets without deleting or fabricating evidence.",
    "reviewerStatus": "PENDING",
    "failurePoint": "Type/table/index/function/trigger name collision; lock or statement timeout; unsupported PostgreSQL version; invalid scope or tenant-qualified audit foreign key; uniqueness/check mismatch; mismatched audit-event identity; runtime grant reconciliation failure; transition/deletion guard failure; lost concurrent increments; duplicate/missing first or final audit event; or schema drift.",
    "transactionBehavior": "Explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='5min'. This migration contains only additive DDL and no data statements. Runtime create/increment/finalize operations use application transactions, per-window advisory serialization, row locking, compare-and-set finalization, tenant-qualified evidence validation, and an ENABLE ALWAYS trigger that accepts only an exact open increment or a one-way post-window finalization.",
    "reversibility": "Schema-only rollback is safe only while the table and linked audit-event set remain empty. Once denial evidence exists, dropping the table, trigger, or events is prohibited; keep the additive schema compatible with application rollback and use a reviewed forward correction.",
    "decisionTrigger": "Stop rehearsal or promotion on checksum mismatch, predecessor row/content delta, unexpected initial bucket/event count, invalid 5-60-minute window acceptance, duplicate tenant/key/window bucket, lost or decreasing count, more or fewer than one first event, any count-one summary, duplicate count-greater-than-one summary, cross-tenant/scope/evidence acceptance, mismatched evidence acceptance, early/mixed/repeated finalization, mutable finalized or identity state, excess runtime table/column privilege, allowed DELETE/TRUNCATE, unsafe dimension, timer overrun, restore non-equivalence, or schema drift.",
    "owner": "Database Engineering owns the additive schema and integrity invariants; Backend and Platform own the atomic service and bounded systemd finalizer; Security, QA, and Release independently verify cardinality, failure-deny behavior, concurrency, restore, and hosted activation evidence.",
    "verification": "Exact hash 9a7af7d9be895db844f23385f6a35eed05e56420001d630959816a1417c401de deployed as part of a fresh 122-migration PostgreSQL 17 disposable lifecycle. Seven PostgreSQL integration cases passed, including exact concurrent count/single-first-event behavior, fixed attacker-independent dimensions, 5/15/60-minute separation, one-way finalization, tenant/scope/evidence mismatch rejection, invalid-window and duplicate rejection, and UPDATE/DELETE/TRUNCATE/finalized-row immutability. The same lifecycle passed 17/17 append-only checks, runtime role reconciliation, and automatic teardown. The Hostinger verifier additionally attests the exact reviewed denial-trigger function bodies, language, owner, security mode, and search path. Populated predecessor snapshots, isolated restore equivalence, and hosted runtime evidence remain required before production approval. No production migration has been applied.",
    "expectedRecoveryTime": "Migration failure is bounded by the five-second lock and five-minute statement timeouts. The systemd job is bounded to at most 500 rows per transaction and 55 seconds per invocation. Measure forward-fix and restore time against approved hosted RPO/RTO during rehearsal."
  },
  {
    "migration": "20260722140000_approval_step_eligibility_routing",
    "sha256": "3ed81de2be4d5eada10095877b38ba4d18d1548835f33c84859051ebca94339d",
    "risk": "Incorrect normalized permission, assignment, scope-group, prohibited-actor, tenant/company, activation, or immutability constraints could expose approvals to an ineligible actor, strand a current step, mutate historical routing context, or create missing/duplicate activation evidence. Index creation and trigger installation acquire locks on existing approval and audit tables.",
    "expectedDataEffect": "Add routing enums and nullable/defaulted ApprovalInstanceStep metadata; create empty relational scope-group, scope-target, and prohibited-actor tables; add eligibility/page indexes and fail-closed routing validation/immutability triggers; and add a partial unique canonical activation-audit index. Update, delete, and backfill zero predecessor rows. Existing steps remain routingSchemaVersion=0 until the separately reviewed idempotent 18-document-type backfill runs.",
    "recovery": "The explicit transaction rolls back every type, column, constraint, table, index, function, and trigger on failure. Keep additive schema during application rollback. After normalized routing or activation events exist, do not drop or rewrite routing/audit history; disable the read switch and use a reviewed forward correction or verified evidence-preserving restore.",
    "reviewerStatus": "PENDING",
    "failurePoint": "Lock or statement timeout; type/object collision; PostgreSQL UNIQUE NULLS NOT DISTINCT incompatibility; predecessor assignment inconsistency; tenant/company/brand/location/permission/actor mismatch; empty scope group; mutable v1 routing context; duplicate activation event; checksum mismatch; backfill gap; or schema drift.",
    "transactionBehavior": "Explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='120s'. This migration contains additive DDL only and no data statements. Routing writers and activation events execute in the caller's workflow transaction; normalized reads/actions remain feature-gated until the zero-gap backfill check passes.",
    "reversibility": "Before normalized writes, application rollback can retain the backward-compatible additive schema. After v1 routing rows or immutable activation events exist, dropping the schema is unsafe; retain it, turn off the normalized read switch, and use a reviewed forward fix or verified restore without deleting audit history.",
    "decisionTrigger": "Stop rehearsal or promotion on checksum mismatch, predecessor row/content delta, timeout, cross-tenant/company acceptance, invalid target shape, empty group acceptance, assignment XOR failure, missing live permission/scope enforcement, direct-user bypass, prohibited-actor acceptance, v1 routing mutation, missing/duplicate activation audit, page/count mismatch, nonzero backfill gaps at cutover, second-deploy delta, restore non-equivalence, or Prisma schema drift.",
    "owner": "Database and Backend Engineering own the normalized routing schema, transactional writer, eligibility predicate, cutover gate, and activation audit. Security and QA independently verify authorization, segregation, immutability, exact pagination, backfill completeness, concurrency, and audit cardinality before Release approval.",
    "verification": "Prisma format, schema validation, and client generation succeeded; focused TypeScript checking succeeded. Static service tests cover bounded exact-snapshot pagination, live permission/role/scope/resource predicates, prohibited actors, zero-gap cutover checks, explicit CREATED versus WAITING activation provenance, and the canonical activation-event uniqueness index. Independent PostgreSQL fresh/populated predecessor rehearsal, exact 18-type idempotent backfill, concurrency negatives, second deploy, drift, and restore evidence remain required. No production migration has been applied.",
    "expectedRecoveryTime": "Migration lock failure is bounded by five seconds and statements by 120 seconds. Measure populated-table index/trigger duration, backfill runtime, forward-fix decision time, and evidence-preserving restore against approved hosted RPO/RTO during independent rehearsal."
  },
  {
    "migration": "20260722150000_approval_routing_prisma_alignment",
    "sha256": "957005159cb7af77d558ec4d7522e09fc3ad59f5d614697f9b7139775474d488",
    "risk": "Changing approval routing timestamps from timestamptz to Prisma's timestamp(3) representation, temporarily replacing the dependent routing-context trigger, and rebuilding two routing indexes acquires locks and can shift stored instants if conversion is timezone-dependent. Incorrect trigger or index recreation could weaken routing immutability or alter assigned/due-soon pagination plans.",
    "expectedDataEffect": "Convert five approval-routing timestamp columns to TIMESTAMP(3) using an explicit UTC conversion and recreate the two routing page indexes in the exact Prisma column order. Update no business value, assignment, permission, scope, status, audit event, or source record.",
    "recovery": "The explicit transaction rolls back the trigger drop/recreation, both index drops, all type conversions, and both index recreations together on failure. Preserve the already-applied 13000 and 14000 migrations. Correct drift through a reviewed forward migration; do not rewrite applied migration history.",
    "reviewerStatus": "PENDING",
    "failurePoint": "Lock or statement timeout; missing predecessor trigger/function/index/column; non-UTC conversion; timestamp precision/value delta; trigger definition or ENABLE ALWAYS mismatch; index definition mismatch; checksum mismatch; or remaining Prisma drift.",
    "transactionBehavior": "Explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='120s'. The dependent routing trigger is dropped under the same transactional table lock, timestamp/index alignment runs, and the exact trigger is recreated with ENABLE ALWAYS before the transaction can commit.",
    "reversibility": "Application rollback can retain the Prisma-aligned timestamp and index representation. If correction is required, use a reviewed forward migration with explicit UTC conversion and value snapshots; never amend the already-applied predecessor migrations.",
    "decisionTrigger": "Stop rehearsal or promotion on checksum mismatch, timeout, timestamp null/count/value drift, timezone-dependent conversion, missing/non-ALWAYS/changed routing trigger, missing or changed routing index columns/order, non-empty Prisma drift, second-deploy delta, or restore non-equivalence.",
    "owner": "Database Engineering owns the additive corrective migration and exact drift proof; QA and Release independently verify UTC value preservation, indexes, second deploy, and restore evidence.",
    "verification": "Prisma schema validation, client generation, focused TypeScript/tests/lint, and a fresh PostgreSQL migrate-diff must pass for this exact hash. Populated UTC timestamp snapshots, query plans, second deploy, and restore evidence remain required before rehearsal approval.",
    "expectedRecoveryTime": "Lock failure is bounded by five seconds and statements by 120 seconds. Measure populated type/index rewrite time and forward-fix/restore time against approved hosted RPO/RTO."
  },
  {
    "migration": "20260722160000_authentication_throttle_windows",
    "sha256": "3b34a7eea689ec327bdabf783d744d4ca9d3af16e0692e101faa612d6ba40d61",
    "risk": "Incorrect authentication throttle dimensions, counters, transition guards, locking, key rotation, or tenant/account relationships could permit brute-force authentication, cause service-wide lockout, leak attacker-selected identifiers into durable keys, fragment active counters, or create unbounded PostgreSQL rows.",
    "expectedDataEffect": "Create two closed enums and one initially empty AuthenticationThrottleWindow table with fixed/global/shard and resolved tenant/account dimensions, server-keyed bucket identity, exact counters, UTC windows, retention metadata, tenant-qualified foreign keys, bounded checks/indexes, an ENABLE ALWAYS transition/retention trigger, and an ENABLE ALWAYS truncate rejection trigger. Update, delete, and backfill zero predecessor rows.",
    "recovery": "The explicit transaction rolls back types, table, constraints, indexes, function, and trigger together on failure. Before throttle rows exist, schema rollback may use a verified restore. After recording begins, disable caller activation if needed and use a reviewed forward fix; do not rewrite counters or applied migration history.",
    "reviewerStatus": "PENDING",
    "failurePoint": "Lock or statement timeout; enum/table/function/trigger collision; invalid dimension shape; cross-tenant account acceptance; counter or timestamp constraint mismatch; non-ALWAYS trigger; lost concurrent reservation; incorrect threshold dimension; HMAC rotation fragmentation; unbounded shard rows; checksum mismatch; or Prisma drift.",
    "transactionBehavior": "Explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='120s'. The migration is additive DDL only. Runtime reservation uses one deterministically ordered multi-row INSERT ON CONFLICT statement so PostgreSQL acquires conflict-row locks briefly and atomically increments every dimension; only serialization/deadlock failures receive at most three transaction retries. Successful verification atomically converts one failure reservation to one success across all reserved dimensions.",
    "reversibility": "Application rollback can retain the additive table while callers are disabled. Once security counters exist, do not drop or fabricate them during rollback; use a reviewed forward migration and retain rows through their configured retention boundary.",
    "decisionTrigger": "Stop rehearsal or promotion on checksum mismatch, predecessor data delta, timeout, raw identifier/source persistence, unknown-input rows above the fixed shard formula, cross-tenant/account acceptance, lost or negative counters, request != failure+success, denied non-exceeded dimensions, missing threshold time, invalid mutation acceptance, key rotation with active old-version windows, non-empty Prisma drift, second-deploy delta, or restore non-equivalence.",
    "owner": "Security and Backend Engineering own throttle policy, keyed dimensions, reservation/release integration, and quiescent key rotation; Database Engineering owns schema/transition integrity; QA and Release independently verify concurrency, bounded cardinality, failure-deny behavior, edge-rate assumptions, retention, and restore evidence.",
    "verification": "Prisma format/validation/client generation, web typecheck, six focused unit tests, targeted lint, and static migration checks passed. The parent-run disposable PostgreSQL contract passed 5/5 in 5.77 seconds: 200 concurrent reservations produced the exact 100 allowed/100 denied threshold with no unavailable result, 1,999 of 2,000 varying unknown requests were denied while rows stayed within the fixed shard formula, tenant/account rows remained separated, successful release converted exact counters, and tamper/negative/shape mutations were rejected. Fresh migrate-diff through 17000 was empty. Caller integration, hosted concurrency/load evidence, edge exact-source rate limiting, cleanup execution, second deploy, and restore evidence remain pending.",
    "expectedRecoveryTime": "Migration failure is bounded by five seconds and statements by 120 seconds. Runtime requests fail closed on persistence failure. Measure contention, forward-fix, retention cleanup, and restore time against approved hosted RPO/RTO during rehearsal."
  },
  {
    "migration": "20260722170000_approval_routing_child_trigger_always",
    "sha256": "d1744ab352fd5e8102a7f9d7d1faf3377f767f75c7daec5a5b1b2c8b3517c79a",
    "risk": "Approval routing child context and immutability triggers that remain origin-only can be bypassed by replication-role execution, admitting cross-scope or mutable v1 routing rows. Enabling five triggers ALWAYS acquires locks on the three routing child tables and can expose an unexpected incompatible trigger state.",
    "expectedDataEffect": "Change only trigger enablement mode to ALWAYS for the existing scope-group immutability, scope-target context/immutability, and prohibited-actor context/immutability triggers. Update, delete, insert, and backfill zero rows; preserve every trigger body and routing rule.",
    "recovery": "The explicit transaction rolls back every trigger-mode change together on timeout, missing trigger, or SQL failure. Keep ALWAYS enforcement during application rollback; correct any incompatibility with a reviewed forward migration rather than disabling routing integrity.",
    "reviewerStatus": "PENDING",
    "failurePoint": "Lock or statement timeout; missing/renamed predecessor trigger; wrong table/trigger pairing; trigger not marked ALWAYS; checksum mismatch; replication-role bypass; or schema drift.",
    "transactionBehavior": "Explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='120s'. All five ALTER TABLE ENABLE ALWAYS operations commit or roll back together and contain no data statement.",
    "reversibility": "The stronger trigger mode is backward compatible and should remain in place. Reverting to origin-only is a security weakening and requires a separately approved decision and recovery control.",
    "decisionTrigger": "Stop rehearsal or promotion on checksum mismatch, timeout, predecessor data delta, any missing/non-ALWAYS child trigger, replication-role acceptance of an invalid context or mutation, second-deploy delta, drift, or restore non-equivalence.",
    "owner": "Database Engineering owns trigger-mode integrity; Security and QA independently verify replication-role context/mutation negatives; Release verifies exact hash, second deploy, and restore evidence.",
    "verification": "Exact hash d1744ab352fd5e8102a7f9d7d1faf3377f767f75c7daec5a5b1b2c8b3517c79a deployed as part of a fresh 122-migration PostgreSQL 17 disposable lifecycle. The role verifier proved all six parent/child routing-context and immutability triggers have exact reviewed table, name, type, function OID, and ENABLE ALWAYS semantics and attested both routing function bodies. A rollback-only superuser probe set session_replication_role=replica, attempted to change an immutable v1 assignee, received SQLSTATE 55000 APPROVAL_ROUTING_CONTEXT_IMMUTABLE, and verified the assignment remained unchanged. The approval backfill suite passed 3/3 and the lifecycle passed 17/17 append-only checks with automatic teardown. Populated predecessor locking, second deploy, restore, and hosted evidence remain required before production approval. No production migration has been applied.",
    "expectedRecoveryTime": "Lock failure is bounded by five seconds and statements by 120 seconds. Measure forward-fix and restore time against approved hosted RPO/RTO during rehearsal."
  },
  {
    "migration": "20260722180000_approval_routing_backfill_audit_uniqueness",
    "sha256": "9b0b519b6152490a3599f963e9d8462b64d1a4427d4d2e70c0f04359332310cf",
    "risk": "Adding uniqueness to the existing AuditEvent table acquires a table/index-build lock and fails if predecessor data already contains more than one approval.step_routing_backfilled event for the same tenant and approval instance. An incorrect predicate or key could either permit competing provenance or reject unrelated audit events.",
    "expectedDataEffect": "Create one partial unique index over tenantId, entityType, entityId, and eventType only where entityType is ApprovalInstance and eventType is approval.step_routing_backfilled. Insert, update, delete, and backfill zero rows; preserve all predecessor audit data. The application backfill remains separately feature-gated and off until readiness is proven.",
    "recovery": "The explicit transaction rolls back the index creation on timeout, duplicate predecessor data, or SQL failure. Keep the uniqueness guard after any backfill event exists. If its definition is incorrect, disable the backfill job and deploy a reviewed forward migration; never delete or rewrite immutable audit evidence to make index creation succeed.",
    "reviewerStatus": "PENDING",
    "failurePoint": "Lock or statement timeout; duplicate predecessor provenance; index-name collision; incorrect tenant/entity/event predicate; checksum mismatch; concurrent backfill insertion during index creation; second-deploy delta; schema drift; or restore non-equivalence.",
    "transactionBehavior": "Explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='120s'. The migration performs one CREATE UNIQUE INDEX and no data statement. The application dry-run exercises the exact serializable apply path and deliberately aborts successful legacy-instance simulations; apply uses a coordinator transaction-scoped advisory lock plus serializable per-instance transactions.",
    "reversibility": "Before any matching backfill event exists, a reviewed restore can abandon the additive index. After provenance exists, dropping the guard weakens audit cardinality and is not an ordinary rollback; retain it and use a reviewed forward correction.",
    "decisionTrigger": "Stop rehearsal or promotion on checksum mismatch, timeout, any predecessor duplicate, predecessor row/content delta, incorrect or non-unique index definition, acceptance of a duplicate matching event, rejection of an unrelated event, dry-run writes, dry-run/apply blocker disagreement, partial blocked-instance routing writes, duplicate apply audit, concurrent-worker acceptance, second-deploy delta, drift, or restore non-equivalence.",
    "owner": "Database Engineering owns the partial unique guard and migration evidence; Backend Engineering owns dry-run/apply parity, transactional rollback, idempotency, and advisory serialization. Security, QA, and Release independently verify immutable audit cardinality, populated predecessor safety, restore, and hosted cutover evidence.",
    "verification": "Exact hash 9b0b519b6152490a3599f963e9d8462b64d1a4427d4d2e70c0f04359332310cf deployed successfully as migration 121 of 121 on a fresh uniquely named PostgreSQL 17 disposable database under the controlled migrator/runtime role lifecycle. The focused runtime-role integration suite passed 3/3: dry-run and apply surfaced the same ineligible-actor blocker while dry-run persisted zero routing children/audits; the eligible instance applied once and reran with one CURRENT result and one audit; all blocked-instance child, v1, and audit writes rolled back; a concurrent worker was rejected while the transaction advisory lock was held; catalog inspection found the exact unique partial index; and a duplicate matching event was rejected while cardinality remained one. The lifecycle also passed 17/17 append-only PostgreSQL guard tests and automatically tore the database down. Populated predecessor duplicate scan, lock-timeout failure, second deploy, Prisma drift, restore, and hosted rehearsal evidence remain required before approval. No production migration has been applied.",
    "expectedRecoveryTime": "Migration lock failure is bounded by five seconds and statements by 120 seconds. The backfill defaults to dry-run and each invocation is bounded to 100 rows and 50 seconds. Measure populated index-build duration, forward-fix decision time, and evidence-preserving restore against approved hosted RPO/RTO."
  },
  {
    "migration": "20260722190000_authentication_throttle_atomic_reservations",
    "sha256": "cdc40371e34e3bbc6d345995751acf60d8aba6e23400dd38aee5e67a4aa9614b",
    "risk": "This corrective migration makes PostgreSQL the authority for throttle status and key/policy generation, adds an operator-only transition function, and couples each successful authentication to a signed one-time reservation and immutable success event. Incorrect control-row transition, key overlap, lock ordering, counter conversion, ownership, or function grants could bypass admission limits, deadlock authentication, double-consume a reservation, admit stale generations, or expose operator authority to the runtime role.",
    "expectedDataEffect": "Create the singleton AuthenticationThrottleControl table and closed status enum; use the existing throttle-window generation, key/policy, previous-key overlap, and reservation identity fields introduced by the predecessor migration; install ENABLE ALWAYS control/window transition and removal guards; install one runtime-executable shared-lock function and one migrator-only operator CAS function; initialize one control row from the reviewed deployment configuration. Rewrite no tenant, user, session, audit, inventory, finance, or approval business rows.",
    "recovery": "The explicit transaction rolls back every type, table, column, constraint, function, trigger, grant, and initialization write together. Before authentication admission is enabled, a verified pre-migration restore may abandon the slice. After reservations or success evidence exist, pause admission through the reviewed operator path, retain control/windows/audit evidence, and deploy a forward correction; never delete counters, fabricate success events, or restore an older active key generation over newer evidence.",
    "reviewerStatus": "PENDING",
    "failurePoint": "Lock or statement timeout; incompatible existing throttle rows; invalid/null status; active/previous key identity reuse; unexpired previous generation; owner/grant drift; runtime operator access; inconsistent lock order; unsigned/tampered/replayed reservation; counter mismatch; control-generation conflict; pause/reservation race; checksum mismatch; second-deploy delta; schema drift; or restore non-equivalence.",
    "transactionBehavior": "Explicit PostgreSQL transaction with bounded lock and statement timeouts. Runtime reservations lock the singleton control row and all dimensions in canonical dimensionType/bucketKey order, then return a domain-separated HMAC proof. Success completion revalidates the proof, locks the same dimensions in the same order, requires the matching immutable success AuditEvent, and converts exactly one conservative failure reservation. Operator ACTIVE/PAUSED or key/policy transitions use an expected-generation compare-and-swap restricted to the migrator role.",
    "reversibility": "The additive schema is compatible with an application rollback only while new admission is paused and the prior application does not write incompatible counters. Once the new generation records reservations or success evidence, rollback is operationally difficult: retain schema/evidence, pause admission, and forward-fix. A key rollback is allowed only when no new-version reservation exists; otherwise rotate forward at a later aligned boundary.",
    "decisionTrigger": "Stop rehearsal or promotion on checksum mismatch, invalid/null operator status acceptance, key-version/fingerprint reuse, old-generation admission after rotation, pause bypass, lost or double counter conversion, reservation replay/tamper acceptance, missing success-audit coupling, runtime operator execution, deadlock/timeout outside the bounded policy, unsafe function/trigger drift, unbounded row/cardinality growth, predecessor data delta, second-deploy delta, restore mismatch, or hosted load/alert failure.",
    "owner": "Security and Backend Engineering own authentication admission, signed reservations, Argon2 work bounds, and caller integration. Database Engineering owns the control/counter schema, exact function and trigger contract, grants, migration, and restore integrity. Platform owns Caddy admission and Hostinger monitoring. QA and Release independently verify load, races, failure-deny behavior, migration/restore, and activation evidence.",
    "verification": "Exact hash cdc40371e34e3bbc6d345995751acf60d8aba6e23400dd38aee5e67a4aa9614b deployed as migration 122 of 122 on repeated fresh PostgreSQL 17 disposable lifecycles. The control race probe rejected null status, key identity reuse, and unexpired previous generations; proved one-winner CAS, pause/reservation serialization, paused admission denial, resume, and idempotent restore. Nine PostgreSQL throttle cases passed, including 200-way threshold serialization, 2,000 unknown-input bounded cardinality, tenant separation, environment drift denial, exact success conversion, proof tamper/replay/rollback rejection, independent success reservations, counter/shape guards, key-readiness output, and runtime operator denial. Four operations, eight edge/trusted-proxy, 17 append-only, 14 authentication-database, and 20 authorization-manifest cases passed. The Hostinger verifier attests exact reviewed control/window function bodies and grants. Populated predecessor, isolated restore, threshold calibration, staged load, alert acknowledgement/escalation, and exact hosted credential evidence remain required before production approval.",
    "expectedRecoveryTime": "Migration lock failure is bounded by the migration timeouts and runtime admission fails closed. Local pause/resume and race probes complete within one disposable lifecycle. Measure hosted pause propagation, edge rollback, contention, forward-fix, key rotation, and isolated restore against approved RPO/RTO before activation."
  },
  {
    "migration": "20260722200000_approval_routing_step_order_guard",
    "sha256": "792c04eee347c96cc5a1d4c2e9422ee1ccbabd5f659b6baf51d787af2ef2773b",
    "risk": "A normalized v1 approval step could previously be reordered after routing certification because stepOrder was omitted from the predecessor immutability trigger. The corrective index build acquires a bounded table lock and must not accept a conflicting index definition.",
    "expectedDataEffect": "Create one ApprovalInstance tenant/company/status readiness index and one ENABLE ALWAYS stepOrder immutability trigger. Rewrite, insert, update, delete, and backfill zero business rows.",
    "recovery": "The explicit transaction rolls back the index, function, and trigger together on lock timeout, statement timeout, name collision, or SQL failure. Keep the guard and index through application rollback; removing the guard while v1 rows exist would reopen retroactive route mutation.",
    "reviewerStatus": "PENDING",
    "failurePoint": "Lock or statement timeout; conflicting index or trigger name; wrong trigger enablement; v1 stepOrder mutation acceptance; v0 backfill incompatibility; checksum mismatch; second-deploy delta; schema drift; or restore non-equivalence.",
    "transactionBehavior": "Explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='120s'. It creates one strict index, one guard function, and one row trigger, then enables the trigger ALWAYS; it contains no data statement.",
    "reversibility": "The additive controls are backward compatible and should remain on application rollback. Any defect requires a reviewed forward correction rather than disabling normalized routing integrity.",
    "decisionTrigger": "Stop rehearsal or promotion on checksum mismatch, timeout, any predecessor data delta, wrong index definition, missing/non-ALWAYS trigger, SQLSTATE other than 55000 for a prohibited v1 reorder, changed data after a rejected reorder, second-deploy delta, drift, or restore mismatch.",
    "owner": "Database Engineering owns the index, trigger, migration, and restore integrity; Backend owns readiness behavior; Security and QA independently verify mutation denial and action behavior; Release verifies exact hash and hosted rehearsal evidence.",
    "verification": "Exact hash 792c04eee347c96cc5a1d4c2e9422ee1ccbabd5f659b6baf51d787af2ef2773b deployed as migration 123 of 123 on a fresh PostgreSQL 17 disposable database. The combined approval-routing PostgreSQL suite passed 25/25: direct v1 stepOrder mutation failed with SQLSTATE 55000, the row remained unchanged, the trigger was ENABLE ALWAYS, the exact readiness index existed, and backfill/inbox/action cases remained green. Populated predecessor locking, second deploy, Prisma drift, restore, production-volume query-plan, and hosted evidence remain required before production approval. No production migration has been applied.",
    "expectedRecoveryTime": "Migration lock failure is bounded by five seconds and statements by 120 seconds. Measure populated index-build, forward-fix, and isolated restore time against approved hosted RPO/RTO during rehearsal."
  },
  {
    "migration": "20260722210000_approval_integrity_petty_cash_intents",
    "sha256": "926779eba192e1d6e113eda5691294bc8bef93ed6d606734ef6b61576c3b723f",
    "risk": "The pending-approval partial unique index fails on any duplicate tenant/company/document tuple; the petty-cash backfill can expose incomplete normalized lineage; new checks and always-enabled triggers can reject incompatible writes; and the append-only intent relation must not admit cross-scope, stale-version, duplicate-step, or mutable financial evidence.",
    "expectedDataEffect": "Fail closed without changing data when duplicate PENDING approval tuples exist. Create one partial unique approval-instance index; add nullable current proposal and non-null proposal-version fields; initialize only eligible AWAITING_APPROVAL petty-cash requests with complete matching pending lineage; add proposal-state checks; and create the typed immutable PettyCashApprovalStepIntent relation, indexes, lineage trigger, and append-only guards. Do not select a duplicate winner, delete approval history, settle AP, or mutate inventory, bank, journal, supplier, or payment balances.",
    "recovery": "On duplicate preflight failure, stop and perform authorized manual audited reconciliation through controlled workflow actions before retry; never auto-delete, merge, or rewrite approval history. The explicit transaction rolls back the index, columns, backfill, constraints, table, functions, and triggers together on timeout or SQL failure. The expand-stage proposal constraint deliberately permits a predecessor application to terminalize a backfilled request while retaining unknown additive proposal fields. Keep normalized routing disabled during rollback; after intent rows exist, retain them and use a reviewed forward correction or verified restore rather than dropping immutable evidence.",
    "reviewerStatus": "PENDING",
    "failurePoint": "Lock or statement timeout; duplicate pending approval tuple; incompatible object name; invalid petty-cash amount/version state; incomplete or cross-scope approval lineage; non-ALWAYS trigger; accepted intent mutation; uniqueness or idempotency failure; checksum mismatch; second-deploy delta; Prisma drift; predecessor data delta outside the bounded backfill; or restore non-equivalence.",
    "transactionBehavior": "One explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='5min'. Duplicate preflight precedes all DDL. The migration creates the partial unique index, adds and conditionally backfills proposal fields, installs checks and the typed intent table, and enables lineage and append-only triggers ALWAYS; any failure rolls back the entire migration.",
    "reversibility": "Application rollback retains the additive proposal fields, constraint, uniqueness guard, and intent relation. The predecessor application may move a backfilled request to a terminal or revision status without clearing fields it does not know; normalized routing must remain disabled, and a later compatible application or forward migration performs cleanup. Once immutable intent evidence exists, dropping fields, guards, or history is not an ordinary rollback; use an evidence-preserving forward correction or verified restore.",
    "decisionTrigger": "Stop rehearsal or promotion on checksum mismatch, any duplicate pending tuple, automatic or unaudited reconciliation, unexpected predecessor data mutation, incorrect backfill population, invalid amount/version acceptance, lineage or cross-scope acceptance, mutable intent history, duplicate step/order/idempotency acceptance, timeout, non-ALWAYS trigger, second-deploy delta, schema drift, or restore non-equivalence.",
    "owner": "Database Engineering owns migration, uniqueness, checks, triggers, backfill integrity, and recovery; Backend Engineering owns canonical transaction behavior and policy-gated use; Finance/Operations own the unresolved petty-cash amount policy; Security, QA, and Release independently verify scope, immutability, populated predecessor safety, concurrency, and restore evidence.",
    "verification": "Exact hash 926779eba192e1d6e113eda5691294bc8bef93ed6d606734ef6b61576c3b723f applied as migration 124 of 126 on a fresh PostgreSQL 17 disposable database. PostgreSQL tests verify duplicate-pending rejection, bounded proposal checks, active-chain lineage and cross-scope negatives, valid intent insertion, uniqueness/idempotency, append-only UPDATE/DELETE/TRUNCATE denial, ENABLE ALWAYS triggers, and predecessor-style status terminalization with proposal fields retained and the proof rolled back. Hostinger reconciliation and verification grant runtime only SELECT/INSERT on PettyCashApprovalStepIntent and attest both trigger-function bodies; runtime destructive probes fail closed. Populated predecessor cardinality/lock-timeout, second deploy, report/export equivalence, isolated restore, and hosted credential evidence remain required before production approval. No production migration has been applied.",
    "expectedRecoveryTime": "Migration lock failure is bounded by five seconds and statements by five minutes. Measure manual reconciliation, populated index/backfill duration, rollback-compatible application recovery, forward-fix, and isolated restore against approved hosted RPO/RTO before production approval."
  },
  {
    "migration": "20260722220000_purchase_order_amendment_pending_status",
    "sha256": "e715b261a9eb5983d6ae1132b7d09e3a6c7a6d4e117c6b20b00c31674f54ac3d",
    "risk": "Replacing the PurchaseOrder status check takes a bounded table lock and can fail if predecessor rows contain an unrecognized status. An incomplete replacement could reject the implemented AMENDMENT_PENDING workflow or weaken the closed status vocabulary.",
    "expectedDataEffect": "Drop and recreate only PurchaseOrder_status_check to include the already implemented AMENDMENT_PENDING status. Insert, update, delete, and backfill zero PurchaseOrder or related rows.",
    "recovery": "The explicit transaction rolls back the constraint replacement together on timeout or validation failure. Keep the widened check during application rollback because AMENDMENT_PENDING is an existing documented application status; correct any defect with a reviewed forward migration or verified restore without rewriting purchase-order history.",
    "reviewerStatus": "PENDING",
    "failurePoint": "Lock or statement timeout; incompatible predecessor status; missing or differently named predecessor constraint; incorrect closed status list; checksum mismatch; second-deploy delta; Prisma drift; report/export mismatch; or restore non-equivalence.",
    "transactionBehavior": "One explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='5min'. It replaces one check constraint and contains no data statement; any failure rolls back the replacement.",
    "reversibility": "The corrected check is backward compatible with the predecessor application and should remain installed. Removing AMENDMENT_PENDING would break an existing controlled amendment path; use a reviewed forward correction rather than an automatic down migration.",
    "decisionTrigger": "Stop rehearsal or promotion on checksum mismatch, timeout, predecessor row/content delta, any accepted unknown status, rejection of an implemented status, missing constraint, second-deploy delta, drift, report/export mismatch, or restore non-equivalence.",
    "owner": "Database Engineering owns the constraint and migration; Procurement Engineering owns status/workflow parity; QA and Release independently verify populated predecessor behavior, exact hash, redeploy, drift, and restore evidence.",
    "verification": "Exact hash e715b261a9eb5983d6ae1132b7d09e3a6c7a6d4e117c6b20b00c31674f54ac3d applied as migration 125 of 126 on a fresh PostgreSQL 17 disposable database and the approval-routing database suite completed successfully. The data dictionary includes AMENDMENT_PENDING in the Purchase Order status vocabulary. Populated predecessor, lock-timeout rollback, second deploy, report/export equivalence, isolated restore, and hosted evidence remain required before production approval. No production migration has been applied.",
    "expectedRecoveryTime": "Lock failure is bounded by five seconds and statements by five minutes. Measure populated constraint validation, forward-fix, and isolated restore against approved hosted RPO/RTO before production approval."
  },
  {
    "migration": "20260722230000_budget_revision_atomic_step_activation",
    "sha256": "c1caa9dcd936f633b8196c3cc9bf1cace48e6a358b70a7d60c99a00ea09353af",
    "risk": "Replacing the normalized approval-step routing trigger takes a bounded table lock. Incorrect semantics could reject Budget Revision WAITING-to-PENDING activation, permit mutable certified routing context, or separate dueAt from the atomic activation write.",
    "expectedDataEffect": "Replace only the ApprovalInstanceStep v1 routing-context trigger function so one guarded WAITING-to-PENDING activation may initialize activatedAt and a previously-null dueAt atomically. Insert, update, delete, and backfill zero approval or business rows.",
    "recovery": "The explicit transaction rolls back the function replacement on timeout or SQL failure. Keep the compatible strengthened function during application rollback. If runtime behavior is incorrect, keep normalized routing disabled and deploy a reviewed forward correction or verified restore; never rewrite approval-step history.",
    "reviewerStatus": "PENDING",
    "failurePoint": "Lock or statement timeout; missing predecessor function; wrong activation exception; mutable assignee/scope/prohibited-actor context; non-ALWAYS trigger drift; checksum mismatch; second-deploy delta; Prisma drift; or restore non-equivalence.",
    "transactionBehavior": "One explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='5min'. CREATE OR REPLACE changes one trigger function; no table data statement is executed, and the existing ENABLE ALWAYS trigger remains bound to the function OID.",
    "reversibility": "The replacement preserves predecessor v1 immutability and adds only the documented atomic activation exception. It is compatible with application rollback and should remain installed; use a reviewed forward correction rather than weakening routing integrity.",
    "decisionTrigger": "Stop rehearsal or promotion on checksum mismatch, timeout, predecessor row/content delta, rejected valid atomic activation, accepted partial or unauthorized routing mutation, trigger/function drift, second-deploy delta, schema drift, or restore mismatch.",
    "owner": "Database Engineering owns trigger semantics and migration integrity; Backend Engineering owns Budget Revision activation and lock ordering; Security and QA independently verify immutable context and atomic transition behavior; Release verifies exact hash, redeploy, and restore evidence.",
    "verification": "Exact hash c1caa9dcd936f633b8196c3cc9bf1cace48e6a358b70a7d60c99a00ea09353af applied as migration 126 of 126 on a fresh PostgreSQL 17 disposable database. The approval-routing PostgreSQL suite completed successfully with Budget Revision activation carrying dueAt in the same guarded step transition. Function-body attestation and the existing ENABLE ALWAYS trigger contract passed. Populated predecessor, lock-timeout rollback, second deploy, isolated restore, and hosted evidence remain required before production approval. No production migration has been applied.",
    "expectedRecoveryTime": "Lock failure is bounded by five seconds and statements by five minutes. Measure populated function replacement, forward-fix, and isolated restore against approved hosted RPO/RTO before production approval."
  },
  {
    "migration": "20260724010000_controlled_evidence_qualification_foundation",
    "sha256": "5a799dc444df3fec56fb9e59251b30d471f544a928103f03ea63f08d8c70d1b6",
    "risk": "The additive qualification foundation creates policy and immutable-history structures, strengthens existing controlled-evidence and scan lineage, replaces scan append-only guards atomically, and adds composite uniqueness to populated Attachment, AttachmentScanAttempt, ControlledEvidenceAttachment, AuthSession, ApprovalInstance, and ApprovalInstanceStep relations. Existing cross-tenant corruption, duplicate composite identities, lock contention, incorrect trigger/routine privileges, or canonicalization drift must fail closed rather than leave partial controls.",
    "expectedDataEffect": "Create four empty policy/qualification tables plus one empty append-only activation-event table, indexes, scoped constraints, canonicalization and validation routines, and ENABLE ALWAYS guards. Rewrite, insert, update, delete, or backfill zero business, attachment, scan, policy, qualification, inventory, approval, finance, or audit rows. Validate additive tenant/company foreign keys on existing controlled-evidence links without changing their contents.",
    "recovery": "The explicit transaction and bounded timeouts roll back all DDL, guard replacement, constraints, routines, and grants together. Before any policy or qualification fact exists, a verified pre-migration restore may abandon the slice. Once immutable activation or qualification history exists, retain all facts and guards, keep runtime qualification disabled, and deploy an evidence-preserving forward correction; never delete history to roll back an adapter or policy.",
    "reviewerStatus": "APPROVED_FOR_REHEARSAL",
    "reviewerIdentity": "Codex Decision Chair after independent Architecture, Security, QA, Database, and Backend review",
    "reviewedAtUtc": "2026-07-23T18:30:00Z",
    "failurePoint": "Lock or statement timeout; pre-existing composite-key duplicate or tenant/company mismatch; foreign-key validation failure; partial guard replacement; routine/trigger body, type, deferral, owner, search-path, or ACL drift; runtime policy mutation; forged canonical/hash/cardinality/readiness fact acceptance; checksum-encoding mismatch; second-deploy delta; schema drift; report/export delta; or restore non-equivalence.",
    "transactionBehavior": "One explicit PostgreSQL transaction with SET LOCAL lock_timeout='5s' and statement_timeout='15min'. Existing scan guards are dropped and recreated inside that transaction, so failure restores the predecessor guards. New tables start empty; existing-table uniqueness and NOT VALID then VALIDATE scope constraints run under bounded deployment locks. Runtime qualification uses append-only inserts with deferred commit-time selection/cardinality and mutable evidence-readiness revalidation.",
    "reversibility": "The additive schema is compatible with the feature-disabled application checkpoint. Keep composite scope keys, scan immutability, and evidence-history guards through application rollback. Destructive schema rollback is acceptable only before activation after proving every new table empty; otherwise disable integration and forward-fix while preserving immutable facts.",
    "decisionTrigger": "Stop rehearsal or promotion on checksum mismatch, timeout, any predecessor row/content delta, nonempty new tables after migration, failed FK/unique validation, guard or role drift, accepted mutation/forgery/scope mismatch, failed realistic base64 qualification, concurrency/idempotency failure, second-deploy delta, Prisma drift, report/export mismatch, or restore non-equivalence.",
    "owner": "Database Engineering owns migration, constraints, canonical routines, append-only guards, role reconciliation, and recovery. Backend owns the supplied-transaction qualifier and empty adapter registry. Security and QA independently verify scope, privilege, forgery, concurrency, rollback, and dormant reachability. Finance, Operations, Security, and Release own the unresolved DEC-0047 policy and activation gates.",
    "verification": "Static/focused evidence passes Prisma validation, 7 database contracts, 8 qualifier tests, 8 database-role tool tests, exact routine/trigger integrity assertions, authorization-manifest coverage, lint, typecheck, full package tests, production build, secret review, and release-tool self-tests. Twenty-three disposable PostgreSQL cases are authored but unexecuted because DISPOSABLE_DATABASE_ADMIN_URL is unavailable. Architecture, Security, and QA approve only the dormant source checkpoint; populated predecessor, migration/trigger/ACL behavior, redeploy, drift, report/export, restore, hosted, policy, adapter, and activation evidence remain required before production approval. No production migration has been applied.",
    "expectedRecoveryTime": "Migration lock failure is bounded by five seconds and statements by fifteen minutes. Measure populated constraint/index duration, forward-fix, second deploy, and isolated restore against approved hosted RPO/RTO during rehearsal."
  },
  {
    "migration": "20260724120000_document_number_sequences",
    "sha256": "da5cd2436684f0cee04bfec991802c988cb6a7c10fea4f65a45e8606d573db77",
    "risk": "The sequence-row insert and scoped unique index can fail if a predecessor already contains a conflicting document-number allocation.",
    "expectedDataEffect": "Create the company/document-type/year sequence table and seed no business rows; existing document records remain unchanged.",
    "recovery": "Stop on duplicate or partial DDL, inspect the migration journal and sequence objects, and restore the predecessor backup or apply a reviewed forward correction. Never rewrite issued document numbers.",
    "reviewerStatus": "PENDING",
    "failurePoint": "Lock or statement timeout, conflicting object/index, checksum mismatch, second-deploy delta, schema drift, or restore non-equivalence.",
    "transactionBehavior": "Prisma migration transaction; inspect the journal and created sequence objects before retry if deployment is interrupted.",
    "reversibility": "Keep the additive sequence table through application rollback; use a reviewed forward correction or verified restore rather than deleting allocated sequence history.",
    "decisionTrigger": "Stop on any predecessor data delta, duplicate allocation, wrong unique scope, checksum mismatch, redeploy delta, drift, or restore mismatch.",
    "owner": "Database Engineering owns the sequence schema and recovery; Procurement/Receiving Engineering owns allocation parity; QA and Release verify exact numbers and redeploy.",
    "verification": "Hash-bound inventory entry added for exact-SHA disposable PostgreSQL rehearsal; populated predecessor, concurrent allocation, restore, and hosted evidence remain required.",
    "expectedRecoveryTime": "Measure lock, forward-fix, and isolated restore duration against the approved hosted RPO/RTO."
  },
  {
    "migration": "20260724130000_goods_receipt_create_idempotency",
    "sha256": "e4dd8776ae6ebf42c53f87a61669049f89386fc7ca2e435c5f6cc711bd00ccb2",
    "risk": "The tenant/company/idempotency-key unique index can fail if an existing duplicate key is present; the additive hash columns must not alter existing receipts.",
    "expectedDataEffect": "Add nullable retry key and canonical request-hash columns plus a scoped unique index; no existing receipt data is rewritten.",
    "recovery": "Stop on duplicate keys or partial DDL, reconcile through an audited forward fix or restore the predecessor backup, and never delete receipts or audit history to satisfy the index.",
    "reviewerStatus": "PENDING",
    "failurePoint": "Duplicate key, lock/DDL failure, checksum mismatch, second-deploy delta, Prisma drift, or restore non-equivalence.",
    "transactionBehavior": "Prisma migration transaction; inspect migration journal and index state before retry after interruption.",
    "reversibility": "The nullable additive columns are backward compatible. Retain idempotency evidence once populated; use a forward correction or verified restore rather than deleting keys.",
    "decisionTrigger": "Stop on duplicate or changed predecessor receipt/audit rows, wrong unique scope, checksum mismatch, redeploy delta, drift, or restore mismatch.",
    "owner": "Database Engineering owns the migration; Receiving Engineering owns replay/conflict behavior; Security, QA, and Release verify scope and concurrency.",
    "verification": "Hash-bound inventory entry added; fresh disposable lifecycle and receiving serialization evidence previously passed for the predecessor candidate, while populated migration/restore/hosted evidence remains required for final approval.",
    "expectedRecoveryTime": "Measure lock, forward-fix, and isolated restore duration against the approved hosted RPO/RTO."
  },
  {
    "migration": "20260724140000_stock_count_immutable_attempt_foundation",
    "sha256": "68b492fe94f23ef4fcce7ff7b5b8419ba914569612603fdbe78c78a60ee091ac",
    "risk": "The additive attempt tables backfill every existing Stock Count session/line and add foreign keys, unique indexes, and immutable history guards. A scope mismatch, duplicate legacy key, or trigger defect must fail closed without rewriting count evidence.",
    "expectedDataEffect": "Backfill one immutable attempt and attempt-line row for each existing session/line using the legacy IDs as deterministic lineage; populate current-attempt and adjustment source links; add no inventory movement or balance effects.",
    "recovery": "The migration must run as one transaction. On failure, restore the predecessor backup or use a reviewed forward correction; once attempt history exists, preserve it and keep Count Variance disabled rather than deleting evidence.",
    "reviewerStatus": "PENDING",
    "failurePoint": "Lock/statement timeout, duplicate legacy lineage, foreign-key mismatch, trigger or immutability defect, checksum mismatch, second-deploy delta, schema drift, or restore non-equivalence.",
    "transactionBehavior": "One explicit transaction is required for table creation, backfill, source-link updates, constraints, indexes, and ENABLE ALWAYS history guards; no partial backfill is acceptable.",
    "reversibility": "Application rollback retains the additive attempt tables and guards. Destructive rollback is permitted only before activation after proving the new tables empty; otherwise use an evidence-preserving forward correction or verified restore.",
    "decisionTrigger": "Stop on any legacy row/content delta, missing attempt-1 lineage, mutable terminal evidence, cross-scope acceptance, duplicate attempt/line identity, redeploy delta, drift, or restore mismatch.",
    "owner": "Database Engineering owns migration/backfill/guards; Stock Count Engineering owns cutover; Security, QA, Operations, and Release verify lineage, cutoff, concurrency, blind-count, and recovery behavior.",
    "verification": "Prisma schema validation passes. Disposable PostgreSQL migration/backfill, append-only guard, cutoff/concurrency, source-link, redeploy, drift, restore, and hosted evidence remain mandatory before Count Variance activation or production approval.",
    "expectedRecoveryTime": "Measure populated backfill, lock/statement timeouts, forward-fix, and isolated restore against the approved hosted RPO/RTO."
  },
  {
    "migration": "20260724150000_supplier_quote_create_idempotency",
    "sha256": "269e139e5388f6fb9dcf253953dd520ca579abdac29a93c3539fe59a2b2d88e2",
    "risk": "The nullable tenant/company/idempotency-key unique index can fail if a predecessor contains duplicate non-null keys; quote rows and audit history must not be rewritten.",
    "expectedDataEffect": "Add nullable quote retry-key and canonical request-hash columns plus a scoped unique index; existing supplier quotations remain unchanged.",
    "recovery": "Stop on duplicate keys or partial DDL, reconcile through an audited forward fix or predecessor restore, and preserve all quote and audit evidence.",
    "reviewerStatus": "PENDING",
    "failurePoint": "Duplicate key, lock/DDL failure, checksum mismatch, second-deploy delta, Prisma drift, or restore non-equivalence.",
    "transactionBehavior": "Prisma migration transaction; inspect the migration journal and index state before retry after interruption.",
    "reversibility": "The additive nullable columns are backward compatible. Retain populated retry evidence through application rollback; use a forward correction or verified restore rather than deleting keys.",
    "decisionTrigger": "Stop on any predecessor data delta, wrong unique scope, checksum mismatch, duplicate replay, redeploy delta, drift, or restore mismatch.",
    "owner": "Database Engineering owns the migration; Quotations Engineering owns canonical hashing and replay behavior; Security, QA, and Release verify scope and concurrency.",
    "verification": "Prisma schema/type validation and focused quote tests pass. Disposable PostgreSQL exact retry, changed-payload, cross-scope, concurrent race, migration, redeploy, restore, and hosted evidence remain mandatory before production approval.",
    "expectedRecoveryTime": "Measure lock, forward-fix, concurrent replay, and isolated restore duration against the approved hosted RPO/RTO."
  },
  {
    "migration": "20260724160000_supplier_quote_commercial_fields",
    "sha256": "84786525a8be1dd2282252d62ed64033db0a10d31d525ee02bb980ed32dc54a9",
    "risk": "The additive Decimal commercial columns must preserve existing quote totals and reject negative computed totals without changing historical quote or audit facts.",
    "expectedDataEffect": "Add subtotal, tax, discount, freight, other-charge, and supplier-accreditation snapshot columns; backfill subtotal from the existing recorded total and default other components to zero.",
    "recovery": "Stop on DDL, backfill, checksum, or constraint failure; restore the predecessor or apply a reviewed forward correction while preserving quote and audit history.",
    "reviewerStatus": "PENDING",
    "failurePoint": "Lock/DDL failure, decimal/backfill mismatch, checksum mismatch, second-deploy delta, Prisma drift, or restore non-equivalence.",
    "transactionBehavior": "Prisma migration transaction; inspect migration journal and column/backfill state before retry after interruption.",
    "reversibility": "The additive fields and zero defaults are backward compatible. Keep populated commercial snapshots through application rollback; use a forward correction or verified restore rather than deleting quote facts.",
    "decisionTrigger": "Stop on any historical total delta, negative or rounded total mismatch, wrong currency interpretation, checksum mismatch, redeploy delta, drift, or restore mismatch.",
    "owner": "Database Engineering owns migration/backfill; Quotations Engineering owns Decimal validation and comparison semantics; Security, QA, Reporting, and Release verify authorization, export parity, concurrency, and recovery.",
    "verification": "Prisma validation and focused quote tests pass. Disposable PostgreSQL migration/backfill, Decimal arithmetic, scope, recommendation revalidation, export parity, redeploy, restore, and hosted evidence remain required before production approval.",
    "expectedRecoveryTime": "Measure lock, backfill, forward-fix, and isolated restore duration against the approved hosted RPO/RTO."
  }
]
```

<!-- MIGRATION_SAFETY_REGISTER_JSON_END -->
