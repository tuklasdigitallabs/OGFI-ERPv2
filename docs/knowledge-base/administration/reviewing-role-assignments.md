# Reviewing role assignments

On a role’s access page, use Assigned Users search to find active assignments by name or email. Results are paginated, and each user shows only a bounded preview of active scopes; open the user record for authoritative scope detail. Role permission changes still require the existing controlled reason, authorization, and audit process.
