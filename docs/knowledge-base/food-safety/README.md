# Food Safety

## Review log activity

Open a compliance log to review its read-only Activity section. Activity is immutable audit history for the log and cannot be edited or deleted from the workspace.

- The Food Safety log register is paginated on the server. Search, business date, log type, and status filters remain selected while moving between pages; dashboard summary cards and CSV export are separate views.
