name: DISABLED — Staging Rollback Rehearsal

on:
  workflow_dispatch:
    inputs:
      rollback_release_version:
        description: UNAVAILABLE — this workflow fails closed until the DEC-0248 same-fence rollback exists.
        required: true
        type: string

jobs:
  rollback:
    runs-on: ubuntu-latest
    steps:
      - name: DEC-0248 rollback authority is unavailable
        run: |
          echo 'DEC-0248 same-fence rollback orchestrator is not implemented; staging rollback is disabled.' >&2
          exit 78
