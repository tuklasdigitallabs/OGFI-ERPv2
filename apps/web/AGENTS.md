# Web App Rules

Follow the root `AGENTS.md`. Browser components must not perform privileged database writes. Route handlers and server actions must delegate controlled actions to server services with authorization, validation, approval, and audit checks.
